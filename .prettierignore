dist
node_modules
pnpm-lock.yaml
src-tauri/target
src-tauri/gen
src-tauri/Cargo.lock

# Hand-authored / skill-authored prose — preserve author's spacing (prettier's
# markdown rewrites — blank-line-before-list, prose-wrap — fight the narrative
# voice). Same rationale as docs/ + workflow-system/; the memory files, the CHANGELOG
# narrative, and the installed skill bodies are all hand/skill-authored prose.
CLAUDE.md
README.md
docs/
workflow-system/
runtimes.md
CHANGELOG.md
.claude/memory/
.claude/skills/
# Cross-repo handoff notes at the root — same hand-authored prose class as the
# above (prettier rewrites `*emphasis*` -> `_emphasis_` and reflows the status
# tables in these). The list simply predated root-level HANDOFF-*.md files.
HANDOFF-*.md

# Generated files whose source of truth is a generator, NOT prettier. Formatting
# these breaks the generator's own `--check` drift-guard, which regenerates in
# memory and diffs against the committed file (exit 1 on drift).
#   _dots.generated.css  <- tooling/demo/extract-dot-css.mjs (from src/App.css)
tooling/demo/_dots.generated.css

# Nested Homebrew tap — its own independent git repo
homebrew-claudesk/
