{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main window and the PiP NSPanel surface. Both share Claudesk's single trust boundary (one window, many workspaces; the PiP is an auxiliary status surface, not a separate trust domain), so both get core:default — notably core:event for the workspace-status + pip-frame listen/emit fan-out the PiP subscribes to (M5 WP3).",
  "windows": ["main", "pip"],
  "permissions": ["core:default", "opener:default", "dialog:default", "updater:default", "process:default"]
}
