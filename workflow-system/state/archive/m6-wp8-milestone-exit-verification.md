# Feature: M6 WP8 — Milestone-exit verification

**Workflow:** feature
**State:** Completed 2026-06-28
**Created:** 2026-06-28
**Drive mode:** autopilot

## Retrospect
- **What changed in our understanding:** The agent-observable slice of milestone-exit verification turned out *larger* than planned — the `tauri` MCP bridge could confirm not just the UI-layer checks but a genuine **backend-process outcome** (WP11's `term_spawn` shell reaping: the `/bin/zsh` child count under the claudesk PID dropped 3→2 on terminal close). That was originally scoped as an operator-carry Phase-2 check; the bridge + a `ps` child-process count let the agent confirm it live in dev. The agent-vs-operator boundary is more porous than the plan assumed when a backend outcome happens to be observable via an external `ps`/`pgrep` alongside the bridge.
- **Assumptions that held:** The bridge-driving fidelity boundaries from the M5/M6 CLAUDE.md caveats held exactly — DOM-read / JS-exec / click / screenshot / synthetic-keyboard-for-zoom all high-fidelity; the React-fiber `onClick` workaround was needed for tab-switch + wrap-toggle (synthetic DOM `.click()` didn't reach React's handler, as documented). Reload wiped the bridge element-ref cache (expected). The split-cycle's "stuck at 3:1 under repeated synthetic onClick" was correctly diagnosed as a stale-closure artifact, not a bug.
- **Assumptions that were wrong:** None material. The verification found ZERO blocking failures and required ZERO back-loops to any owning WP — every M6 exit criterion held. (A friend request — editor font 13→11 — arrived mid-session and was handled as a standalone commit, not a WP8 deliverable.)
- **Approach delta:** Two deltas. (1) The mid-session font-parity request (operator, "no ceremony") was committed standalone (`3895a32`) rather than folded into WP8 — keeping the verification-only WP clean. (2) Phase 1's verify-self was driven directly from the orchestrator via `mcp__tauri__*` (NOT the `feature-verify-self-runner` Playwright subagent, which assumes Playwright-MCP tool names the bridge doesn't expose) — exactly as the CLAUDE.md caveat prescribes; the standard "spawn the runner" step was correctly bypassed for the bridge path.

## Problem Statement
Milestone 6 (friend-requested QoL polish) has all build WPs shipped on local `main` (WP1/1b/2/3/4/5/6/7/9/10/11 — unpushed). WP8 is the milestone-exit gate: confirm **every M6 exit criterion** holds against the real running app before M6 closes and v0.2.2 is cut. This is **verification-only — no new feature code**. Any failure surfaces as a back-loop to the owning WP, not an in-WP fix. The verification splits along the proven seam (CLAUDE.md, M5 WP2/WP3): the agent drives the main-webview-observable slice live via the `tauri` MCP bridge; backend-lifecycle + installed-`.app`-PATH-parity outcomes that the webview cannot see are carried to operator verify-human, batched at the `/release` gate per memory [[installed-build-verify-deferred-to-release]].

## Work Tree

- [x] Phase 1: Agent-drivable exit criteria (live via the `tauri` MCP bridge)  <!-- status: done — all leaves [x]; all agent-drivable M6 exit criteria PASS, 0 blocking -->
  **Relevance check (before Phase 2):** Requester still needs this: yes — M6 must close before v0.2.2. Requirements unchanged: yes. Solution still feasible: yes — Phase 2 is checklist composition only. No superior alternative: yes. **Verdict:** proceed.
  **Observable outcomes:** (all driven against `pnpm tauri:dev` + `mcp__tauri__driver_session{start, port:9223}`, scratch repo `tmp/scratch/scratch-a` opened as the workspace)
  - CLI (static gate, pre-bridge): `pnpm tsc --noEmit` exits 0; `pnpm eslint .` exits 0; `pnpm vite build` exits 0; `cargo --manifest-path src-tauri/Cargo.toml test` reports the full suite green (≈780 frontend + backend lib pass). No new feature code → expected all-green baseline.
  - Browser (WP3 split control): `webview_dom_snapshot` shows `[data-testid="workspace-split-control"]` with `[data-testid="split-collapse-cc"]`, `[data-testid="split-cycle-btn"]`, `[data-testid="split-collapse-ed"]`. `webview_interact{click}` on `split-cycle-btn` changes the displayed ratio token; `webview_screenshot` shows the split width visibly change and the **terminal re-fits with no clipped/garbled PTY** (xterm re-fit via ResizeObserver). Clicking `split-collapse-cc` then `split-collapse-ed` confirms mutual-exclusion collapse and terminal re-fit each time.
  - Browser (WP4 focus-scoped CC-terminal zoom): focus the LEFT half (click into the CC xterm); `webview_execute_js` reads persisted `localStorage["claudesk.terminal.fontSize"]` as the zoom source of truth (NOT post-dispatch DOM font — per the WP10/WP11 verify learning). Dispatch ⌘= / ⌘- / ⌘0 via `webview_keyboard` (or the capture-phase listener path) → the persisted terminal fontSize increments / decrements / resets; the **editor** font is unchanged when CC is focused.
  - Browser (WP5 editor wrap toggle): with the editor focused, `webview_dom_snapshot` shows `.editor-wrap-toggle[aria-pressed]`; default `aria-pressed="false"` (wrap OFF preserved). Click it → `aria-pressed="true"` and `webview_execute_js` reads `localStorage["claudesk.editor.lineWrap"]` flipped (persisted). Reload-survival: the persisted key holds across a `webview` reload.
  - Browser (WP6 FileTree gitignored-but-editable): open the FileTree for `tmp/scratch/scratch-a`; `webview_dom_snapshot` shows a gitignored-but-editable file is **present** in the tree (the heavy-dir policy, NOT gitignore-honoring — `.env`/`.session.md`/`.claude/*` shown; `.git/` + heavy dirs excluded). Create or confirm an `.env` in the scratch repo, click it → it opens in the in-app editor (editable). *(Scratch setup: `tmp/scratch/scratch-a` gets a `.gitignore` listing `.env` + an `.env` file with content, so the tree-includes-it assertion has a target.)*
  - Browser (WP10 right-panel terminal zoom): switch the right panel to the terminal tab; focus the right-panel terminal (`[data-testid="term-pane"]`, RIGHT half, `data-focus-half="right"`). Dispatch ⌘= / ⌘- / ⌘0 → the **terminal** zooms (shares `claudesk.terminal.fontSize`), NOT the editor. Confirm via the persisted-key read.
  - Browser (WP11 multiple right-panel terminals): `webview_dom_snapshot` shows `[data-testid="term-tab-row"]` with the ＋ button. Click ＋ → a second terminal sub-tab appears (●1 ●2); `webview_interact{click}` switches between them (keep-mounted; scrollback persists across switch). Hover-✕ / scoped-⌘W closes the focused terminal; the disallow-last rule keeps ≥1 terminal. (Shell-reap-on-close is a backend outcome → carried to Phase 2.)
  - Browser (WP9 empty-PiP suppression, partial-observable): with **no workspace open** (picker view), `webview_*{windowId:'pip'}` confirms the PiP panel is not shown / not mirroring. (Full blur-driven suppression is a window-manager outcome → carried to Phase 2.)
  - [x] P1.1 Static gate first — `tsc --noEmit`, `eslint`, `vite build`, `cargo test` — record the green baseline (no feature code → confirms the tree compiles + suite intact post-WP11). If any fail, STOP and back-loop (regression, not a verification miss).  <!-- status: done — tsc 0 err, eslint 0 err (1 pre-existing warn), vite 1.13s clean, vitest 780/780, cargo 295/295 -->
  - [x] P1.2 Scratch prep — ensure `tmp/scratch/scratch-a` has a `.gitignore` (listing `.env`) + an `.env` with content, committed to its baseline, so the WP6 tree-visibility + open-and-edit assertions have a concrete target. (`tmp/` is gitignored repo-wide — does not touch real tracked state.)  <!-- status: done — scratch-a/.gitignore ignores .env (committed 8af79bc-child); scratch-a/.env has SECRET_KEY+API_URL content; git check-ignore confirms .env IS gitignored -->
  - [x] P1.3 Bring up the bridge — `pnpm tauri:dev` (background), `mcp__tauri__driver_session{start, port:9223}`, open `tmp/scratch/scratch-a` from the picker → a real workspace mounts. Confirm `__TAURI_INTERNALS__` present.  <!-- status: done — bridge bound 9223, __TAURI_INTERNALS__ present, scratch-a workspace mounted + CC spawned -->
  - [x] P1.4 Drive WP3 (split-drag/collapse + terminal re-fit), WP4 (focus-scoped CC zoom), WP5 (wrap toggle) — the LEFT-half + editor slice.  <!-- status: done — WP3/WP4/WP5 all PASS (see Discoveries) -->
  - [x] P1.5 Drive WP6 (FileTree .env visible + editable), WP10 (right-panel terminal zoom), WP11 (multi-terminal open/switch/close + scrollback) — the RIGHT-half slice.  <!-- status: done — WP6/WP10/WP11 all PASS incl. live backend shell-reap (see Discoveries) -->
  - [x] P1.6 Drive WP9's webview-observable partial (no-workspace → PiP not mirroring via `windowId:'pip'`).  <!-- status: done — no-workspace → only main window exists, no pip window; reactive after open -->
  - [x] P1.7 Bridge teardown (CLAUDE.md caveat d) — `driver_session{stop}`, then kill `target/debug/claudesk` + `tauri.js dev`, ports 1420/9223 freed.  <!-- status: done — driver stopped, dev procs killed, ports free, prod untouched -->
  - [x] verify-auto  <!-- status: done — static gate green (tsc/eslint/vite/vitest 780/cargo 295); 0 changes to src/ or src-tauri/src/ (verification-only WP) -->
  - [x] verify-self  <!-- status: done — ALL agent-drivable criteria PASS via the tauri MCP bridge: WP3 split-cycle+refit, WP4 CC zoom, WP5 wrap toggle, WP6 .env visible+editable, WP9 empty-PiP partial, WP10 right-term zoom, WP11 multi-term open/switch/close+reap+disallow-last. 0 blocking. See Discoveries log. -->
  - [x] verify-human  <!-- status: done — operator approved 2026-06-28 -->
    - [x] P1.vh.1 Animated split-collapse re-fit — operator approved.  <!-- status: done -->
    - [x] P1.vh.2 Editor/terminal font parity (13→11) — operator approved.  <!-- status: done -->
  - [x] verify-codify  <!-- status: done — NO new tests (verification-only WP, 0 feature code; M6 criteria already codified by owning WPs). Font change (13→11) covered by fontZoom.test.ts (anchored on DEFAULT_FONT_PX). Full suite green: vitest 780/780, cargo 295/295. No integration boundary. -->

- [x] Phase 2: Operator-carry exit criteria (installed `.app` + backend-lifecycle — batched at the `/release` gate)  <!-- status: done — all leaves [x]; operator-carry checklist composed + tagged DEFERRED-TO-RELEASE; confirmed at v0.2.2 /release gate -->
  **Observable outcomes:** (operator-driven against a freshly-built **installed** `/Applications/Claudesk.app` launched from Finder/Dock — NOT `pnpm tauri:dev` — per the installed-build smoke-test convention; the agent CANNOT observe these, so they are carried to verify-human and confirmed at the release gate)
  - CLI/operator (WP1+WP1b telemetry live in prod): after a few real turns in the installed `.app`, `<app_data_dir>/com.claudesk.app/status-channel.log` is being written (`STATUS event=… outcome=emitted|dropped`, `REGISTRY op=…`, `HOOK write-failed …` line shapes) — the passive probe is confirmed running. Already shipped v0.2.1; re-confirm it survived M6 builds.
  - CLI/operator (WP2 stuck-`Running` dot fix): a real CC turn whose last shell cwd was a **subdirectory** of the workspace root ends cleanly (e.g. run `cd src-tauri && cargo …` then let the turn finish) → the status dot flips **Idle**, and `status-channel.log` shows the `Stop` line `resolved=ws-N outcome=emitted` (ancestor/longest-prefix match), NOT `resolved=none outcome=dropped`. The fix (`bafee80`) confirmed at the live intermittent-bug surface in the installed build.
  - Operator (WP7 no-yolo next-spawn): toggle the no-yolo setting (picker checkbox AND/OR native View-menu item — they stay in sync via the `cc-yolo` broadcast); open a **new** workspace → CC spawns **without** `--dangerously-skip-permissions` (a real permission prompt appears). Default stays yolo-ON across a fresh install. Verify in the installed build (GUI-PATH spawn parity).
  - Operator (WP11 term_spawn PATH parity): in the installed `.app`, open a right-panel terminal (`term_spawn`) → the shell spawns and reaps on close (`ps -eo pid,ppid,comm | awk '$2==<claudesk-pid>'` shows the child appear then disappear). The GUI-minimal-PATH class (env_path mitigation) holds for `term_spawn` too.
  - Operator (WP9 empty-PiP suppression, blur-driven): with **no workspace open**, ⌘Tab away from Claudesk (blur) → the PiP panel stays **hidden** in every mode (Auto auto-summon gated on count>0; On-mode pinned panel reactive to count). Open ≥1 workspace, blur → PiP appears; close all → PiP hides.
  - Operator (WP4/WP10 real-keyboard zoom): real ⌘+/⌘−/⌘0 keystrokes route to whichever half holds focus (left CC, right terminal, or editor) — the synthetic-Enter / raw-xterm-typing low-fidelity boundary means real-keyboard confirmation belongs to the operator.
  - [x] P2.1 Compose the operator verify-human checklist — done; the copy-pasteable checklist lives below in `## Operator-carry checklist (v0.2.2 /release gate)`.  <!-- status: done -->
  - [x] P2.2 Mark each carried check **DEFERRED-TO-RELEASE** — done; each item in the checklist below is tagged DEFERRED-TO-RELEASE (memory [[installed-build-verify-deferred-to-release]]).  <!-- status: done -->

## Operator-carry checklist (v0.2.2 /release gate)

**All items below are DEFERRED-TO-RELEASE** (memory [[installed-build-verify-deferred-to-release]]): the operator runs them at the `/release` v0.2.2 gate against a **freshly-built, Finder/Dock-launched installed `/Applications/Claudesk.app`** (`com.claudesk.app`, NOT `pnpm tauri:dev`), BEFORE the Homebrew bump. They need the real GUI-minimal-PATH spawn + backend process lifecycle + native window-manager blur that the dev MCP bridge can't fully judge. The agent already GREEN'd everything else (Phase 1). Use the `tmp/scratch/scratch-{a,b,c}` throwaways for anything that spawns/answers a CC session.

- [ ] **OC.1 — WP1/WP1b telemetry survives in prod** [DEFERRED-TO-RELEASE]. After a few real CC turns in the installed `.app`, confirm `~/Library/Application Support/com.claudesk.app/status-channel.log` is being written (line shapes: `STATUS event=… outcome=emitted|dropped`, `REGISTRY op=…`, `HOOK write-failed …`). The passive probe (shipped v0.2.1) still runs after the M6 build stack.
- [ ] **OC.2 — WP2 stuck-`Running` dot fix** [DEFERRED-TO-RELEASE]. In the installed `.app`, open a workspace, let CC run a turn whose **last shell cwd is a subdirectory** of the workspace root (e.g. ask CC to `cd src-tauri && cargo --version`), let the turn end cleanly → the status dot flips **Idle** (blue→grey), and `status-channel.log`'s `Stop` line reads `resolved=ws-N outcome=emitted` (longest-prefix/ancestor match), NOT `resolved=none outcome=dropped`. (Fix `bafee80`; intermittent ~once/day, so also just watch that the dot never sticks in normal use.)
- [ ] **OC.3 — WP7 no-yolo NEXT-spawn** [DEFERRED-TO-RELEASE]. In the installed `.app`, UNcheck "Skip Permission Prompts (yolo)" (picker checkbox OR the native View-menu item — confirm they stay in sync via the `cc-yolo` broadcast). Open a **new** workspace → the spawned CC runs **without** `--dangerously-skip-permissions` (a real permission prompt appears on first tool use). Re-check it → next spawn is yolo again. Default stays yolo-ON on a fresh install.
- [ ] **OC.4 — WP11 `term_spawn` PATH parity + reap** [DEFERRED-TO-RELEASE]. In the installed `.app`, open a right-panel terminal (`term_spawn`) → the shell actually spawns under the GUI-minimal PATH (env_path mitigation holds; not just dev). Open 2–3, close them → each `/bin/zsh` child reaps (`ps -eo pid,ppid,comm | awk -v p=<claudesk-pid> '$2==p'` shows the shells appear then disappear on close). *(Reap was bonus-confirmed in dev Phase 1 — this re-confirms it under the installed GUI-PATH.)*
- [ ] **OC.5 — WP9 blur-driven empty-PiP suppression** [DEFERRED-TO-RELEASE]. In the installed `.app` with **no workspace open**, ⌘Tab away (blur Claudesk) → the PiP NSPanel stays **hidden** in every PiP mode (Auto auto-summon gated on count>0; On-mode pinned panel reactive). Open ≥1 workspace, blur → PiP appears; close all workspaces → PiP hides. "No PiP when there's nothing to mirror."
- [ ] **OC.6 — WP4/WP10 real-keyboard zoom routing** [DEFERRED-TO-RELEASE]. In the installed `.app`, press real ⌘+/⌘−/⌘0 with focus in (a) the left CC terminal, (b) a right-panel terminal, (c) the editor → each routes to the focused surface only (terminal zoom shares `claudesk.terminal.fontSize`; editor uses `claudesk.editor.fontSize`, now defaulting to 11 to match). *(Dev bridge confirmed the routing via synthetic keys; this is the real-keyboard re-confirm.)*
  - [x] verify-auto  <!-- status: done — Phase 2 added 0 source code (docs-only checklist); git confirms no src/ or src-tauri/src/ changes. Nothing to compile/test. -->
  - [x] verify-self  <!-- status: done — No integration boundary; phase adds an isolated documentation artifact only (the operator-carry checklist). No running-system surface to observe (Phase 2 outcomes are all operator-carry, DEFERRED-TO-RELEASE). No subagent spawned against a non-existent surface (per CLAUDE.md). 0 blocking. -->
  - [x] verify-human  <!-- status: done — AUTO-SKIPPED (drive_mode=autopilot; all 4 gates clean: no integration boundary, verify-self all-PASS, no consuming-surface outcome). Phase 2 deliverable is a docs artifact (operator-carry checklist); operator confirms its items at the v0.2.2 /release gate. -->
  - [x] verify-codify  <!-- status: done — No integration boundary; docs artifact only, nothing to codify. No source changed in Phase 2 (git-confirmed); Phase-1 green suite (vitest 780/cargo 295) stands. No new tests warranted. -->

## Current Node
- **Path:** Feature > review-quality (complete) → finalize
- **Active scope:** ALL PHASES COMPLETE; ship + review-quality done. Review: 0C/0M/2 MINOR (font-parity comment-only + sibling-test bare literals) → auto-backlogged (autopilot) to backlog-quality-findings.md + pointer in backlog.md. Next: finalize. Commits 3895a32 + 2248f96 (NOT pushed; operator cuts v0.2.2 via /release).
- **Blocked:** none
- **Unvisited:** Phase 1 verify group (verify-auto → verify-self [drives P1.3–P1.7] → verify-human → verify-codify), then Phase 2 (P2.1 → P2.2 → verify group)
- **Open discoveries:** none

## Discoveries
<!-- Format: [SURFACED-<date>] <target node> — <summary>
     Each entry is also logged to workflow/backlog.md -->
- none yet

### verify-self progress log (P1.3–P1.6, bridge-driven 2026-06-28)
- **P1.3 bridge bring-up ✓** — `pnpm tauri:dev` up (com.claudesk.app.dev), bridge bound 127.0.0.1:9223, `driver_session{start, port:9223}` connected, `__TAURI_INTERNALS__` present, picker rendered. scratch-a opened from recents → real workspace mounted (CC session spawned, "bypass permissions on" visible = WP7 default-yolo-ON confirmed at the real spawn).
- **WP9 partial ✓** (P1.6) — with NO workspace open, `manage_window{list}` showed ONLY the `main` window (no `pip` window exists); `webview{windowId:'pip'}` → "Window 'pip' not found". After opening scratch-a, the `pip` window appeared (reactive). Empty-PiP suppression confirmed at the webview-observable layer. (Blur-driven full suppression → operator, Phase 2.)
- **WP6 ✓** (P1.5) — FileTree for scratch-a shows `.claude/`, `.env`, `.gitignore`, `hello.txt`, `README.md`. `.env` (git check-ignore confirms gitignored) AND `.claude/settings.local.json` shown = heavy-dir policy (gitignore NOT honored). Clicked `.env` → opened in editor, `contenteditable="true"`, content `SECRET_KEY=...`/`API_URL=...` rendered = reachable + editable.
- **WP5 ✓** (P1.4) — wrap toggle default `aria-pressed="false"`, persisted `claudesk.editor.lineWrap="false"`, title "Soft-wrap off (⌘\)". Clicked → `aria-pressed="true"`, persisted `"true"`, `.cm-lineWrapping` class applied live, title "Soft-wrap on". Toggled back OFF (default restored). Default-OFF preserved ✓.
- **WP3 ✓** (P1.4) — split-cycle btn: `2:2` (640/640) → `1:3` (left 320, right 960) → `3:1` → wraps to `2:2`. xterm `.xterm-viewport` width tracked the left-half width on every change (640→320→640) = terminal re-fits cleanly, no clipping. Collapse CC/ED buttons present with correct `aria-pressed` wiring; animated collapse re-fit best judged visually → carried to verify-human.
- **WP4 ✓** (P1.4) — focused left CC terminal (`xterm-helper-textarea` active, `data-focus-half="left"`); ⌘+ grew the TERMINAL font 11→12 (persisted `claudesk.terminal.fontSize=12`, `.xterm-rows` rendered 12px) while the EDITOR stayed at 11 (untouched). ⌘0 reset to 11. Focus-scoped routing to CC confirmed.
- **WP10 ✓** (P1.5) — switched right panel to Terminal tab, focused the right-panel `[data-testid="term-pane"]`; ⌘+ zoomed THAT terminal 11→12 (shares `claudesk.terminal.fontSize`), editor stayed at 11; ⌘0 reset to 11. (Observed: a backgrounded terminal doesn't re-render the shared font until its own focus/resize tick — shared-key-applied-to-active-terminal design, not a bug; the WP10 criterion "focus right terminal → ⌘zoom routes there, not editor" passes definitively.)
- **WP11 ✓** (P1.5) — `＋` opened a 2nd terminal (`term-tab-row`: `●1 ✕ | ●2 ✕ | ＋`, paneCount 2, both kept-mounted = display-toggle). Switch via tab fiber-onClick flipped `aria-selected` (●1 ↔ ●2). Closing ●2 reaped a backend shell (`/bin/zsh` count under claudesk pid dropped 3→2 — REAL PTY reap, the backend outcome we expected to carry to operator was confirmable live here). Down to 1 tab → remaining `●1` has NO close button (disallow-last enforced). New-terminal `＋` titled "New terminal (⌘T)", close titled "Close terminal N".
- **WP7 (UI layer) ✓ partial** — picker `picker-yolo` checkbox present + checked (default yolo-ON); the spawned CC showed "bypass permissions on" = yolo argv actually applied. (no-yolo NEXT-spawn behavior → operator, Phase 2.)
- **P1.7 bridge teardown ✓** — `driver_session{stop}`; killed dev `target/debug/claudesk` + `tauri.js dev`; ports 1420/9223 confirmed free; no stray procs; installed prod `.app` untouched.
- **NET: ALL agent-drivable Phase-1 exit criteria PASS** (WP3, WP4, WP5, WP6, WP9-partial, WP10, WP11, WP7-UI-layer). Zero blocking failures. Backend reap (WP11) bonus-confirmed live. Remaining criteria are genuinely operator/installed-only → Phase 2.
- **Mid-session operator change (NOT part of WP8):** editor `DEFAULT_FONT_PX` 13→11 to match terminal — committed standalone `3895a32`, verified live via the same bridge session (⌘0 reset 20→11px = terminal default). Logged here for provenance only; not a WP8 deliverable.

## Code-Quality Review — m6-wp8-milestone-exit-verification

### Strengths
- The round-trip test re-anchor from hardcoded `13/14` literals onto `DEFAULT_FONT_PX + 1` is exactly right: it makes the test robust to future default changes instead of re-breaking on every retune (this very commit would have broken the old literal-based assertion).
- The one-line behavioral change (default 13→11) is paired with a WHY-comment naming the parity target (`DEFAULT_TERMINAL_FONT_PX`) and the user-visible effect.
- Scope discipline excellent for a verification-only WP: production footprint is a single constant + comment; all other activity kept to live-bridge verification + operator-carry docs.
- Test inline tick-comments updated in lockstep — no stale literal drift.

### Issues
**CRITICAL** — (none)
**MAJOR** — (none)
**MINOR**
- [src/components/workspace/editor/fontZoom.ts:14] Parity claim "matches `DEFAULT_TERMINAL_FONT_PX`" is comment-enforced only — `DEFAULT_FONT_PX = 11` is an independent literal duplicating `terminalFontZoom.ts`'s `11`. A future terminal retune silently drifts the editor + makes the comment a lie. A structural test (`DEFAULT_FONT_PX === DEFAULT_TERMINAL_FONT_PX`) or an import would make the parity load-bearing. Low: tiny values, co-located, drift is cosmetic.
- [src/components/workspace/editor/__tests__/fontZoom.test.ts:31-33,47-48] Sibling `clampFontSize`/`nextFontSize` tests still use bare `13` literals as sample inputs (now arbitrary, no longer the default) — slight readability inconsistency next to the newly default-relative round-trip test. Trivial; any in-range integer works for those probes.

### Assessment
Well-built, appropriately-tiny change for a verification-only milestone-exit WP. Single production edit (default 13→11 for editor/terminal parity) is correct, motivated by a clear WHY-comment, and the test change improves robustness. No debt, no scope creep. Only standing weakness: the headline invariant (editor + terminal share a default size) lives in a comment, not code — minor latent gap, backlog-suited.

### If you disagree
Operator: dismiss any finding by editing this section in the WIP and marking the line `[DISMISSED]` before `feature-finalize` archives the file.

## Notes
- **Verification-only WP — no new feature code.** verify-codify on both phases is expected to be a no-op or a thin assertion that the existing M6 test suite covers the criteria (the per-WP codified tests already exist from each WP's own finalize). Do NOT write new feature tests here; if a gap is found, that's a back-loop SURFACE to the owning WP, not a WP8 deliverable.
- **Back-loop on any miss:** an exit criterion that fails routes to the owning WP (WP3/4/5/6/7/9/10/11 or WP2's fix), not an in-WP patch. WP8's job is to find-and-route, not fix.
- **Scratch repos** at `tmp/scratch/scratch-{a,b,c}` (gitignored, each a git repo w/ baseline commit) — used for all bridge verify-self that spawns/answers a CC session.
- **WP3 shape note:** WP3 shipped as a **collapse + ratio-preset cycle** control (`workspace-split-control`), reframed at spec time from free-drag. The exit criterion "drag-resizable; terminal re-fits cleanly" is met by the ratio-cycle + collapse mechanism (each ratio change re-fits the xterm) — verify against the as-shipped control, not a drag handle.
- **WP6 policy note:** the as-shipped policy is **heavy-dir exclusion, gitignore NOT honored** (re-base) — so `.env`/`.session.md`/`.claude/*` ARE shown; `.git/` + heavy dirs excluded. The exit criterion ".env reachable + editable" is met by this; verify against the heavy-dir model, not a gitignore-allowlist.
