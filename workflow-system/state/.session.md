---
paused: 2026-07-20 16:20
workflow: product
step: roadmap (re-opened + complete) → BLOCKED on companion repo before M10.9/M11
resume_skill: /session-start
state_file: docs/product/roadmap.md
---

# Session Pause

- **Last completed:** A full arc this session — (1) `/release` **v0.2.9** (published + verified via in-app self-updater), (2) backlog sweep, (3) `/product-wbs` for **M11** (5 WPs), (4) `/product-context` M11 cycle-open resync, then (5) a **"secondary non-workflow user" design discussion** that produced a NEW milestone + a cross-repo handoff. The decision: gate ALL workflow-coupled Claudesk UI behind a single **`workflow_features_enabled` setting, default OFF for everyone**; OFF = byte-identical (no dead affordances); enable = pure Claudesk UI flip (touches NOTHING in `~/.claude/`); a one-time **evangelistic invite** + step-by-step install instructions + soft `~/.claude/skills/` presence check + one-click disable. Re-opened `/product-roadmap` → inserted **Milestone 10.9 (workflow-features opt-in gate)** before M11 (M11 now gated behind it; M14 Settings/README refined to a two-tier framing); refined `vision.md` §Target Audience ("no concession" → two-tier: workflow-independent lite-IDE core for any CC user + opt-in gated workflow layer); captured the design prior **`gate-substrate-dependent-feature-class-behind-default-off-opt-in`** (default set by APPLICABILITY not audience-size — operator's Candidate-A call). Committed in THIS repo: `a48f38d` (roadmap+vision), `bb10be2` (design prior). **Companion repo `my-claude-code-customization` got the handoff (`HANDOFF-from-claudesk-2026-07-20.md` + 5 SURFACEs in its `workflow/backlog.md`) — left UNCOMMITTED per operator instruction (that repo is committed by hand).**

- **Next action:** **Drive the companion repo (`my-claude-code-customization`) to do its part FIRST** — it's the execution-order blocker for Claudesk's M10.9. Its 5 handed-over items (in its own `workflow/backlog.md` as `SURFACE-2026-07-20-CLAUDESK-*`): #2 doc-folder unify → #1 standalone uninstall + canonical install → #3 pause-ambiguity → #4 research-collision → #5 onboarding design (brainstorm-first). That work happens **in the other repo** (`/Users/stayman/Personal/projects/my-claude-code-customization` — or via its `_ref` symlink), on its own workflow. When it ships, it returns the canonical install copy + settled folder layout + onboarding flow to Claudesk (the handoff's "return contract"), which unblocks Claudesk M10.9.

- **Open questions/blockers:** **BLOCKED (by design):** Claudesk M10.9 (gate + rich invite + onboarding surface) is execution-blocked on the companion repo shipping its part. Do NOT build the Claudesk invite/onboarding before the companion repo settles (would hardcode against a moving target). The **onboarding design (#5) is explicitly brainstorm-first** — the operator wants to brainstorm it together, not have it auto-specced.

- **Notes:**
  - **Execution order (locked 2026-07-20):** roadmap re-open (DONE) → **companion-repo updates** → Claudesk **M10.9** (gate + invite + onboarding) → **M11** (docs-viewer, now gated).
  - **Two ways to resume:**
    1. **Preferred / on-critical-path:** switch to the companion repo and drive its 5 items (its handoff + native SURFACEs are ready to triage there via its own `/product-roadmap` or `/session-start`).
    2. **Parallel-but-optional:** Claudesk **M11's WBS is still valid + unblocked** (`docs/product/wbs.md`, 5 WPs, first feature = WP1 render-approach probe via `/feature-plan`) — the *non-gated* substrate can be built in parallel; M11's Docs tab ultimately sits behind the M10.9 gate but the render/panel work doesn't depend on the gate existing. If resuming Claudesk work directly, `/feature-plan` for M11 WP1 is the entry.
  - **Cross-repo split (recorded in roadmap M10.9 + the handoff):** Claudesk owns *presentation surfaces* (gate setting, OFF=byte-identical, Settings toggle, invite dialog, onboarding surface, soft presence check, M11 doc-discovery following the companion's layout). Companion repo owns *content + mechanics* (install/uninstall, doc-folder unify, pause/research disambiguation, onboarding/tutorial). Seam: Claudesk presents, companion authors — Claudesk never hardcodes workflow internals.
  - **State:** origin/main = `54dcc53` + backlog-sweep `f797bf8` (both PUSHED, v0.2.9 live). Local HEAD = `bb10be2`, **4 commits ahead of origin, unpushed** per commit-only-when-asked (`8a1c9dc` M11 WBS/context, `a48f38d` roadmap+vision, `bb10be2` design-prior — and the earlier `f797bf8` IS pushed; `8a1c9dc` onward are local). `docs/product/wbs.md` carries a stale `## Session Pause` marker at EOF (operator said leave it; `/session-resume` would strip it, but this pause uses `/session-start` since the next move is a fresh classification — the marker is harmless, ignore or hand-strip later).
  - **No `workflow/wip/` item** — this is product-workflow planning state, not a feature-in-flight. `resume_skill: /session-start` (not `/session-resume`) because the next move is choosing a workflow (companion-repo work vs. Claudesk M11), i.e. a fresh classification, not resuming a single paused thread. State file = `docs/product/roadmap.md` (the artifact just re-opened).
  - **Drive mode:** none (no active WIP frontmatter).
