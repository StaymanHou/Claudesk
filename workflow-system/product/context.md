---
stage: context
state: complete
updated: 2026-07-28
---

> Revision 2026-07-28: **M10.9 cycle open — the companion-repo block CLEARED.** The `my-claude-code-customization` repo shipped all five items Claudesk handed it 2026-07-20 and returned `HANDOFF-from-mccc-2026-07-28.md`, satisfying the operator's execution order (workflow repo → Claudesk M10.9 → M11). Ran `/product-wbs` for **M10.9 — Workflow-features opt-in gate** (5 WPs) then this context resync. **`## Current Milestone` replaced with M10.9**; M11 demoted to "Next up" with its decomposition **parked verbatim** at `m11-wbs-parked.md`. **⚠️ The WBS's load-bearing finding: an audit of `src/` + `src-tauri/src/` found ZERO shipped workflow-coupled surfaces** — no workflow-file reader (M3 WP5's watcher was dropped), `RightPanel` is a 3-member union with no `"docs"`, no live `⌘⇧` chord is coupled, nothing reads `~/.claude/skills/`; `hook_install` and the time-analytics dashboard are both **universal** (generic CC lifecycle events) and must keep working with the gate OFF. So OFF=byte-identical is *already* true ⇒ **no de-wiring work**, and the milestone's weight is the **gate seam** (the door M11–M13 must come through, + a guard test that bites when a future surface bypasses it) plus the **on-ramp** — the inverse of the naive sizing. WP order is serial by design: WP1 probe (Settings-surface shape + invite trigger — **there is no Settings UI today**; the 4 existing settings live in a picker strip only reachable with no workspace focused, `⌘,` unbound, and M14 already owns the *consolidated* surface) → WP2 gate + seam + OFF-invariant guard → WP3 invite + soft presence check → WP4 onboarding scope decision (a documented no-op is legitimate) → WP5 exit verify incl. proving the guard bites. Design prior `[[gate-substrate-dependent-feature-class-behind-default-off-opt-in]]` is this milestone's own origin (rule-2 agree, high confidence): default OFF set by **applicability**, never audience size; no new prior captured. **Cross-repo §4c constraint carried into the WBS:** the ONLY stable coupling is the command name `/tutorial-getting-started` (no flow/steps/copy/path-fork/permission-mode hardcoding — proven, the upstream mode rec changed `acceptEdits`→`auto` with zero Claudesk changes), and invite copy may never promise a "5-minute" tour (honest ~10–15 min real run, structurally pinned upstream); WP3.3 adds a copy-fidelity test making both enforceable on our side. Also settled this pass (operator directed full resolution rather than deferring): **`SURFACE-2026-07-28-M11-DOCS-LIST-PATHS-STALE`** — M11's `docs_list` paths migrated to the `workflow-system/` layout, **curated ordered set chosen over flat glob** (`design-priors.md`+`transitions.md` added at the order tail), **`archive/<cycle>/` explicitly NOT discoverable**, `.session.md` gitignored-but-present so discovery must not filter on git-tracked, `*wbs*.md` stays a glob, + `docs_list` tolerates the legacy layout (20+ rotating projects, not all migrated). **Resync work beyond the milestone header (this repo migrated to `workflow-system/` at `aacc687` but `CLAUDE.md` hadn't caught up):** 25 stale `docs/product/` + `workflow/` path references migrated — including the **dead** `_ref/claude-customization/docs/product/transitions.md` pointer (the companion repo migrated too); the **Project Structure tree rewritten** (was doubly stale — pre-migration layout *and* a WP1-era 4-module skeleton vs. today's ~20); and the **audience stance corrected to two tiers** (the overview still read "no design concession," a stance the 2026-07-20 vision revision refined and M10.9 implements). **Two real defects found in reconciliation, both the same migration miss:** `.gitignore`'s pointer-ignore still read `workflow/.session.md`, so the pattern didn't match and the migration's `git mv` **swept `.session.md` into the index** (it was git-tracked, contrary to policy — pattern fixed, `git rm --cached`, ignore verified); and `.prettierignore` still listed `workflow/`, leaving the unified root's hand-authored prose **unprotected** from Prettier's list-rewrites (fixed to `workflow-system/`). Memory symlink already linked — no change. origin/main in sync at `6a633e7`; this cycle's edits are uncommitted (commit-only-when-asked).
>
> Prior revision 2026-07-20: **M10.5 (QoL polish bucket) CLOSED + RELEASED as v0.2.9** (`/release` — GitHub 4-asset release + Homebrew tap `auto_updates true`, verified end-to-end on the operator's real installed `.app` **via the in-app self-updater**; discharged the WP7/M10.5-WP2/WP3/WP4 installed-`.app` carries; `SURFACE-2026-07-07-WP2-FMT-DRIFT` backlog-resolved as already-normalized). Then ran **`/product-wbs` for M11 — Workflow-docs markdown viewer** (the next execution milestone) → `wbs.md`, **5 WPs**, + this context resync. Replaced the `## Current Milestone` header with **Current Milestone: M11 — Workflow-docs markdown viewer**: a read-only `Docs` right-panel tab (4th `RightPanelHost` panel) rendering the focused project's conventional docs as formatted markdown — an **attention/re-orientation feature** ([[m7-docs-viewer-intent]]), not a generic preview. **WP1 render-approach probe** (S — pick the markdown-renderer dep [none exists today], GFM task-list/frontmatter fidelity, link-intercept model, CSP) ∥ **WP2 panel + workflow-ordered list + `docs_list`/`docs_read`** (M, reuses `validate_root`) → **WP3 formatted read-only render + auto-select-on-open + link nav** (M) → **WP4 scroll-preserving live reload on `fs-change`** (S — resolves `SURFACE-2026-07-07-DOCS-VIEWER-RELOAD-PRESERVE-SCROLL`, reuses the QoL-WP0 watcher) → **WP5 milestone-exit verify** (XS). Operator decisions locked at WBS: (1) auto-select most-relevant doc on open (`.session.md`→wip→roadmap; `[PRIOR: primary-surface-is-zero-ceremony-not-a-mode]`), (2) workflow-ordered list. Design prior `[[new-surface-must-earn-its-place-against-existing-ones]]` fired (rule-2 agree): the Docs tab's non-overlap vs Editor is *formatted render + curated set + auto-select* → earns its place; "never write" is the prior trimming edit affordances. No new prior captured. **No net-new arch element** (one new panel + two read-only commands + one renderer dep, all reusing existing seams — no P8 back-loop). The cross-project workflow-position *watcher/tree* (`SURFACE-2026-06-22-WP5-DROPPED-WATCH-WORKFLOW-DOC-HIERARCHY`) decided **NOT in M11** (single-workspace read-side only). First feature: **WP1** (a probe → `/feature-plan`). `.gitignore` verified fully conformant + memory symlink verified linked — no change. origin/main in sync at `54dcc53`/`f797bf8` (v0.2.9 published); the new `wbs.md` + backlog edit are uncommitted (commit-only-when-asked).
>
> Prior revision 2026-07-18: **M10 (In-app auto-updater) COMPLETE + CLOSED** (`/product-finalize`, commit `cbe893d` — all 6 WPs shipped, milestone-exit verdict **GO** on the operator's real Homebrew prod install [0.2.7→0.2.8 in-app self-update, zero brew commands, clean Gatekeeper relaunch]; the brew decision reversed mid-milestone → option B, brew self-updates in-app too, WP6 re-planned as Phase 1 error-surface fold + Phase B1 revert-to-one-self-update-path + Phase B2 live pass; published v0.2.7 + v0.2.8, tap at 0.2.8 `auto_updates true`; `arch.md` "Milestone 10 architecture" resynced AS-DESIGNED→AS-BUILT; wbs.md + research.md archived to `docs/product/archive/milestone-10-in-app-auto-updater/`; backlog swept — `SURFACE-2026-07-06-M10-IN-APP-AUTO-UPDATER` resolved + 3 WP4 findings closed by WP6). Then ran `/product-wbs` for **M10.5 — QoL polish bucket** (the next execution milestone) → `wbs.md`, **4 WPs**, + this context resync. Replaced the `## Current Milestone` header with **Current Milestone: M10.5 — QoL polish bucket**: **WP1 PiP top-right default** (S, known-shape UX over `pip/commands.rs`, reuses the `pip_move` main-thread `setFrameOrigin:` seam) ∥ **WP2 active-close confirmation** (M, extends the QoL-WP1 close-confirm gate with a busy/active probe) → **WP3 CC/terminal kill-clean-exit** (M, reproduce-first — `/feature-reproduce`) ∥ **WP4 CC I/O encoding mojibake fix** (M, reproduce-first). Design prior consulted at WBS: `explicit-selectable-mode-over-inferred-mode` (its risk-surface-vs-value rule) **fires on WP2** (rule-3 tie-break, disclosed) → lean CC-status-based active-detection over fragile foreground-process detection for v1; the other three priors don't fire; no new prior captured. **⚠️ Code-seam re-verification at WBS surfaced a hypothesis correction:** the encoding item's (WP4) SURFACE prime-suspect FE fix is ALREADY shipped (`decodeBase64`→`Uint8Array`→`term.write(bytes)`, xterm-safe) — so the real cause is elsewhere (input-side `encodeBase64` `& 0xff` truncation / xterm cross-`write()` buffering / locale), making reproduce-first doubly load-bearing (captured in wbs.md WP4 + the SURFACE). No net-new arch element (all 4 WPs extend existing subsystems — no P8 back-loop). M10.5 stays an OPEN bucket (append WP5+ if more pre-release QoL surfaces). `.gitignore` verified fully conformant to the canonical policy + memory symlink verified linked — no change. origin/main BEHIND local by 3 commits (the M10 finalize/verdict + this cycle-close, local-only per commit-only-when-asked).
>
> Prior revision 2026-07-16: **M9 (Time-analytics panel) COMPLETE + CLOSED** (`/product-finalize`, commit `27743ff` — all 10 WPs shipped, cycle-scoped docs archived to `docs/product/archive/milestone-9-time-analytics/`). Ran the full product flow for **M10 — In-app auto-updater** this session: `/product-research` (→ `research.md`, M10-scoped: `tauri-plugin-updater` v2 + `tauri-plugin-process` + minisign + static `latest.json` on GH Releases; the 3 hard problems resolved) → `/product-arch` (→ new `arch.md` "Milestone 10 architecture" section: update-artifact/manifest/minisign pipeline, install-source detection, self-quarantine-clear) → `/product-wbs` (→ `wbs.md`, **6 WPs**, operator-approved) → this context resync. Replaced the `## Current Milestone` header with **Current Milestone: M10 — In-app auto-updater**: **WP1 = the risk-gating PROBE** (unsigned-relaunch self-quarantine-clear on a REAL unsigned installed build; GO=seamless self-clear / FALLBACK=instruct-user dialog) → WP2 updater core → WP3 brew detect-and-defer ∥ WP4 user-control UX ∥ WP5 `/release` pipeline → WP6 exit-verify. Two decisions LOCKED (Homebrew detect-and-defer; stay-unsigned+minisign). Design priors consulted at WBS: `operator-helpful-friend-misfiring-as-offswitchable-setting` (agrees — `update_notifications_enabled` default-ON off-switchable) + `explicit-selectable-mode-over-inferred-mode` (agrees — low-surface UX + front-loaded risk probe); both rule-2 agree, no new prior, no override. Also captured **M10.5 — QoL polish bucket** (open collection, after M10 before the release; 4 dogfooding papercuts — PiP top-right default, active-close confirm, proper CC/terminal kill-exit, CC I/O encoding fix; `SURFACE-2026-07-16-M10.5-QOL-BUCKET`), sliding no tail (numbered 10.5). `.gitignore` already conformant + memory symlink already linked — no change. origin/main BEHIND local by the entire M9+M10 tree (local-only, unpushed per commit-only-when-asked).
>
> Prior revision 2026-07-06: Resynced root `CLAUDE.md` for the **Milestone 9** cycle open (Time-analytics panel — absorb `claude-time`). M8 (demo assets) closed 2026-06-29; the `mirror-fill-from-bottom` deferred-backlog fix shipped + closed 2026-07-06 (ship `99aca94`, finalize `2f630b6`). Replaced the `## Current Milestone` header with **Current Milestone: M9 — Time-analytics panel**: WP1 probe (freeze segment-model JSON contract + prove `dashboard.jsx`→dark TSX) → WP2 hook-4→9-events + write-gated SQLite writer → **WP3 reclassifier REDESIGN (not a port** — operator flagged the current classifier isn't what he wants; mandatory `/feature-spec` to lock each metric's definition+measurement first; `SURFACE-2026-07-06-M9-RECLASSIFIER-IS-REDESIGN-NOT-PORT`) → WP4 segment-model query layer → WP5 tracking toggle (universal-vs-workflow-coupled flag, **default OFF**) → WP6 dark dashboard tab + lazy-load (folds in `SURFACE-2026-06-19-CM6-BUNDLE-SIZE-LAZY-LOAD`) → WP7 deprecate + installed-`.app` exit-verify. First feature: **WP1** (a probe → `/feature-plan`). Design priors consulted at WBS: `operator-helpful-friend-misfiring-as-offswitchable-setting` — decision-2 (default-OFF-for-friend-cost) is a **disclosed override** of it (locked decision wins), no new prior; `explicit-selectable-mode-over-inferred-mode` agrees with the legible toggle. One net-new arch element: a **feature-local SQLite DB** (scoped exception to the flat-JSON-no-DB key decision, which governs the *project list*; reconciled into `arch.md` at WP7 — NOT an arch-gap back-loop). Also this session: inserted **M10 — In-app auto-updater** (right after M9, before the next release; two decisions LOCKED — Homebrew detect-and-defer + stay-unsigned-with-quarantine-clear; `SURFACE-2026-07-06-M10-IN-APP-AUTO-UPDATER`), sliding the tail +1 (docs-viewer M11 / auto-resume M12 / skill-orch M13 / polish M14). `.gitignore` already conformant (all canonical policy lines present) — no change. Memory symlink already linked — no change. origin/main BEHIND local by the M8-cycle + mirror-fix commits (local-only, unpushed per commit-only-when-asked).
>
> Prior revision 2026-06-29: Resynced root `CLAUDE.md` for the **Milestone 7** cycle open (menu-bar status item — the third status surface). M6 (friend-QoL) closed + released as v0.2.2 (`/release` — GitHub release + Homebrew tap `c532ba7`, installed-`.app` verified). Replaced the `## Current Milestone` section with **Current Milestone: M7 — Menu-bar status item**: linear WP1 probe (tray + popover-window + `tauri-plugin-positioner` mechanics, `tauri#13633` blur-to-hide reliability) → WP2 tray icon + `aggregate_state` fold + atomic icon swap + native right-click menu → WP3 left-click popover `WebviewWindow` (3rd Vite entry, per-workspace status list, row-click navigates) → WP4 exit verify (native glyph + out-of-focus DEFERRED-TO-RELEASE). Arch §B.2 promoted to designed-for-build (Revision 2026-06-29). Design priors consulted: `operator-helpful-friend-misfiring-as-offswitchable-setting` does NOT fire; `explicit-selectable-mode-over-inferred-mode` (risk-surface-vs-value) fires on the doc-hierarchy-watcher re-triage → watcher re-anchored M7 → **M8** (NOT an M7 WP). `.gitignore` already conformant — no change. origin/main in sync (`1d01eb9`).
>
> Prior revision 2026-06-27: Resynced root `CLAUDE.md` for the **Milestone 6** cycle open (friend-QoL, open collection). M4 + M5 closed; M5 (PiP) released as v0.2.0 (`/release` — GitHub release + Homebrew tap `7717bd9`). Replaced the `## Current Milestone` section with **Current Milestone: M6 — Friend-requested QoL polish**: WP1 file-based status-channel logging (probe) → WP2 fix the stuck-`Running` dot (LEAD correctness item) → parallel polish track WP3 drag-split / WP4 focus-scoped terminal zoom / WP5 editor auto-wrap toggle / WP6 FileTree reaches gitignored editables / WP7 no-yolo setting → WP8 milestone-exit verify. Design priors consulted (`operator-helpful-friend-misfiring-as-offswitchable-setting` agrees with WP5+WP7 off-switchable defaults). Reconciled `.gitignore` to the canonical artifact-tracking lines (added `.claude/settings.local.json`, `workflow/.session.md`, `tests/results/*.json`, `__pycache__/`, `*.pyc`). origin/main in sync (`50902af`).
>
> Prior revision 2026-06-22 (b): Resynced root `CLAUDE.md` for the **Milestone 4** cycle open. M3 closed (`/product-finalize`, commit `99b9398` — WP5 `.session.md` watcher dropped as the wrong file). Replaced the `## Current Milestone` section with **Current Milestone: M4 — Multi-workspace UX (filmstrip + center stage)**, the dogfood-replace point: WP1 N-cost probe → WP2 N>1 lift (+ picker error-surfacing) → WP3 filmstrip (tiles + status dots + `serializeAsHTML()` mirror + `⌘⇧+digit` switch + drag-reorder + static center-stage tile) → WP4 collapse → WP5 verify; WP4b (left/right focus indicator) parallel. Operator scope decisions captured (cost-probe-first, `⌘⇧+digit` reserved-chord switch, focus indicator added, doc-hierarchy watcher deferred to M6). All commits local-only.
>
> Prior revision 2026-06-22 (a): Resynced root `CLAUDE.md` for the **Milestone 3** cycle open. Replaced the stale "Current Phase" (Phase 1) section with **Current Milestone: M3 — CC lifecycle & state plumbing** (status broadcaster + Unix-socket hook channel + `.session.md` watcher; WP1–WP6). Recorded M1 + M2 as COMPLETE/CLOSED, the 2026-06-22 dogfood-first roadmap resequence (M3 → M4 multi-workspace → M5 PiP-unconditional → M6 menu-bar → M7/M8 later), and updated the status-surface-order Key Decision (PiP before menu-bar, unconditional — the old menu-bar-first/dogfood-gate plan is dropped). All commits local-only (no remote yet).
>
> Prior revision 2026-06-15: Resynced with the major product revision (multi-window → single-window with tabbed workspaces; filmstrip + center stage + menu-bar + conditional PiP; xterm.js DOM-renderer only; Unix-socket hook channel; status broadcaster → three surfaces; tab-shell substrate ships in Phase 1; thumbnail-rendering probe gates the filmstrip strategy). Phase 1 WPs renumbered (WP1–WP9).
>
> Prior revision 2026-05-22: Resynced with two new Phase-2 features — Smart auto-resume (three-branch decision tree) and Drive-mode selector + indicator.

# Context

Project `CLAUDE.md` generated at `CLAUDE.md` (project root). It captures the project overview, tech stack, project structure, host-based dev environment justification, setup instructions, development conventions, the current milestone, and key decisions — all derived from the product docs under `workflow-system/product/`. (The Setup & Ecosystem Gotchas section also records pnpm-v11 / ESLint-v9-pin / Prettier-ignore pitfalls from M1's scaffold.)

**Active milestone:** Milestone 10.9 — Workflow-features opt-in gate. M1–M10.5 complete + closed (M10.5 closed 2026-07-19, released as v0.2.9 2026-07-20). WBS at `workflow-system/product/wbs.md` (5 WPs). Unblocked 2026-07-28 by the companion repo's return contract. No net-new arch element (one `Option<bool>` settings field + two commands + one frontend seam + one existence check + one dialog) → no `/product-arch` back-loop.

**First feature:** WP1 — Probe: Settings-surface shape + invite trigger/persistence (size S). **Entry point `/feature-plan`** — it is a probe with two bounded questions, both answerable against the as-built app; no full `/feature-spec` needed.

**Next milestone:** M11 — Workflow-docs markdown viewer. Decomposition parked at `workflow-system/product/m11-wbs-parked.md` (5 WPs, paths + curation corrected); returns to `wbs.md` at M10.9's `/product-finalize`. Its Docs tab is gated behind M10.9's `workflow_features_enabled`, so WP2 there must register the panel through M10.9's seam.

**To record at `/product-finalize`:** the **gate seam** belongs in `arch.md` as a durable architectural contract (it is the door M11/M12/M13 come through, so it must outlive this cycle's WBS), plus the `AppSettings` field inventory growing to six.

**`.gitignore`:** reconciled — **one defect fixed**, the stale `workflow/.session.md` pattern that let the migration track the session pointer (now `workflow-system/state/.session.md`, untracked + ignore-verified). All other canonical lines already present; project-specific entries preserved. **`.prettierignore`:** same migration miss fixed (`workflow/` → `workflow-system/`), which had left the unified root's prose unprotected from Prettier. **Memory symlink:** already linked (`~/.claude/projects/-Users-stayman-Personal-projects-claudesk/memory` → repo `.claude/memory`).

**Carried forward, deliberately not done here:** `CLAUDE.md` is ~46k chars, over the harness's 40k prune threshold (it will warn at session start). `/util-prune-claude-md` is the remedy — the verify-self/MCP-bridge caveat chain and the milestone history are the obvious extraction candidates. Folding an editorial prune into a path/milestone resync would have made this diff unreviewable.
