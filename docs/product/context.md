---
stage: context
state: complete
updated: 2026-06-15
---

> Revision 2026-06-15: Resynced root `CLAUDE.md` with the major product revision (multi-window → single-window with tabbed workspaces; filmstrip + center stage + menu-bar + conditional PiP; xterm.js DOM-renderer only; Unix-socket hook channel; status broadcaster fanning out to three surfaces; tab-shell substrate ships in Phase 1; thumbnail-rendering probe gates Phase 2 filmstrip strategy). Phase 1 work packages renumbered (now WP1–WP9) and re-described. Active phase + first feature unchanged in identity but updated in description: still WP1 — Tauri 2 project scaffold + dev environment.
>
> Prior revision 2026-05-22: Resynced root `CLAUDE.md` with two new Phase 2 features added to vision/roadmap/arch/wbs — Smart auto-resume on project open (three-branch decision tree) and Drive-mode selector + indicator in window header. Active phase + first feature unchanged.

# Context

Project `CLAUDE.md` generated at `CLAUDE.md` (project root). It captures the project overview, tech stack, project structure (Phase 1 target shape including the `_ref/` symlink and tab-shell substrate components), host-based dev environment justification, setup instructions, development conventions, current phase, and key decisions — all derived from the product docs under `docs/product/`.

**Active phase:** Phase 1 — Bare Shell + Tab Substrate (PoC).

**First feature:** WP1 — Tauri 2 project scaffold + dev environment.
