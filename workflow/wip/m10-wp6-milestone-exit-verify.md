# Feature: M10 WP6 — Milestone-exit verify (in-app auto-updater)

**Workflow:** feature
**State:** plan (complete — RE-PLANNED via F23 back-loop 2026-07-18 around the reversed brew decision, option B) → build (Phase B1)
**Created:** 2026-07-17
**drive_mode:** autopilot

## Problem Statement

M10's updater engine is fully built (WP2 core flow, WP3 brew detect-and-defer, WP4
user-control UX, WP5 `/release` pipeline — all shipped, uncommitted-to-remote on the
batched M9+M10 tree). Three load-bearing behaviors were **legitimately deferred to
WP6** because they only fire on a REAL published release + a REAL installed build:
WP1's unsigned-relaunch Gatekeeper verdict (GO self-clear vs FALLBACK instruct-user),
WP3's real-`/Caskroom/` brew detection, and WP5's real live publish + self-update.
WP6 is the milestone-exit gate that verifies all of them end-to-end. It also folds in
the one substantive WP4 code gap: the `useUpdater` error surface (`phase==="error"` /
`errorMessage`) is produced but has **zero consumers** — an apply failure surfaces
nothing (the banner silently reverts), a real hole in the "user is always in control"
promise. That failure path first fires live at WP6, so it's wired + verified here.
The FLOOR insight: the current M10 tree (v0.2.5, updater-capable) IS the first
updatable build, so WP6's "older build to update from" is the v0.2.5 floor itself —
no separate old version is manufactured; publish v0.2.6 and verify the floor
self-updates to it.

**Prior art de-risking the 3rd-party surfaces:** WP1 (the completed probe WP) already
verified the `tauri-plugin-updater` artifact/manifest/minisign shapes empirically; the
engine is built through WP2–WP5. No new probe gate. Preserved WP1 artifacts + probe
keys at `~/claudesk-m10-probe/`; the FRESH release key (`774E2E8429FDF78A`) at
`~/.claudesk-release-keys/` is the one WP6's live release MUST use (NOT the probe key).

**[RE-PLANNED 2026-07-18 — F23 back-loop: the brew DECISION was REVERSED, so the brew half of the prior plan is thrown out and reworked.]**
The prior plan (both its original direct-download-first shape AND the 2026-07-17 brew-first
revision below) rested on the **detect-and-defer** premise: a brew install must NOT
self-update. That premise was **reversed by the operator (2026-07-17)** after a cited
deep-research pass showed it rested on a false "brew can't self-update" claim. **New locked
decision (option B):** brew-cask installs **self-update in-app exactly like direct-download**
— the cask declares `auto_updates true`, each release bumps `CFBundleVersion`, and a later
`brew upgrade` reconciles via `Info.plist` (Homebrew PR #21882, live Jul 2026) instead of
downgrading. `SURFACE-2026-07-17-M10-BREW-DECISION-REVERSED-TO-SELF-UPDATE`.

**What this re-plan does** (the rework, per the reversal SURFACE):
1. **Add `auto_updates true`** to the Homebrew cask (`/opt/homebrew/Library/Taps/staymanhou/homebrew-claudesk/Casks/claudesk.rb`, remote `StaymanHou/homebrew-claudesk`).
2. **Verify `CFBundleVersion` bumps per release** — confirm the built `.app`'s `Info.plist` `CFBundleVersion` actually increments from `tauri.conf.json` `version` (the reconciliation precondition PR #21882 needs).
3. **REMOVE the install-source detect-and-defer gate** — revert the WP3 backend (`InstallSource`/`install_source`/`install_source_from_bundle` + the `updater_check` source classification + `updater_apply`'s Homebrew refusal + the `install_source` field on `UpdateCheckResult`) AND WP6-Phase-2's brew FE branch (the `isBrew` copy-`brew upgrade` button in `UpdateNotifyBanner`, the `install_source`/`isBrew` wiring in `App.tsx`/`useUpdater`/`updaterPrefs`, the symmetric-brew comments in `updateNotifyState`/`updateFlowState`, `copyToClipboard.ts` + its CSS/tests) so **every** install flows the one normal self-update path.
4. **Re-verify (live)** that a **brew-installed** updater-capable build self-updates cleanly (same GO/FALLBACK Gatekeeper machinery as direct-download) AND that a later `brew upgrade` **reconciles** (not downgrades).

**KEEP Phase 1 (error-surface fold)** — `UpdaterStatusRow` + the `useUpdater` `errorMessage`/`statusNote`/`dismissError`/`dismissStatusNote` surface + the fallback-vs-error reconciliation + menu-check feedback are **install-source-agnostic** and unaffected by the reversal. It stays exactly as shipped.

**Consequence for the WP1 Gatekeeper verdict (important):** the earlier "brew is the priority, direct-download is lower-stakes, so the self-clear/Gatekeeper GO/FALLBACK verdict is a demotable standalone WP" reasoning (`SURFACE-2026-07-17-M10-DIRECT-DOWNLOAD-SELFUPDATE-VERIFY-STANDALONE-WP`) **no longer holds** — under option B, brew installs ALSO run the self-clear/relaunch path, so the WP1 GO/FALLBACK verdict is now **load-bearing for the operator + friends' real (brew) install base**. The verdict is therefore **pulled back into WP6's live pass** as the central end-to-end check, and the demoted standalone WP is closed as absorbed (brew self-update = the same path).

WP6 (re-planned) = **Phase 1 (done, kept)** + **Phase B1 (revert to one self-update path: cask `auto_updates true` + CFBundleVersion + remove the gate + remove the brew FE branch)** + **Phase B2 (brew-first live milestone-exit pass: real brew install self-updates clean + `brew upgrade` reconciles + the load-bearing Gatekeeper GO/FALLBACK verdict + skip/disable/cancel + smoke)**.

*The two superseded plans below (the OLD direct-download-first Phase 2 and the 2026-07-17 brew-first Phase 2/Phase 3) are retained in the tree for audit — both are now dead; Phase B1/B2 replace them.*

## Work Tree

- [x] Phase 1: Fold the WP4 error-surface finding (error phase consumed + menu feedback + fallback-vs-error reconciled)  <!-- status: done — all impl (P1.1–P1.5) + verify group complete; verify-human DEFERRED-to-Phase-2 (operator decision), substance agent-verified via bridge -->
  **Observable outcomes:**
  - Browser (MCP bridge): with the updater hook driven into `phase==="error"` (inject via a test seam / forced reject), the app renders a visible error affordance carrying `errorMessage` (a `data-testid`-tagged element with the message text) — NOT a silent revert to the Update…/Skip/Dismiss banner. Verified via `webview_dom_snapshot` / `webview_execute_js` reading the rendered error element.
  - CLI: `pnpm exec tsc --noEmit` exits 0; `pnpm exec eslint src/` exits 0 (App.tsx + updater/* changes lint clean); `pnpm vite build` exits 0 (no broken imports/JSX across the change).
  - CLI: `pnpm exec vitest run src/updater` — new/updated unit tests for the error-surface + menu-check-feedback pure logic pass; full FE suite green (no regression).
  - Console: no new JS console errors on app mount with the error-surface render path present (bridge `webview_execute_js` reads `console` capture / a clean mount snapshot).
  - [x] P1.1 Wire the error surface in `src/App.tsx`: render a dismissible error affordance when `updater.phase === "error"` (carry `updater.errorMessage`), sitting in the same in-flow app-shell region as the notify banner (NOT an absolute overlay — same misclick-safe posture as the WP4 F12 fix). Add a dismiss action that returns the hook to `idle` and clears the message. Tag it `data-testid="update-error"` (+ the message element) for bridge/vitest observation.  <!-- status: done — new src/updater/UpdaterStatusRow.tsx (DUMB view, in-flow, error>note precedence, returns null when nothing to show); mounted in App.tsx directly below the notify banner, above app-shell-scene; `data-testid="update-error"` + `update-error-message` + `update-error-dismiss`. New `.update-banner-error` CSS variant (amber-red) in App.css. -->
  - [x] P1.2 Add a `dismissError` (or reuse `dismissBanner`) path to `useUpdater` so the error affordance can be cleared → `phase="idle"`, `errorMessage=null`. Keep the interface honest (no dead exports).  <!-- status: done — `dismissError` (clears errorMessage → idle) + `dismissStatusNote` added to the UseUpdater interface + returned; both wired into UpdaterStatusRow. -->
  - [x] P1.3 Reconcile the fallback-vs-error race (MINOR #2, `SURFACE-2026-07-17-QUALITY-WP4-FALLBACK-VS-ERROR-RACE`): in `useUpdater.confirmUpdate`, when `QUARANTINE_FALLBACK_ACTIVE` is true the quarantine dialog is the SOLE post-install surface — do NOT also set `phase="error"`/`errorMessage` on that same path (early-return after showing the fallback dialog, or gate the error-set on `!QUARANTINE_FALLBACK_ACTIVE`). Add a one-line comment naming the single-surface invariant so WP6/Phase-2's potential one-line flip stays truly one-line-safe.  <!-- status: done — confirmUpdate's fallback branch now sets fallbackBundlePath + setPhase("idle") + `return;` BEFORE the error-set, with a single-post-install-surface invariant comment. Guarded structurally (return precedes setPhase("error")) in updaterWiring.test.ts. -->
  - [x] P1.4 Route the native-menu "Check for Updates…" outcome to feedback (MINOR #1, `SURFACE-2026-07-17-QUALITY-WP4-MENU-CHECK-DISCARDS-OUTCOME`).  <!-- status: done — chose the SINGLE-App-level-surface path: extracted pure statusNoteForOutcome/statusNoteForCheckError into updateFlowState.ts; checkNow sets statusNote; UpdaterStatusRow renders it. Since the App status row renders over BOTH scenes, REMOVED the picker's now-duplicate toast branches (picker KICKS-only) so menu + picker share one surface (no double-surface). Picker wiring test updated to the new reality. -->
  - [x] P1.5 Add/extend unit tests.  <!-- status: done — updateFlowState.test.ts: 4 tests for statusNoteForOutcome (update-available→null / up-to-date / brew-defer / check-error). updaterWiring.test.ts: 5 App.tsx+CSS wiring guards (UpdaterStatusRow mounted, error phase+message+dismiss fed, status-note+dismiss fed, row precedes scene, .update-banner-error exists) + 2 useUpdater structural guards (fallback early-return precedes setPhase("error"); statusNote helpers used). pickerUpdateNotificationsWiring.test.ts updated to KICKS-only. 73/73 pass (was 63). -->
  - [x] verify-auto  <!-- status: done — all gates PASS: tsc --noEmit exit 0; eslint (App.tsx + updater/ + ProjectPicker.tsx) exit 0; scoped vitest (src/updater + src/components/picker) 73/73 (10 new); vite build clean 1.23s (no broken imports/JSX across the UpdaterStatusRow/useUpdater/picker change). -->
  - [x] verify-self  <!-- status: done — ALL 3 outcomes PASS, driven LIVE via the tauri MCP bridge (9223) against the real Claudesk-Dev WKWebView (com.claudesk.app.dev; prod pid 1800 untouched, PID-scoped teardown). Integration boundary present (App.tsx + ProjectPicker.tsx existing UI) — outcomes cite them. (1) Error affordance: drove real phase="error"+errorMessage via fiber setters → `update-error` row rendered live (role="alert", amber .update-banner-error, message verbatim, dismiss btn), BANNER ABSENT (refutes the WP4 MAJOR silent-revert gap); dismiss→clean idle. Screenshot in scratchpad wp6-error-surface.jpg. (2) Manual-check note: real picker Check-for-updates click → checkNow()→real updater_check IPC errored (no published latest.json)→error note live (data-note-kind=error, role=alert, "Could not check for updates."); info branch (up-to-date) rendered live (data-note-kind=info, role=status, neutral variant). (3) Clean mount: __TAURI_INTERNALS__ present, no Vite error overlay after all toggles, picker interactive, dev log 0 errors/panics/warnings. -->
  - [x] verify-human  <!-- status: DEFERRED 2026-07-17 (operator decision) — boundary present (App.tsx + ProjectPicker.tsx existing UI) so F11 auto-skip was forbidden; agent presented the spot-check, operator chose DEFER → fold Phase 1's live human confirmation into Phase 2's SINGLE installed-build pass (alongside the WP1 Gatekeeper GO/FALLBACK + WP3 real-brew defer verdicts that were themselves deferred for the same reason — the real end-to-end needs a published release + the installed .app, not a throwaway dev spot-check). SUBSTANCE is agent-verified: verify-self live-drove all 3 boundary-touching outcomes via the MCP bridge (error surface renders + banner-no-longer-reverts, manual-check note surfaces, clean mount) — only the operator's live installed-build CONFIRMATION is carried. Marked [x] to unblock verify-codify on the agent-verified basis; the carried confirmation is tracked in Phase 2 P2.3 + backlog SURFACE-2026-07-17-M10-WP6-PHASE1-HUMAN-VERIFY-DEFERRED. -->
    - [x] P1.verify-human.1 Operator spot-check — DEFERRED into Phase 2's installed-build pass (agent live-verified via bridge: status line appears + dismissible + does not occlude filmstrip/picker).  <!-- status: DEFERRED → Phase 2 P2.3 -->
  - [x] verify-codify  <!-- status: done — NO additional coverage warranted; the repo has NO jsdom/testing-library/renderHook harness (confirmed) so the source-text-guard + pure-logic posture IS the codified model (live DOM is bridge-verified in verify-self, not re-asserted in vitest — matches updaterWiring.test.ts's own header). Boundary (App.tsx+ProjectPicker.tsx) satisfied end-to-end LIVE in verify-self (real picker click→real IPC→note render; real error phase→error affordance). 10 tests written in build cover the one genuinely-testable branching (statusNoteForOutcome ×4) + all wiring guards (UpdaterStatusRow mounted/fed, .update-banner-error, fallback early-return-precedes-setPhase-error invariant, picker de-dup). Full suite 105 files / 1164 pass (was 1154; +10; 0 regressions), exit 0. No triage (no failures). -->

  **Relevance check (before Phase 2):**
  - Requester still needs this: yes — WP6 is the M10 milestone-exit gate; the deferred WP1/WP3 live verdicts still need the installed-build pass Phase 2 produces.
  - Requirements unchanged: yes — the FLOOR-based approach + the four exit-criteria are unchanged.
  - Solution still feasible: yes — WP2–WP5 all shipped; the release key + tap + gh auth are present.
  - No superior alternative discovered: yes — the runbook + one live pass remains the right shape for operator-only live checks.
  **Verdict:** proceed

- [~] Phase 2 (OLD — SUPERSEDED by the F23 back-loop 2026-07-17): Operator runbook + staging around DIRECT-DOWNLOAD as the load-bearing check.  <!-- status: SUPERSEDED — the runbook was oriented on the wrong priority (direct-download C1 as headline); it is REWRITTEN brew-first in the new Phase 3. P2.1/P2.2 + their verify-auto/self were valid work but the deliverable is being redone. Retained here for audit trail; the new code Phase 2 + brew-live Phase 3 replace it. -->

- [x] Phase 2 (SUPERSEDED by the F23 re-plan 2026-07-18 — the brew DECISION was reversed; this code is REVERTED in Phase B1): Brew real-check-and-notify (CODE — make the Homebrew path check for real + notify with a copy-to-clipboard `brew upgrade` instruction)  <!-- status: SHIPPED-THEN-SUPERSEDED — the code (real brew check + copy-`brew upgrade` banner) was built + agent-verified this cycle, but the detect-and-defer premise it rests on is reversed. Phase B1 reverts the brew-specific parts (keeps the install-source-agnostic Phase-1 error surface). Retained for audit; Backend 548 lib + FE 106/1170 was green at ship. -->
  **Observable outcomes:**
  - CLI: `cargo test` (updater module) green — new/updated tests pin that `updater_check` for a Homebrew source runs the real `check()` (returns `available_version` when newer, `install_source:"homebrew"`) rather than short-circuiting; `updater_apply` still refuses for Homebrew. `cargo clippy --all-targets -- -D warnings` exit 0.
  - CLI: `pnpm exec tsc --noEmit` exit 0; `pnpm exec eslint src/` exit 0; `pnpm test` (frontend vitest) green — `shouldAutoNotify` returns true for a Homebrew install with a newer version (no longer suppressed); `manualCheckOutcome` returns `update-available` for Homebrew+available and `up-to-date` for Homebrew+none (not `brew-defer`); the copy-to-clipboard helper + banner isBrew-branch logic unit-tested.
  - Browser (MCP bridge): drive a Homebrew-shaped check result into the live app → the update banner appears with the version + a **`brew upgrade claudesk` button that copies to clipboard on click** (Copied! affordance), and NO in-app Update button. Verified via `webview_execute_js` (read the clipboard write / the Copied! state) + `webview_dom_snapshot`.
  - Console: no new JS console errors on mount or on the copy interaction.
  - [x] P2.1 (BACKEND) `updater_check` runs the real check for Homebrew (no short-circuit).  <!-- status: done — commands.rs: removed the pre-check Homebrew short-circuit; updater_check now classifies source, runs the SAME updater.check() for both, returns available_version (Some when newer / None up-to-date) with install_source from the detected source. updater_apply's Homebrew refusal kept (belt-and-suspenders). Module-doc + UpdateCheckResult field docs rewritten to "brew checks for real, defers only the ACTION". 21 updater tests pass; clippy --all-targets clean (fixed a doc_lazy_continuation on the reshape note). -->
  - [x] P2.2 (FRONTEND state) `updateNotifyState.ts` symmetric brew.  <!-- status: done — shouldAutoNotify: dropped the `install_source==="homebrew"` suppression (now install-source-agnostic: {enabled, newer, not-skipped}). manualCheckOutcome: retired "brew-defer" — brew classifies by available_version like direct (update-available / up-to-date); the type is now `"up-to-date" | "update-available"`. statusNoteForOutcome narrowed to the 2 outcomes (removed the brew-defer note). Header comments updated. -->
  - [x] P2.3 (FRONTEND view) banner isBrew → copy-to-clipboard button.  <!-- status: done — new src/updater/copyToClipboard.ts (BREW_UPGRADE_CMD const + copyToClipboard: async Clipboard API → textarea/execCommand fallback, never throws). UpdateNotifyBanner: isBrew branch shows the SAME "Claudesk X is available" text + a primary `brew upgrade claudesk` button (data-testid update-banner-brew-copy) that copies on click + flashes "Copied!" (data-copied toggle, 1.6s revert) + keeps Skip/Dismiss; NO in-app Update button for brew. New .update-banner-btn-copy CSS. -->
  - [x] P2.4 (TESTS) updated + added.  <!-- status: done — backend: replaced the stale short-circuit test with homebrew_check_carries_the_real_available_version_no_short_circuit + apply_still_refuses_homebrew. FE: updateNotifyState.test.ts — brew+newer→shouldAutoNotify true (+ still-suppressed-when-disabled/skipped/up-to-date), manualCheckOutcome brew symmetric; removed the retired brew-defer note test in updateFlowState.test.ts; new copyToClipboard.test.ts (cmd string + success + never-throws); banner isBrew-copy source-text guards in updaterWiring.test.ts. FE vitest 79 pass (updater+picker); full gates green. -->
  - [x] verify-auto  <!-- status: done — all gates PASS: cargo test updater 21; cargo clippy --all-targets -D warnings exit 0; tsc --noEmit exit 0; eslint exit 0; full frontend vitest 106 files/1170 (was 1164; +6 net, 0 regressions); vite build clean 1.18s. -->
  - [x] verify-self  <!-- status: done — ALL outcomes PASS, driven LIVE via the tauri MCP bridge (9223) against the real Claudesk-Dev WKWebView (com.claudesk.app.dev; PID-scoped teardown, ports freed). Integration boundary present (banner UI + updater_check) — outcomes cite the banner. Drove a Homebrew-shaped update-available result (install_source=homebrew, available_version=0.2.6) into the live banner state → banner rendered "Claudesk 0.2.6 is available. Update via Homebrew:" + a `brew upgrade claudesk ⧉` copy button (title/testid correct) + Skip + Dismiss, and NO in-app Update button (inAppUpdateBtnPresent=false — brew never self-installs). Clicking the copy button captured "brew upgrade claudesk" to the clipboard (spy) and a 150ms sampler caught the full Copied! lifecycle (data-copied true+text "Copied!" for ~750ms → auto-revert to the command). No Vite error overlay; dev log 0 errors/panics; app interactive after reset. Screenshot scratchpad/wp6p2-brew-banner.jpg. -->
  - [x] verify-human  <!-- status: DEFERRED 2026-07-17 (operator decision) — boundary present (banner UI + updater_check) so F11 auto-skip was forbidden; agent presented, operator chose DEFER → the live brew-banner confirmation folds into Phase 3's single brew-first operator milestone-exit pass (same pattern as Phase 1). SUBSTANCE is agent-verified: verify-self live-drove the brew copy-banner via the MCP bridge (banner + copy button + Copied! + no in-app Update button, all PASS). Only the real-brew-install confirmation is carried → Phase 3 P3.3. Marked [x] to unblock verify-codify on the agent-verified basis. -->
    - [x] P2.verify-human.1 Live installed-build brew-banner confirmation — DEFERRED into Phase 3's operator pass (agent live-verified the dev-app render via bridge).  <!-- status: DEFERRED → Phase 3 P3.3 -->
  - [x] verify-codify  <!-- status: done — NO additional coverage warranted; matches the repo testing model (pure-logic vitest + source-text guards + bridge live-verify; no jsdom for components). Boundary (banner + updater_check) satisfied end-to-end LIVE in verify-self. Coverage written in build: backend homebrew_check_carries_real_available_version + apply_still_refuses_homebrew; FE updateNotifyState symmetric-brew (brew+newer→notify, suppress-when-disabled/skipped/uptodate, manualCheckOutcome symmetric) + copyToClipboard.test.ts (cmd/success/never-throws) + banner isBrew-copy source-text guards; retired the stale brew-defer note test. Full suite green: backend 548 lib + FE 106 files/1170, 0 regressions, 0 failures (no triage). -->

  **Relevance check (before Phase 3):**
  - Requester still needs this: yes — Phase 3's brew-first live pass is the M10 brew-path exit gate (operator's stated priority).
  - Requirements unchanged: yes — brew-first + the four exit-criteria (minus demoted direct-download) hold.
  - Solution still feasible: yes — Phase 2 code shipped; release key + tap + gh ready.
  - No superior alternative discovered: yes — the brew-first runbook + one operator live pass remains the right shape.
  **Verdict:** proceed

- [~] Phase 3 (SUPERSEDED by the F23 re-plan 2026-07-18 — this was a brew-DEFER live pass; the decision reversed to brew-SELF-UPDATE, so this pass is replaced by Phase B2): Brew-focused live milestone-exit pass (rewrite the runbook brew-first; operator-driven)  <!-- status: SUPERSEDED — P3.1/P3.2 agent slice (brew-DEFER runbook) is now stale; the live P3.3 was never run. Phase B2 replaces it with a brew-SELF-UPDATE live pass. The old runbook at ~/claudesk-m10-probe/WP6-OPERATOR-RUNBOOK.md is stale (defer-based) → Phase B2 P.B2.1 rewrites it. Retained for audit. -->
  **Observable outcomes:**
  - CLI (agent slice): `WP6-OPERATOR-RUNBOOK.md` REWRITTEN brew-first — the load-bearing pass is: real brew-install → real check → banner with the copy-to-clipboard `brew upgrade claudesk` instruction when a newer version exists → `brew upgrade` actually updates it; plus skip/disable/cancel + smoke; direct-download self-update REMOVED (demoted to a standalone WP). Committed tree hygienic (version at source-of-truth, no key/endpoint/scratchpad leak); tsc + cargo check clean.
  - Operator (live — carried to verify-human): from a REAL `brew`-cask install of an updater-capable build, "Check for updates" runs a REAL check; when a newer version is published the banner shows it with the **copy-to-clipboard `brew upgrade claudesk`** instruction (NO in-app Update button); running `brew upgrade claudesk` actually lands the new version; `install_source()==Homebrew` confirmed against the REAL `/Caskroom/` layout; no brew desync. (Records the WP3 real-brew verdict — reshaped from "always-defer" to "real-check-defer-action-only".)
  - Operator (live — carried to verify-human): auto-notify on launch fires for a brew install when a newer version exists (symmetric with direct-download); skip-version suppresses re-notify; disable-notifications stops auto-check (manual still works); the Phase-1 error surface + manual-check note behave.
  - Operator (live — carried to verify-human): installed-`.app` smoke — GUI-PATH parity (spawns `claude`), per-identity pref isolation (dev vs prod), no regression to status dots / filmstrip / pip / tray / dashboard.
  - [x] P3.1 (AGENT) Rewrite `WP6-OPERATOR-RUNBOOK.md` brew-first.  <!-- status: done — rewritten brew-first at ~/claudesk-m10-probe/WP6-OPERATOR-RUNBOOK.md. Load-bearing = C1 brew real-check→copy-to-clipboard defer (up-to-date sub-case confirms the reshape even without a 2nd release; newer-version banner was bridge-verified); C2 brew auto-notify symmetric; C3 skip/disable (cancel N/A for brew); C4 error-surface + up-to-date note (Phase 1); C5 smoke. Explains the WP6 reshape (brew checks for real, defers only the ACTION). Direct-download self-update section REMOVED → points to the demoted standalone WP. Kept commit+push + /release v0.2.6 preconditions. Report template feeds P3.4. Fixed a stray doubled code fence. -->
  - [x] P3.2 (AGENT) Stage + hygiene gate.  <!-- status: done — ALL clean: runbook brew-first (10 structural markers, 8 balanced code fences); version 0.2.5 source-of-truth; tauri.conf pubkey = fresh key 774E2E8429FDF78A; no key/endpoint/scratchpad/password leak in the working diff; tap SSH remote + gh authed. GAP (carried, unchanged): M9+M10 tree LOCAL-only — commit+push before /release (in the runbook preconditions). -->
  - [ ] P3.3 (OPERATOR — carried to verify-human) Run the brew-first runbook live: commit+push → /release v0.2.6 → brew-install → real check → copy-instruction banner → brew upgrade → confirm; + skip/disable/cancel + smoke; report results.  <!-- status: NOT-STARTED -->
  - [ ] P3.4 (AGENT, after operator reports) Record the brew milestone-exit verdict to `docs/product/wbs.md` "Probe outcomes" (WP3 real-brew reshaped-behavior + WP5 live-publish). Mark M10 brew-path exit criteria met/not-met; note the direct-download standalone WP as the remaining deferred item.  <!-- status: NOT-STARTED -->
  - [x] verify-auto  <!-- status: done — agent-slice staging readiness PASS: runbook brew-first (5 exit-criteria C1–C5, SUPERSEDED note = direct-download removed from live checks, copy-to-clipboard load-bearing, 8 balanced code fences); tree hygienic (version 0.2.5, no leak); tsc --noEmit exit 0 (Phase 3 = docs only, no source regressed; cargo check was clean at Phase 2 end). P3.3/P3.4 operator-gated. -->
  - [x] verify-self  <!-- status: done — NO integration boundary (Phase 3 adds isolated artifacts only: the rewritten runbook doc + verification records). Spawned feature-verify-self-runner: the ONE agent-verifiable outcome (staging readiness) PASS — runbook brew-first w/ all 5 headers + BREW-FIRST title + copy-to-clipboard (4x) + SUPERSEDED direct-download note + 8 balanced fences; version 0.2.5; leak grep clean across unstaged+staged+untracked; tsc 0. The 5 live brew-path outcomes (real check→copy-defer, auto-notify symmetric, skip/disable, error-surface, smoke) correctly CARRIED (not FAIL) — operator-only (real brew install + published release); the dev-app brew banner render was already bridge-verified in Phase 2 verify-self. Carried to verify-human P3.3. -->
  - [ ] verify-human  <!-- status: SUPERSEDED (was in-progress) — this brew-DEFER live pass never ran; replaced by Phase B2's verify-human (brew SELF-UPDATE). Not the live cursor. -->
    - [ ] P3.verify-human.1 (C1, load-bearing) Brew real-check → copy-defer: real brew-install v0.2.6 → Check for updates → OBSERVE real check (up-to-date msg when current, NOT unconditional defer); when a newer version exists, banner + copy-to-clipboard `brew upgrade claudesk` button (no in-app Update button); clicking copies; /Caskroom/ confirmed; no brew desync; not translocated.  <!-- status: NOT-STARTED -->
    - [ ] P3.verify-human.2 (C2) Brew auto-notify symmetric: relaunch brew app with a newer version published → banner auto-appears on launch (gated by notifications pref).  <!-- status: NOT-STARTED -->
    - [ ] P3.verify-human.3 (C3) Skip/disable behave for brew (skip persists + manual re-offers; disable stops auto-check, manual still works; cancel N/A for brew).  <!-- status: NOT-STARTED -->
    - [ ] P3.verify-human.4 (C4 + Phase-1 carried) Error-surface + up-to-date note: offline check → "Could not check for updates." surfaces; up-to-date → "Claudesk is up to date." note.  <!-- status: NOT-STARTED -->
    - [ ] P3.verify-human.5 (C5) Installed-.app smoke: claude spawns (GUI PATH); dev/prod pref isolation; no regression to dots/filmstrip/pip/tray/dashboard.  <!-- status: NOT-STARTED -->
  - [ ] verify-codify  <!-- status: NOT-STARTED -->

- [x] Phase B1: Revert to one self-update path (option B) — cask `auto_updates true` + CFBundleVersion check + remove the detect-and-defer gate + remove the brew FE branch  <!-- status: done 2026-07-18 — all impl P.B1.1–P.B1.5 + full verify group complete (verify-auto full gate PASS; verify-self LIVE PASS via MCP bridge; verify-human DEFERRED-by-operator → Phase B2; verify-codify no-new-coverage per repo model). Backend 539 lib + 6 integ + FE 105 files/1163 green, 0 fail. Cask auto_updates true staged (not pushed). -->
  **Observable outcomes:**
  - CLI: `cargo test` (updater module) green — the `install_source`/`InstallSource`/`install_source_from_bundle` symbols and their tests are GONE (no dead code); `updater_check` returns `UpdateCheckResult` **without** an `install_source` field and runs the one real `check()` for every install; `updater_apply` has **no** Homebrew refusal branch (`grep -rn "Homebrew\|Caskroom\|install_source\|BREW_DEFER" src-tauri/src/updater/` returns nothing). `cargo clippy --all-targets -- -D warnings` exit 0.
  - CLI: `pnpm exec tsc --noEmit` exit 0; `pnpm exec eslint src/` exit 0; `pnpm test` (frontend vitest) green — the `isBrew`/`install_source`/`copyToClipboard`/`BREW_UPGRADE_CMD` symbols are GONE (`grep -rin "isBrew\|install_source\|copyToClipboard\|BREW_UPGRADE\|brew-defer\|brew upgrade" src/` returns nothing outside a comment noting the removal); `UpdateNotifyBanner` renders only the Update…/Skip/Dismiss action set (no brew copy button); `UpdateCheckResult` TS interface has no `install_source`; `App.tsx` passes no `isBrew` prop; `copyToClipboard.ts` + `copyToClipboard.test.ts` deleted; the `.update-banner-btn-copy` CSS removed from `App.css`.
  - CLI: `pnpm vite build` exit 0 (no broken imports/JSX across the removal).
  - CLI (cask): `brew style /opt/homebrew/Library/Taps/staymanhou/homebrew-claudesk/Casks/claudesk.rb` exit 0 (or `brew audit --cask` clean for the stanza) with `auto_updates true` present; `grep -n "auto_updates true" <cask>` matches.
  - CLI (CFBundleVersion): after `pnpm tauri build` (or reusing a prior build's `Info.plist`), `defaults read <built .app>/Contents/Info.plist CFBundleVersion` (and `CFBundleShortVersionString`) equals the `tauri.conf.json` `version` (e.g. `0.2.6`) — confirming Tauri propagates the version to `CFBundleVersion` so PR #21882's reconciliation has a monotonic value to read. If Tauri does NOT set a distinct `CFBundleVersion` (Tauri historically sets both keys to `version`), record that string-equal-to-version IS the monotonic value brew compares (versions are semver-ordered), and note any `/release` change needed.
  - Console (bridge, optional): with the reverted banner driven into an available-update state, the live banner shows the single Update… action for a normal (non-brew-branched) check result — no copy button, no console errors.
  - [x] P.B1.1 (BACKEND revert) Remove the install-source gate from `src-tauri/src/updater/`: delete the `InstallSource` enum + `CASKROOM_SEGMENT` + `install_source_from_bundle` + `install_source` from `mod.rs` (and their tests: `caskroom_path_*`, `applications_path_*`, `unresolved_bundle_*`, `caskroom_substring_*`, `translocated_path_*`); keep `resolve_bundle_path`/`quarantine_clear_command`/`clear_own_quarantine` (self-clear core — still needed by option B). In `commands.rs`: drop the `install_source` field from `UpdateCheckResult`, the `install_source_str` fn + its test, the `BREW_DEFER_MSG` const + its two tests (`brew_defer_message_*`, `homebrew_check_carries_*`, `apply_still_refuses_homebrew`), and the `updater_apply` Homebrew-refusal branch; rewrite the `updater_check`/`updater_apply` module doc + the `## WP3` doc block to describe the single self-update path (note the reversal + SURFACE id). Keep the `use super::InstallSource` import removal clean.  <!-- status: done — mod.rs: removed InstallSource/CASKROOM_SEGMENT/install_source_from_bundle/install_source + their 6 tests; added a "one self-update path — no install-source gate" doc block. commands.rs: removed install_source field + install_source_str + BREW_DEFER_MSG + the 3 brew tests + the updater_apply Homebrew refusal + the updater_check source classification; rewrote the module doc; ADDED update_check_result_has_no_install_source_field guard (3-key serde shape). lib.rs: fixed 2 stale WP3 doc comments (mod decl + invoke_handler). `cargo test updater` → 12 pass, 0 fail. -->
  - [x] P.B1.2 (FRONTEND revert) Remove the brew branch: delete `src/updater/copyToClipboard.ts` + `src/updater/__tests__/copyToClipboard.test.ts`; drop the `install_source` field from the `UpdateCheckResult` interface in `updaterPrefs.ts`; drop the `isBrew` prop + brew branch (the copy button + the `isBrew` text) from `UpdateNotifyBanner.tsx` (banner renders only Update…/Skip/Dismiss + the progress bar); drop `isBrew={…install_source==="homebrew"}` from the `App.tsx` banner call site; remove the `.update-banner-btn-copy` CSS block from `App.css`; excise the reshaped-brew commentary in `updateNotifyState.ts` (the fn bodies stay — `shouldAutoNotify` is already install-source-agnostic, `manualCheckOutcome` already returns the 2-outcome type; just the brew-explaining comments go) + `updateFlowState.ts` (`statusNoteForOutcome`'s brew-defer note is already retired — clean the stale reference); update `ProjectPicker.tsx`'s two brew-defer comments (L80, L284) to drop the brew-specific mention.  <!-- status: done — git rm copyToClipboard.ts + its test; UpdateNotifyBanner.tsx rewritten (no isBrew/copy branch, single Update…/Skip/Dismiss + progress); updaterPrefs UpdateCheckResult loses install_source; App.tsx drops isBrew prop + fixes the status-row comment; App.css .update-banner-btn-copy block removed; updateNotifyState/updateFlowState/useUpdater/UpdaterStatusRow/ProjectPicker brew comments cleaned. Sweep confirms only intentional removal-noting comments + guard-test absence assertions remain. -->
  - [x] P.B1.3 (CASK) Add `auto_updates true` to `claudesk.rb`, update the caveats, `brew style` clean.  <!-- status: done — added `auto_updates true` + a self-update comment to /opt/homebrew/Library/Taps/staymanhou/homebrew-claudesk/Casks/claudesk.rb; rewrote the "re-run xattr after each brew upgrade" caveat to reflect in-app self-update. `brew style --fix` applied (canonical stanza order — my auto_updates + 3 pre-existing depends_on/app ordering offenses); `brew style` now clean (0 offenses). Staged on the tap, NOT pushed (Phase B2 live pass confirms first). -->
  - [x] P.B1.4 (CFBundleVersion) Confirm the built `.app`'s `Info.plist` carries a `CFBundleVersion` that increases per release (or is string-equal to the semver `version`, which brew orders). If a distinct monotonic build number is needed, wire it (Tauri `bundle.macOS` / a `/release` step) — otherwise record that the semver `version` IS the monotonic value and no `/release` change is needed. Document the finding in the WIP + carry any `/release` note to Phase B2's runbook.  <!-- status: done — FINDING: Tauri sets BOTH CFBundleShortVersionString AND CFBundleVersion to the tauri.conf.json `version` (verified on the built v0.2.6 .app: both = "0.2.6"). No distinct monotonic build number; the semver `version` IS the monotonic value PR #21882 compares (versions are semver-ordered, and `version` increments per release by construction). A self-updated 0.2.7 bundle vs a cask still at 0.2.6 reconciles (0.2.7 ≥ 0.2.6). NO /release change needed; no bundle.macOS wiring needed. -->
  - [x] P.B1.5 (TESTS) Update the updater test suites to the reverted reality: remove the install-source/brew tests (backend + FE), keep/adjust the self-clear + progress + banner-action tests; add a guard test that `UpdateCheckResult` has no `install_source` field (backend serde shape) + that the banner exposes only the Update…/Skip/Dismiss test-ids (FE source-guard). Full backend + FE suites green, 0 regressions.  <!-- status: done — backend guard update_check_result_has_no_install_source_field added (commands.rs). FE: updaterWiring.test.ts brew describe block replaced with a "single self-update action set, no brew branch" block (3 tests: exposes Update…/Skip/Dismiss, banner has zero brew artifacts, App.tsx passes no isBrew/install_source); updateNotifyState.test.ts helper drops install_source + 2 vestigial brew tests removed; updateFlowState.test.ts + pickerUpdateNotificationsWiring.test.ts brew comments/assertions cleaned. Full-suite gate run in verify-auto. -->

  **CFBundleVersion note (P.B1.4 → carry to Phase B2 P.B2.1 runbook):** Tauri sets both `CFBundleShortVersionString` and `CFBundleVersion` to the semver `version`. That IS the monotonic value PR #21882 reconciles against — no `/release` change, no distinct build number needed. The runbook's `brew upgrade`-reconciles check just needs: the self-updated bundle's `CFBundleVersion` (= the newer semver) ≥ the cask version.
  - [x] verify-auto  <!-- status: done — FULL gate PASS 2026-07-18: cargo test 539 lib + 6 integ (0 fail; −9 removed brew/install-source tests, +1 no-install_source-field guard); cargo clippy --all-targets -D warnings exit 0; tsc --noEmit exit 0; eslint src/ 0 errors (1 pre-existing unrelated XtermPane.tsx warning); full vitest 105 files/1163 (0 fail; −1 file [deleted copyToClipboard.test.ts]); vite build clean 1.15s (no broken imports/JSX across the revert). Registry updated (vite build 1.15s / pnpm test 1.75s). -->
  - [x] verify-self  <!-- status: done — LIVE PASS via the tauri MCP bridge (9223) against the real Claudesk-Dev WKWebView (com.claudesk.app.dev; no prod app running → PID-scoped teardown, ports 1420+9223 freed). Integration boundary present (banner UI + updater_check IPC) — outcome cites the banner. (1) Clean mount: __TAURI_INTERNALS__ present, React root mounted, no Vite error overlay, console 0 messages (0 errors/warnings). (2) Reverted banner: injected a synthetic available-update result (REVERTED shape, NO install_source field) via the useUpdater `banner` setter (fiber hook 53, confirmed by the adjacent phase="idle" hook 54) → the banner rendered with EXACTLY the single self-update action set: headline "Claudesk 0.2.7 is available." (no "Update via Homebrew:" suffix) + 3 buttons update-banner-update/skip/dismiss, and hasBrewCopyBtn=FALSE + no "brew upgrade"/"Copied!"/"Homebrew" text anywhere (the removed brew branch). (3) Real manual-check flow: the picker "Check for updates" click drove the real updater_check IPC → returned the reverted UpdateCheckResult shape successfully → "Claudesk is up to date." info note rendered (proves the backend struct-shape change works end-to-end live). Screenshot scratchpad/wp6-b1-reverted-banner.jpg shows both surfaces. Backend process-lifecycle outcomes (real self-clear/relaunch, real brew install→reconcile, WP1 Gatekeeper GO/FALLBACK) are operator-only → CARRIED to Phase B2 verify-human. -->
  - [x] verify-human  <!-- status: DEFERRED 2026-07-18 (operator decision) — boundary present (banner UI + updater_check IPC) so F11 auto-skip was forbidden; agent presented the checklist, operator chose DEFER → fold Phase B1's live human confirmation into Phase B2's SINGLE operator milestone-exit pass (same pattern as Phase 1 + Phase 2 of this WP). SUBSTANCE is agent-verified: verify-self live-drove the boundary's dev-observable slice via the MCP bridge (reverted banner single Update…/Skip/Dismiss action set + no brew branch + real updater_check IPC round-trip + clean mount, all PASS). Only the operator's live installed-build confirmation is carried → Phase B2 P.B2.3. Marked [x] to unblock verify-codify on the agent-verified basis; carried confirmation tracked in backlog SURFACE-2026-07-18-M10-WP6-PHASEB1-HUMAN-VERIFY-DEFERRED. -->
    - [x] P.B1.verify-human.1 (dev-observable spot-check) — DEFERRED into Phase B2's operator pass (agent live-verified the reverted-banner render via the bridge: single Update…/Skip/Dismiss action set, no brew copy button).  <!-- status: DEFERRED → Phase B2 P.B2.3 -->
  - [x] verify-codify  <!-- status: done — NO additional coverage warranted; matches the repo testing model (pure-logic vitest + source-text guards + bridge live-verify; no jsdom/renderHook for components). The revert is a SUBTRACTION → the right regression protection is ABSENCE guards, all written in build/P.B1.5: backend `update_check_result_has_no_install_source_field` (pins the reverted 3-key updater_check serde contract — the consuming surface); FE updaterWiring "single self-update action set, no brew branch" (banner exposes Update…/Skip/Dismiss + not.toContain isBrew/copyToClipboard/BREW_UPGRADE_CMD/update-banner-brew-copy/Copied!; App.tsx passes no isBrew/install_source). Pure behavior (shouldAutoNotify/manualCheckOutcome install-source-agnostic) still covered by surviving updateNotifyState.test.ts. Consuming-surface end-to-end (real IPC→real banner) was bridge-verified LIVE in verify-self. No obsolete brew assertion remains (swept). Full suites GREEN: backend 539 lib + 6 integ + FE 105 files/1163, 0 failures (no triage). -->

  **Relevance check (before Phase B2):**
  - Requester still needs this: yes — Phase B2's brew self-update live pass is the M10 brew-path exit gate (the operator's stated priority, now under option B).
  - Requirements unchanged: yes — option B + the exit criteria hold; Phase B1 landed the code the live pass verifies.
  - Solution still feasible: yes — the revert shipped clean (all gates green + bridge live-verified); cask staged, key/tap/gh ready.
  - No superior alternative discovered: yes — the brew-first runbook + one operator live pass remains the right shape for the operator-only live checks.
  **Verdict:** proceed

- [ ] Phase B2: Brew self-update live milestone-exit pass (option B) — real brew install self-updates clean + `brew upgrade` reconciles; load-bearing Gatekeeper GO/FALLBACK verdict  <!-- status: NOT-STARTED; depends on Phase B1 -->
  **Observable outcomes:**
  - CLI (agent slice): `~/claudesk-m10-probe/WP6-OPERATOR-RUNBOOK.md` REWRITTEN for option B (brew SELF-UPDATE, NOT defer) — the load-bearing pass is: real brew-cask install of an updater-capable build → in-app "Update…" → download → minisign-verify → install → **self-clear quarantine → relaunch clean** (the WP1 GO/FALLBACK verdict, now brew-relevant) → then a later `brew upgrade claudesk` **reconciles** to the newer bundle (does NOT downgrade). Plus skip/disable/cancel + smoke. The stale brew-DEFER sections are removed. Committed tree hygienic (version at source-of-truth, no key/endpoint/scratchpad leak); `pnpm exec tsc --noEmit` + `cargo check` clean.
  - Operator (live — carried to verify-human): from a REAL `brew`-cask install of an updater-capable build, "Check for updates" runs a real check; when a newer version is published the banner shows the **in-app Update button** (same UX as direct-download — NO brew-specific copy affordance); confirming runs download → install → self-clear → relaunch into the newer version with **no Gatekeeper "damaged" block** (records the WP1 **GO/FALLBACK verdict** on a real unsigned brew-installed `.app` — the central milestone-exit check). If FALLBACK, the app flips `QUARANTINE_FALLBACK_ACTIVE=true` → the instruct-user quarantine dialog.
  - Operator (live — carried to verify-human): after the in-app self-update, a subsequent `brew upgrade claudesk` **reconciles** (PR #21882 reads the self-updated bundle's `Info.plist` `CFBundleVersion`, sees it ≥ the cask version, and skips/updates cleanly) rather than **downgrading** to the cask's pinned version. `auto_updates true` is present in the installed cask (`brew info claudesk` shows it). No brew metadata corruption (`brew reinstall claudesk` recoverable if drift appears).
  - Operator (live — carried to verify-human): auto-notify on launch fires when a newer version exists (gated by the notifications pref); skip-version suppresses re-notify; disable-notifications stops auto-check (manual still works); cancel leaves the app untouched; the Phase-1 error surface + manual-check note behave.
  - Operator (live — carried to verify-human): installed-`.app` smoke — GUI-PATH parity (spawns `claude`), per-identity pref isolation (dev vs prod), no regression to status dots / filmstrip / pip / tray / dashboard.
  - [x] P.B2.1 (AGENT) Rewrite `~/claudesk-m10-probe/WP6-OPERATOR-RUNBOOK.md` for option B: brew SELF-UPDATE as the load-bearing pass (real brew install → in-app update → self-clear → relaunch → `brew upgrade` reconciles). Remove the stale brew-DEFER sections + the "direct-download is a separate demoted WP" framing (direct-download is now the SAME path as brew — one check covers both). Keep the commit+push + `/release` preconditions + the fresh key `774E2E8429FDF78A`. Report template feeds P.B2.4.  <!-- status: done — fully rewritten for option B. Load-bearing = C1 brew SELF-UPDATE (in-app Update… → download → minisign-verify → install → self-clear → relaunch clean = the WP1 GO/FALLBACK verdict, now brew-relevant). C2 = the option-B-specific `brew upgrade` RECONCILES-not-downgrades check (auto_updates true + CFBundleVersion≥cask). C3 auto-notify / C4 skip/disable/cancel / C5 error-surface / C6 smoke. STEP A' added (publish 0.2.7 as the update TARGET — self-update needs a newer published version). Removed the copy-to-clipboard-defer live checks + the direct-download-demoted-WP framing (direct = same path now). Carried the CFBundleVersion=semver finding. Report template feeds P.B2.4; closes the absorbed direct-download SURFACE. Old defer runbooks noted SUPERSEDED. -->
  - [x] P.B2.2 (AGENT) Stage + hygiene gate: cask change staged on the tap; app version at source-of-truth; `tauri.conf.json` pubkey = fresh key `774E2E8429FDF78A`; no key/endpoint/scratchpad/password leak in the working diff; tap + gh authed. Confirm the M9+M10 tree is committed+pushable before `/release`.  <!-- status: done — ALL clean: runbook 35 option-B markers, copy-to-clipboard only in the ABSENT-assertion + SUPERSEDED-note lines (not a live affordance), 10 balanced code fences; version 0.2.6 source-of-truth (FLOOR; runbook STEP A' cuts 0.2.7); pubkey = fresh key 774E2E8429FDF78A; NO secret-key/password leak in the repo diff or runbook (only out-of-repo ~/.claudesk-release-keys/ PATH refs); gh authed (StaymanHou); cask auto_updates true staged (M Casks/claudesk.rb, unpushed → P.B2.3 pushes after live confirm); tsc --noEmit exit 0 (docs-only phase, no source regressed). CORRECTED the tree-state precondition: HEAD == origin/main == 32465f5 (v0.2.6 PUSHED); only the WP6 working tree is uncommitted (not "M9+M10 all local" — that was stale). -->
  - [ ] P.B2.3 (OPERATOR — carried to verify-human) Run the option-B runbook live: commit+push → cask `auto_updates true` pushed → `/release` a newer version (from the v0.2.6 floor, e.g. v0.2.7) → real brew install of the floor → in-app update → self-clear → relaunch clean → `brew upgrade` reconciles; + skip/disable/cancel + smoke; report results (incl. the GO/FALLBACK Gatekeeper verdict).  <!-- status: NOT-STARTED (operator-gated — leaves = the verify-human C1–C6 checklist below) -->
  - [ ] P.B2.4 (AGENT, after operator reports) Record the option-B milestone-exit verdict to `docs/product/wbs.md` "Probe outcomes": the WP1 GO/FALLBACK Gatekeeper verdict (now brew-relevant), the brew self-update + `brew upgrade` reconciliation result, WP5 live-publish. Mark the M10 exit criteria met/not-met. Close `SURFACE-2026-07-17-M10-DIRECT-DOWNLOAD-SELFUPDATE-VERIFY-STANDALONE-WP` as absorbed (brew self-update = the same self-clear path).  <!-- status: NOT-STARTED -->
  - [x] verify-auto  <!-- status: done — agent-slice staging-readiness PASS 2026-07-18: runbook is option-B (24 SELF-UPDATE/GO-FALLBACK/reconcile/auto_updates markers; all 6 exit-criteria headers C1–C6; title = "BREW SELF-UPDATE (option B)"); 10 balanced code fences; version 0.2.6 source-of-truth (FLOOR; STEP A' cuts 0.2.7); pubkey = fresh key 774E2E8429FDF78A; NO secret-key/password leak in repo diff + runbook; tsc --noEmit exit 0 (docs-only phase — no NEW source touched, Phase-B1 source stays green). P.B2.3/P.B2.4 operator-gated. -->
  - [x] verify-self  <!-- status: done — NO integration boundary (Phase B2 adds isolated artifacts only: the rewritten option-B operator runbook + WIP/docs; no source change). Spawned feature-verify-self-runner: the ONE agent-verifiable outcome (runbook staging readiness) PASS — title "BREW SELF-UPDATE (option B)"; C1 = brew SELF-UPDATE with GO/FALLBACK Gatekeeper verdict; C2 = option-B `brew upgrade` reconciles-not-downgrades; banner = single Update…/Skip/Dismiss with NO live copy affordance (the 4 copy-to-clipboard hits are all absent-assertion or SUPERSEDED-audit lines); 10 balanced fences; no minisign secret leak; tsc --noEmit exit 0. The 6 live brew-path outcomes (C1 self-update GO/FALLBACK, C2 reconcile, C3 auto-notify, C4 skip/disable/cancel, C5 error-surface, C6 smoke) correctly CARRIED (operator-only — real published 0.2.7 release + real brew install; not FAIL) → verify-human P.B2.3. -->
  - [ ] verify-human  <!-- status: in-progress — GENUINE PAUSE, operator RUNNING THE LIVE PASS NOW (chose "I'll run it now" 2026-07-18). The M10 milestone-exit gate; NOT auto-skipped — the load-bearing deliverable IS the operator's live brew self-update verdict (the auto-skip "decision-artifact known-limitation" case). Awaiting the operator's report (paste the WP6-OPERATOR-RUNBOOK.md report template) → then P.B2.4 records the verdict + marks M10 exit criteria + closes WP6. Leaves = C1–C6 from ~/claudesk-m10-probe/WP6-OPERATOR-RUNBOOK.md; folds in the carried Phase-1 + Phase-2 + Phase-B1 live confirmations. -->
    - [ ] P.B2.verify-human.1 (C1, load-bearing — WP1 GO/FALLBACK verdict) After `/release` 0.2.7 + a real brew install of 0.2.6: click "Check for updates" → banner shows the single **Update…/Skip/Dismiss** set (no brew copy button, no "Homebrew" text) → Update… → download → install → self-clear → **relaunch clean into 0.2.7** = GO, or Gatekeeper "damaged" block → instruct-user dialog = FALLBACK. Record the verdict + version-after + not-translocated.  <!-- status: NOT-STARTED -->
    - [ ] P.B2.verify-human.2 (C2, option-B specific) After the self-update, `brew upgrade claudesk` **reconciles** (leaves the app at 0.2.7 — does NOT downgrade to 0.2.6); `auto_updates true` present in the installed cask; no unrecoverable brew desync.  <!-- status: NOT-STARTED -->
    - [ ] P.B2.verify-human.3 (C3) Auto-notify on launch fires for a newer published version (gated by the notifications pref).  <!-- status: NOT-STARTED -->
    - [ ] P.B2.verify-human.4 (C4) Skip persists + manual re-offers; disable stops auto-check (manual still works); cancel leaves the app untouched.  <!-- status: NOT-STARTED -->
    - [ ] P.B2.verify-human.5 (C5 + Phase-1 carried) Error-surface: offline check → "Could not check for updates."; up-to-date → "Claudesk is up to date." note.  <!-- status: NOT-STARTED -->
    - [ ] P.B2.verify-human.6 (C6) Installed-`.app` smoke: `claude` spawns (GUI PATH); dev/prod pref isolation; no regression to dots/filmstrip/pip/tray/dashboard.  <!-- status: NOT-STARTED -->
  - [ ] verify-codify  <!-- status: NOT-STARTED -->

## Current Node
- **Path:** Feature > Phase B2 > verify-human — PAUSED. The M10 milestone-exit gate; agent slice (verify-auto + verify-self) PASS. **Verification RE-SCOPED 2026-07-18 to the REAL user path (operator correction).**
- **⭐ THE verification (only this):** a **Homebrew-installed** Claudesk **self-updates in-app to a newer release with ZERO brew commands** — the exact path current users (brew installs, incl. the operator's own prod app) experience. Sequence: [1] user already on a brew install (prod = 0.2.5) → **`brew upgrade` ONCE onto the updater-capable 0.2.7** (the last brew cmd they run for an update; NOT the thing tested) → [2] agent cuts a **no-op 0.2.8** release as the target → [3] in-app Check-for-updates → Update → **self-updates 0.2.7→0.2.8, no brew**. The GO/FALLBACK Gatekeeper-relaunch verdict is the load-bearing outcome. **OUT OF SCOPE (operator directive): the `brew upgrade` downgrade/reconcile edge case — brew upgrade already works, not verifying it.**
- **What already shipped (agent):** WP6 tree pushed `4955463`; cask `auto_updates true` `06e2d63`; v0.2.7 published (updater-capable, endpoint resolves) + tap bumped to 0.2.7 `9867409`; version-bump `2d99744` + runtimes `2591798`. HEAD == origin/main == `2591798`, tree clean. **NOTE:** the earlier "install 0.2.6 from DMG → self-update to 0.2.7 → brew-upgrade-reconcile" framing was WRONG (agent misunderstanding) and is discarded — the DMG-copy step was never run.
- **Active scope / gating:** the operator runs STEP 1 (`brew upgrade` to 0.2.7) from `~/claudesk-m10-probe/WP6-OPERATOR-RUNBOOK.md`, then tells the agent "on 0.2.7" → **agent's cue for STEP 2** (cut the no-op 0.2.8 release as the self-update target) → operator runs STEP 3 (in-app self-update to 0.2.8, no brew) and reports. **Awaiting the operator: `brew upgrade` to 0.2.7, then the "on 0.2.7" signal.**
- **On report received:** P.B2.4 records the verdict to `wbs.md` "Probe outcomes" (WP1 GO/FALLBACK + brew self-update + reconcile + WP5 live-publish), marks M10 exit criteria met/not-met, closes the absorbed direct-download SURFACE → verify-human `[x]` → verify-codify → ship.
- **RESUME POINTER:** if this session ends before the report, resume by pasting the runbook report (or `/feature-verify-human`) — the pending items are checklist leaves P.B2.verify-human.1–6.
- **Tree state (2026-07-18):** HEAD == origin/main == `32465f5` (v0.2.6 PUSHED). Only the WP6 working tree is uncommitted (Phase 1 + Phase 2 + the Phase B1 revert + docs) — the operator's first runbook step commits+pushes it.
- **⚠️ REVERSAL (locked 2026-07-17; re-planned 2026-07-18):** M10 Homebrew decision reversed detect-and-defer → **(B) brew installs SELF-UPDATE in-app too**. `SURFACE-2026-07-17-M10-BREW-DECISION-REVERSED-TO-SELF-UPDATE`. v0.2.6 FLOOR stands; only the brew design changed. Phase B1 (revert to one self-update path) DONE + agent-verified; Phase B2 = the brew self-update live operator milestone-exit pass.
- **Progress:** Phase 1 (error-surface fold) DONE. Phase B1 (revert) DONE. Phase B2 agent slice (runbook + hygiene) DONE; live operator pass carried. Old Phase 2/Phase 3 SUPERSEDED (retained for audit).
- **Unvisited (sequence-of-execution):** Phase B2 verify-auto (agent-slice readiness) → verify-self (agent-slice: the ONE agent-verifiable outcome = runbook staging readiness; the 6 live outcomes CARRIED, not FAIL) → verify-human = P.B2.3 (OPERATOR live brew self-update pass — carries Phase 1 + Phase 2 + Phase B1 confirmations + the load-bearing WP1 Gatekeeper GO/FALLBACK verdict) → P.B2.4 (record verdict + close the absorbed direct-download SURFACE) → verify-codify.
- **Open discoveries:** `SURFACE-2026-07-17-M10-DIRECT-DOWNLOAD-SELFUPDATE-VERIFY-STANDALONE-WP` closes as absorbed at P.B2.4 (brew self-update = the direct-download path). `SURFACE-2026-07-17-M10-WP3-BREW-DETECTION-LIVE-DEFERRED` + `-WP3-BREW-REALCHECK-RESHAPE` describe the reverted gate/reshape — close at WP6 finalize noting the reversal. `SURFACE-2026-07-18-M10-WP6-PHASEB1-HUMAN-VERIFY-DEFERRED` rides P.B2.3. OLD Phase 2/Phase 3 SUPERSEDED, retained for audit.
- **F23 re-plan note (2026-07-18):** the prior brew-first plan was built on the now-reversed detect-and-defer premise. This re-plan threw out the brew-specific code + the defer live pass and reworked to option B (one self-update path for all installs). It also PULLED BACK the WP1 Gatekeeper GO/FALLBACK verdict from the demoted direct-download standalone WP into WP6's live pass — under option B the self-clear/relaunch path runs for brew installs (the real user base), so that verdict is now load-bearing.

**Relevance check (before Phase B1 — the reverting phase):**
- Requester still needs this: yes — WP6 is the M10 milestone-exit gate; option B is the locked decision and the shipped brew code contradicts it (blocks M10 close).
- Requirements unchanged: NO (deliberately) — the brew requirement was REVERSED by the operator; this re-plan IS the response to that change. All other M10 requirements (user-control UX, minisign, self-clear, floor-based verify) hold.
- Solution still feasible: yes — the revert is a subtraction of already-understood code + a two-line cask change + a CFBundleVersion check; the deep-research pass de-risked the PR #21882 reconciliation contract.
- No superior alternative discovered: yes — one self-update path is simpler than the reverted per-source branch and matches the idiomatic VS-Code cask pattern.
**Verdict:** proceed

## Code-Quality Review
<!-- Dismiss-or-fold decisions for the WP2/WP3/WP4 backlog findings this WP touches.
     Filled at finalize. WP4 MAJOR + MINOR#1 + MINOR#2 are RESOLVED by Phase 1;
     WP4 MINOR#3 (on_download_finish downloaded:0) rides any updater/commands.rs touch
     (not in Phase-1 scope unless a commands.rs edit lands); WP2/WP3 MINORs are
     documentary/accept-as-limitation — decide at finalize. -->

## Discoveries
<!-- Format: [SURFACED-<date>] <target node> — <summary>
     Each entry is also logged to workflow/backlog.md -->
[SURFACED-2026-07-17] Phase 3 (P3.3) — Phase 1's live human spot-check DEFERRED by operator into the brew-first live milestone-exit pass. Substance agent-verified via MCP bridge; only live operator confirmation carried. Logged as SURFACE-2026-07-17-M10-WP6-PHASE1-HUMAN-VERIFY-DEFERRED.
[SURFACED-2026-07-17] product:wbs — direct-download self-update verification (incl. the WP1 unsigned-relaunch Gatekeeper GO/FALLBACK verdict) DEMOTED to a standalone WP after M10 WP6 (operator directive: brew is the priority, direct-download is lower-stakes — the operator + friends install via brew, which never self-installs). Logged as SURFACE-2026-07-17-M10-DIRECT-DOWNLOAD-SELFUPDATE-VERIFY-STANDALONE-WP.
[SURFACED-2026-07-18] Phase B2 (P.B2.4) — the 2026-07-17 demotion above is REVERSED-BY-CONSEQUENCE: under option B, brew installs self-update via the SAME self-clear/relaunch path, so the WP1 Gatekeeper GO/FALLBACK verdict is now load-bearing for the real (brew) user base and is pulled back into WP6's Phase B2 live pass. SURFACE-2026-07-17-M10-DIRECT-DOWNLOAD-SELFUPDATE-VERIFY-STANDALONE-WP closes as absorbed at P.B2.4 (no separate WP needed — brew self-update IS the direct-download path).
[SURFACED-2026-07-18] Phase B2 (P.B2.3) — Phase B1's live human spot-check DEFERRED by operator into the Phase B2 operator milestone-exit pass (same pattern as Phase 1 + Phase 2). Substance agent-verified via the MCP bridge (reverted banner single action set + no brew branch + real IPC round-trip). Only live operator confirmation carried. Logged as SURFACE-2026-07-18-M10-WP6-PHASEB1-HUMAN-VERIFY-DEFERRED.
