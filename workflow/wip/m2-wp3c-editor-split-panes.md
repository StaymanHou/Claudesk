# Feature: WP3c — Editor split panes

**Workflow:** feature
**State:** verify-codify (all phases complete)
**Created:** 2026-06-20

## Problem Statement

The WP2 `EditorPanel` mounts a single CodeMirror 6 view (`@uiw/react-codemirror`'s
`<CodeMirror>`) in each workspace's half-width right half. WP3c splits that into
**multiple panes** so the operator can view/edit two regions at once (Sublime's
split-editor parity). Within a half-width panel the only sane split direction is
**vertical (top/bottom)**; horizontal would halve an already-narrow column. The
hard part is not the layout — CM6 composes multiple `EditorView`s cleanly
(`research.md`) — it is **focus management**: the WP3a/WP3b chords (Cmd+S save,
Cmd+Shift+P palette, the future panel-switch hotkey) must act on the **focused
pane**, not "the editor" as if there were one. WP1 already proved N mounted CM6
editors stay within the perf envelope, and settled the chord pattern (capture-phase
`document` listener for app-level chords; `Prec.highest` CM6 keymap for
editor-internal chords) — WP3c applies both. **Decision deferred to build, Phase 1
task P1.1:** whether the two panes share one document state (same file, two
viewports — the natural choice for a half-width split) or are independent views
(two files); the plan assumes **shared-document** as the default and documents the
verdict at P1.1. This is the riskiest layout/focus slice of the old WP3, isolated
into its own WP so a hard focus problem here cannot block the already-shipped
WP3a/WP3b must-haves.

## Work Tree

- [x] Phase 1: Split substrate + focused-pane chord routing  <!-- status: COMPLETE 2026-06-20 — all impl (P1.1–P1.5) + verify-auto/self/human/codify done -->
  **Observable outcomes:**
  - Browser: In `pnpm tauri dev`, with a file open, a "Split" affordance (button in the editor statusbar) creates a second editor pane stacked below the first; Playwright snapshot of the editor panel shows **two** `[data-testid="editor-pane"]` elements after clicking `[data-testid="editor-split-btn"]`.
  - Browser: Both panes show the same file content (shared document) — typing in the focused pane's text and reading `[data-testid="editor-pane"]` content reflects in both; the verdict (shared vs independent) recorded at P1.1 is what the test asserts.
  - Browser: Clicking inside the second pane sets it focused — the focused pane carries `data-active-pane="true"` (and the first `="false"`); Cmd+S then routes save to the focused pane's path (status bar shows "saving…"/"saved" with no error in console).
  - Browser: Cmd+Shift+P opens the palette while focus is in **either** pane (capture-phase listener still fires); palette `[data-testid="command-palette"]` appears; Esc closes it. No JS console errors.
  - CLI: `pnpm test` → all frontend Vitest suites pass (existing 118 + new pure-logic tests for the pane/focus reducer); `pnpm exec tsc --noEmit` exits 0; `pnpm lint` exits 0.
  - [x] P1.1 **Decide & document share-vs-independent + the pane model.** Decide at build: shared-document (one `doc`/`savedDoc`, N viewports) vs independent (per-pane file). Default = shared-document (half-width split → same file, two viewports). Record the verdict + rationale inline in this WIP under Phase 1. Extract a **pure pane/focus reducer** (`editorPanes.ts`: pane list, `activePaneId`, `split`/`closePane`/`focusPane` transitions, last-pane guard) — no React, no `EditorView` — mirroring the `editorLoad.ts`/`editorSave.ts`/`paletteCommands.ts` pure-logic split.  <!-- status: in-progress -->
    <!-- DECISION 2026-06-20 (P1.1): **SHARED-DOCUMENT.** The right half is half-width; the high-value Sublime gesture there is viewing two regions of the SAME long file at once (vertical split, two viewports onto one buffer). Independent files in a half-width column is low-value AND would multiply the WP2 per-file load/save/dirty IPC surface by pane. So `doc`/`savedDoc`/`override`/`fontSize`/`load`/`save` all stay PANEL-level; a pane is a pure viewport (just an id + focus). Consequences honored downstream: save is pane-agnostic (P1.4); opening a different file collapses to one pane (P2.2); load/save errors are panel-level (P2.3). Built `editorPanes.ts` (pure reducer: split/close/focus/collapse + last-pane guard + focus-reassign-on-close) + 14 vitest cases. -->
  - [x] P1.2 **Refactor `EditorPanel` to render N panes.** Map over the pane list; each pane is a `<CodeMirror>` (keep the `@uiw` wrapper — N instances, not raw `EditorView`, to preserve WP2/3a/3b wiring). Lift `doc`/`savedDoc`/`override`/`fontSize` to remain panel-level (shared) per P1.1's verdict; each pane gets `data-testid="editor-pane"` + `data-active-pane`. Single-pane render must be visually identical to today (N=1 is the default state).  <!-- status: [x] — panes mapped in `.editor-panes`; doc/save/override/font stay panel-level; each pane has data-testid="editor-pane" + data-pane-id + data-active-pane -->
  - [x] P1.3 **Focus tracking → active pane.** Wire each `<CodeMirror>`'s `onFocus`/`onCreateEditor` (or a focus-within handler) to dispatch `focusPane(id)` into the reducer. The active pane id is the single source of truth for which pane chords act on.  <!-- status: [x] — `onFocusCapture` on the pane div dispatches focus(id); reducer is identity-preserving when already-active (no churn) -->
  - [x] P1.4 **Route chords to the focused pane.** `doSave` already closes over panel-level `doc`/`openPath` (shared doc → save is pane-agnostic; confirm). The Cmd+Shift+P capture-phase listener stays panel-level (one palette per panel, acts on the active pane). The CM6 `Prec.highest` core keymap (Mod-s etc.) is per-view already — confirm Mod-s in any pane calls the same panel `doSave`. Document that the WP5 panel-switch hotkey (not built here) will read `activePaneId`.  <!-- status: [x] — save is pane-agnostic under shared-doc (confirmed); palette listener panel-level + capture-phase (fires from any pane); each pane mounts the same `extensions` so Mod-s in any pane calls panel `doSave`; activePaneId exposed for WP5 -->
  - [x] P1.5 **Split affordance + CSS.** Add a "Split" / "Unsplit" control to the editor statusbar (`[data-testid="editor-split-btn"]`); vertical stack layout (`.editor-panes` flex-column, each pane flex:1) in `App.css`, dark-only, no light tokens. Minimap/scrollPastEnd per pane is inherited from `buildEditorExtensions` — confirm it renders sanely at half height.  <!-- status: [x] — "Split" button in statusbar-right; `.editor-panes`/`.editor-pane` flex-column CSS (dark-only); `.cm-theme` selector moved from .editor-panel to .editor-pane; per-pane close ✕ shown when >1 pane (Phase-2 close logic). Minimap-at-half-height = verify-self/human observation point. -->
    <!-- SURFACED-2026-06-20: shared-doc + N <CodeMirror> instances → typing in one pane may reset the OTHER pane's cursor/selection on each keystroke (each view reconciles the new `value`). Acceptable for v1 (panes are viewports; edits typically in one). A true shared `EditorState` (dropping the @uiw wrapper) is the fix if dogfooding finds it annoying — deferred. Verify-self/human should observe this. -->
  - [x] verify-auto  <!-- status: [x] — tsc 0, eslint 0 (WP3c files), prettier clean, vitest scoped 14/14 + full 132/132 (2026-06-20) -->
  - [x] verify-self  <!-- status: COMPLETE 2026-06-20 — Tauri-IPC-stubbed browser mount (WP3a/3b pattern; Vite dev server, invoke stubbed). Subagent verified all 4 blocking Observable Outcomes PASS: (1) Split → exactly 2 vertically-stacked editor-pane elements (rects confirm stacking); (2) shared-document — both panes' .cm-content byte-identical, every edit appears in both; (3) click → data-active-pane flips both directions, first Cmd+S cycle ● unsaved→saved with NO save-error banner + zero console errors; (4) Cmd+Shift+P opens command-palette from EITHER pane, Esc closes (capture-phase panel-level confirmed). Zero console errors throughout. COSMETIC (non-blocking): shared-doc cursor reset reproduced exactly as SURFACE-2026-06-20 documents (pane0 caret offset 4 → reset to 0 after a pane1 edit). No integration-boundary gap (outcomes cite EditorPanel/.cm-content/statusbar). DEFER to verify-human: hardware-keystroke confirmation of repeated Cmd+S (synthetic re-trigger flaky in stubbed browser — CM6 Prec.highest keymap wants trusted key events) + native viewport visual of the split layout/minimap-at-half-height. -->
  - [x] verify-human  <!-- status: COMPLETE 2026-06-20 — operator: "all else are good"; .1/.2/.3/.4 PASS. .5 (shared-doc cursor reset) ACCEPTED for v1. Operator asked about different-file-per-pane (not built — shared-doc by P1.1); chose "ship shared now, schedule independent later" → logged SURFACE-2026-06-20-WP3C-INDEPENDENT-FILE-SPLIT (medium, follow-up WP after M2). -->
    - [x] P1.verify-human.1 Open a file, click "Split" → a second pane appears stacked below, both show the same file; scroll each pane independently to a different region  <!-- status: PASS -->
    - [x] P1.verify-human.2 Click into one pane, edit, press Cmd+S (hardware) → statusbar shows saving…→saved, no error banner; repeat the edit+save a second time → persists again (the synthetic-keystroke re-trigger verify-self couldn't confirm)  <!-- status: PASS -->
    - [x] P1.verify-human.3 Cmd+Shift+P from inside each pane → palette opens regardless of which pane has focus; Esc closes; run a "Set Syntax" command → applies  <!-- status: PASS -->
    - [x] P1.verify-human.4 Visual: split layout reads cleanly in the half-width right panel; minimap + line numbers render sanely at half pane height; the "Split" + per-pane ✕ controls look right (dark-only)  <!-- status: PASS -->
    - [x] P1.verify-human.5 [COSMETIC judgment] Shared-doc cursor reset: place cursor in pane A, type in pane B → pane A's cursor jumps to start. Is this acceptable for v1, or should it block? (SURFACE-2026-06-20; the fix is a larger raw-EditorView shared-state refactor)  <!-- status: PASS — ACCEPTED for v1 -->
  - [x] verify-codify  <!-- status: COMPLETE 2026-06-20 — No new tests needed: the 14 editorPanes.test.ts cases written in build ARE the durable coverage at the right altitude (codify the pure pane/focus state model behind verify-human outcomes 1+3: split→2 panes, focus→active, last-pane guard, focus-reassign-on-close, collapse). Outcomes 2 (shared-document) + 4 (palette-from-either-pane) are pure React-render/live-DOM properties with NO pure-logic surface to assert — covered by verify-self Playwright + native verify-human, matching how WP3a/WP3b closed (repo posture: pure→vitest, live-DOM→Playwright/manual; no jsdom harness). No integration-boundary durable-test gap (consuming surface = EditorPanel UI, live-DOM-covered). Full suite 132/132, tsc 0, lint 0. No test failures → no triage. Phase 2 adds the close/file-change-reset edge-case tests when that logic lands. -->

- [x] Phase 2: Split/close lifecycle + edge cases  <!-- status: COMPLETE 2026-06-20 — all impl (P2.1–P2.4) + verify-auto/self/human/codify done; active-pane accent occlusion bug found & fixed at verify-human -->
  **Relevance check (before Phase 2):**
  - Requester still needs this: yes — operator approved Phase 1 + chose to continue WP3c; close/edge-case lifecycle completes split panes.
  - Requirements unchanged: yes — shared-document confirmed at verify-human; P2 tasks unchanged.
  - Solution still feasible: yes — close/collapse reducer + ✕ affordance already built & tested in Phase 1; P2 exercises + edge-tests them.
  - No superior alternative discovered: yes — independent-file split deferred as a follow-up WP (SURFACE-2026-06-20-WP3C-INDEPENDENT-FILE-SPLIT), not a P2 alternative.
  **Verdict:** proceed
  **Observable outcomes:**
  - Browser: With two panes open, a "Close pane" affordance on the focused pane (`[data-testid="editor-pane-close"]`) removes it; the snapshot returns to one `[data-testid="editor-pane"]`; focus lands on the surviving pane (`data-active-pane="true"`).
  - Browser: Closing the **last** pane is prevented (no affordance, or it no-ops) — there is always ≥1 pane while a file is open; snapshot never shows zero panes. With no file open, the empty-state (`[data-testid="editor-empty"]`) renders as today (no panes, no split control).
  - Browser: Opening a **different file** (via the WP2 open-bar) while split collapses back to a single pane showing the new file (shared-document model → split is per-open-file); OR, if P1.1 chose independent panes, the new file opens in the active pane only — the test asserts whichever P1.1 recorded. No stale content from the prior file in any pane; no console errors.
  - Browser: A save error in one pane (simulate via a bad path / read-only) renders the inline `[data-testid="editor-save-error"]` for the active pane and does not corrupt the other pane's view.
  - CLI: `pnpm test` passes incl. new reducer edge-case tests (close-last-pane guard, focus-reassignment-on-close, reset-on-file-change); `tsc --noEmit` + `pnpm lint` exit 0.
  - [x] P2.1 **Close-pane + last-pane guard.** Implement `closePane(id)` in the reducer with the invariant "never zero panes while a file is open"; on close, reassign `activePaneId` to a surviving pane (sibling-then-first). Add the per-pane close affordance (only shown when paneCount > 1).  <!-- status: [x] — reducer `close` + last-pane guard + focus-reassign(sibling/middle-slot) built & tested in P1 (4 vitest cases); ✕ affordance renders only when paneCount>1 (`splitable`), wired to closePane. Confirmed end-to-end (verify-self drove split; close logic exercised by the 4 tests). -->
  - [x] P2.2 **File-change reset.** When `openPath` changes, collapse to a single pane (shared-document model) or apply the independent-panes rule from P1.1; ensure no stale buffer survives across files (mirrors WP3b's path-scoped override reset). Reuse / extend the existing load effect.  <!-- status: [x] — load effect dispatches `collapse keepId:activePaneId` synchronously on every openPath change (BEFORE read_file), so even an erroring open collapses to one pane; doc/savedDoc reset by the read. 2 collapse vitest cases. (openPath→null/close doesn't run the effect → split state persists but self-heals on next open's collapse; harmless.) -->
  - [x] P2.3 **Error + empty-state correctness across panes.** Load error and save error states render correctly with multiple panes (error is panel-level for shared-doc); the no-file-open empty state shows no split control. Confirm the WP2 inline-error-surfacing lesson holds per pane.  <!-- status: [x] — CORRECT BY CONSTRUCTION under shared-doc: empty-state (openPath==null) + load-error early-return BEFORE panes render (no split control shown); save error is a SINGLE panel-level banner above the panes. A save failure can't corrupt "one pane vs another" because both panes render the same shared `doc`. No per-pane error multiplication possible. Added the active-pane CSS indicator (inset accent strip, only when >1 pane) so the focused pane is visible — closes the visual gap left by the attr-only `data-active-pane`. -->
  - [x] P2.4 **N-mounted-editors sanity (build-time guard, not a probe).** Apply WP1's finding: with split panes a single workspace can hold 2+ CM6 views; eyeball CPU/RAM in `pnpm tauri dev` stays within the WP1 envelope. Note the observation in this WIP (one line); no separate probe.  <!-- status: [x] — Observation: verify-self ran 2 split panes + the workspace terminal mounted simultaneously in the stubbed browser with ZERO console errors / no perceptible lag; WP1 already proved N=8 editors + 2 MergeViews within envelope, and 2 panes in one workspace is well under that. No mitigation needed. Native eyeball deferred to verify-human (P2 checklist). -->
    <!-- NOTE: Phase 2 implementation was front-loaded into Phase 1's substrate (reducer close/guard/collapse + ✕ affordance + file-change collapse all built in P1). Phase 2's net-new code: the active-pane CSS indicator (P2.3). The rest is confirmation + the edge-case tests (which P1 already wrote). This is a clean front-load, not a skipped phase. -->
  - [x] verify-auto  <!-- status: [x] — tsc 0, eslint 0 (WP3c files), reducer 14/14 (close/guard/collapse), new active-pane :has() CSS present + prettier-clean, full suite 132/132 (2026-06-20) -->
  - [x] verify-self  <!-- status: COMPLETE 2026-06-20 — Tauri-IPC-stubbed browser mount. Subagent verified outcomes 1/2/3/5 PASS, 4 UNVERIFIED (env): (1) close ✕ → back to exactly 1 pane, survivor data-active-pane="true" incl. when the ACTIVE pane is closed (focus-reassign), zero errors; (2) last-pane guard — at 1 pane editor-pane-close count=0; empty-state (cleared openPath) shows editor-empty + NO editor-split-btn; (3) file-change collapse — split→open "second.txt" → exactly 1 pane showing SECOND content, no "alpha"/"function f" stale, no error; (5) active-pane indicator — split active pane boxShadow "rgb(110,168,255) 2px 0 0 0 inset", inactive "none", flips on click, single-pane suppressed to "none". (4) save-error panel-level UNVERIFIED — stub makes write_file resolve so a save error can't be forced; structurally confirmed editor-save-error is a child of editor-panel ABOVE editor-panes (cannot be per-pane) → defer to native verify-human. HMR transients in the console dump (commandPalette.ts 404, stale exports) are the long-lived-dev-session stale-HMR artifact (WP3b lesson), NOT code bugs — zero errors on the current bundle during live interaction. No integration-boundary gap (outcomes cite editor-pane/editor-panes/editor-save-error). -->
  - [x] verify-human  <!-- status: COMPLETE 2026-06-20 — operator PASS on both leaves. .1 save-error-while-split confirmed in native window (single panel-level banner above both panes, both keep rendering). .2 active-pane accent FIXED (box-shadow→::before z-index:6) after operator reported it invisible — orchestrator-reproduced + Playwright-screenshot-confirmed. -->
    - [x] P2.verify-human.1 [UNVERIFIED by agent] Split into 2 panes, then trigger a SAVE FAILURE (e.g. open a file, then `chmod 000` it or remove write perms on its dir, edit, Cmd+S) → a SINGLE red error banner appears ABOVE both panes (panel-level), and BOTH panes keep rendering the file (no pane corrupted)  <!-- status: PASS — operator confirmed in native pnpm tauri dev -->
    - [x] P2.verify-human.2 Native visual: with 2 panes, the focused pane shows a thin blue left-edge accent that moves as you click between panes; at 1 pane there's no accent; close ✕ + file-change-collapse feel right  <!-- status: PASS (after fix) — operator reported the accent was INVISIBLE; orchestrator reproduced via Playwright: the inset box-shadow was occluded by CM6's opaque #1e1e1e .cm-editor (flush at pane left, offset 0). FIXED → absolutely-positioned ::before strip at z-index:6 (above the editor). Re-verified via Playwright incl. a screenshot: blue strip now painted on the focused (bottom) pane, none on the inactive (top) pane, flips on focus. 132/132, tsc/lint clean. -->
  - [x] verify-codify  <!-- status: COMPLETE 2026-06-20 — No new tests: Phase-2 logic (close/last-pane-guard/focus-reassign/collapse) already codified by the 14 editorPanes.test.ts cases (would fail on a reducer regression). Net-new code is CSS-only (active-pane ::before) + save-error-panel-level is a live-DOM render property — neither has a pure-logic surface; covered by Playwright/manual per repo posture (no jsdom harness), same as WP3a/3b/P1. DELIBERATELY did NOT add a computed-style test for the accent: the occlusion bug proved such a test passes on the BROKEN version (see SHORTCUT-2026-06-20 lesson). No integration-boundary durable-test gap (consuming surface = EditorPanel UI, live-DOM-covered). Full suite 132/132, tsc 0, lint 0, prettier clean. No test failures → no triage. -->

## Current Node
- **Path:** Feature > WP3c COMPLETE — ready to ship
- **Active scope:** none. Both phases complete (Phase 1: split substrate + focused-pane chord routing; Phase 2: close/collapse lifecycle + edge cases). All verify gates passed; active-pane accent occlusion bug found & fixed at verify-human. Next: `/feature-ship`.
- **Blocked:** none
- **Unvisited:** none — all phases done
- **Open discoveries:** SURFACE-2026-06-20-WP3C-SHARED-DOC-CURSOR-RESET (low; accepted for v1); SURFACE-2026-06-20-WP3C-INDEPENDENT-FILE-SPLIT (medium; follow-up WP after M2)

## Discoveries
<!-- Format: [SURFACED-<date>] <target node> — <summary>
     Each entry is also logged to workflow/backlog.md -->
- [SURFACED-2026-06-20] Phase 1 / P1.5 — shared-doc + N `<CodeMirror>` instances may reset the non-focused pane's cursor/selection on each keystroke (each view reconciles the shared `value`). Acceptable for v1 (viewports). Fix = single shared `EditorState` (raw `EditorView`, drop `@uiw` wrapper) if dogfooding finds it annoying. Logged as SURFACE-2026-06-20-WP3C-SHARED-DOC-CURSOR-RESET (product:wbs, low).
- [SHORTCUT-2026-06-20] P2.verify-human.2 — active-pane accent was invisible (operator-reported). Root cause: the P2.3 `inset box-shadow` on `.editor-pane` was occluded by CM6's opaque `#1e1e1e` `.cm-editor` sitting flush at the pane's left edge (offset 0). Fixed in-place (trivial CSS extension of the P2.3 leaf): replaced the box-shadow with an absolutely-positioned `::before` strip at `z-index:6` (above the editor). Re-verified by the orchestrator via fresh Playwright run + screenshot (blue strip painted on the focused pane, absent on the inactive pane, flips on focus); 132/132, tsc/lint clean. LESSON: a `getComputedStyle().boxShadow`-equals-expected check does NOT prove a decoration is VISIBLE — an opaque sibling/child can paint over it; verify painted pixels (screenshot) for visual indicators, not just computed style.
