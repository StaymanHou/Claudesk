# Feature: M9 WP7 — Deprecate standalone claude-time + milestone-exit verify

**Workflow:** feature
**State:** finalize (complete) — COMPLETED 2026-07-16
**Created:** 2026-07-16
**Drive mode:** autopilot

## Problem Statement

M9 absorbed the standalone `claude-time` time-tracker into Claudesk as a native, default-OFF right-panel analytics tab (WP1–WP6.5 shipped). WP7 is the M9 closer: retire the standalone tool now that Claudesk owns the capability, and resync the durable docs (`CLAUDE.md` + `arch.md`) with the as-built subsystem so the architecture record matches reality before the milestone closes. This is a **documentation-resync + dependency-decoupling-confirmation** feature — not net-new app code. The three tasks (WBS WP7): (1) document the standalone retirement + confirm Claudesk has **no runtime dependency** on `claude-time-hook.pl` (ground-truth confirmed: Claudesk installs its own `claudesk-hook.pl`; the only in-tree `claude-time` references are port-lineage comments); (2) resync `arch.md` line 245 ("four events" → the as-built **10** `CLAUDESK_EVENTS`), line 222 ("no SQLite, no app-managed DB" → carve out the M9 feature-local analytics DB as a scoped exception governing the *project list*, not analytics), line 250 (`claude-time` "coexistence" → deprecation), add a dedicated M9 time-analytics arch section (`time_store` + per-identity SQLite DB + `reclassify` module + query layer + dashboard tab + the universal `time_tracking_enabled` feature-flag pattern), fold in the two carried arch-resync SURFACEs (the global-dashboard **mount-scope=data-scope** principle, `SURFACE-2026-07-08...GLOBAL`; and note the session-termination model as-built), and refresh the `CLAUDE.md` "Current Milestone" + Key-Decisions surface for M9-complete; (3) milestone-exit verify — the doc/decoupling slice the agent CAN verify statically + via build; the **installed-`.app`** exit-verify (Finder-launched, GUI-PATH + hook-registration parity) is genuinely operator-only per `[[installed-build-verify-deferred-to-release]]` and is CARRIED to the operator / `/release` gate.

## Work Tree

- [x] Phase 1: Deprecate + doc-resync (all three WP7 tasks — one coherent doc-sync unit)  <!-- status: done — all impl (P1.1–P1.5) + verify-auto + verify-self + verify-human(auto-skipped) + verify-codify(no-op-with-rationale) complete; all children [x]. -->
  **Relevance check (single-phase feature — no phase-advance gate needed):** N/A — the plan has one phase; no Phase 2 to gate.
  **Observable outcomes:**
  - CLI (dependency-absence): `grep -rn "claude-time-hook\.pl\|claude_time_hook" src-tauri/src src/` returns ZERO runtime references (only comment/doc lineage mentions of `claude-time` remain — a runtime dependency on the *standalone* hook script does not exist). Documented in the retirement note.
  - CLI (arch.md — event set resynced): `grep -n "four events" docs/product/arch.md` returns NOTHING (the stale "four events" claim is gone); `grep -n "10\|ten" docs/product/arch.md | grep -i "event"` shows the as-built 10-event set (`CLAUDESK_EVENTS: [&str; 10]`) narrated, with the 4 status events vs. 6 time-analytics-only events distinguished.
  - CLI (arch.md — SQLite exception carved): `grep -n "SQLite\|sqlite" docs/product/arch.md` shows the M9 feature-local analytics DB documented as a **scoped exception** — the "no SQLite / flat JSON" rule is re-stated as governing the **project list** only; the analytics store is a per-identity SQLite DB at the app-data dir.
  - CLI (arch.md — deprecation, not coexistence): `grep -n "claude-time" docs/product/arch.md` no longer frames it as live "coexistence"; a deprecation/retirement note is present (standalone tool retired; Claudesk owns the capability; manual removal of a user's own `claude-time-hook.pl` registration is documented as the user's action — Claudesk does NOT delete a hook it didn't install).
  - CLI (arch.md — M9 subsystem section added): `grep -n "time_store\|reclassify\|time-analytics\|time_tracking_enabled" docs/product/arch.md` shows a dedicated M9 time-analytics architecture section covering `time_store` (write side), `reclassify` (pure-logic redesign), the query layer, the dashboard tab, and the `time_tracking_enabled` default-OFF feature-flag write-gate.
  - CLI (arch.md — carried SURFACEs folded): `grep -n "mount-scope\|data-scope\|session.termination\|reconcil" docs/product/arch.md` shows the global-dashboard mount-scope=data-scope principle AND the session-termination model recorded as as-built arch notes.
  - CLI (CLAUDE.md refreshed): `grep -n "M9\|Milestone 9" CLAUDE.md` shows the Current-Milestone block updated to M9-complete (all WPs shipped) rather than mid-execution; no stale "WP7 is next" framing remains in the Current-Milestone status.
  - CLI (build smoke — docs-only change compiles clean): `cargo build` (in `src-tauri/`) exits 0 and `pnpm tsc --noEmit` exits 0 — confirms the doc-resync introduced no accidental code edits that break the build (defensive; expect no code touched).
  - Backlog: the two carried WP7-resync SURFACEs (`SURFACE-2026-07-08...GLOBAL` arch-writeup carry; the claude-time absorb/deprecate closure) are resolved/updated per the delete-on-resolve rule at finalize — noted here as the finalize target, not done in build.
  - [x] P1.1 Confirm + document the dependency decoupling: run the dependency-absence grep across `src-tauri/src` + `src/`; author the standalone-`claude-time` retirement note in `arch.md` (Claudesk owns the capability; no runtime dep on `claude-time-hook.pl`; document the user-side manual removal path — Claudesk stops *depending*, does not *delete*).  <!-- status: done — grep confirms ZERO runtime refs to claude-time-hook.pl; only a doc-comment (mod.rs:14) + a preservation test-fixture (settings_with_claude_time / merge_is_additive_and_preserves_existing_hooks — the proof of side-by-side/no-delete design). Retirement note added to arch.md Key Decisions. -->
  - [x] P1.2 Resync `arch.md` — event set (§245: 4→10, distinguish status vs time-analytics-only events, point to `wp1-time-analytics-probe-outcome.md` §(d) for the delta), SQLite scoped-exception (§222), deprecation-not-coexistence (§250).  <!-- status: done — §245 rewritten to the 10-event set (4 status + 6 time-analytics-only, wire fields extended); §222 rewritten with the SQLite scoped-exception carve-out; §250 replaced with the deprecation note. -->
  - [x] P1.3 Add the dedicated M9 time-analytics architecture section to `arch.md` — `time_store` write side + per-identity SQLite DB path, `reclassify` redesign module, query layer (rows→segment DTOs), dashboard tab (day/week/month/metrics/compare), the `time_tracking_enabled` default-OFF universal feature-flag write-gate; fold in the carried mount-scope=data-scope principle + the WP6.5 session-termination model as as-built notes.  <!-- status: done — new "## Milestone 9 architecture" section appended (mermaid + as-built modules: write side, feature-local SQLite DB path, native-signal source, reclassify REDESIGN, session-termination model, query layer w/ ms-precision + minute-quant fix, GLOBAL dashboard mount-scope=data-scope, universal feature-flag). -->
  - [x] P1.4 Refresh `CLAUDE.md` — Current-Milestone block to M9-complete (WP7 done, ready for `/product-finalize`); update Key-Decisions/Tech-Stack surface where it still describes M9 as in-flight or omits the absorbed subsystem; keep it terse (no full arch dump — CLAUDE.md points, arch.md details).  <!-- status: done — Current-Milestone header → "✅ ALL WPs SHIPPED"; WP7 status line filled in; new M9 Status bullet added; roadmap-order line M9→"awaiting finalize", M10 next. -->
  - [x] P1.5 Build smoke — `cargo build` + `pnpm tsc --noEmit` exit 0 (confirm the docs-only change touched no code).  <!-- status: done — tsc --noEmit exit 0 (228ms); cargo build exit 0 (13.60s incremental, no errors). git status confirms only docs + WIP changed, no code. Registry updated. -->
  - [x] verify-auto  <!-- status: done — 13/13 observable-outcome content greps pass (dependency-absence 0 refs; arch.md 4→10 + status/time split + SQLite exception + deprecation + M9 section + mount-scope/session-termination folded; CLAUDE.md M9-complete). Build smokes green (tsc --noEmit exit 0, cargo build exit 0). One grep flagged FAIL was a pattern artifact (markdown italics in "*time-analytics-only* events") — content confirmed present. -->
  - [x] verify-self  <!-- status: done — docs-only resync, NO integration boundary (no code file backing any endpoint/UI/CLI/job was touched; git diff --name-only = docs only, zero code files), NO running-system surface (no Playwright/MCP-bridge runner spawned per the project convention against a non-existent surface). Static slice PASS: code CLAUDESK_EVENTS[10]==arch.md; the 6 time-analytics events match the code set exactly; DB basename + default-OFF flag claims match code; WIP tree internally consistent. No blocking, no cosmetic. Two human checks carried to verify-human: (1) operator accuracy review of arch.md/CLAUDE.md vs as-built; (2) operator-only installed-.app milestone-exit smoke → /release gate. -->
  - [x] verify-human  <!-- status: AUTO-SKIPPED per drive_mode=autopilot — all 4 auto-skip gates clean (autopilot; verify-self all-PASS; NO integration boundary — docs-only, zero code files; no outcome cites a modified consuming surface). Affirmation + read-time-veto pointers printed in chat. The two operator checks below are the read-time-veto items (accuracy review) + a carried-to-release item (installed-.app), neither blocking WP7's close. -->
    - [x] Operator reviews the resynced `arch.md` + `CLAUDE.md` for accuracy against the as-built subsystem  <!-- status: AUTO-SKIP read-time-veto — surfaced in chat with specific pointers (arch.md "## Milestone 9 architecture" + the 3 edited Key Decisions + CLAUDE.md Current-Milestone). Operator may `/feature-build` this leaf if a mischaracterization needs correction. Docs cross-checked against as-built code at verify-self (event set, DB path, default-OFF flag all match). -->
    - [x] **CARRIED (operator-only, → `/release` gate):** installed-`.app` milestone-exit smoke test — Finder/Dock-launched build, GUI-PATH parity, hook registration (10 events present in `~/.claude/settings.json`), toggle ON → a day of usage → per-project breakdown renders in the native tab; toggle OFF → zero storage/IO + status dots unaffected. Per `[[installed-build-verify-deferred-to-release]]` + the installed-build smoke-test convention (this WP touches hook registration + external-process env).  <!-- status: CARRIED to operator / next /release gate — not blocking WP7 close (per installed-build-verify-deferred-to-release standing preference; M9 milestone-exit criterion verified at release, like M5/M6/M7 before it). -->
  - [x] verify-codify  <!-- status: done — NO codified test warranted (no-op-with-rationale, NOT a skipped obligation). This phase introduced ZERO runtime behavior (docs-only resync — arch.md/CLAUDE.md/wbs.md/runtimes.md, no code files), so there is no behavior a test could pin against regression. The candidate guard (a test coupling arch.md's "10 events" prose to CLAUDESK_EVENTS array size) was rejected as over-engineering: it would assert on documentation not behavior, break on any arch.md reword, and add false-failure noise to guard a one-time point-in-time architecture record (future drift is caught by the next milestone's WP7-style resync, not CI). The verification that mattered (docs↔code cross-check: event set / DB path / default-OFF flag) already ran as verify-auto + verify-self greps. Regression sanity: full existing suite re-run green — 524 lib + 6 integ backend + 101 files / 1120 frontend, 0 failures — confirming the docs change broke nothing. No integration boundary. -->

## Test Triage
_(none — no test failed; regression suite all-green: 524 lib + 6 integ backend + 1120 frontend.)_

## Current Node
- **Path:** Feature > finalize (COMPLETE) — archived 2026-07-16, commit `3c6bfa4`
- **Active scope:** none — feature fully closed
- **Blocked:** none
- **Unvisited:** none — single-phase feature fully verified + reviewed + finalized
- **Open discoveries:** none
- **Review-quality:** 0 CRITICAL / 0 MAJOR / 3 MINOR (1 fixed in place, 2 auto-backlogged). No refactor invoked.
- **Outcome:** WBS WP7 ✅ → all M9 WPs shipped → F30 to `/product-finalize` (cycle-close). No tech debt (docs-only). Backlog resolved: SURFACE-2026-07-08-M9-WP6A-DASHBOARD-IS-GLOBAL-NOT-PER-WORKSPACE. Local commit only (16 ahead of origin/main, not pushed — batched M9 tree).

## Discoveries
<!-- Format: [SURFACED-<date>] <target node> — <summary>
     Each entry is also logged to workflow/backlog.md -->
- Ground-truth captured at plan time (2026-07-16): (a) `CLAUDESK_EVENTS: [&str; 10]` at `src-tauri/src/hook_install/mod.rs:74` — 4 status (`UserPromptSubmit`/`Stop`/`Notification`/`PostToolUse`) + 6 time-analytics-only (`PreToolUse`/`PostToolUseFailure`/`SubagentStart`/`SubagentStop`/`SessionStart`/`SessionEnd`); the code doc-comment already narrates the 4→10 delta + points to `wp1-time-analytics-probe-outcome.md` §(d). (b) `time_tracking_enabled: Option<bool>` in `config_store/settings.rs`, default `false`, gates the write path at `lib.rs:248` via `time_store::commands::tracking_enabled` — the universal feature-flag. (c) No runtime `claude-time` dependency in `src-tauri/src` or `src/` — only port-lineage comments in `time_store`/`reclassify`. (d) arch.md resync anchors: L222 (SQLite), L245 (4 events), L250 (coexistence). (e) Backlog directs two extra arch-resync carries HERE: `SURFACE-2026-07-08...GLOBAL` (mount-scope=data-scope, lines 126/128) + the session-termination model as-built note.
- [SHORTCUT-2026-07-16] review-quality MINOR #1 (arch.md:249 hook-schema omitted `source`/`prompt_length_chars`) — fixed IN PLACE during review-quality rather than backlogged: it was a one-line factual gap I introduced in this very diff, in the exact section under review; correcting it (+ disambiguating wire-`source` from the DB `source` column) strengthens the section. Re-verified: verify-auto content greps still pass. The other 2 MINOR (wbs.md stale frontmatter → deferred to /product-finalize by design; CLAUDE.md WP2 "5 wire fields" → pre-existing WP2-era text) auto-backlogged.

## Code-Quality Review — m9-wp7-deprecate-claude-time

Reviewer (code-quality-reviewer subagent) cross-checked every material architectural claim against source; all held. **0 CRITICAL / 0 MAJOR / 3 MINOR.**

### Strengths
- The three highest-stakes claims (10-event `CLAUDESK_EVENTS` 4/6 split, `time_tracking_enabled` default-`false`, `time-analytics.sqlite` per-identity WAL+busy_timeout) all match source verbatim — grounded in real code.
- SQLite scoped-exception carve-out (§222) re-states the flat-JSON rule as project-list-only + justifies the exception (query-shaped, high-volume, multi-writer).
- Deprecation note nails "stop depending, do not delete" + cites `merge_is_additive_and_preserves_existing_hooks` as the proof.
- Every cross-ref resolves (`wp1-time-analytics-probe-outcome.md` §(d) + all 5 SURFACE IDs real). Subtle as-built details correct (WP6.5 precedence, `SESSION_IDLE_CAP_MS`=30min, `dur_ms` ms-precision, 6-kind Family split).

### Issues
**CRITICAL** — (none)
**MAJOR** — (none)
**MINOR**
- [arch.md:249] Hook JSON-line schema omitted `source` + `prompt_length_chars` (both emitted; `source` is central to the M9 section). — **FIXED IN PLACE** (added both + disambiguated wire-`source` [CC SessionStart tag] from the DB `source` column). Not backlogged.
- [wbs.md:5] Frontmatter `updated: 2026-07-15` / `state: complete` / "only WP7 remains" now stale post-WP7. — AUTO-BACKLOGGED (low); resync deferred to `/product-finalize` by design (this diff correctly only stripped the resolved session-pause block).
- [CLAUDE.md:162] Unchanged WP2 status line says "10-event hook + **5 wire fields**"; actual new-field count is 6. — AUTO-BACKLOGGED (low); pre-existing WP2-era text, not introduced by this diff.

### Assessment
High-quality documentation resync — every material architectural claim cross-checked against source and held (event set, DB shape/path, default-OFF gate wiring, decoupling posture, reclassifier-is-redesign, session-termination precedence, minute-quantization fix all accurate). The new "Milestone 9 architecture" section is well-structured and *why*-bearing. Advances the architecture record rather than accruing debt. Only finding of substance was a within-section field-enumeration gap, fixed in place.

### If you disagree
Operator: dismiss any backlogged finding by editing this section and marking the line `[DISMISSED]` before `feature-finalize` archives the WIP.

## Retrospect
- **What changed in our understanding:** WP7 was framed in the WBS as "*remove* the separate `claude-time-hook.pl` registration / decide whether Claudesk un-registers it." The build revealed the decoupling was **already complete by construction** — Claudesk never depended on the standalone hook at all (it installs its own `claudesk-hook.pl` and matcher-groups are keyed on that basename). The `merge_is_additive_and_preserves_existing_hooks` test is the *proof* Claudesk deliberately preserves a co-resident `claude-time-hook.pl` rather than touching it. So the "decide the un-register path" open question collapsed to "confirm + document the already-correct posture" — WP7 was smaller and cleaner than its WBS description implied.
- **Assumptions that held:** the whole feature was docs-only (zero code files changed, as planned); the as-built ground-truth matched the code exactly (event set 10, DB path, default-OFF flag all verified); the installed-`.app` exit-verify is operator-only and carried to the release gate (as every prior milestone exit was).
- **Assumptions that were wrong:** none material. One small self-introduced gap (the hook-schema wire-field line omitted `source`/`prompt_length_chars`) surfaced at review-quality and was fixed in place — a good-catch, not a wrong assumption.
- **Approach delta:** single-phase `/feature-plan` (skipped `/feature-spec` — no requirements ambiguity, the WBS task list *was* the spec). verify-self ran the honest static slice (docs↔code cross-check) rather than spawning a live-surface runner against a non-existent surface (docs-only change has no running surface). verify-human auto-skipped per the autopilot gate (no integration boundary) with the doc-accuracy review surfaced as a read-time veto. All consistent with the project's documented verify-posture conventions.

## Communicate
> **Feature complete:** M9 WP7 (Deprecate standalone claude-time + doc-resync) has shipped. Claudesk's architecture docs (`arch.md` + `CLAUDE.md`) now record the as-built M9 time-analytics subsystem — the absorbed 10-event hook, the feature-local SQLite store, the reclassifier redesign, the dashboard tab, the session-termination model, and the default-OFF `time_tracking_enabled` gate — and the standalone `claude-time` tool is documented as deprecated (Claudesk owns the capability; it has zero runtime dependency on `claude-time-hook.pl` and never deletes a hook it didn't install). This **completes all M9 work packages** — the milestone is ready for `/product-finalize` to close the cycle. Verify by reading `docs/product/arch.md` → "Milestone 9 architecture"; the installed-`.app` milestone-exit smoke test is carried to the next `/release` gate.
>
> Requester = operator — closure notice for self-record.
