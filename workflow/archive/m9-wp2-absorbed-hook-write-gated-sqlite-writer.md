# Feature: M9 WP2 — Absorbed hook + write-gated SQLite writer

**Workflow:** feature
**State:** COMPLETED 2026-07-07 — shipped dc3b89e (local); review-quality clean (3 MINOR auto-backlogged); archived at finalize.
**Created:** 2026-07-07
**drive_mode:** autopilot

## Problem Statement

M9 absorbs the standalone `claude-time` tracker into Claudesk. WP1 (probe) froze the segment-model contract and enumerated the exact hook delta. **WP2 builds the substrate the reclassifier (WP3) will read: extend Claudesk's CC hook from 4→10 events, add 4 wire fields, and persist every event into a per-identity SQLite DB — gated so it only writes when tracking is ON.** The write path must be a *second, gated* consumer of the existing `HookEvent` stream: the M3 status machine (idle/running/awaiting dots) MUST stay byte-for-byte unchanged. Per the absorption decisions, the Perl hook stays a pure socket-forwarder (no `sqlite3` subprocess like claude-time's) — the **Rust `time_store` module owns the DB write**. The `events` schema must carry a `source` discriminator (`cc-hook` vs `claudesk-native`) so WP2.5 can write native-signal rows into the same store. Privacy invariant throughout: prompt **length only, never text**; never tool I/O.

**Inputs (frozen by WP1):**
- `docs/product/wp1-time-analytics-probe-outcome.md` §(d) — the 4→10 event delta + 4 wire fields + `PostToolUseFailure` confirmed distinct + the SQLite `events(ts, session_id, cwd, event, tool_name, agent_type, meta)` schema.
- `docs/product/wbs.md` → WP2 (tasks) + WP2.5/WP5 notes (schema must be WP2.5-source-aware; the write-gate is read by WP5's toggle later).
- Reference: `_ref/claude-customization/tools/claude-time/hook.pl` (the per-event field extraction + `meta` JSON shape + WAL/`.timeout` bootstrap) — Claudesk ports the *field mapping* and *schema*, NOT the direct-sqlite3 write (Rust owns the write).

**Key seam facts (surveyed):**
- `hook_install/mod.rs::CLAUDESK_EVENTS` = 4 events; registration is additive/idempotent/reversible/per-identity — extend the const, tests re-green.
- `resources/claudesk-hook.pl` forwards a fixed field set + `timestamp`; extend it with per-event field extraction (`prompt_length_chars`, `tool_use_id`, `agent_type` from `subagent_type`, `source`), preserving exit-0-unconditional + privacy.
- `hook_socket/mod.rs::HookEvent` + `parse_line` model the wire; add the new snake_case fields (`#[serde(default)]`) + key-shape tests.
- `status_broadcaster::event_to_state` maps only the 4 current events and drops unmapped ones — a new `PreToolUse`/`SubagentStart`/etc. must NOT flip a dot (verify the drop).
- **The fan-out seam is `lib.rs:194-217`:** `start_on_launch` returns one `mpsc::Receiver<HookEvent>` handed *once* to `start_broadcaster`. `mpsc::Receiver` is single-consumer — adding `time_store` as a second drain requires a tee/fan-out (the accept-loop sends to N senders, or a fan-out thread re-broadcasts).
- New dep: **`rusqlite`** (bundled SQLite) — the one net-new arch element M9 introduces (WBS "Net-new architecture"; scoped exception to "flat JSON, no DB" which governs the *project list*, not this feature-local analytics DB). Reconciled into `arch.md` at WP7.

## Work Tree

- [x] Phase 1: Extend the hook + wire contract (4→10 events, +4 fields, status path unchanged)  <!-- status: [x] COMPLETE — all impl + verify nodes done -->
  **Observable outcomes:**
  - CLI: `cargo test -p claudesk` (or `cargo test` in `src-tauri/`) exits 0 — new `parse_line` key-shape tests for `prompt_length_chars` / `tool_use_id` / `agent_type` / `source` pass; existing M3+QoL-WP2 hook_socket + hook_install tests stay green (302 baseline + new).
  - CLI: a Rust unit test asserts `CLAUDESK_EVENTS` == the 10-event set (`UserPromptSubmit`, `Stop`, `Notification`, `PostToolUse`, `PreToolUse`, `PostToolUseFailure`, `SubagentStart`, `SubagentStop`, `SessionStart`, `SessionEnd`) AND that `merge_claudesk_hooks` registers one Claudesk group per event over the production-shape fixture (additive/idempotent/reversible still hold at N=10).
  - CLI: a status-invariant test — feeding a `PreToolUse` / `SubagentStart` / `SessionStart` / `SessionEnd` / `PostToolUseFailure` `HookEvent` through `status_broadcaster::event_to_state` returns `None` (no dot flip); the 4 mapped events still map as before.
  - CLI: privacy key-shape test — a parsed `UserPromptSubmit` line carries `prompt_length_chars` (int) and the port's field-extraction never emits raw `prompt` text into the time-row path (assert the length-only field; raw `prompt` still allowed for the status snippet, unchanged).
  - CLI: `perl -c src-tauri/resources/claudesk-hook.pl` exits 0 (the extended hook still compiles under macOS stdlib Perl).
  - [x] P1.1 Extend `CLAUDESK_EVENTS` (4→10) in `hook_install/mod.rs`; update the const doc; re-green the registration tests (additive/idempotent/reversible/per-identity over the 10-event set). Update the `settings_with_claude_time()` fixture to the 10-event production shape.  <!-- status: [x] done — const is [&str;10], fixture built via loop over CLAUDESK_EVENTS, test renamed claudesk_events_is_the_full_ten_event_set -->
  - [x] P1.2 Extend `resources/claudesk-hook.pl`: per-event field extraction mirroring claude-time's handler table — `prompt_length_chars` (length only) on UserPromptSubmit; `tool_use_id`+`tool_name` on Pre/Post/PostToolUseFailure; `agent_type` (from `subagent_type`) on SubagentStart/Stop; `source` on SessionStart. Preserve exit-0-unconditional, the `message`/`notification_type` forwarding for status, and the write-failed status-channel.log trace. Keep raw `prompt` forwarding (status still needs the snippet) — length is an ADDITIONAL field, not a replacement.  <!-- status: [x] done — perl -c OK; privacy: prompt_length_chars = length()+0 only -->
  - [x] P1.3 Extend `HookEvent` + `parse_line` in `hook_socket/mod.rs`: add `prompt_length_chars: Option<u64>`, `tool_use_id`/`tool_name`/`agent_type`/`source: Option<String>` (all `#[serde(default)]`, snake_case verbatim, no `rename_all`). Update the module wire-contract doc-comment. Add per-field key-shape unit tests + a camelCase-rejection guard covering the new fields.  <!-- status: [x] done — 5 new hook_socket tests pass (added tool_name beyond the plan's 4 — reclassifier needs per-tool rollups per WP1 §d) -->
  - [x] P1.4 Add the status-invariant test in `status_broadcaster` proving the 6 new events map to `None` (no dot flip) and the 4 existing map unchanged.  <!-- status: [x] done — m9_time_analytics_events_are_status_neutral passes; ev()/notif() helpers extended with the 5 new None fields -->
  - [x] verify-auto  <!-- status: [x] done — scoped: hook_socket 16 pass, hook_install 25 pass, status_broadcaster 36 pass; perl -c OK; clippy -D warnings clean -->
  - [x] verify-self  <!-- status: [x] done — subagent (fresh invocation): all 5 CLI outcomes PASS, 0 blocking/cosmetic. Privacy invariant independently grep-confirmed (length-only). Integration boundary covered by m9_time_analytics_events_are_status_neutral (cites event_to_state). No live-webview surface this phase — live end-to-end is a Phase 3 outcome. -->
  - [x] verify-human  <!-- status: [x] AUTO-SKIPPED (drive_mode=autopilot; gates a/b/c/d clean) — Phase 1 adds isolated artifacts (5 HookEvent fields, 6 status-neutral events, Perl extraction branches) that no LIVE surface consumes yet; status path provably unperturbed (CLI-tested). Live end-to-end is a Phase 3 outcome. -->
  - [x] verify-codify  <!-- status: [x] done — Rust behaviors already codified at build (TDD); added the gap: 5 end-to-end Perl-hook-output tests (src-tauri/tests/hook_pl_output.rs) asserting field extraction + the LENGTH-ONLY privacy invariant + status-event-shape-unchanged. Full suite 329 pass (324 lib + 5 integ), clippy --all-targets -D warnings clean. No integration boundary (isolated artifacts). No triage. -->

- [x] Phase 2: `time_store` module — per-identity SQLite, schema bootstrap, gated INSERT  <!-- status: [x] COMPLETE — all impl + verify nodes done -->
  **Observable outcomes:**
  - CLI: `cargo test` exits 0 — `time_store` tests pass: schema bootstrap creates the `events` table + indexes idempotently over a temp DB path; an INSERT with the gate ON writes exactly one row with the expected column values (incl. `source` discriminator = `"cc-hook"` and `meta` JSON holding `prompt_length_chars`/`tool_use_id`/`source`); an INSERT with the gate OFF writes ZERO rows (verified by row count == 0, zero-IO gate).
  - CLI: a test proves the DB path resolves under `app_data_dir()` pattern (injected path in tests; the real `time_store_path(app)` derives `<app-data>/time-analytics.sqlite`, bundle-id-per-identity like `hook.sock`) — the resolver is unit-tested against a `TempDir` the same way `hook_socket_path` is.
  - CLI: privacy assertion test (Claudesk-side port of `privacy_check.sh`) — mapping a `UserPromptSubmit` `HookEvent` (with a raw `prompt`) to a persisted row NEVER stores the prompt text anywhere (no column, no `meta` key holds it — only `prompt_length_chars`); mapping a `PreToolUse` never stores tool I/O.
  - CLI: `cargo build` exits 0 with `rusqlite` (bundled feature) added — no system-libsqlite dependency, bundle stays self-contained.
  - [x] P2.1 Add `rusqlite` (with the `bundled` feature) to `src-tauri/Cargo.toml`. Confirm `cargo build` links the bundled SQLite (no host libsqlite requirement).  <!-- status: [x] done — rusqlite 0.32 bundled; cargo build --lib links it, no host lib -->
  - [x] P2.2 New `src-tauri/src/time_store/mod.rs`: pure core — `event_to_row(&HookEvent) -> Option<TimeRow>` (meta JSON from prompt_length_chars/tool_use_id/source, stamps source="cc-hook"), SCHEMA_SQL DDL, `bootstrap(conn)`, `insert_row(conn, &TimeRow)`. Schema = `events(ts, session_id, cwd, event, tool_name, agent_type, source, meta)` w/ the WP2.5-ready `source` discriminator + 2 indexes. 10 tests over in-memory Connection (incl. row_never_carries_prompt_text privacy assertion + parameterized INSERT, no SQL-injection hand-quoting).  <!-- status: [x] done -->
  - [x] P2.3 `time_store/commands.rs`: `time_store_path(app)` (`<app-data>/time-analytics.sqlite`, sibling to hook.sock, per-identity), `TimeStore` holder (Mutex<Connection>), `open_and_bootstrap(app)` (WAL + 2000ms busy_timeout + schema), `write_gated(event, gate_on)` INSERTs iff gate_on (zero-IO no-op when OFF). 5 tests.  <!-- status: [x] done -->
  - [x] P2.4 Write-gate hook-point `tracking_enabled(app) -> bool` returning `false` (default OFF at WP2; WP5 replaces the body with the persisted toggle read). Single call-site so WP5 changes one body. Privacy assertion covered by row_never_carries_prompt_text (mod.rs) + user_prompt_submit_row_carries_length_in_meta_not_text.  <!-- status: [x] done -->
  - [x] verify-auto  <!-- status: [x] done — scoped: time_store:: 15 tests pass (schema/gate-ON/gate-OFF-zero-IO/privacy/default-OFF/source-discriminator); rusqlite bundled links at build; clippy --all-targets -D warnings clean -->
  - [x] verify-self  <!-- status: [x] done — subagent (fresh): all 4 CLI outcomes PASS, 0 blocking/cosmetic. Independently confirmed: event_to_row reads only .prompt_length_chars never .prompt; time_store_path mirrors hook_socket_path (app_data_dir + sibling basename, per-identity); rusqlite bundled present. No integration boundary (isolated module; Phase 3 wires it live). -->
  - [x] verify-human  <!-- status: [x] AUTO-SKIPPED (drive_mode=autopilot; gates a/b/c/d clean) — Phase 2 adds an isolated dormant module (time_store: mod.rs+commands.rs), rusqlite dep, and a call-site-less `mod time_store;`. No live consumer until Phase 3's fan-out; no integration boundary. -->
  - [x] verify-codify  <!-- status: [x] done — build-time TDD (15 tests) covered pure core + gate; codify closed the real-file gap: extracted open_at_path(&Path) (removed an AppHandle test barrier) + 2 tests proving WAL actually set on a real file + rows persist across reopen (in-memory tests can't show WAL/persistence). Full suite 346 pass (341 lib + 5 integ), clippy --all-targets clean. No integration boundary. No triage. -->

- [x] Phase 3: Fan-out — wire `time_store` as a second, gated drain of the `HookEvent` stream (status path untouched)  <!-- status: [x] COMPLETE — all impl + verify nodes done (verify-human vh.2 live PASS 2026-07-07; verify-codify 350 pass) -->
  **Observable outcomes:**
  - CLI: `cargo test` exits 0 — a fan-out test proves ONE socket-delivered stream reaches BOTH consumers: the status transform still produces its updates AND (gate ON) a `time_store` row is written for the same event; (gate OFF) the status transform is unaffected and zero rows are written. The status path's existing end-to-end test (`end_to_end_socket_to_transform_…`) stays green.
  - CLI: `cargo build` + `cargo clippy -- -D warnings` exit 0 (the new `lib.rs` fan-out wiring compiles clean).
  - Live (via MCP bridge, scratch workspace): with the gate FORCED ON (a dev-only override for the check — since WP5's toggle isn't built yet), opening `tmp/scratch/scratch-a` and driving a CC turn (prompt → tool → stop via IPC/click, not raw xterm typing) results in rows appearing in `<dev-app-data>/time-analytics.sqlite` (`sqlite3 … "SELECT count(*), group_concat(DISTINCT event) FROM events"` shows >0 rows across multiple event kinds incl. a Pre/PostToolUse pair) AND the status dot still transitions Running→Idle correctly (status path unaffected). *[Carried to verify-human if the gate-force override isn't cleanly drivable in-session — see verify-self note.]*
  - CLI: `sqlite3 <dev-app-data>/time-analytics.sqlite "SELECT event,source FROM events LIMIT 5"` shows `source` == `cc-hook` on every row (WP2.5-discriminator populated).
  - [x] P3.1 Fan-out at the accept-loop send site: `accept_loop` now takes `Vec<Sender<HookEvent>>` and `retain`s senders on each `tx.send(event.clone())` — exits only when ALL receivers drop (one consumer teardown never deafens the other). New `spawn_listener_fanout(listener, txs)`; `spawn_listener(l, tx)` kept as a `#[cfg(test)]` single-sender wrapper (production uses fanout; tests still use it). Status receiver semantics identical.  <!-- status: [x] done -->
  - [x] P3.2 `lib.rs` setup: `HookSocketState` now holds `status_receiver` + `time_receiver` (two channels, listener fans out to both). After `start_on_launch`, `start_broadcaster(handle, status_rx)` unchanged; NEW `time_store::commands::start_writer(handle, time_rx)` drain calls `write_gated(event, tracking_enabled(app))` per event. `open_and_bootstrap` manages the `TimeStore`; a DB-open failure is surfaced + the writer simply isn't started — status path entirely unaffected (independent drains).  <!-- status: [x] done -->
  - [x] P3.3 Dropped the `#![allow(dead_code)]` from both time_store files (module is live via lib.rs); `spawn_listener` gated `#[cfg(test)]` (its only remaining callers are tests). `cargo clippy --all-targets -D warnings` clean.  <!-- status: [x] done -->
  - [x] verify-auto  <!-- status: [x] done — scoped: hook_socket 18 pass (incl. fanout_delivers/survives), time_store 18 pass (incl. socket_stream_fans_out e2e); clippy --all-targets -D warnings clean (allows dropped) -->
  - [x] verify-self  <!-- status: [x] done — subagent (fresh): all 5 outcomes PASS, 0 blocking. Fan-out tee + independence + socket→both-drains e2e + status-untouched (36 status_broadcaster + full 344 lib + 5 integ green) + clippy clean. Wiring trace CONFIRMED the independence invariant: a time-store open failure hits an Err arm that only declines to start the writer — it does NOT early-return or skip the status wiring (a time-store failure cannot deafen the dots). LIVE rows-land + clean-two-drain-launch on the real .app CARRIED to verify-human/WP5 (needs the real WP5 toggle ON + installed-app; forcing the gate now = throwaway edit verifying a non-shipped state). -->
  - [x] verify-human  <!-- status: [x] COMPLETE — vh.2 driven live via MCP bridge 2026-07-07; vh.1 carried to WP5 (needs real toggle ON) -->
    - [x] P3.verify-human.1 (CARRIED-TO-WP5/RELEASE): with WP5's tracking toggle ON, drive a CC turn in a workspace → rows appear in `<dev-app-data>/time-analytics.sqlite` (`sqlite3 … "SELECT count(*),group_concat(DISTINCT event) FROM events"` >0 across multiple event kinds incl. a Pre/PostToolUse pair), `source`=`cc-hook` on every row.  <!-- status: [x] CARRIED-TO-WP5 — rows-land requires WP5's real toggle ON; forcing gate now = verifying a non-shipped state. Explicitly deferred, not skipped. -->
    - [x] P3.verify-human.2 (LIVE, load-bearing): the assembled app launches clean with the two-drain wiring AND the status dot still transitions Running→Idle correctly (the status-path-untouched invariant, observed on the real app, not just CLI).  <!-- status: [x] PASS — driven live 2026-07-07 via tauri MCP bridge (com.claudesk.app.dev v0.2.5, port 9223). Clean two-drain launch (no panic, focus-probe fired). Opened scratch-a → real cc-1 session spawned. Drove 2 real CC turns via cc_input byte-injection (IPC, CR-terminated — NOT raw xterm). A 100ms sampler captured the FULL transition idle→running→idle on the live dot (both header + filmstrip tile). Screenshot confirms both turns executed real ("Hi"; 1-2-3-4-5) + header reads Idle. BONUS gate-OFF invariant confirmed: DB has exact events schema + journal_mode=wal + 0 ROWS despite real UserPromptSubmit/Stop hooks flowing the fan-out → write path genuinely dormant (WP5 builds toggle). Teardown clean (driver stop + TaskStop + ports 1420/9223 freed). -->
    - [x] P3.verify-human.1 stays deferred to WP5/release (rows-land needs WP5's real toggle ON).  <!-- status: [x] CARRIED-TO-WP5 -->
  - [x] verify-codify  <!-- status: [x] done — integration boundary (fan-out wiring in lib.rs/accept_loop) already covered end-to-end: socket_stream_fans_out_to_both_status_and_time_store (gate ON) + fanout_delivers/survives + end_to_end_socket_to_transform (status path green). Codify closed the gap: +1 test socket_stream_fans_out_but_gate_off_writes_nothing_status_still_flows — the gate-OFF half of the outcome (WP2-default posture proven through the real fan-out stream, not just the direct write path). Full suite 350 pass (345 lib + 5 integ), clippy --all-targets -D warnings clean. No triage (0 failures). -->


## Current Node
- **Path:** Feature > review-quality COMPLETE → finalize
- **Active scope:** All 3 phases COMPLETE + shipped (dc3b89e) + review-quality done (0 CRITICAL / 0 MAJOR / 3 MINOR, all auto-backlogged to backlog-quality-findings.md under `# m9-wp2… — 2026-07-07`; pointer in backlog.md). NEXT: F39 → /feature-finalize (autopilot chains).
- **Blocked:** none.
- **Open discoveries:** none
- **Carried to WP5:** P3.verify-human.1 (rows-land needs WP5's real toggle ON — verifying a non-shipped state now would be throwaway).
- **Blocked:** none
- **Unvisited:** Phase 3 verify group (verify-auto → verify-self [live via MCP bridge: gate-force-ON, scratch workspace, rows land + status dot still transitions] → verify-human → verify-codify)
- **Open discoveries:** none
- **Build note (Phase 3):** kept `spawn_listener` as `#[cfg(test)]` (production uses `spawn_listener_fanout`; the single-sender form now has only test callers). `tracking_enabled` still defaults OFF, so the live writer is dormant until WP5 — verify-self's "rows land" check needs a temporary gate-force-ON override.
- **Build note (P2 codify):** extracted `open_at_path(&Path)` from `open_and_bootstrap` so the real-file WAL/persist behavior is testable without an AppHandle.
- **Build note (Phase 1):** added `tool_name` to `HookEvent` beyond the plan's 4 fields — reclassifier needs per-tool rollups (WP1 §d).
- **Blocked:** none
- **Unvisited:** Phase 2 verify group (verify-auto → verify-self → verify-human → verify-codify), then Phase 3 (fan-out seam → lib.rs wiring → drop the time_store dead-code allow → verify)
- **Open discoveries:** none
- **Build note (Phase 2):** `time_store` module carries a scoped `#![allow(dead_code)]` (both mod.rs + commands.rs) because Phase 3's fan-out is what calls `open_and_bootstrap`/`write_gated` from lib.rs — the allow is REMOVED at Phase 3 (mirrors the WP3-era hook_socket allow dropped once WP4 drained the receiver). `tracking_enabled` defaults OFF (a `false` body); WP5 replaces it with the persisted toggle read.
- **Build note (Phase 1):** added `tool_name` to `HookEvent` beyond the plan's stated 4 fields — reclassifier needs per-tool rollups (WP1 §d). Harmless snake_case + `#[serde(default)]` superset.
- **Blocked:** none
- **Unvisited:** Phase 1 verify group (verify-auto → verify-self → verify-human → verify-codify), then Phase 2 (rusqlite dep → time_store core → path+holder → gate hook-point → verify), then Phase 3 (fan-out seam → lib.rs wiring → module reg → verify)
- **Open discoveries:** none
- **Build note:** added `tool_name` to `HookEvent` beyond the plan's stated 4 fields — the reclassifier needs per-tool rollups (WP1 §d: claude-time's hook forwards `tool_name`); harmless superset, snake_case + `#[serde(default)]` like the others.

## Discoveries
<!-- Format: [SURFACED-<date>] <target node> — <summary>
     Each entry is also logged to workflow/backlog.md -->

## Retrospect
- **What changed in our understanding:** The single hardest thing wasn't the SQLite write — it was that the M3 `HookEvent` receiver is a single-consumer `mpsc::Receiver`, so adding a second drain required a genuine fan-out (`spawn_listener_fanout` with `retain`-until-all-drop), not just a `.clone()`. The `retain` semantics (loop exits only when ALL receivers drop) is what makes the independence invariant hold by construction — a time-store teardown can never deafen the status dots.
- **Assumptions that held:** The plan's seam survey was accurate — `CLAUDESK_EVENTS` extended cleanly (registration loop untouched), `status_broadcaster::event_to_state` dropped the 6 new events to `None` with no dot flip, and the port of claude-time's field-extraction + schema mapped directly. The "Rust owns the write, Perl stays a pure forwarder" absorption decision was right — no `sqlite3` subprocess needed.
- **Assumptions that were wrong:** None material. Two small superset decisions beyond the plan's stated 4 wire fields: added `tool_name` (reclassifier needs per-tool rollups, WP1 §d) and extracted `open_at_path(&Path)` so real-file WAL/persistence is testable without an `AppHandle`. Both were harmless, additive, and made the tests stronger.
- **Approach delta:** Implementation matched the plan's 3-phase structure exactly (hook+wire → time_store module → fan-out). The only process delta was verify-human: the live dot-flip check was blocked one session because the MCP bridge had been disabled in `settings.local.json` (re-enabled, driven in the resumed session). The load-bearing behavior (clean two-drain launch + gate-OFF dormancy) was confirmed either way; the visual dot transition was the last live confirmation.

## Code-Quality Review — m9-wp2-absorbed-hook-write-gated-sqlite-writer

### Strengths
- The independence invariant (DB-open failure never deafens the status dots) is genuinely load-bearing and is enforced structurally, not just by comment: the `lib.rs` setup wires the time-store in a self-contained `match` arm whose `Err` branch only declines to start the writer, and `spawn_listener_fanout`'s `retain`-until-all-drop loop means one consumer teardown can't kill the other. Both are pinned by tests (`fanout_survives_one_consumer_dropping`, `socket_stream_fans_out_but_gate_off_writes_nothing_status_still_flows`).
- The privacy invariant is defended at three layers — Perl (`length($prompt)+0`, never re-emitting text), Rust mapping (`event_to_row` reads only `prompt_length_chars`), and persistence — and each layer has a dedicated regression test, including an end-to-end Perl-subprocess test that asserts no non-`prompt` field carries the text.
- Parameterized `insert_row` (`rusqlite::params!`) is a clean, deliberate improvement over claude-time's hand-quoted `sql_q` string interpolation — SQL-injection-safe by construction, and the doc-comment calls out the delta.
- The pure/wiring split (`time_store/mod.rs` pure functions over `Connection`; `commands.rs` owns `AppHandle` path resolution + the managed holder) mirrors the established `hook_socket`/`config_store` module shape, and the in-memory-vs-real-file test split (`open_at_path` extracted so WAL/persistence is testable without an `AppHandle`) is thoughtful.
- `CLAUDESK_EVENTS` growth from 4→10 was done without touching the registration loop (`merge_claudesk_hooks` iterates the const), and the `settings_with_claude_time` fixture was refactored to build from the const so it can never drift out of sync with the event set.

### Issues
**CRITICAL**
- (none)

**MAJOR**
- (none)

**MINOR**
- [src-tauri/tests/hook_pl_output.rs:124] The privacy leak assertion checks `!s.contains("SECRET")` against a hardcoded literal, while the injected prompt is `"SUPER SECRET PROMPT…"`. The test only catches a leak because the operator happened to embed the substring `SECRET` in the prompt — it does not assert against the actual `secret` variable. A future author who changes the prompt string could silently weaken this to a no-op guard. Comparing against the real `secret` value (or a distinctive sentinel derived from it) would make the privacy check self-consistent. — *Why it matters: the privacy invariant is the feature's most important contract; its end-to-end test should not depend on a coincidental substring.*
- [src-tauri/src/time_store/mod.rs:123] `ts` falls back to `0` when `HookEvent::timestamp` is absent. The production Perl hook always stamps `timestamp`, so this is unreachable today, but a `ts=0` row would sort to the epoch and could corrupt WP3's time-ordered reclassification if any non-hook source (WP2.5 native signals) ever forgets to stamp. Given the `source`-discriminator design explicitly anticipates a second writer, a debug-log or a `None`-drop on absent timestamp would be a cheaper guard than a silent 0. — *Why it matters: a load-bearing ordering key silently defaulting to a sentinel is a latent data-quality trap for the downstream consumer this feature exists to feed.*
- [src-tauri/src/time_store/mod.rs:62] Minor schema asymmetry: `tool_name` and `agent_type` are first-class columns, but `source` (SessionStart) and `prompt_length_chars` (UserPromptSubmit) live inside the `meta` JSON blob. This faithfully mirrors claude-time, so it's defensible, but it means WP3 must query two shapes (columns for some fields, JSON extraction for others). A one-line note in the schema doc-comment on *why* `tool_name`/`agent_type` earned columns while the others stayed in `meta` (query-frequency? claude-time parity?) would save the WP3 author a spelunk. — *Why it matters: reduces WP3 onboarding cost; purely a documentation nit since the shape is intentional.*

### Assessment
This is a well-built feature that lands a genuinely tricky change — teeing a single-consumer stream into two independent drains while holding the M3 status path byte-for-byte constant — cleanly and with the invariants defended by construction rather than by hope. The fan-out, the gate-OFF zero-IO fast path, and the DB-open-failure isolation are each both commented *and* test-pinned, and the tests read as behavioral contracts (independence, gate ON/OFF, privacy) rather than line coverage. It advances the codebase: the `source` discriminator and the pure/wiring split visibly pre-pay for WP2.5 and WP3 without over-building, and the one net-new dependency (rusqlite bundled) is scoped and documented against the "flat JSON, no DB" convention it appears to violate. Future readers will find it clear — the doc-comments carry the *why*. The findings are all MINOR polish; nothing here warrants a refactor pass.

### If you disagree
Operator: dismiss any finding by editing this section in the WIP file and marking the line `[DISMISSED]` before `feature-finalize` archives the WIP.
