# Changelog

## 2026-07-20

- **Feature shipped:** Backlog-paydown sweep WP2 — fixed the two M9 WP6b-4 flexible-timeline MAJOR latent bugs: (1) `framedRange`'s day-granular readout could emit a **31-inclusive-day span** from a legal-but-day-misaligned max-zoom-out viewport → the reactive `RangePicker` stuck in a permanent red-border "Range too long (31 > 30)" error on a value the operator never typed; fixed by capping the emitted span to `MAX_RANGE_DAYS` inclusive days **after** the coordinate clamp, and deriving both the picker max and the readout cap from the single `MAX_ZOOM_OUT_SPAN_MIN/1440` so the two "30" bounds can't drift; (2) `AutoExtendWatcher.firingRef` could **latch `true` permanently** when a no-op extend at a coordinate edge left the loaded window unchanged (the `[lo,hi]` clearing effect never fired), and because one ref gated both directions a latched `older` guard could block a later legit `newer` extend; fixed by extracting a pure `nextExtendGuard(dir, currentGuard)` that releases the guard on a no-extend tick (ordered before the in-flight hold) and rewiring the watcher to always run the debounced tick through it (removing the `if (firingRef.current) return` early-return that was the latch's teeth). Reproduce-first: deterministic red-then-green pure-fn tests for both; full suite 1185 passed; tsc/eslint clean. review-quality 0 CRITICAL / 0 MAJOR / 2 MINOR (legibility nits, auto-backlogged → fold into sweep WP6). Two live dashboard checks (picker no-red-border at max zoom-out pan; auto-extend fires both directions after hard-panning opposite edges) carried to the next `/release` gate.
- **Backlog resolved:** SURFACE-2026-07-15-QUALITY-WP6B4-FRAMEDRANGE-PICKER-OFFBYONE — closed by sweep WP2 (framedRange span-cap + MAX_RANGE_DAYS/MAX_ZOOM_OUT_SPAN_MIN reconciliation).
- **Backlog resolved:** SURFACE-2026-07-15-QUALITY-WP6B4-AUTOEXTEND-FIRINGREF-LATCH — closed by sweep WP2 (pure `nextExtendGuard` release-on-null + AutoExtendWatcher rewire).
- **Feature shipped:** Backlog-paydown sweep WP3 — consolidated the `time_store/query.rs` ↔ `reclassify` duplication-of-composition cluster (pure refactor, behavior unchanged): exposed a single `reclassify::ai_component_spans` primitive that `ai_busy_intervals` now merges and `build_metrics` reads un-merged for effort (one encoding of AI-family membership); promoted `merge_spans` to `pub(crate)` and reduced query.rs's `merge_intervals` to a thin adapter (one interval-merge impl); extracted a shared `group_events_by_sid` helper folding the `<unknown>`-sentinel session-keying idiom out of `build_day`/`capped_events`/`human_kind_ms`; deleted the byte-duplicate query-local `local_date_of_ms` (now uses `local_date_of`); `pub(crate)`-exported `local_midnight_ms`/`local_date_of` and dropped the near-verbatim `commands.rs` tz-helper copies; and added the `ai_busy_intervals`-re-derivation + sub-second-display clarifying comments. BE 547/547 + FE 1185/1185 + clippy `--all-targets -D warnings` + cargo fmt clean; net −33 lines.
- **Backlog resolved:** SURFACE-2026-07-15-QUALITY-WP6C1-AI-COMPONENT-SPAN-DUP — closed by sweep WP3 (`reclassify::ai_component_spans` primitive is the single AI-family-membership encoding; `ai_busy_intervals` merges it, `build_metrics` sums it un-merged).
- **Backlog resolved:** SURFACE-2026-07-15-QUALITY-WP6C1-MERGE-INTERVALS-DUP — closed by sweep WP3 (`merge_spans` promoted to `pub(crate)`; `merge_intervals` reduced to a thin adapter over it — one interval-merge impl).
- **Backlog resolved:** SURFACE-2026-07-15-QUALITY-WP6C1-BY-SID-GROUPING-DUP — closed by sweep WP3 (shared `group_events_by_sid` helper; the three inline `by_sid`/`<unknown>` copies now route through it).
- **Backlog resolved:** SURFACE-2026-07-15-QUALITY-WP6C1-PRIMITIVE-REWALK-UNCOMMENTED — closed by sweep WP3 (`build_metrics` now notes `ai_busy_intervals` re-derives the tool/subagent/burst spans it merges).
- **Backlog resolved:** SURFACE-2026-07-15-QUALITY-WP6C1-FMTMSDUR-SUBSECOND-COMMENT — closed by sweep WP3 (comment at `fmtMsDur`'s sub-second seam: "0s" is expected display-rounding; the guarded anti-pattern is minute-flooring on the summing side).
- **Backlog resolved:** SURFACE-2026-07-15-QUALITY-WP6C2-LOCAL-DATE-OF-MS-DUP — closed by sweep WP3 (deleted the query-local `local_date_of_ms`; `events_in_days` uses the module's single `local_date_of`).
- **Backlog resolved:** SURFACE-2026-07-08-QUALITY-WP4-DUP-TZ-MATH-HELPERS — closed by sweep WP3 (`local_midnight_ms`/`local_date_of` `pub(crate)`-exported; the `commands.rs` copies dropped — one tz-boundary-math impl, no drift).
- **Feature shipped:** Backlog-paydown sweep WP4 — deduped three co-located dashboard-component copies (frontend-only pure refactor, behavior unchanged): (1) extracted `useRafViewportSetter()` in `ViewportContext.tsx` — the "one viewport write per animation frame + no leak on unmount" invariant that `useTimelineGestures` and `Minimap` had **byte-duplicated** now has a single home; (2) extracted a shared `NavPill` primitive in `Chrome.tsx` and reduced `MonthNav`/`WeekNav` to thin wrappers over it (the ~60-line near-copy collapsed; every `data-*` attribute the live-drive + wiring tests read is preserved verbatim); (3) `weekMath.ts`'s byte-identical private `dateToIso`/`mondayIdx` now import `monthMath`'s already-exported `todayDateIso`/`mondayIndex` (aliased to keep call sites unchanged). FE 1185/1185 + build + eslint + prettier clean; the `dashboardWiring` source-text guard on the `data-week-nav` selector was updated to assert the preserved contract through the new `NavPill` wiring.
- **Backlog resolved:** SURFACE-2026-07-13-QUALITY-WP6B1-DUP-RAF-SCHEDULESET (MAJOR) — closed by sweep WP4 (`useRafViewportSetter()` hook owns the rafRef + unmount-cancel + bound `scheduleSet`; both gesture consumers collapsed onto it).
- **Backlog resolved:** SURFACE-2026-07-14-QUALITY-WP6B3-WEEKNAV-MONTHNAV-DUP — closed by sweep WP4 (shared `NavPill` primitive; `MonthNav`/`WeekNav` are thin wrappers preserving their exact `data-*` surface).
- **Backlog resolved:** SURFACE-2026-07-14-QUALITY-WP6B3-WEEKMATH-MONTHMATH-HELPER-DUP — closed by sweep WP4 (`weekMath` imports `monthMath.todayDateIso`/`mondayIndex` instead of re-implementing them).
- **Feature shipped:** Backlog-paydown sweep WP5 — dead-code / unconsumed-field / speculative-param cleanup (Rust + TS, behavior unchanged): deleted the dead `viewportFromRange` seed helper + its 4 tests (superseded by `seedViewportToday`); dropped the unconsumed `CompareSide.range` DTO field from the Rust struct + producer + TS type (per-side window bounds live at `metrics.window`); collapsed `topBlockingShift`'s unreachable `human→agent` branch (the normalized split makes `|haShift| === |ahShift|` always, so the tie-break always chose `agent→human`); narrowed `ProjectPicker`'s vestigial `onCheckForUpdates?: () => Promise<{outcome}>` to `() => void` (fires-and-forgets); documented `EditorPanel`'s defensive-only null-`openPath` branch as unreachable in the shipped wiring; and — the header item — **replaced `reclassify`'s module-wide `#![allow(dead_code)]` with 8 targeted per-item `#[allow(dead_code)]`** (each with a one-line why), so the honest dead-code signal is back for the whole module while the tested-but-not-yet-consumed analytics primitives stay. BE 547/547 + FE 1181/1181 (−4 = the removed `viewportFromRange` tests) + clippy `--all-targets -D warnings` + fmt + eslint + prettier clean.
- **Backlog resolved:** SURFACE-2026-07-15-QUALITY-WP6B4-DEAD-VIEWPORTFROMRANGE — closed by sweep WP5 (deleted the dead `viewportFromRange` + its `describe` block; `seedViewportToday` is the sole seed path).
- **Backlog resolved:** SURFACE-2026-07-15-QUALITY-WP6C2-COMPARESIDE-RANGE-UNCONSUMED — closed by sweep WP5 (dropped the unconsumed `CompareSide.range` field from Rust DTO + producer + TS type + the 2 pinning tests; bounds live at `metrics.window`).
- **Backlog resolved:** SURFACE-2026-07-15-QUALITY-WP6C2-TOPBLOCKINGSHIFT-DEAD-BRANCH — closed by sweep WP5 (collapsed to the single `agent→human` axis; the normalized split made the `human→agent` label unreachable).
- **Backlog resolved:** SURFACE-2026-07-08-QUALITY-WP6A-EDITORPANEL-DEAD-EMPTY-BRANCH — closed by sweep WP5 (documented the null-`openPath` branch as a defensive-only, shipped-wiring-unreachable null guard, per the finding's preferred fix; `lazyBundleWiring.test.ts` pins that PaneTabs owns the empty case).
- **Backlog resolved:** SURFACE-2026-07-18-QUALITY-WP6-PICKER-CHECK-UPDATES-VESTIGIAL-RETURN-TYPE — closed by sweep WP5 (narrowed `onCheckForUpdates?` to `() => void`; the Promise-returning `checkNow` still assigns).
- **Backlog resolved:** SURFACE-2026-07-08-QUALITY-WP4-P20-ALLOW-STAYS-MULTI-WP-CONSUMER — closed by sweep WP5 (precondition met at M9-complete; the module-scoped `#![allow(dead_code)]` narrowed to 8 targeted per-item allows, clippy `--all-targets -D warnings` clean).
- **Feature shipped:** Backlog-paydown sweep WP6 — the one-line comment / perf / naming nit fold-in pass (the bulk of the MINOR findings, behavior-preserving across Rust + TS). Beyond the doc/comment one-liners, three items were real (small) code changes: (1) `reclassify` now precomputes surface change-points into `GapContext` (a sorted `(ts,is_editor)` vector + `partition_point` lookup) so per-gap classification is O(gaps·log n) instead of rescanning the full event list per gap — with `surface_change_points` as the single source of truth for the deterministic `(ts,surface)` ordering shared by the precompute and the retained free `surface_is_editor_at` (test/point-lookup entry point); (2) `time_set_active_context` collapsed from THREE lock acquisitions (compare → write → reread-for-emit) to ONE — `set_active_context` now returns `(surface_changed, post_write_snapshot)` under a single guard, which also let the change-detection test assert at the source instead of via a stored-surface proxy; (3) `authoritative_end` gained an iterator core (`earliest_end_marker`) so `dangling_sessions` drops its per-session owned-clone. Plus: `encodeBase64` rebuilt via a chunked `String.fromCharCode(...)` spread (mirrors `decodeBase64`, overflow-safe on large pastes); the hook-privacy integration test now asserts against the ACTUAL prompt value, not the coincidental `"SECRET"` substring; a `MAX_FRAMED_DAYS === MAX_RANGE_DAYS` tie assertion; the `pip` placeholder/anchor WHY consolidated to one canonical site; and ~20 comment/naming/a11y one-liners (Chrome/RangePicker header drift, Minimap stopProp semantics, DayTimeline mixed-viewport doc, dayStats double-sumActive hoist, weekMath guard-only, App fallback-string constant, confirmDialog pluralization hoist, ProjectPicker setter-underscore alias + permission-select `aria-label`, mirrorTrim reconstruction assumption). BE 547/547 lib + integ + clippy `--all-targets -D warnings` + fmt clean; FE 1181/1181 + tsc + vite build + eslint clean. The 2 `WP7-*` self-resolve findings (`WBS-FRONTMATTER-STALE`, `CLAUDEMD-WP2-WIREFIELD-COUNT`) are left for `/product-finalize`; `WP3-MOD-LAYOUT-LIST-INCOMPLETE` + `WP3-RESOLUTION-ASYMMETRY-UNREMARKED` were retired as MOOT (their `install_source` target was deleted by the M10 WP6 brew-decision reversal).
- **Backlog resolved:** SURFACE-2026-07-20-QUALITY-WP2-EXTEND-FIRE-DIR-NARROW-ARTIFACT — closed by sweep WP6 (one-line comment: `&& dir` narrows for TS, `fire` already implies a direction).
- **Backlog resolved:** SURFACE-2026-07-20-QUALITY-WP2-MAXFRAMED-MAXRANGE-PARALLEL-DERIVATION — closed by sweep WP6 (exported `MAX_FRAMED_DAYS` + a `MAX_FRAMED_DAYS === MAX_RANGE_DAYS` tie assertion; the two derivations stay separate to avoid a viewport↔RangePicker cycle, but a `+1`-style drift is now caught).
- **Backlog resolved:** SURFACE-2026-07-19-QUALITY-WP4-ENCODEBASE64-CONCAT-SYMMETRY — closed by sweep WP6 (chunked `String.fromCharCode(...)` spread mirroring `decodeBase64`, arg-count-bounded for large pastes).
- **Backlog resolved:** SURFACE-2026-07-19-QUALITY-WP4-STALE-RED-TEST-COMMENT — closed by sweep WP6 (trimmed the reproduce-first RED framing to "guards the UTF-8-bytes contract").
- **Backlog resolved:** SURFACE-2026-07-19-QUALITY-WP3-KILL-ALL-STALE-3S-DOC — closed by sweep WP6 (kill_all doc now states ~800ms, not the stale 3s).
- **Backlog resolved:** SURFACE-2026-07-19-QUALITY-WP3-NONE-PGID-COMMENT-OVERPROMISE — closed by sweep WP6 ("never worse" scoped to reaping + notes the history-save lost on the None-pgid path).
- **Backlog resolved:** SURFACE-2026-07-18-QUALITY-WP2-PLURALIZATION-CO-LOCATION — closed by sweep WP6 (one `single` predicate drives every plural/singular pick in `quitWhileActiveSpec`).
- **Backlog resolved:** SURFACE-2026-07-18-QUALITY-WP2-FALLBACK-STRING-PARITY — closed by sweep WP6 (one shared `UNNAMED_WORKSPACE` constant across the close gate + the app-quit busy list).
- **Backlog resolved:** SURFACE-2026-07-18-QUALITY-WP1-SINGLE-WRITER-COMMENT — closed by sweep WP6 (comment now states both PiP accessors run on the main thread).
- **Backlog resolved:** SURFACE-2026-07-18-QUALITY-WP1-WHY-TRIPLICATED — closed by sweep WP6 (placeholder/positioned-gate/FE-contract WHY consolidated to the canonical `pip_resize` anchor site; `anchor_top_right` + `PipAutoState::positioned` point back to it).
- **Backlog resolved:** SURFACE-2026-07-18-QUALITY-WP1-FE-MOUNT-RESIZE-CONTRACT-UNDOCUMENTED — closed by sweep WP6 (FE→BE mount-resize contract folded into the canonical `pip_resize` WHY as fact 3).
- **Backlog resolved:** SURFACE-2026-07-17-QUALITY-WP3-MOD-LAYOUT-LIST-INCOMPLETE — retired MOOT by sweep WP6 (the `install_source*` fns it referenced were deleted by the M10 WP6 brew-decision reversal; the layout list is now accurate — no code change).
- **Backlog resolved:** SURFACE-2026-07-17-QUALITY-WP3-RESOLUTION-ASYMMETRY-UNREMARKED — retired MOOT by sweep WP6 (`install_source()` no longer exists post-M10-WP6-reversal; the canonicalize-vs-not asymmetry is gone — no code change).
- **Backlog resolved:** SURFACE-2026-07-17-QUALITY-WP2-CURRENT-VERSION-DUAL-PROVENANCE — closed by sweep WP6 (dual-provenance comment on the updater current-version read).
- **Backlog resolved:** SURFACE-2026-07-15-QUALITY-WP6C2-CHROME-HEADER-COMMENT-DRIFT — closed by sweep WP6 (Chrome header now states all five views enabled as of WP6c-2, not "Compare stays disabled").
- **Backlog resolved:** SURFACE-2026-07-15-QUALITY-WP6B4-RANGEPICKER-STALE-HEADER — closed by sweep WP6 (RangePicker header updated to the D8 reactive-readout model + MAX_RANGE_DAYS=30 cap, dropping the superseded WP6b-2/31-day framing).
- **Backlog resolved:** SURFACE-2026-07-14-QUALITY-WP6B3-ISFUTUREMONDAY-GUARD-CLARITY — closed by sweep WP6 (guard-only `if (!isoToDate(mondayIso)) return true;` + comment that the compare is lexicographic on the iso strings).
- **Backlog resolved:** SURFACE-2026-07-13-QUALITY-WP6B1-DAYTIMELINE-DOC-MIXED-VIEWPORT — closed by sweep WP6 (docstring now spells out the two intentional viewport-access patterns: prop for the bounds-only leaves, context for the ruler/grid that also need the adaptive interval).
- **Backlog resolved:** SURFACE-2026-07-13-QUALITY-WP6B1-MINIMAP-STOPPROP-COMMENT — closed by sweep WP6 (comment corrected: `preventDefault` suppresses text selection; `stopPropagation` is defensive belt-and-suspenders, the Minimap being a sibling of the timeline).
- **Backlog resolved:** SURFACE-2026-07-08-QUALITY-WP6.5-DANGLING-CLONE-PER-SESSION — closed by sweep WP6 (`authoritative_end` gained an `earliest_end_marker` iterator core; `dangling_sessions` feeds `evs.iter().copied()` and drops the per-session owned clone).
- **Backlog resolved:** SURFACE-2026-07-08-QUALITY-WP6.5-FIRST-GAP-WINS-IMPLICIT — closed by sweep WP6 ("first oversized idle gap wins" made explicit in the reclassify comment).
- **Backlog resolved:** SURFACE-2026-07-08-QUALITY-WP6A-DAYSTATS-DOUBLE-SUMACTIVE — closed by sweep WP6 (single `sessActive = sumActive(segs)` walk feeds both the running total and the longest-session pick).
- **Backlog resolved:** SURFACE-2026-07-08-QUALITY-WP6A-ONOPENDASHBOARD-PROP-ASYMMETRY — closed by sweep WP6 (comment on the ProjectPicker side explaining the deliberate optional-vs-required split from Filmstrip).
- **Backlog resolved:** SURFACE-2026-07-08-QUALITY-WP5-SETTER-UNDERSCORE-FOOTGUN — closed by sweep WP6 (import aliased to `persistTimeTracking` so the useState setter takes the clean `setTimeTrackingEnabled` name; the persister call in `handleToggleTracking` rewired to the alias).
- **Backlog resolved:** SURFACE-2026-07-08-QUALITY-WP4-AI-BUSY-COMPUTED-TWICE — closed by sweep WP6 (comment noting the AI-busy union is walked twice per window as a known, bounded redundancy).
- **Backlog resolved:** SURFACE-2026-07-06-QUALITY-MIRRORTRIM-LOSSY-RECONSTRUCTION — closed by sweep WP6 (header note documenting the empty-join reconstruction assumption of zero inter-row content + why the common-path early-return sidesteps it).
- **Backlog resolved:** SURFACE-2026-07-02-QUALITY-CCMODE-SELECT-A11Y-NAME — closed by sweep WP6 (explicit `aria-label="Permission mode"` on the picker permission-mode select).
- **Backlog resolved:** SURFACE-2026-07-02-QUALITY-CCMODE-BARE-DOC-COMMENTS — closed by sweep WP6 (Auto/DontAsk enum docs enriched with semantic why).
- **Backlog resolved:** SURFACE-2026-07-07-QUALITY-PRIVACY-TEST-COINCIDENTAL-SUBSTRING — closed by sweep WP6 (the hook-privacy integration test compares against the ACTUAL `secret` value, not the coincidental `"SECRET"` literal — holds for any prompt text).
- **Backlog resolved:** SURFACE-2026-07-07-QUALITY-TS-SILENT-EPOCH-FALLBACK — closed by sweep WP6 (epoch-0 sentinel comment on the time_store fallback).
- **Backlog resolved:** SURFACE-2026-07-07-QUALITY-SCHEMA-COLUMN-VS-META-ASYMMETRY — closed by sweep WP6 (column-vs-meta split documented in the schema SQL).
- **Backlog resolved:** SURFACE-2026-07-07-QUALITY-ACTIVECTX-TRIPLE-LOCK — closed by sweep WP6 (`set_active_context` collapses the three lock acquisitions to one, returning `(surface_changed, snapshot)`).
- **Backlog resolved:** SURFACE-2026-07-07-QUALITY-ACTIVECTX-POISON-DISPOSITION — closed by sweep WP6 (folded into the single-lock pass; the old compare-path poison comment is superseded by the one-guard implementation, which surfaces poison as an Err via `set_active_context`).
- **Backlog resolved:** SURFACE-2026-07-07-QUALITY-WP3-SURFACE-HELPER-NOT-IN-GAPCONTEXT — closed by sweep WP6 (surface resolution precomputed into `GapContext` with a `partition_point` point-lookup; the hot per-gap path no longer rescans the event list).
- **Backlog resolved:** SURFACE-2026-07-07-QUALITY-WP3-WORKING-CREDIT-PREDICATE-INLINE-RESCAN — closed by sweep WP6 (was already extracted as `GapContext::awaiting_in_gap`; verified + retained).
- **Backlog resolved:** SURFACE-2026-07-13-QUALITY-MINQUANT-DOUBLE-NEGATIVE-GUARD — closed by sweep WP6 (double-negative-guard clarifying comment in query.rs).
- **Backlog resolved:** SURFACE-2026-06-22-QUALITY-WP2-MINORS — closed by sweep WP6 (the two remaining open sub-items resolved: #1 the `hook_install` set-executable doc reconciled with the `/usr/bin/perl <script>` invocation form; #3 the coarse `NotAnObject` variant split into `HooksNotAnObject` + `EventNotAnArray(String)` for a precise malformed-shape message).
- **Feature shipped:** Backlog-paydown sweep WP7 — `editor_fs` backend hardening (fully closed the two accepted-gap trust-boundary findings, test-first red→green): Phase 1 tightened `resolve_within` to canonicalize the *full* resolved target when it exists and re-assert `starts_with(root_canon)`, so a **leaf** symlink inside the workspace whose target escapes root is now rejected on read/write/stat/delete/trash (previously followed — leaking/clobbering the outside file); the not-yet-existing-leaf write path is preserved (parent-canonicalize as before). Phase 2 added the pure `validate_root(known_roots, requested_root)` helper and wired all six `editor_fs` commands (`read_file`/`write_file`/`stat_file`/`delete_file`/`trash_path`/`create_dir`) to authenticate the frontend-supplied `root` against the known project list (`config_store::read_projects`, resolved server-side off the real `app_data_dir()`) via an injected `AppHandle` before honoring it — a `root` that is neither a known project nor a descendant of one is now rejected, mirroring `config_store`'s server-side-derivation posture. `AppHandle` is Tauri-injected, so the FE `invoke` shape (`{root, path[, contents]}`) is unchanged. Module + `resolve_within` + `commands.rs` docs restated to the now-enforced two-layer invariant (accepted-gap language dropped). Reproduce-first: 2 runtime-red leaf-symlink tests + 1 compile-gap `validate_root` red, all now green; +4 codify pins (destructive-ops guard, descendant-honored, stale-record-skipped, non-existent-root Io, canonical-form tolerance for the false-positive-rejection risk). BE 556/556 lib + 6 integ + 1 shell-history + clippy `--all-targets -D warnings` + fmt clean; FE 1181/1181 + tsc + vite build clean. Live positive-path IPC exercise (a real project's file still reads/saves through the running/installed `.app`) carried to the next `/release` gate per installed-build-verify-deferred-to-release.
- **Backlog resolved:** SURFACE-2026-06-19-QUALITY-WP2-RESOLVE-WITHIN-LEAF-SYMLINK — closed by sweep WP7 (leaf-symlink full-target canonicalize in `resolve_within`; a leaf symlink escaping root now rejected on read/write/stat/delete/trash, pinned by the reproduce reds + the destructive-ops codify pin).
- **Backlog resolved:** SURFACE-2026-06-19-QUALITY-WP2-BACKEND-TRUSTS-FRONTEND-ROOT — closed by sweep WP7 (the `validate_root` seam: the six `editor_fs` commands now authenticate the frontend-supplied `root` against the known project list before honoring it, mirroring `config_store`'s server-side derivation).

## 2026-07-19

- **Feature shipped:** M10.5 WP3 — closing a workspace or quitting Claudesk now terminates its CC/shell session with a **SIGHUP-first process-group kill** (brief clean-exit attempt → `killpg(SIGHUP)` + grace → `killpg(SIGKILL)` → reap), which fixes both the ~5s close latency + subagent-orphan risk AND — the operator's primary concern — the **lost right-panel shell command history** (SIGHUP lets an interactive zsh run its on-exit history save, so closing without typing `exit` no longer drops your terminal history; SIGTERM/SIGKILL would lose it). `libc` added as a direct dep; the kill sequence is a pure walkable value the executor iterates and a unit test pins; a committed integration test (`tests/shell_history_on_kill.rs`) drives a real `zsh -l -i` proving SIGHUP-saves/SIGKILL-loses. review-quality 0 CRITICAL / 0 MAJOR / 3 MINOR (doc/observability nits, auto-backlogged).
- **Backlog resolved:** SURFACE-2026-07-16-M10.5-QOL-BUCKET (item 3, "proper kill/exit") — delivered as M10.5 WP3; the item remains open for WP4 (encoding fix), the last of the four.
- **Feature shipped:** M10.5 WP4 — garbled `�` characters in CC input/output are fixed at both (reproduce-first-confirmed) root causes: the frontend `encodeBase64` now encodes real UTF-8 bytes (`TextEncoder`) instead of truncating multi-byte code units with `charCodeAt(i) & 0xff`, so pasted emoji/accented/arrow glyphs reach CC intact; and the shared spawn env (`color_tty_env()`, used by both the CC and right-panel-shell spawns) now sets `LANG`/`LC_ALL=en_US.UTF-8`, fixing UTF-8 **output** mangling in the installed `.app` (a Finder-launched build inherits the launchd env where `LANG` is unset → the spawned process defaults to the ASCII `C` locale). The output half is installed-`.app`-only (it never reproduces in `pnpm tauri:dev`, which inherits the shell's UTF-8 `LANG`) → its live check rides the next `/release` gate. FE 1176/1176 + BE 547/547 + clippy `--all-targets` clean; review-quality 0 CRITICAL / 0 MAJOR / 2 MINOR (cosmetic, auto-backlogged).
- **Milestone:** M10.5 (QoL polish bucket) is now feature-complete — all 4 work packages shipped (WP1 PiP top-right default, WP2 active-close confirmation, WP3 proper CC/terminal kill clean-exit, WP4 CC I/O encoding fix); `/product-finalize` closes the cycle.
- **Backlog resolved:** SURFACE-2026-07-16-M10.5-QOL-BUCKET — fully closed: all four dogfooding papercuts shipped as M10.5 WP1–WP4 (the bucket stayed at 4, no WP5+ added). *(Supersedes the earlier same-day partial-resolution note for item 3 alone.)*
- **Product cycle complete:** Milestone 10.5 (QoL polish bucket) — four pre-release dogfooding papercuts fixed (PiP opens top-right, active-close/app-quit confirmation, SIGHUP-first history-preserving CC/terminal kill, CC I/O UTF-8 encoding), all extending existing subsystems with no new architecture; durable docs (arch.md, roadmap.md) resynced and the cycle WBS archived. Next execution milestone: M11 (docs-viewer). Standing carry: WP2/WP3/WP4 installed-`.app` smoke checks ride the next `/release` gate.
- **Backlog resolved:** SURFACE-2026-07-06-M9-NATIVE-SIGNALS-BEAT-GAP-INFERENCE — resolved at M9 WP2.5+WP3 (native-signal source shipped + reclassifier measures the human states claude-time only inferred); the backlog entry outlived its close and is cleared in the backlog-paydown sweep's WP1 delete pass (its close had skipped the delete-on-resolve step).
- **Backlog resolved:** SURFACE-2026-07-06-M9-RECLASSIFIER-IS-REDESIGN-NOT-PORT — resolved at M9 WP3 (ran spec-first as a genuine metric-definitions redesign, not a port); the backlog entry outlived its close and is cleared in the backlog-paydown sweep's WP1 delete pass.
- **Backlog resolved:** SURFACE-2026-06-20-WP10-ARROW-KEY-TREE-NAV — DROPPED 2026-06-24 (arrow-key FileTree nav not wanted; click-to-open + click-to-toggle suffices); the terminal-status entry is cleared in the backlog-paydown sweep's WP1 delete pass.
- **Backlog resolved:** SURFACE-2026-07-15-M9-WP6B4-SURFACE-FORK-PRIOR-OVERRIDE — noted-only (an operator override of the `new-surface-must-earn-its-place` prior on its own axis, disclosed in the WP6b-4 WIP/spec, no design-priors.md edit owed); the terminal-status entry is cleared in the backlog-paydown sweep's WP1 delete pass.
- **Task closed:** Backlog-paydown sweep WP1 (delete-clutter pass) — removed 15 terminal-status SURFACE blocks + 2 resolved code-quality finding-bodies from the backlog, and rewrote 4 partially-resolved finding blocks down to their open remainder; a hygiene subtraction clearing residue that several M9/M10 closes left behind by skipping delete-on-resolve.

## 2026-07-18

- **Feature shipped:** M10 WP6 — the in-app auto-updater's **milestone-exit verification**, re-planned around a mid-milestone reversal of the Homebrew decision (detect-and-defer → **option B: brew installs self-update in-app too**): Phase 1 folded in the WP4 error-surface gap (a new `UpdaterStatusRow` surfaces an apply failure + manual-check note that were previously produced-but-unconsumed, so a failed update no longer silently reverts the banner); Phase B1 reverted to **one self-update path for every install** — removed the backend `InstallSource`/`install_source`/`BREW_DEFER` gate + `updater_apply`'s Homebrew refusal + the `install_source` DTO field, deleted the frontend `isBrew`/copy-`brew upgrade` banner branch + `copyToClipboard.ts`, kept the self-clear core, added absence-guards, added `auto_updates true` to the cask, and confirmed `CFBundleVersion` = the semver version (the monotonic value `brew upgrade` reconciles against); Phase B2 published v0.2.7 (updater-capable) + a no-op v0.2.8 target and ran the live pass. **Milestone-exit verdict: GO** — verified on the operator's REAL Homebrew prod install: a brew-installed 0.2.7 self-updated in-app to 0.2.8 with **zero Homebrew commands**, relaunching clean past Gatekeeper (the exact path current users experience). Backend 539 lib + 6 integ, FE 105 files / 1163; clippy `--all-targets`/tsc/eslint/vite-build clean; the reverted banner live-verified via the MCP bridge. review-quality: 0 CRITICAL / 0 MAJOR / 1 MINOR (a vestigial picker return type, auto-backlogged).
- **Backlog resolved:** SURFACE-2026-07-17-M10-BREW-DECISION-REVERSED-TO-SELF-UPDATE — the reversal's next-session rework (cask `auto_updates true` + CFBundleVersion + remove the install-source gate + remove the brew FE branch + re-verify) was executed as WP6 Phase B1/B2.
- **Backlog resolved:** SURFACE-2026-07-17-M10-WP3-BREW-DETECTION-LIVE-DEFERRED — moot: the install-source detection gate was removed under option B, so there is nothing left to live-verify.
- **Backlog resolved:** SURFACE-2026-07-17-M10-WP3-BREW-REALCHECK-RESHAPE — reverted: the reshaped brew-real-check-then-defer behavior was removed with the gate (brew now takes the same self-update path as direct-download).
- **Backlog resolved:** SURFACE-2026-07-17-M10-DIRECT-DOWNLOAD-SELFUPDATE-VERIFY-STANDALONE-WP — absorbed: under option B, brew self-update IS the direct-download self-clear/relaunch path (one path, one verdict), so no separate direct-download WP is needed; the GO verdict covers it.
- **Backlog resolved:** SURFACE-2026-07-18-M10-WP6-PHASEB1-HUMAN-VERIFY-DEFERRED — the deferred Phase B1 live confirmation ran in the WP6 Phase B2 operator milestone-exit pass (GO).
- **Backlog resolved:** SURFACE-2026-07-17-M10-WP6-PHASE1-HUMAN-VERIFY-DEFERRED — the deferred Phase 1 error-surface live confirmation folded into the same WP6 Phase B2 live pass.
- **Backlog resolved:** SURFACE-2026-07-17-M10-WP1-LIVE-VERIFY-DEFERRED — the WP1 unsigned-relaunch Gatekeeper GO/FALLBACK verdict was the load-bearing WP6 check: **GO** on a real installed build.
- **Backlog resolved:** SURFACE-2026-07-17-M10-WP2-PROBE-HARNESS-STALE-SIG — the WP6 live pass used the real published `latest.json`/`.sig` (not the stale harness fixture), and the `~/claudesk-m10-probe/` harness was deleted at close.
- **Milestone:** M10 WP6 — Milestone-exit verify (real Homebrew self-update path); this completes **all 6 M10 work packages** (WP1–WP6), so Milestone 10 (In-app auto-updater) is done and ready for `/product-finalize`.
- **Product cycle complete:** Milestone 10 (In-app auto-updater) — Claudesk now checks for, downloads, verifies (minisign), and installs a newer version from inside the app, with the user always in control (skip a version / disable notifications / cancel-confirm; no silent updates), self-clearing the unsigned-relaunch Gatekeeper quarantine so both brew and direct-download installs self-update identically; verified GO on a real Homebrew prod install. Cycle docs archived to `docs/product/archive/milestone-10-in-app-auto-updater/`.
- **Backlog resolved:** SURFACE-2026-07-06-M10-IN-APP-AUTO-UPDATER — the milestone request (in-app updater with skip/disable/cancel-confirm, before the next release) is fully delivered across WP1–WP6.
- **Backlog resolved:** SURFACE-2026-07-17-QUALITY-WP4-ERROR-STATE-UNCONSUMED — the WP4 MAJOR error-surface gap was folded into WP6 Phase 1 (a new `UpdaterStatusRow` consumes `phase==="error"`/`errorMessage`, so a failed apply no longer silently reverts the banner).
- **Backlog resolved:** SURFACE-2026-07-17-QUALITY-WP4-MENU-CHECK-DISCARDS-OUTCOME — WP6 Phase 1 wired manual-check feedback via a pure `statusNoteForOutcome`, so a native-menu check that finds you current now surfaces a note.
- **Backlog resolved:** SURFACE-2026-07-17-QUALITY-WP4-FALLBACK-VS-ERROR-RACE — WP6 Phase 1 reconciled the fallback-dialog ↔ error-phase surfaces under a single-post-install-surface invariant (fallback dialog XOR error phase).
- **Feature shipped:** M10.5 WP1 — the PiP panel now opens at a **150px inset from the screen's top+right edges** on first summon (instead of NSPanel's ~center default) and preserves a later operator drag (re-anchoring fresh per launch via an in-session `positioned` flag); the anchor runs inside `pip_resize` — where the real content-driven size lands — not the placeholder-sized build branch, verified live via the MCP bridge (3/3 outcomes) + operator verify-human.
- **Milestone:** M10.5 WP1 (PiP default position = top-right corner) — the first of Milestone 10.5's four QoL work packages; 3 remain (WP2 active-close ∥ WP3 kill-clean-exit ∥ WP4 encoding fix), so the milestone stays open.
- **Feature shipped:** M10.5 WP2 — closing a workspace whose Claude Code is mid-work (`running`/`awaiting_input`), or quitting Claudesk while any workspace is busy, now prompts a confirm before destroying in-flight work instead of silently killing it; built in 3 phases — (1) the per-workspace close gate extends the existing unsaved-editor guard via a shared `isActiveState` predicate + a reasons-object `closeWorkspaceSpec` composing dirty/active into one dialog, (2) the app-quit path is FRONTEND-DECIDES (native `CloseRequested` → `prevent_close` + a bare `quit-requested`; the frontend computes the busy set it already owns and shows an enumerated "still working — quit anyway?" confirm → `quit_now` → a shared `perform_quit_teardown` + `app.exit`), (3) a raw right-panel terminal deliberately contributes no running-command signal (CC-status-only for v1, documented at the `spawn_shell` seam). Live-verified via the MCP bridge (incl. a real `quit_now` → clean exit-0) + operator verify-human on both close + ⌘Q paths; FE 105 files/1173 + BE 543 tests green; review-quality 0 CRITICAL / 0 MAJOR / 3 MINOR (cosmetic, auto-backlogged). Installed-`.app` smoke carried to the next `/release` gate.
- **Milestone:** M10.5 WP2 (Active-close confirmation) — the second of Milestone 10.5's four QoL work packages; 2 remain (WP3 kill-clean-exit ∥ WP4 encoding fix, both reproduce-first), so the milestone stays open.

## 2026-07-17

- **Feature shipped:** M10 WP2 — the production in-app updater **core**: a stable `src-tauri/src/updater/` module exposing `updater_check` (version pre-flight) and `updater_apply` (the full `check → download → minisign-verify → install → self-clear-quarantine → relaunch` flow), with the unsigned-relaunch self-clear (`xattr -dr com.apple.quarantine` on the app's own bundle) inserted at the WP1-proven macOS seam (after `install()` returns, before `relaunch()`) and the split `download()`/`install()` preserving a clean cancel boundary for WP4. The WP1 probe scaffolding (`updater_probe/` + `UpdaterProbePanel`) was removed, its durable self-clear pure-core (+ 6 tests) migrated intact; a minimal **throwaway** "Check for updates" trigger ships until WP4 builds the polished user-control UX. Live verify-self via the MCP bridge confirmed the widget renders + the `updater_check` IPC round-trip fires end-to-end (returns the expected "no `latest.json` published yet" error — WP5's job); the destructive download→install→relaunch + Gatekeeper self-clear is carried to WP6's installed-build milestone-exit verify (operator-deferred). review-quality: 0 CRITICAL / 0 MAJOR / 3 MINOR (doc-drift, auto-backlogged). Uncommitted — batched on the unpushed M9+M10 tree per commit-only-when-asked.
- **Milestone:** M10 WP2 — Updater core (plugin wiring + check→download→install→self-clear→relaunch flow); the M10 critical-path core. Remaining M10: WP3 (brew detect-and-defer) ∥ WP4 (user-control UX) ∥ WP5 (/release pipeline) → WP6 (milestone-exit verify).
- **Feature shipped:** M10 WP3 — the **brew detect-and-defer gate**: the in-app updater now detects a Homebrew-cask install (a `/Caskroom/` bounded path-component in the canonicalized bundle path) and **defers to `brew upgrade`** instead of self-installing (which would desync brew's version bookkeeping), while a direct-download `/Applications` install self-updates normally; `updater_check` short-circuits before the network check for a brew install and `updater_apply` refuses (belt-and-suspenders), sharing one defer message. The classifier is a pure, false-positive-guarded function (matches the `Caskroom` path *component*, not the substring; unresolved/translocated/dev ⇒ the safe `DirectDownload` default) with 15 updater tests; live verify-self via the MCP bridge confirmed the dev binary classifies DirectDownload (the direct-download flow is unregressed). The real-brew `/Caskroom/` positive detection + App-Translocation observation are deferred to WP6's milestone-exit verify (needs a real brew install of an updater-capable build — gated on WP5's pipeline). review-quality: 0 CRITICAL / 0 MAJOR / 3 MINOR (documentary, auto-backlogged). Uncommitted — batched on the unpushed M9+M10 tree per commit-only-when-asked.
- **Backlog resolved:** SURFACE-2026-07-17-QUALITY-WP2-LIBRS-INVOKE-COMMENT-STALE — closed by WP3's P1.5 doc-drift fold (the `lib.rs` invoke-handler comment above the updater entries reframed from "WP1 PROBE — throwaway" to production wording).
- **Backlog resolved:** SURFACE-2026-07-17-QUALITY-WP2-CARGO-DEP-COMMENT-STALE — closed by WP3's P1.5 doc-drift fold (the `Cargo.toml` updater/process dep comment reframed from "WP1 spike wiring" to production wording).
- **Milestone:** M10 WP3 — Install-source detection (the brew detect-and-defer gate); 2 of 6 M10 WPs now shipped (WP2, WP3). Remaining M10: WP4 (user-control UX) ∥ WP5 (/release pipeline) → WP6 (milestone-exit verify).
- **Feature shipped:** M10 WP5 — the **`/release` publishing pipeline** now builds, signs, and publishes the in-app-updater artifacts (`Claudesk.app.tar.gz` + minisign `.sig` + a signed `latest.json` manifest) alongside the existing `.dmg`, so a running Claudesk can self-update: `/release` gains a signing env-var export before the build (`TAURI_SIGNING_PRIVATE_KEY` = key CONTENTS not path), a `.sig`-missing STOP guard, a `latest.json` generator (version from `tauri.conf.json`, `signature` = the `.sig` VERBATIM), a four-asset `gh release create`, and an endpoint-resolves check — plus the trust anchor was **rotated off the throwaway WP1 probe key onto a fresh release-only minisign keypair** (stored out-of-repo at `~/.claudesk-release-keys/`, key id `774E2E8429FDF78A`, its base64 pubkey swapped into `tauri.conf.json`). Proven end-to-end without a live publish: the composed manifest's signature verifies against the configured pubkey (VERIFY_OK real / VERIFY_FAIL tampered — the exact `tauri-plugin-updater` path). No app code changed (release-tooling + one config value); 539 lib + 6 integ backend tests still pass. The real live publish + real installed-build self-update are carried to WP6's milestone-exit verify (which uses the fresh key). review-quality: 0 CRITICAL / 2 MAJOR / 1 MINOR — all resolved in-place ($VER live-var-vs-placeholder convention; transient `latest.json` gitignore+cleanup; un-substituted `notes` marker). Uncommitted-to-remote — batched on the unpushed M9+M10 tree per commit-only-when-asked.
- **Backlog resolved:** SURFACE-2026-07-17-M10-WP5-SIGNING-KEY-ENV-VAR-IS-STRING-NOT-PATH — both signing gotchas applied in the `/release` skill (env var is the key CONTENTS not `_PATH`; `latest.json` `signature` is the `.sig` contents VERBATIM, not re-base64-encoded).
- **Milestone:** M10 WP5 — `/release` publishing pipeline (updater artifacts + signed manifest); 3 of 6 M10 WPs now shipped (WP2, WP3, WP5). Remaining M10: WP4 (user-control UX) → WP6 (milestone-exit verify).
- **Feature shipped:** M10 WP4 — the in-app updater's **user-control UX + persistence**, replacing WP2's throwaway trigger so the milestone's promise ("the user is always in control — no silent/forced updates") is real: two `config_store` prefs per bundle-identity (`update_notifications_enabled`, **default ON**, off-switchable per the operator-benefit design-prior; + `skipped_version`), a real `updater-download-progress` event (replacing WP2's no-op download callback), pure frontend gating (`shouldAutoNotify`/`manualCheckOutcome` — the skip-list + disable filter is frontend-side per arch.md), and the polished surface: a **non-modal top-of-window in-flow banner** (Update…/Skip-this-version/Dismiss + a live progress bar + a Homebrew-defer branch), a confirm dialog, the WP1-fallback quarantine dialog (built + seam-gated behind a default-off flag WP6 can flip), a "Check for Updates…" app-menu item, and a picker "Update notifications" toggle + "Check for updates" button — all wired App-level via a `useUpdater` hook; the throwaway `UpdaterTrigger.tsx` is deleted. The banner is an in-flow app-shell row (NOT an absolute overlay) after an operator-caught verify-human back-loop — an absolute banner would overlay the filmstrip's top strip and a slow-network late-load could steal a tile click; the reserved row (regression-guarded) removes that hazard. Banner/confirm/progress/cancel + the picker toggle round-trip + the manual-check were all driven **live via the MCP bridge**; the native menu-item click, the real-endpoint outcome toasts (up-to-date / brew-defer / update-available), and the destructive download→install→relaunch are carried to WP6's installed-build milestone-exit verify. Backend 547 lib + 6 integ, clippy `--all-targets` clean; frontend 105 files / 1154 tests, tsc/eslint/vite-build clean. review-quality: 0 CRITICAL / 1 MAJOR / 3 MINOR — all auto-backlogged (the MAJOR: the hook's `error`/`errorMessage` state is produced but unconsumed, so an apply failure currently surfaces nothing — a strong WP6 fold, since WP6 first exercises the failure path live). Uncommitted-to-remote — batched on the unpushed M9+M10 tree per commit-only-when-asked.
- **Milestone:** M10 WP4 — User-control UX + persistence (skip / disable-notifications / cancel-confirm / progress); 4 of 6 M10 WPs now shipped (WP2, WP3, WP4, WP5). Remaining M10: WP6 (milestone-exit verify on a real older unsigned installed build).

## 2026-07-16

- **Feature shipped:** M9 WP7 — the standalone `claude-time` tool is **documented as deprecated** now that Claudesk owns the capability, and the durable architecture docs are **resynced to the as-built M9 subsystem**: `arch.md` gained a new "Milestone 9 architecture" section (the absorbed 10-event hook split 4-status/6-time-analytics-only, the feature-local per-identity `time-analytics.sqlite` store, the `reclassify` redesign, the query layer's ms-precision duration summing, the session-termination model, the GLOBAL-dashboard mount-scope=data-scope principle, and the default-OFF `time_tracking_enabled` write-gate), plus corrected Key Decisions (event set 4→10, a SQLite scoped-exception carve-out to the flat-JSON rule, and coexistence→deprecation); `CLAUDE.md`'s Current-Milestone block flips to M9 all-WPs-shipped. Confirmed Claudesk has **zero runtime dependency** on `claude-time-hook.pl` (it installs its own `claudesk-hook.pl` and, per the `merge_is_additive_and_preserves_existing_hooks` test, deliberately preserves — never deletes — a user's co-resident hook; manual removal is the user's action). Docs-only change (zero code files); the agent-verifiable doc/build slice done in-session (13/13 content greps, tsc + cargo build exit 0, 524+6 backend / 1120 frontend regression green); the installed-`.app` milestone-exit smoke test carried to the next `/release` gate. review-quality: 0 CRITICAL / 0 MAJOR / 3 MINOR (1 fixed in place).
- **Backlog resolved:** SURFACE-2026-07-08-M9-WP6A-DASHBOARD-IS-GLOBAL-NOT-PER-WORKSPACE — its carried-to-WP7 arch-write-up sub-task is now done (the "a surface's mount-scope must match its data-scope" principle is recorded in `arch.md`'s Milestone 9 architecture section, alongside the global-dashboard placement rationale).
- **Milestone:** M9 WP7 — Deprecate standalone claude-time + milestone-exit verify (the M9 closer WP; completing it means **all M9 work packages are shipped** — the milestone is ready for `/product-finalize`).
- **Backlog resolved:** SURFACE-2026-06-26-ABSORB-CLAUDE-TIME-INTO-CLAUDESK — the M9-lead SURFACE, fully closed by the M9 cycle: claude-time is absorbed into Claudesk as a native, default-OFF time-analytics panel (10-event hook + write-gated per-identity SQLite + reclassifier redesign + native-signal fusion + dashboard tab) and the standalone tool is deprecated.
- **Backlog resolved:** SURFACE-2026-06-19-CM6-BUNDLE-SIZE-LAZY-LOAD — the CM6-bundle startup-trim, folded into M9 WP6a: the editor/diff/search/dashboard surfaces are lazy-loaded behind Suspense, dropping the main bundle ~63% (gzip 362→111 kB) and clearing the 500 kB chunk-size warning.
- **Backlog resolved:** SURFACE-2026-07-08-WP4-CHRONO-DEP-ADDED — the M9 query layer's direct `chrono` 0.4 dependency (local-timezone day-boundary math) is now reconciled into `arch.md` (the Milestone 9 architecture query-layer note).
- **Product cycle complete:** Milestone 9 (Time-analytics panel — absorb claude-time) — all 10 WPs shipped; Claudesk now natively measures (not just infers) where CC session time goes per project, behind a default-OFF tracking toggle, with the standalone claude-time tool deprecated. Durable docs resynced (arch.md "Milestone 9 architecture" + roadmap M9→complete), cycle-scoped docs archived to `docs/product/archive/milestone-9-time-analytics/`. Installed-`.app` milestone-exit smoke test carried to the next `/release` gate.

## 2026-07-15

- **Feature shipped:** M9 WP6b-4 — the time-analytics **Day view is now a continuous, no-mode, video-editor-style flexible timeline**: it opens framed on today and you fluidly **zoom out and pan back through days → a week → up to a month with pure gestures** — no mode switch, no "type a range to unlock multi-day." It pre-loads the last 14 days and **auto-extends** (fetches ~7 more days) as you pan toward an edge; the date picker became a **reactive readout of what the camera frames** plus a jump-to teleport (not a gate); zoom-out caps at ~30 days; project rows are a **single-open accordion** (one expanded at a time). This RE-SPEC replaced the rejected "range-mode" design (D1) — the original build's coordinate math + multi-day render engine were reused wholesale under a new fixed-origin data-window model. Frontend-only over WP4's data; live-verified via the MCP bridge + the operator's real-trackpad approval; pure math + wiring vitest-pinned (99 files / 1083 tests).
- **Backlog resolved:** SURFACE-2026-07-15-M9-DAY-VIEW-AS-PRIMARY-FLEXIBLE-TIMELINE — closed by the WP6b-4 re-spec (the Day view is now the continuous flexible timeline the operator asked for; the D1 mode/gate model that this SURFACE flagged as wrong is superseded, verified on the real trackpad).
- **Backlog resolved:** SURFACE-2026-07-13-QUALITY-MINQUANT-DAYTIMELINE-SUMKIND-DUP — killed the third and last live copy of the minute-quantization anti-pattern (`DayTimeline`'s day-view `away` pill summed the minute-quantized `end - start`, under-counting sub-minute away gaps); routed it through the corrected, pinned `kinds.ts::sumByKind` (`dur_ms`-based) and deleted the local `sumKind`.
- **Feature shipped:** M9 WP6c-1 — the time-analytics dashboard gained a **Metrics tab**: window-level aggregate analytics that answer "where did the time actually go" — a 5-tile headline (Active session · Human activity · AI effort · Away · Parallel) over a 6-section breakdown (Engaged session · AI agent + subagent · Tool call with top-5 tools · Human active · Concurrency stratification k=1…4+ · Blocking split), each showing wall-clock vs. effort-time with a parallelism multiplier. The producer is a native Rust `build_metrics` (a new `{kind:"metrics"}` query) **re-derived onto WP3's 6-kind model** — NOT a mechanical port of claude-time: AI-reasoning folds into AI-agent, human = typing + reviewing, human-blocks-agent = reviewing only, and every duration is summed at **millisecond precision** so sub-minute AI tool-calls stay visible (they render in seconds, e.g. `23s`, instead of flooring to zero). Engaged-session time consumes the WP6.5-capped bursts so a dead/dangling session no longer inflates the numbers. Live-verified on the real dev DB via the MCP bridge (every re-derivation identity confirmed) + operator-approved. 512 backend + 1099 frontend tests green. (WP6c-2, the A/B Compare view + presets, remains — the Compare tab stays disabled until it lands.)
- **Feature shipped:** M9 WP6c-2 — the time-analytics dashboard gained a **Compare tab**: an A/B side-by-side that answers "is my AI-leverage & parallelism trending up or down" — pick **WoW** (this week vs last), **MoM** (this month vs last), **Today vs a trailing-7-day baseline**, or a **Custom** A/B pair, and see 8 effectiveness rows (parallelism ×, AI-effort/human-wall, a blocking-split bar, a concurrency-mix bar, AI-agent + tool-call + human + engaged-session absolutes) with a per-row Δ (B−A). The producer is a native Rust `build_comparison_data` (a new `{kind:"compare", spec}` query) that reuses WP6c-1's `build_metrics` per side and **skips the dead `deltas` map** (the frontend recomputes deltas from the two metrics trees); preset day-math (incl. leap-Feb + Jan-rollover) is resolved backend-side. The 2 color-family bar blocks route through the AI-vs-human `familyOf`/`colorForKind` seam. Live-verified on the real dev DB via the MCP bridge (all 4 presets, correct bounds, no NaN) + operator-approved; a cosmetic bar-overflow was caught + fixed at self-verification. 524 backend + 1120 frontend tests green. **This completes WP6c** (all 3 sub-WPs: research + Metrics + Compare) — only WP7 (deprecate the standalone claude-time + milestone-exit verify) remains for M9.
- **Backlog resolved:** SURFACE-2026-07-06-M9-COLOR-FAMILIES-AI-VS-HUMAN — fully closed by WP6c-2 (the AI-vs-human color families, locked+built at WP3 and hued at WP6a, got their last consumer: the Compare tab's 2 EffectivenessRow bar blocks — blocking-split and concurrency-mix — now route through the `familyOf`/`colorForKind` seam; the design intent is realized end-to-end).
- **Milestone:** M9 WP6c — Metrics / Headline / Compare panels (all 3 sub-WPs shipped: WP6c-research + WP6c-1 Metrics tab + WP6c-2 Compare view; the aggregate + A/B analytics surfaces the timeline views don't provide. WP7 deprecate + exit-verify still remains to close M9).
## 2026-07-14

- **Feature shipped:** M9 WP6b-2 — the time-analytics dashboard's remaining time-shape surfaces are now live: a **Week** rollup grid (per-project stacked bars over the 6-kind model), a **Month** contribution-calendar (dark-anchored intensity ramp; click a day to **drill into that day's timeline**), the Day view gained a **date picker + prev/next** (the Custom tab was merged away as a strict subset of Day — a 1-day span is just Day), and clicking a segment bar opens a **SidePanel** inspecting that session (active-of-wall time, mini timeline, 6-kind activity breakdown, tool-calls, prompts). All pure render surfaces over WP4's existing data — no new backend; live-verified via the MCP bridge, pure math + wiring vitest-pinned (99 files / 1015 tests).
- **Milestone:** M9 WP6b-2 — Week / Month / SidePanel + Custom→Day merge (the render-surface ports over existing data; the date-range picker + multi-day timeline forked to WP6b-4, per-week nav to WP6b-3 — both still remain in M9, alongside WP6c metrics/compare + WP7 deprecate).
- **Feature shipped:** M9 WP6b-3 — the time-analytics **Week** view gained `‹ ›` prev/next date navigation: step back to any past week (rendered as the day-count-agnostic rollup grid via a new optional `monday` anchor on the `{kind:"week"}` query — no multi-day-timeline dependency, back-compatible), with "next" disabled at the current week and a tab-switch reset. Also fixed a broader **empty-period nav trap** (class fix): stepping Week *or* Day to a period with no activity used to replace the whole surface with a full-screen empty-state — trapping the operator with no way forward; now every nav-bearing view keeps its toolbar + arrows and shows an inline "No activity" body, so you can always step back to an active period. Live-verified via the MCP bridge (Week + Day), pure iso-week math vitest-pinned (99 files / 1032 tests).
- **Backlog resolved:** SURFACE-2026-07-14-M9-DAY-WEEK-DATE-NAV-MISSING — both halves closed (Day-nav resolved-by-merge at WP6b-2 P3; Week-nav shipped in WP6b-3 via the `{kind:"week", monday}` anchor keeping Week as the rollup grid).
- **Backlog resolved:** SURFACE-2026-07-14-M9-EMPTY-PERIOD-NAV-TRAP — closed by the WP6b-3 F12 class fix (`hasData = trackingEnabled` for all nav-bearing views + an inline empty body inside the view, keeping the nav; covered both the Week trap and the pre-existing Day trap from WP6b-2 P3).
- **Milestone:** M9 WP6b-3 — Per-period Week-nav + the empty-period nav-trap class fix (DESCOPED to Week-nav; Day-nav was delivered by WP6b-2 P3. WP6b-4 multi-day timeline + WP6c metrics/compare + WP7 deprecate still remain in M9).

## 2026-07-13

- **Feature shipped:** M9 WP6b-1 — the time-analytics day timeline is now **interactively zoomable/pannable with a Minimap**: WP6a's fixed viewport became a shared, clamped `ViewportContext` driven by drag-pan + ctrl/cmd-wheel + trackpad-pinch (cursor-anchored) zoom + a `0`/"Fit day" reset, with an adaptive hour ruler (1h→30m→15m→…→1m as you zoom); and the priority **Minimap** overview strip — an all-projects density track with a draggable rectangle (body = pan, edges = zoom, background-click = re-center) — reads/writes the same viewport so it always shows where in the day you're looking, even at extreme zoom. Frontend-only over WP4's data; pure math vitest-pinned (56 tests), gestures live-verified via the MCP bridge.
- **Backlog resolved:** SURFACE-2026-07-13-M9-WP6B1-REF-SYMLINK-MISSING — closed by recreating the gitignored `_ref/claude-customization` symlink at spec-entry (the companion-repo source-of-truth for the claude-time reference the M9 ports read).
- **Milestone:** M9 WP6b-1 — Interactive day-timeline viewport + Minimap (the operator-declared must-have; the first of the WP6b render/interaction surfaces — WP6b-2 week/month/side-panel + WP6c metrics/compare still remain in M9).
- **Feature shipped:** M9 defect fix — sub-minute AI-doing no longer zeroes in the time-analytics dashboard: the query layer was summing per-kind durations from minute-quantized segment endpoints, so AI tool-execution (Pre→Post ~1s apart) floored to zero and read as `ai_doing: 0` for tool-heavy sessions; now a true-ms `dur_ms` rides each segment and per-kind totals are summed at ms precision then rounded once (render positions stay minute-quantized — WP1 contract intact). Reproduced red-green, 3 regression pins, live-verified against the real dev DB (a tool-heavy session went ai-doing 0 → ~57s).
- **Backlog resolved:** SURFACE-2026-07-13-M9-WP4-MINUTE-QUANTIZATION-ZEROES-SUBMINUTE-AI-DOING — closed by the `m9-fix-minute-quantization-ai-doing` fix (carry `dur_ms`, sum on it, round once; `start/end` stay minute-quantized).

## 2026-07-08

- **Feature shipped:** M9 WP4 — a native Rust query layer (`time_store::query`) reads the time-analytics SQLite events for a day/week/custom window, runs the WP3 reclassifier, and emits the segment-model contract (WP1-frozen shape revised to WP3's 6-kind enum) as `DayPayload`/`WeekPayload` over a `time_analytics_query { scope, window }` Tauri command — global all-projects view with a per-project breakdown, snake_case DTOs pinned on both IPC sides, live-verified against the real app via the MCP bridge (the week query returned real rows with git-basename alias resolution).
- **Backlog resolved:** SURFACE-2026-07-07-QUALITY-WP3-TRAILING-OPEN-AWAIT-FALLS-TO-AWAY — closed by WP4 Phase 1 (a still-open AwaitingInput span at the data tail is now bounded at the window end instead of dropped, with a pinning test; kept as defensive correctness since the symptom is latent under the operator's equal silence/away thresholds).
- **Backlog resolved:** SURFACE-2026-07-07-QUALITY-WP3-SURFACE-TIE-BREAK-ORDER-DEPENDENT — closed by WP4 Phase 1 (`surface_is_editor_at` now sorts candidates by `(ts, surface)` and takes the last, making a same-ms surface tie deterministic regardless of input order, with a both-orderings pinning test).
- **Milestone:** M9 WP4 — Segment-model query layer ported to Rust.
- **Feature shipped:** M9 WP5 — a persisted `time_tracking_enabled` flag (default OFF) now gates both time-analytics write paths: the `tracking_enabled` write-gate reads the flag from `settings.json` (degrading to OFF on any error so a corrupt file can never kill the shared status-dot drain), and a "Time tracking" checkbox in the project picker (seed + broadcast-sync + optimistic-set, mirroring the permission-mode dropdown) turns it on — live-verified end-to-end via the MCP bridge, including on-disk persistence with the other settings preserved. Establishes the reusable universal-vs-workflow-coupled feature-flag pattern.
- **Milestone:** M9 WP5 — Tracking toggle (universal-vs-workflow-coupled feature-flag pattern, default OFF).
- **Feature shipped:** M9 WP6a — the native time-analytics **global day-view dashboard**: a dark, full-window all-projects view (mounted once, the picker-overlay pattern — NOT a per-workspace tab) reachable from both the picker scene and any open workspace via ⌘⇧A or an analytics button, rendering today's per-project timeline (session rows tiled by the 6-kind AI-cool/human-green palette, hour ruler, summary strip, legend) from the WP4 query; session overlaps render for same-project concurrency only; and the dashboard + CodeMirror chunks now lazy-load, cutting the main bundle from 1,156 kB to 426 kB and clearing the 500 kB warning.
- **Backlog resolved:** SURFACE-2026-07-08-M9-WP6A-DASHBOARD-IS-GLOBAL-NOT-PER-WORKSPACE — closed by WP6a shipping the dashboard as a global full-window view (mount-scope matches its all-projects data-scope), superseding the initial per-workspace-tab build.
- **Backlog resolved:** SURFACE-2026-07-08-M9-WP6A-OVERLAP-MUST-BE-SAME-PROJECT-ONLY — closed by scoping `detectSessionOverlaps` per-project at the source, so cross-project concurrency renders no overlay (live-verified against the raw dev store).
- **Backlog resolved:** SURFACE-2026-07-08-M9-WP6A-DASHBOARD-FROM-PICKER — closed by moving the dashboard overlay to the app-shell top level + ungating the ⌘⇧A chord + adding a picker analytics button, so the global dashboard opens from the picker scene too.
- **Backlog resolved:** SURFACE-2026-06-19-CM6-BUNDLE-SIZE-LAZY-LOAD — closed by lazy-loading the CodeMirror surfaces (editor/diff/search) below EditorSplit's eager ref handle, evicting CM6 from the initial bundle (main 1,156 kB → 426 kB, 500 kB warning gone).
- **Milestone:** M9 WP6a — Day-view MVP + global-view wiring + CM6/dashboard lazy-load fold-in.
- **Feature shipped:** M9 WP6.5 — the time tracker now **measures** session termination instead of inferring it from the last hook event: a dead/closed/crashed session's row ends at its true end (not stretched to the day edge reading "running all day"), fusing four signals — an explicit `WorkspaceClose` marker on workspace-close/app-quit, a 30-min max-idle cap keyed on idle gaps, CC's `SessionEnd` hook honored as the authoritative end (empirically confirmed to fire on clean/graceful close), and startup reconciliation that closes sessions left dangling by a crash/force-quit — with read-time capping so historical rows self-fix without a destructive backfill; reconciliation + the close marker were live-proven via the MCP bridge.
- **Backlog resolved:** SURFACE-2026-07-08-M9-SESSION-TERMINATION-NOT-TRACKED — closed by WP6.5's fused session-end model (the operator's scratch-b "10:54→13:42 running all day" defect).
- **Milestone:** M9 WP6.5 — Session-termination model (measure session END, don't infer it).

## 2026-07-07

- **Feature shipped:** M9 WP2 — Claudesk's CC hook now forwards the full 10-event set with 5 wire fields (length-only prompt privacy), and a new `time_store` rusqlite module persists every event into a per-identity SQLite DB as a second, gated drain of the hook stream (fanned out so the M3 status dots stay byte-for-byte unchanged, live-verified idle→running→idle on the real binary); the write path ships dormant (gate defaults OFF until WP5) with a `source` discriminator seeded for WP2.5's native signals.
- **Milestone:** M9 WP2 — Absorbed hook + write-gated SQLite writer.
- **Feature shipped:** M9 WP2.5 — Claudesk now captures its own native signals (window focus/blur, real PTY keystroke activity, active right-panel surface, and Claudesk-initiated Sublime/Merge/Finder launches) as a second `source="claudesk-native"` event stream into the same gated `time_store`, attributed to the focused workspace — giving WP3's reclassifier real data to *measure* the human states claude-time could only infer from hook-stream gaps; privacy is enforced by closed-enum type shape (counts/timing/attribution only, never keystroke content — live-proven that injected text appears in 0 DB rows and 0 raw file bytes), all writes gated OFF by default until WP5.
- **Milestone:** M9 WP2.5 — Claudesk-native signal source (focus/blur + PTY keystrokes + registry attribution).
- **Feature shipped:** M9 WP3 — the time-analytics reclassifier is a metric-definitions REDESIGN (not a port of claude-time): a new pure-logic `reclassify` module turns the two-source event stream into typed segments where the human states are MEASURED from Claudesk's native signals rather than inferred from hook-stream gaps — window focus/blur consulted only in AI-idle gaps, a launch or CC-AwaitingInput credits "working" bounded by a 10-min reset-on-activity silence cap (the "popped Sublime vs pulled into a meeting" distinction), reading+thinking honestly collapsed to one `reviewing` kind, and AI activity split into ai-doing/subagent/ai-reasoning; 64 tests incl. a 1:1 scenario suite over the operator's locked A1–A5/B1–B5 truth-table.
- **Backlog resolved:** SURFACE-2026-07-07-M9-NOTIFICATION-TYPE-NOT-PERSISTED — closed by WP3 Phase 1 (persist `notification_type` into the time-store row `meta` so the reclassifier can derive CC's AwaitingInput state, reusing `status_broadcaster`'s classifier as the single source of truth).
- **Milestone:** M9 WP3 — Reclassifier REDESIGN (metric definitions).

## 2026-07-06

- **Feature shipped:** Filmstrip + PiP live-mirror tiles now fill from the bottom — a fresh CC session's banner bottom-anchors at the tile edge clear of the header (instead of being occluded), via a trailing-blank-row trim at the single shared serialize seam so both surfaces inherit the fix.
- **Backlog resolved:** SURFACE-2026-06-25-FILMSTRIP-MIRROR-BANNER-OCCLUDED-AT-SESSION-START — closed by the mirror-fill-from-bottom feature (trimTrailingBlankRows wraps the XtermPane serializer thunk; filmstrip + PiP both inherit it downstream of mirrorFrame).
- **Feature shipped:** M9 WP1 time-analytics probe — froze the segment-model JSON contract (oracle-cited), enumerated the hook delta (4→10 events + 4 wire fields, length-only privacy invariant), and proved `dashboard.jsx` ports mechanically to a dark React-19 TSX tab (verdict GO-WITH-SPLIT into WP6a/6b/6c; darker-fill palette + luminance `textOn` ink locked as the WP6 starting point).

## 2026-07-02

- **Feature shipped:** Replaced the project-picker's binary yolo checkbox with a dropdown of all six Claude Code `--permission-mode` values (default, plan, acceptEdits, auto, dontAsk, bypassPermissions), persisted app-global, mirrored in a native View-menu radio submenu, with legacy `cc_yolo` migration on upgrade.

## 2026-06-30

- **Task closed:** Debt-paydown WP1 (dead-code & dead-dependency removal) — the deletions-first work package of the between-M8/M9 code-quality sweep: removed the dead `ignore = "0.4"` crate (and rewrote the `regex`/`notify-debouncer-full` Cargo.toml comments that falsely claimed a shared `ignore`-walker `.gitignore` contract to describe the real manual-DFS heavy-dir walk), the dead `data-active-pane` JSX attribute + its stale CSS comment cross-refs, the inert `.filmstrip { flex-shrink }` and `.file-tree-dir-status { margin-left }` rules, the unused `backdropRef` in the picker, and the 3 vestigial-on-NSPanel `data-tauri-drag-region` attrs in the PiP panel — all behavior-preserving, gated green by `cargo build`/clippy/302 Rust tests + `tsc`/`vite build`/eslint/780 frontend tests.
- **Backlog resolved:** SURFACE-2026-06-28-QUALITY-WP6-DEAD-IGNORE-DEP — the dead `ignore` dependency removed from Cargo.toml + Cargo.lock and its stale surrounding comments rewritten.
- **Backlog resolved:** SURFACE-2026-06-25-QUALITY-WP7-DEAD-DIR-STATUS-CSS — the no-op `.file-tree-dir-status { margin-left: auto }` deleted and its mis-attributing comment corrected to name `.file-tree-name { flex: 1 }` as the real right-push.
- **Backlog resolved:** SURFACE-2026-06-23-QUALITY-WP4B-DEAD-DATA-ACTIVE-PANE — the dangling `data-active-pane` render-time attribute dropped from EditorSplit.
- **Backlog resolved:** SURFACE-2026-06-23-QUALITY-WP4B-STALE-COMMENT-XREF — the App.css comment cross-ref repointed off the deleted `.editor-pane[data-active-pane]` selector to the live `.editor-split-pane.is-active::before`.
- **Backlog resolved:** SURFACE-2026-06-18-QUALITY-WP5-FILMSTRIP-FLEX-SHRINK — the inert `flex-shrink: 0` removed from the grid-child `.filmstrip`.
- **Backlog resolved:** SURFACE-2026-06-23-QUALITY-WP2-OVERLAY-DEAD-BACKDROPREF — the unused `backdropRef` (and its now-orphaned `useRef` import) removed from PickerOverlay.
- **Backlog resolved:** SURFACE-2026-06-26-QUALITY-WP4-VESTIGIAL-DRAG-REGION — the 3 inert `data-tauri-drag-region` attributes removed from the PiP panel and the 3 stale comments repointed to the real `startPanelDrag`→`pip_move` drag path.
- **Task closed:** Debt-paydown WP2 (thread the status-snippet tooltip) — the live workspace-status map now carries each event's last output/prompt snippet alongside its state (`WorkspaceStatusEntry`), threaded through `useWorkspaceStatus` → `App` → the CenterStage/Workspace header, the filmstrip tiles, and the PiP tiles, so hovering any status dot shows what CC last said (previously the backend sent the snippet but the frontend dropped it and the tooltip always fell back to the status label); the planned git-status path-keying half was dropped as a stale-premise no-op (the nested-repo re-base it targeted already shipped at M2 close).
- **Backlog resolved:** SURFACE-2026-06-22-QUALITY-WP6-SNIPPET-TOOLTIP-DEAD-PATH — the dead `statusSnippet`/tooltip path threaded end-to-end (map stores `{state, snippet}`, `snippetFor` accessor added, all status surfaces pass the snippet) with 4 new reducer/accessor tests.
- **Backlog resolved:** SURFACE-2026-06-21-QUALITY-WP11-GIT-STATUS-PATH-KEYING — stale detail entry reconciled to RESOLVED; the path-keying re-base it described already shipped at M2 close (task `m2-wp11-git-status-path-keying`), so no further code was needed.
- **Task closed:** Debt-paydown WP3 (cross-language menu-id contract pin + picker IPC hygiene) — added a Rust test that reads `menuBridge.ts` as text and pins every functional menu id to its frontend `MENU_IDS` value (a one-char id drift now fails the build instead of silently dead-clicking a menu item), confirmed the picker's IPC error-surfacing was already built at M4 WP2, and folded three picker nits (refresh recents after Open Folder…, document the deliberate `add_project`/`record_open` alias, and stamp `i64::MAX` instead of `0` on the unreal pre-epoch-clock path so a just-opened project sorts first); gated green by 303 Rust tests + clippy and `tsc`/783 frontend tests.
- **Backlog resolved:** SURFACE-2026-06-24-QUALITY-APPMENU-CROSS-LANG-ID-CONTRACT — the unguarded Rust↔TS menu-id contract pinned by a string-grep Rust test asserting each `FUNCTIONAL_IDS` literal appears in `menuBridge.ts`.
- **Backlog resolved:** SURFACE-2026-06-18-QUALITY-PICKER-IPC-NO-ERROR-HANDLING — confirmed resolved; the picker's toast-based IPC error surface (`mapIpcError`, loader `.catch`, per-handler try/catch) was already built at M4 WP2.
- **Backlog resolved:** SURFACE-2026-06-19-QUALITY-WP9-PICKER-PARTIAL-FAILURE-WINDOW — the prune-then-list mount loader's shared catch now surfaces an error toast, closing the partial-failure window.
- **Backlog resolved:** SURFACE-2026-06-18-QUALITY-PICKER-ADD-NO-REFRESH — Open Folder… now prepends `add_project`'s returned record into local recents, restoring symmetry with the remove path.
- **Backlog resolved:** SURFACE-2026-06-18-QUALITY-CMD-ADD-RECORD-IDENTICAL — documented `add_project`/`record_open` as a deliberate alias over `add_or_touch` (the single point of truth) instead of collapsing the two IPC commands.
- **Backlog resolved:** SURFACE-2026-06-18-QUALITY-NOW-MS-EPOCH-SENTINEL — `now_ms` no longer falls back to `0` on a pre-epoch clock (which would sink a just-opened project to the bottom of recents); it logs the fault and stamps `i64::MAX` so the record sorts first.
- **Task closed:** Debt-paydown WP9 (git-status live-refresh on `.git/` ops — folded-in user bug report) — the FileTree git-status badge no longer goes stale after a `git add`/commit/stash/checkout: the `fs-change` watcher now classifies `.git/{index,HEAD,MERGE_HEAD,refs}` writes as a git-status-only signal (`FsChange.git_meta`) that re-fetches the indicator map without re-walking the tree (the `.git/` exclusion that guards the tree against a re-walk storm is kept; the meta signal is routed past it rather than suppressed), where previously a pure-`.git/` op that flipped a file's status with no working-tree change emitted nothing and the badge stayed stale until a remount; gated green by 307 Rust tests + clippy and `tsc`/784 frontend tests, with the live git-op-transition matrix carried to the release gate.
- **Backlog resolved:** SURFACE-2026-06-30-GIT-STATUS-STALE-ON-GIT-OPS — the friend-reported stale-git-badge-until-remount bug fixed by routing `.git/` index/HEAD/refs writes to a git-status-only refresh.
- **Task closed:** Debt-paydown WP4 (effect/listener-thrash cleanup) — of the five grouped findings, two were live React refactors (the ⌘⇧+digit chord listener no longer re-subscribes on every `tiles` churn — `tiles`/`focusWorkspace` lifted into a latest-ref so it registers once per view; and the ⌘T/⌘W keydown branches no longer re-inline the terminal open/close bodies — `addTerminal`/`closeTerminalById` promoted to `useCallback` so the buttons and chords share one impl), two were confirmed already-resolved (the `kill_all` N×3s grace was parallelized at M4 WP2, and `onSessionId` was already ref-held in XtermPane), and the per-workspace PiP-mode subscription was deferred to M9 as the project's intended backend-broadcast-subscribe pattern, not debt; behavior-preserving, gated green by `tsc`/eslint/784 frontend tests/clean `vite build` with the backend untouched (307 Rust tests).
- **Backlog resolved:** SURFACE-2026-06-23-QUALITY-WP3-CHORD-EFFECT-THRASH — the ⌘⇧+digit document keydown listener now reads `tiles`/`focusWorkspace` via a latest-ref and registers once per `[view]` transition instead of re-subscribing on every tile-roster change.
- **Backlog resolved:** SURFACE-2026-06-28-QUALITY-WP11-HANDLER-BRANCH-DUPLICATION — `addTerminal`/`closeTerminalById` promoted to `useCallback` so the ⌘T/⌘W keydown branches and the ＋/✕ buttons share one open/close impl without re-registering the keydown listener.
- **Task closed:** Debt-paydown WP5 (comment/doc-drift sweep, incl. the D2 editor-shell docs) — a behavior-preserving comment-only pass across the backend, frontend, and `tooling/demo/`: narrowed the `editor_fs` symlink-rejection claim to leaf-vs-non-leaf granularity + noted `root` is frontend-trusted (D2 docs-only; the auth-boundary hardening stays deferred), repointed the `pip_toggle`→`pip_set_mode`/`pip_set_visible` doc refs (dropping a broken intra-doc link), distinguished `pip_move`'s raw-`msg_send!` from `set_content_size`'s wrapper, trimmed the `start_broadcaster` Result-contract + `apply_update` test-coverage overclaims, rewrote the stale sublime "removed at WP8" comment to the permanent-escape-hatch reality, consolidated four triplicated rationales (Workspace visible-edge focus, XtermPane rAF-fit-focus, the editor array-identity-reconfigure, the PaneTabs render-fresh-ref, the App.css focus-coexistence, the status-broadcaster unknown-notification fallback) to one canonical anchor + back-references, and cleaned the demo tooling (deleted a dead PiP focus-flag/CSS + a duplicated cursor comment, recast the smoke timeline to four unrelated projects, fixed the README example duration + committed-CSS note + a raw-HTML caution); ~4 of the listed findings were stale premises already-corrected-in-place (confirmed, not re-fixed); gated green by `tsc`/eslint/784 frontend tests/8 demo nodetests + 307 Rust tests/clippy/`rustdoc -D broken_intra_doc_links`.
- **Backlog resolved:** SURFACE-2026-06-19-QUALITY-WP2-RESOLVE-WITHIN-LEAF-SYMLINK — the `editor_fs` doc downgraded to state the actual guarantee (non-leaf symlink rejected, leaf symlink not followed-and-validated); the canonicalize-full-target hardening stays deferred (D2 docs-only).
- **Backlog resolved:** SURFACE-2026-06-19-QUALITY-WP2-BACKEND-TRUSTS-FRONTEND-ROOT — the `editor_fs/commands.rs` doc now states `root` is frontend-supplied/-trusted; the validate-against-config_store hardening stays deferred (D2 docs-only).
- **Backlog resolved:** SURFACE-2026-06-27-QUALITY-WP5-STALE-PIP-TOGGLE-DOC-REFS — the `pip/commands.rs` doc refs repointed `pip_toggle`→`pip_set_mode`/`pip_set_visible` and the broken `[pip_toggle]` intra-doc link removed.
- **Backlog resolved:** SURFACE-2026-06-26-QUALITY-WP4-PIPMOVE-COMMENT-INACCURATE — `pip_move`'s safety comment reworded to distinguish its raw `setFrameOrigin:` `msg_send!` from `set_content_size`'s tauri-nspanel wrapper.
- **Backlog resolved:** SURFACE-2026-06-26-QUALITY-WP4-STALE-AWAITING-SCALE-COMMENT — the `pipFanoutWiring.test.ts` comment corrected to "glows the dot, no size scale" (the P4.2-dropped scale).
- **Backlog resolved:** SURFACE-2026-06-29-QUALITY-M7-APPLY-UPDATE-COMMENT-OVERSELL — trimmed `apply_update`'s "callable from tests' shape" overclaim (the pure `aggregate_alarm` fold is what's tested, not the AppHandle-bound wrapper).
- **Backlog resolved:** SURFACE-2026-06-25-QUALITY-WP3-TRIPLICATED-EFFECT-RATIONALE — the Workspace visible-edge focus comment trimmed to the WKWebview-rAF non-obvious bit + a commit/WIP pointer.
- **Backlog resolved:** SURFACE-2026-06-25-QUALITY-WP2-TRIPLE-RATIONALE-COMMENT — the unknown-notification-type→AwaitingInput rationale consolidated to the `INPUT_NEEDED_NOTIFICATION_TYPES` const doc + back-references.
- **Backlog resolved:** SURFACE-2026-06-20-QUALITY-WP3A-COMMENT-TRIPLICATION — the editor array-identity-reconfigure rationale anchored at the `languageOverrideId` doc; the EditorPanel `useMemo` comment back-references it.
- **Backlog resolved:** SURFACE-2026-06-23-QUALITY-WP4B-COEXISTENCE-COMMENT-DUP — the focus-indicator coexistence rationale consolidated to the WP4b half-accent block; the F12 `.is-active::before` block back-references it.
- **Backlog resolved:** SURFACE-2026-06-19-QUALITY-RAF-FOCUS-DUPLICATION — the XtermPane rAF-fit-focus mount site made canonical; the post-spawn + on-active sites back-reference it.
- **Backlog resolved:** SURFACE-2026-06-22-QUALITY-WP13-MINORS (#1) — the PaneTabs `closeActiveTabRef` comment trimmed to a back-reference to the reporter-ref pattern (#2/#3 deferred to WP6).
- **Backlog resolved:** SURFACE-2026-06-22-QUALITY-WP4-MINORS (#1) — the `start_broadcaster` docstring trimmed to match its `JoinHandle<()>` signature (no error channel; double-start guard at the lib.rs call site); #2 dismissed (the `.expect` precedent stands), #3 folded in.
- **Backlog resolved:** SURFACE-2026-06-22-QUALITY-WP2-MINORS (#4) — the stale `sublime_open` "removed at WP8" comment rewritten to the WP8-redefinition permanent-escape-hatch reality.
- **Backlog resolved:** SURFACE-2026-06-29-QUALITY-M8WP4-DUP-CURSOR-COMMENT — the duplicated cursor-track comment block in `timeline.pip.js` deleted.
- **Backlog resolved:** SURFACE-2026-06-29-QUALITY-M8WP4-DEAD-FOCUS-FLAG — the dead `pipFocused`/`row.focused` flag uses deleted from the demo `shell.js`.
- **Backlog resolved:** SURFACE-2026-06-29-QUALITY-M8WP4-DEAD-FOCUS-CSS — the orphaned `.pip.focused` rule deleted from the demo `shell.css`.
- **Backlog resolved:** SURFACE-2026-06-29-QUALITY-M8WP3-SMOKE-TIMELINE-ONE-SYSTEM-NAMING — the smoke timeline recast from four-services-of-one-system to four unrelated projects.
- **Backlog resolved:** SURFACE-2026-06-29-QUALITY-M8WP2-README-DURATION-DRIFT — the demo README example duration aligned to the wired `smoke` script (`3.2`) with a per-timeline note.
- **Backlog resolved:** SURFACE-2026-06-29-QUALITY-M8WP2-COMMITTED-GENERATED-CSS-UNDOCUMENTED — the demo README now documents `_dots.generated.css` as the committed drift-guard baseline.
- **Backlog resolved:** SURFACE-2026-06-29-QUALITY-M8WP2-SHELL-INNERHTML-RAW-MARKUP — a raw-HTML caution added to the demo `shell.js` TIMELINE-shape doc for scenario authors.
- **Backlog resolved:** SURFACE-2026-06-19-QUALITY-CC-KILL-SIGTERM-COMMENT-DRIFT — confirmed already-corrected (the `cc_kill` doc already says SIGKILL; zero SIGTERM strings).
- **Backlog resolved:** SURFACE-2026-06-20-QUALITY-WP4-STALE-FILE-BASE-DOCLINK — confirmed already-corrected (the rustdoc already links `[file_hunks_core]`, no `file_base_core`).
- **Backlog resolved:** SURFACE-2026-06-20-QUALITY-WP4-WRONG-DIFF-API-COMMENT — confirmed already-corrected (the doc already names `diff_index_to_workdir` for the unstaged path).
- **Backlog resolved:** SURFACE-2026-06-29-QUALITY-M8WP3-STALE-JSDOC-CAST — confirmed already-corrected (the filmstrip-timeline JSDoc already names the recast cast; no api-gateway/web-client refs).
- **Task closed:** Debt-paydown WP6 (duplication-extraction & test/aria hygiene) — six behavior-preserving dedup themes: promoted the thrice-copied `resolve_data_dir` + `PROJECTS_FILE` to one `pub(crate)` source in `config_store` (Theme A); extracted a shared `makeFontZoom` factory behind both `terminalFontZoom`/`editor/fontZoom` and derived the editor default from the terminal default + a `===` parity test (Theme B); wired `aria-controls`/`role=tabpanel` across all three tab rows (Theme E); fixed/strengthened over-claiming tests (the demo "animated" GIF test now counts frames, the Ctrl/Alt cases now set the flags, the duplicated respawn truth-table trimmed, the latch assertion anchored to the relaunch path, the bare-`13`s → a named `SAMPLE_PX`) (Theme F); single-sourced three two-language predicates — a canonical `ChordEvent` type, a `data-split` attribute replacing a re-derived CSS `:has()` selector, and a paired Rust-serde + TS-exhaustive `GitFileStatus` drift guard (Theme H); and extracted `useTauriListen` from the 5×-copied async-listen/cancel/unlisten boilerplate, converting Pip ×4 + usePipFanout + useMirrorTicker ×2 + useWorkspaceStatus (Theme I); plus the two folded WP11 nits (non-UTF-8-path comment, redundant CSS pin); gated green by 308 Rust tests + clippy + rustdoc and `tsc`/eslint/787 frontend tests/8 demo nodetests/clean `vite build`. `PANETABS-COMPONENT-TEST-GAP` buried (needs a jsdom toolchain the repo deliberately lacks).
- **Backlog resolved:** SURFACE-2026-06-28-QUALITY-WP8-FONT-PARITY-COMMENT-ONLY — the editor `DEFAULT_FONT_PX` is now derived from `DEFAULT_TERMINAL_FONT_PX` and pinned by a `===` parity test, so the shared-default invariant is load-bearing, not comment-only.
- **Backlog resolved:** SURFACE-2026-06-28-QUALITY-WP8-SIBLING-TEST-BARE-LITERALS — the bare `13` literals in the fontZoom clamp/step tests replaced with a commented `SAMPLE_PX` const (an arbitrary in-range probe, not the default).
- **Backlog resolved:** SURFACE-2026-06-28-QUALITY-WP11-TABLIST-ARIA-CONTROLS — `aria-controls`/`role=tabpanel`/`aria-labelledby` wired across the term-tab-row, the panel-tab row, and the editor-file tab strip, with a `?raw` source-grep test pinning the panel-tab id pairing.
- **Backlog resolved:** SURFACE-2026-06-29-QUALITY-M8WP5-TEST-NAME-OVERCLAIMS-ANIMATED — the demo-GIF test extended to count Graphic Control Extension markers and assert ≥2 frames, so "animated" is proven rather than just the GIF magic.
- **Backlog resolved:** SURFACE-2026-06-25-QUALITY-WP6-TEST-CTRLALT-NAME-OVERPROMISE — `NewWorkspaceChordEvent` widened to the canonical `ChordEvent` (optional `ctrlKey/altKey`) and the "permissive on Ctrl/Alt" case now actually sets both true.
- **Backlog resolved:** SURFACE-2026-06-25-QUALITY-WP4-REPRO-TEST-DUP-TRUTH-TABLE — the duplicated `shouldSpawnOnActive` four-case truth table removed from the respawn repro file (kept the red-import + dep-tuple-inertness signal + a pointer to the exhaustive guard test).
- **Backlog resolved:** SURFACE-2026-06-25-QUALITY-WP4-UNANCHORED-LATCH-ASSERTION — the `hasSpawnedRef.current = false` source assertion anchored to the adjacent `dispatch({ type: "relaunch" })` so an unrelated ref reset can't satisfy it.
- **Backlog resolved:** SURFACE-2026-06-22-QUALITY-WP13-MINORS — #2 (shared `ChordEvent` type — `CloseTabChordEvent`/`TabSwitchChordEvent` now alias the canonical type) and #3 (the missing Ctrl/Alt-permissive `isCloseTabChord` test case) completed.
- **Backlog resolved:** SURFACE-2026-06-20-QUALITY-WP3C-IS-SPLIT-PREDICATE-DUP — the is-split predicate single-sourced via a JS-set `data-split` attribute the CSS keys off, replacing the re-derived `:has(.editor-split-pane + .editor-split-pane)` selector.
- **Backlog resolved:** SURFACE-2026-06-21-QUALITY-WP11-MINORS — all three closed: the non-UTF-8-path-drop comment added, the redundant `.file-tree-status { margin-left: auto }` removed, and the `GitFileStatus` TS↔Rust drift channel closed with a paired Rust-serde-forms + TS-exhaustive-glyph guard.
- **Task closed:** Debt-paydown WP7 (lint/test-infra hygiene) — the repo's own quality gates now pass clean on a fresh tree: `pnpm eslint .` exits 0 (was 63 errors — the noise was `tooling/demo/**` lacking an env block, fixed with two flat-config blocks giving the `.mjs` scripts Node+browser globals and the `.js` rendered-page files browser globals; the cited gitignored-scratch case was already ignored), `prettier --check .` is clean (was 64 files — ran `pnpm format` tree-wide after loosening two brittle `?raw` source-grep regexes that wrap-break on a format pass, and moved hand/skill-authored prose — `.claude/memory/`, `.claude/skills/`, `CHANGELOG.md` — into `.prettierignore` rather than reformatting it), and the `.mcp.json` server is pinned to `@hypothesi/tauri-mcp-server@0.11.2`; gated green by `tsc`/787 frontend tests/8 demo nodetests, backend untouched. `@typescript-eslint/no-floating-promises` was declined (needs type-aware linting the flat config deliberately lacks — not the gated "cheap + clean").
- **Backlog resolved:** SURFACE-2026-06-27-ESLINT-WALKS-GITIGNORED-SCRATCH-FIXTURE — `pnpm eslint .` exits 0; the cited `src-tauri/tmp/` scratch was already ignored, and the real `tooling/demo/**` `no-undef` noise was fixed with per-glob env blocks (not a blanket ignore — the demo files have nodetests).
- **Backlog resolved:** SURFACE-2026-06-26-PRETTIER-DRIFT-AND-BRITTLE-RAW-REGEX-TEST — the tree was `pnpm format`-normalized and the two brittle `?raw` regexes (`fileTreeGitRollup` useMemo + `terminalListWiring` ref ternary) loosened to whitespace-tolerant token-order matches.
- **Backlog resolved:** SURFACE-2026-06-18-MEMORY-MD-PRETTIER-NITS — `.claude/memory/` (plus `.claude/skills/` and `CHANGELOG.md`) added to `.prettierignore`, consistent with the existing hand-authored-prose exemptions.
- **Backlog resolved:** SURFACE-2026-06-26-QUALITY-WP2-UNPINNED-MCP-SERVER — `.mcp.json` pinned to `@hypothesi/tauri-mcp-server@0.11.2` so verify-self can't float to a broken release on a future `npx -y`.
- **Task closed:** Debt-paydown WP8 (README freshen + status-log size-cap — the sweep's one prod-runtime-behavior change, done last) — freshened the stale README Status block ("Milestones 1–4 shipped" + old roadmap → "Milestones 1–8 shipped, released as v0.2.3", names the PiP + menu-bar surfaces, lists the current M9-time-analytics→M13-polish roadmap), and implemented best-effort size-cap rotation in `status_log` (operator's D3 keep+cap ruling — keep it as a standing prod diagnostic but bound its growth): before each append, a live `status-channel.log` at/over 5 MiB rotates to `status-channel.log.1` (replacing any prior generation) so disk stays bounded to ~2× the cap, with the module doc-comment rewritten from "WP2 will likely demote this" to the keep+cap reality; gated green by 312 Rust tests (+4 rotation-boundary) + clippy, with the github.com README render carried to the next `/release` gate.
- **Backlog resolved:** SURFACE-2026-06-29-QUALITY-M8WP5-STALE-STATUS-BLOCK-NOW-PROMINENT — the README Status block freshened to current milestone reality + the resequenced roadmap.
- **Backlog resolved:** SURFACE-2026-06-27-WP1-STATUS-LOG-KEEP-OR-DEMOTE — the prod status-channel log KEPT (it self-confirms a future status bug) and size-capped via best-effort rotation at 5 MiB, per the operator's D3 keep+cap ruling.
- **Task closed:** Debt-paydown sweep #2 WP1 (stale-finding reconciliation) — deleted 27 closed code-quality findings from `backlog-quality-findings.md` (114→87 findings): the 21 already-archived findings inside the four `> ALL RESOLVED 2026-06-17` scaffold/probe groups (wp1-tauri-scaffold, wp2-cc-pty-probe, wp3-sublime-cli-probe, wp4-thumbnail-rendering-probe) plus 6 findings sweep #1 fixed-in-code but never closed (the cleanup tasks used their own SURFACE IDs), each re-verified stale against current `src/` before deletion — pure bookkeeping, no code change.
- **Backlog resolved:** SURFACE-2026-06-27-QUALITY-WP4-THIRD-ZOOM-MODULE-COPY — closed; the third zoom-module copy was extracted to the shared `makeFontZoom` factory (`fontZoomCore.ts`) by sweep #1 WP6.
- **Backlog resolved:** SURFACE-2026-06-28-QUALITY-WP7-THIRD-RESOLVE-DATA-DIR-COPY — closed; `resolve_data_dir` is now a single `pub(crate)` fn in `config_store::commands` imported by `pip`/`cc_session` (sweep #1 WP6).
- **Backlog resolved:** SURFACE-2026-06-26-QUALITY-WP3-DUP-MIRROR-INTERVAL-CONST — closed; `MIRROR_INTERVAL_MS` is a single export in `useMirrorTicker.ts` with no Filmstrip duplicate (sweep #1 WP6).
- **Backlog resolved:** SURFACE-2026-06-26-QUALITY-WP3-LISTEN-BOILERPLATE-DUP — closed; the 5× `listen().then()` boilerplate was extracted to `useTauriListen` (sweep #1 WP6).
- **Backlog resolved:** SURFACE-2026-06-20-QUALITY-WP5-TERMINAL-SEAM-UNTESTED — closed; the WP9-handoff risk it guarded is resolved — the `panel-terminal` JSX slot + slot-guard test now exist (M6 WP9).
- **Backlog resolved:** SURFACE-2026-06-24-QUALITY-DEVPROD-PROJECTS-FILE-DUP — closed; `PROJECTS_FILE` is now a single `pub(crate) const` imported by `config_store::commands` (sweep #1 WP6), no local copy.
- **Backlog resolved:** SURFACE-2026-06-22-QUALITY-WP6-MINORS (sub-finding #2) — the unfed `last_output_snippet`/`snippet`-prop tooltip path closed; sweep #1 WP2 threaded the snippet end-to-end via `snippetFor` (sub-finding #1, the `stateFor` per-render closure, remains open).
- **Task closed:** Debt-paydown sweep #2 WP2 (comment/doc copyedit pass) — a behavior-preserving comment/doc/string-only sweep across 20 files resolving 24 code-quality findings + closing 2 confirmed-stale ones: documented the symlink-skip + per-dir `read_dir` cost in `fs_index`, the no-spaces-in-`.pl`-segments assumption in `hook_install`, the `to_lowercase()` case-fold contract in `classify_spawn_error`, the label-only-menu-id negative-space test, the unused `TRAY_ID` future-lookup intent, the deliberate `cc-yolo` double-subscribe + silent menu-write-failure, the `Mod-d` double-bind + `Mod-\` memo-dep invariant, the new-file blur-cancel + `validateRelSegments` defense-in-depth framing, the fuzzy-match dot-boundary bonus, and assorted copyedits (the `.diff-commits` typo, the wrap-toggle tooltip → action label, the over-long `search_core` doc line); gated green by `tsc`/eslint/787 frontend tests + clippy `-D warnings`/312 Rust tests/`rustdoc -D broken_intra_doc_links`.
- **Backlog resolved:** SURFACE-2026-06-28-QUALITY-WP10-SHARED-KEY-LAG-COMMENT, SURFACE-2026-06-25-QUALITY-WP8-COMMENT-COPYEDIT-SLIP, SURFACE-2026-06-25-QUALITY-WP8-FALLBACK-COUPLING, SURFACE-2026-06-25-QUALITY-WP5B-GUARD-PARITY-COMMENT, SURFACE-2026-06-25-QUALITY-WP5-NEWFILE-BLUR-DISCARDS, SURFACE-2026-06-25-QUALITY-WP1-DOCSREF-FORWARD-REF-COMMENT, SURFACE-2026-06-25-QUALITY-WP1-OVERNARRATED-X-COMMENT, SURFACE-2026-06-19-QUALITY-WP9-CLASSIFY-CASEFOLD-COMMENT, SURFACE-2026-06-19-QUALITY-WP3-PROBE-SECTION-SHORTHAND, SURFACE-2026-06-28-QUALITY-WP6-DOC-WRAP-NIT, SURFACE-2026-06-28-QUALITY-WP6-SYMLINK-SKIP-UNDOCUMENTED, SURFACE-2026-06-28-QUALITY-WP6-DETECTED-BIG-SYSCALL-COST, SURFACE-2026-06-27-QUALITY-WP5-CLOSED-OVER-FLAG-INVARIANT, SURFACE-2026-06-27-QUALITY-WP5-TITLE-STATE-VS-ACTION, SURFACE-2026-06-20-QUALITY-WP3A-MOD-R-COMMENT-OVERSELL, SURFACE-2026-06-20-QUALITY-WP3C-MIDDLE-CLOSE-INDEX-COMMENT, SURFACE-2026-06-20-QUALITY-WP3A-MOD-D-DOUBLE-BIND, SURFACE-2026-06-18-QUALITY-WP5-GLOBAL-H1-RULE, SURFACE-2026-06-28-QUALITY-WP7-DOUBLE-CC-YOLO-SUBSCRIBE, SURFACE-2026-06-28-QUALITY-WP7-MENU-WRITE-FAILURE-SILENT, SURFACE-2026-06-24-QUALITY-DEVPROD-BASENAME-SPACE-ASSUMPTION, SURFACE-2026-06-25-QUALITY-WP6-DOT-BOUNDARY-RATIONALE, SURFACE-2026-06-29-QUALITY-M7-TRAY-ID-UNUSED-LOOKUP — all closed by the comment/doc edits above.
- **Backlog resolved:** SURFACE-2026-06-20-QUALITY-WP3C-REDUNDANT-JSX-COMMENT, SURFACE-2026-06-19-QUALITY-WP2-SAVEKEYMAP-CHURN — closed as already-resolved-in-code (the comments/coupling they described no longer exist).
- **Task closed:** Debt-paydown sweep #2 WP3 (tiny-logic + test-hygiene sweep) — small, suite-covered code changes resolving 13 findings + closing 1 stale one (backlog 61→47): the standout is a real a11y fix — `inert` on hidden off-viewport workspaces so keyboard focus + screen-reader announcement no longer leak into the backgrounded editor + live PTY; plus `for..in`→`Object.keys` in the git rollup, the Filmstrip ticker effect split into a dedicated active-tile-clear effect + a once-computed background-id array (no join→split round-trip), a no-op guard + honest `aria-label` on the active collapsed pill, git-diff untracked options moved to the unstaged-only branch, a single `commitReady` flag collapsing three DiffPanel render ternaries, a narrowed `visibleKeys` useMemo dep, a scoped PTY-write newline-guard test (was whole-file), and two deliberate-duplication clarifying comments; gated green by `tsc`/eslint/787 frontend tests + clippy `-D warnings`/312 Rust tests.
- **Backlog resolved:** SURFACE-2026-06-23-QUALITY-WP3-OFFVIEWPORT-A11Y, SURFACE-2026-06-25-QUALITY-WP7-FORIN-NO-HASOWNPROPERTY, SURFACE-2026-06-23-QUALITY-WP3-TICKER-EFFECT-DUAL-RESPONSIBILITY, SURFACE-2026-06-23-QUALITY-WP4-BGIDS-JOIN-SPLIT-ROUNDTRIP, SURFACE-2026-06-23-QUALITY-WP4-ACTIVE-PILL-NOOP-PROMOTE, SURFACE-2026-06-20-QUALITY-WP4-DEAD-UNTRACKED-OPTS-STAGED, SURFACE-2026-06-20-QUALITY-WP4-COMMIT-DIFF-GATE-TERNARIES, SURFACE-2026-06-20-QUALITY-WP4POLISH-USEMEMO-DEP, SURFACE-2026-06-20-QUALITY-WP4POLISH-DOUBLE-PREDICATE, SURFACE-2026-06-25-QUALITY-WP8-REDUNDANT-COLLAPSE-DEP, SURFACE-2026-06-25-QUALITY-WP3-OVERBROAD-NEWLINE-GUARD — all closed by the WP3 logic/comment/test edits above.
- **Backlog resolved:** SURFACE-2026-06-29-QUALITY-M7-TRAY-ID-MATCH-DUP, SURFACE-2026-06-28-QUALITY-WP9-LEN-WITHOUT-IS-EMPTY — ack-closed without code change (the tray predicate/match split is the intentional tested-routing-contract pattern, already commented; the `len()`-without-`is_empty()` omission is deliberate and clippy-clean).
- **Backlog resolved:** SURFACE-2026-06-18-QUALITY-WP5-XTERMPANE-EFFECT-DEP — closed as stale; the `[workspaceId]` mount-effect dep it described no longer exists (the effect now ref-syncs on `[onSessionId]`).
- **Task closed:** Debt-paydown sweep #2 WP4 (bury the meh-zone) — moved 16 low-impact + medium-effort/dismiss-candidate + low-risk findings (the fs-watch rewalk/emit/isdir trio, the WP5b descendant-count lag, the WP7 array-alloc, the WP2 overlay-Esc + single-toast-slot, the undiffed mirror emit, the WP9 mode-reread, the sticky z-index coupling, the FileFinder hover-cursor, the CcNotFound plan-drift note, the dev-only eval-nodetest, the bundled eslint-ignore, the WP4 trigger-once StrictMode comment, and the dev-prod overlay window-size coupling whose comment-form is infeasible) from the active quality-findings file to `workflow/backlog-archived.md`, and removed 12 now-empty feature groups — no code change; backlog 47→30 active findings.
- **Backlog resolved:** SURFACE-2026-06-20-QUALITY-WP5-SPLIT-LISTENER-CROSSPOINTER — closed as stale; the RightPanelHost↔SublimeToolbar two-listener split it documented no longer exists (`SublimeToolbar.tsx` was deleted + the `⌘⇧O` hotkey dropped at WP8, button-only now).
- **Task closed:** Debt-paydown sweep #2 WP5 (backend-polish refactor in load-bearing paths) — the isolated, sorted-last, individually-verified high-risk work package, resolving 5 findings across the CC-session + status-broadcaster backends: `resolve_cwd`'s longest-prefix match now ranks by `Path::components().count()` instead of a string-length proxy (the stuck-dot fix path — precise depth, no proxy-reasoning footnote); the WP11 `TerminalEntry {id, sessionId}` always-equal pair collapsed to a single `id` field (re-add a `displayId` if a rename/label feature ever needs the split); and the m2-wp9 buffer→live seam ordering window closed — `mark_ready` now flips-and-flushes the output backlog under one lock hold (`drain_backlog_emitting`) so a reader-thread chunk can't emit ahead of a buffered one, pinned by a new lock-held-across-flush test (313 Rust tests, +1). Two items kept as documented-accepted tradeoffs: `resolve_cwd`'s O(n) linear scan (negligible at ≤100 workspaces) and `cc_ready` holding the registry mutex across the microsecond flush (avoiding it needs an `Arc<dyn CcSession>` migration disproportionate to the gain). Gated green by `tsc`/eslint/787 frontend tests + clippy `-D warnings`/313 Rust tests; the live PTY-ordering behavior (a real reader thread racing `mark_ready`) carried to the release gate as DEFERRED-TO-RELEASE.
- **Backlog resolved:** SURFACE-2026-06-27-QUALITY-WP2-LONGEST-PREFIX-STRLEN-PROXY — `resolve_cwd` now ranks ancestors by path-component count.
- **Backlog resolved:** SURFACE-2026-06-28-QUALITY-WP11-ENTRY-ID-SESSIONID-ALWAYS-EQUAL — the always-equal `{id, sessionId}` pair collapsed to one `id` field.
- **Backlog resolved:** m2-wp9-second-terminal MINOR #1 (buffer→live seam ordering) — closed by the under-lock drain-and-flush in `mark_ready`.
- **Backlog resolved:** SURFACE-2026-06-27-QUALITY-WP2-RESOLVE-CWD-LINEAR-SCAN, m2-wp9-second-terminal MINOR #2 (`cc_ready` registry-lock-across-flush) — closed as documented-accepted tradeoffs (no code change; the comment now states why each is deliberate).
- **Task closed:** Debt-paydown sweep #2 WP2-supplement + fold-back — a completeness-critic pass caught a batch of XS comment/doc nits the WP2 list had missed (Rule 1 = cheap+safe always sweeps): swept the `editorLoad` `_state`→`state` rename, the panel-chord-under-overlay note, the PiP drag click-boundary comment, the split-state app-global-prose + effectiveRailWidth-docstring + refit-asymmetry + intra-feature-phase comments, the dual-reconfigure-path note, the `useWorkspaceStatus` `stateFor`/`snippetFor` `useCallback` memoization (WP6-MINORS #1), and tightened `HOOK_SOCKET_NAME` to module-private (WP3-MINORS #3); then folded the sweep back — the quality-findings backlog dropped from 114 findings at sweep start to **7 surviving category-C anchors**. Gated green by `tsc`/eslint/787 frontend tests + clippy `-D warnings`/313 Rust tests.
- **Backlog resolved:** SURFACE-2026-06-22-QUALITY-WP6-MINORS, SURFACE-2026-06-22-QUALITY-WP3-MINORS, SURFACE-2026-06-22-QUALITY-WP4-MINORS, SURFACE-2026-06-19-QUALITY-WP2-EDITORLOAD-UNDERSCORE-PARAM, SURFACE-2026-06-20-QUALITY-WP6-PANEL-CHORD-UNDER-OVERLAY, SURFACE-2026-06-26-QUALITY-WP4-DRAG-CLICK-BOUNDARY-IMPLICIT, SURFACE-2026-06-27-QUALITY-WP3-APP-GLOBAL-STATE-PROSE, SURFACE-2026-06-27-QUALITY-WP3-EFFECTIVERAIL-DOCSTRING-GUARANTEES, SURFACE-2026-06-27-QUALITY-WP3-REFIT-NUDGE-LEFT-ONLY-ASYMMETRY, SURFACE-2026-06-27-QUALITY-WP3-INTRA-FEATURE-PHASE-COMMENTS, SURFACE-2026-06-27-QUALITY-WP5-DUAL-RECONFIGURE-PATH, SURFACE-2026-06-24-QUALITY-APPMENU-LABEL-ONLY-ID-COMMENT — all closed by the supplement comment/doc/rename edits.
- **Backlog resolved:** SURFACE-2026-06-27-QUALITY-WP4-UNUSED-STATE-VALUE-BINDING, SURFACE-2026-06-25-QUALITY-WP6-CHORD-MAP-XREF-HYGIENE, SURFACE-2026-06-26-QUALITY-WP2-LINGERING-ALLOW-UNUSED-MUT — ack-closed (already-documented / track-only, no action warranted).
- **Backlog resolved:** SURFACE-2026-06-19-QUALITY-CHORD-TS-PHASE-TAG, SURFACE-2026-06-22-QUALITY-WP1-MEASURE-PGREP-GUARD-DEGRADED, SURFACE-2026-06-22-QUALITY-WP1-MEASURE-PERCENTILE-OFFBYONE — closed as dead (the `chord.ts` file + the `measure.sh` probe were deleted/archived).
- **Product cycle complete:** Between-M8-and-M9 debt-paydown sweep #2 (Full Rule-1 reconciliation) — the code-quality backlog went 114 → 7 surviving anchors across 5 WPs (delete-stale → comment sweep → tiny-logic+test → bury meh-zone → backend-polish refactor), with the temporary WBS folded back and deleted; the 3 silent file-op-failure findings collapsed into one anchored "file-op error surface" Defer (net-new UX), 16 meh-zone findings buried, and every remaining survivor is a genuine M9/M10/D2-hardening/release-gate/test-infra anchor.
- **Backlog resolved:** SURFACE-2026-06-27-M5-INSTALLED-BUILD-VERIFY-DEFERRED-TO-RELEASE — the installed-build verification carry closed at the v0.2.4 release gate (brew version 0.2.4, quarantine-clear launch, m2-wp9 PTY-startup-output + subdir-cwd Idle-flip both confirmed on the Finder-launched `.app`).

## 2026-06-29

- **Feature shipped:** Menu-bar status item — ambient alarm + actuator menu (M7) — Claudesk gains a macOS menu-bar (system-tray) item exploiting the one thing the menu bar offers that the shipped M5 PiP does not (location: a strip the operator already watches all day, present even at zero workspaces): a 2-state ambient **alarm** — a template-image glyph (a faithful monochrome Claudesk-icon portrait: rounded window + 4 filmstrip tiles + main CC box) that grows a lower-right corner **badge** when ANY open workspace is `AwaitingInput` and is neutral otherwise (Running + Idle collapse), reduced by a pure unit-tested `aggregate_alarm` fold over the open-workspace states and driven by the existing M3 `workspace-status` broadcast (no broadcaster change), with the glyph swapped blink-free via `set_icon_with_as_template` (main-thread-marshaled internally) and a `forget_workspace` hook into `workspace_deregister` so a closed-while-awaiting workspace can't pin the alarm lit; plus a native **actuator** menu (Show Claudesk / Toggle PiP / Quit) wired backend-side through the existing app-level `on_menu_event` (which fires for tray menu events too), since display-only PiP can't act on the app. New `src-tauri/src/tray/` module + 2 template PNGs + the `tray-icon`/`image-png` tauri features; no new dependency, webview, or Vite entry. The originally-planned popover dashboard (per-workspace list + navigate-on-click + `tauri-plugin-positioner`) was CUT at the spec debate as a strict subset of PiP. Operator-approved live in `pnpm tauri:dev` (glyph + all 4 actuators); native badge-transition + out-of-focus/installed-`.app` checks carried DEFERRED-TO-RELEASE.
- **Milestone:** M7 (Menu-bar status item) — all 3 WPs shipped (WP1 tray icon + ambient alarm, WP2 native actuator menu, WP3 milestone-exit verification).
- **Product cycle complete:** Milestone 7 (Menu-bar status item) — Claudesk gained an ambient menu-bar alarm (badge when any workspace awaits input) + a Show/Toggle-PiP/Quit actuator menu, deliberately scoped to the menu bar's one non-PiP-overlapping virtue (location); durable docs resynced (the as-built `set_icon_with_as_template` / `include_bytes!`-no-config-block corrections), backlog swept, WBS archived to `docs/product/archive/milestone-7-menu-bar-status-item/`.
- **Backlog resolved:** SURFACE-2026-06-29-M7-TRAY-ATOMIC-ICON-METHOD-NAME — closed by the product-finalize durable-doc resync (the swap method is `set_icon_with_as_template`, not the specced `set_icon_and_icon_as_template_atomic`).
- **Backlog resolved:** SURFACE-2026-06-29-M7-TRAY-NO-CONFIG-BLOCK — closed by the resync (glyphs embedded via `include_bytes!`; no `trayIcon` config block, which would duplicate the dynamic tray).
- **Feature shipped:** M8 WP2 — shared demo-build harness — a dev-only, lockfile-isolated `tooling/demo/` Node package (Playwright + ffmpeg) that renders Claudesk's real status-dot UI from a mock status timeline to a looping GIF, sourcing the dot colors/keyframes/animation-timing from `src/App.css` (single source of truth, drift-guarded) so the demo dots stay pixel-identical to the app — the shared machinery the WP3 filmstrip and WP4 PiP demos build on.
- **Feature shipped:** M8 WP3 — filmstrip demo asset — built on the WP2 harness, a looping ~323KB GIF showing four *unrelated* projects (catan-companion/tax-cruncher/hugo-blog/recipe-box — deliberately distinct to convey parallelism across projects, not components of one system): the focused project runs a *live* CC session (cycling spinner, ticking elapsed/token counter, a fast-cascading code-diff hunk + build/test output — visibly "working right now"), another flips to AwaitingInput (blue blink), a mouse cursor glides over and clicks its tile (strong double-ring + flash) to promote it to center stage *still showing a real-shaped AskUserQuestion tool call*, then a separate keyboard `1`/`⏎` answer resumes it running — conveying the "4 projects in flight, one glance tells you which needs you, one click jumps there, then you decide" value; adds frame-deterministic glide-cursor + keycap + busy-session animation layers (`cursorAt.js` + `busyAt.js` + cursor/keycap/busy/diff/askq surface in the harness shell) reusable by the WP4 PiP demo, with CC-pane content matching the real Claude Code TUI cadence (generalized, no sensitive data); operator-approved across several verify-human rounds.
- **Feature shipped:** M8 WP4 — PiP demo asset — built on the WP2/WP3 harness, a looping ~367KB GIF conveying the picture-in-picture value: an always-on-top corner PiP keeps CC monitorable while you work elsewhere, and pings you the moment it needs input. The ~10s, 6-beat demo shows the operator working in an *active* Slack thread (typing a reply; the mouse glides over and clicks a 👍 reaction; teammate messages pop in) while a vertical PiP holds two *unrelated* projects (recipe-box + tax-cruncher) each mirroring a live CC session (spinner + streaming tool output + diff hunks, reusing the WP3 busy cadence); tax-cruncher flips to AwaitingInput (the blue "needs you" ping), the operator hits ⌘+Tab which switches the composition to the *real Claudesk window* (filmstrip + center stage — the faithful UX, not a PiP focus ring) where they answer the AskUserQuestion with `1`/`⏎` and CC resumes — mouse = busywork elsewhere, keyboard = the switch + answer, neither contradicting the other; adds a frame-deterministic active-backdrop layer (`backdropAt.js`: progressive message reveal + char-by-char typing + reaction chips), a keyframe-level region override in the harness shell (pip→filmstrip composition switch), and vertical live-mirror PiP-cell chrome, plus a latent `[hidden]`-override keycap/cursor bug fix that also cleans up the filmstrip demo; the work-backdrop framing (over leisure/YouTube — target audience is pros multitasking real work), the 2-workspace count, and the faithful window-switch ending were operator decisions across 4 verify-human rounds.
- **Feature shipped:** M8 WP5 — embed + place the demo GIFs — commits the two rendered demo assets to `docs/demo/` (`filmstrip.gif` ~334KB, `pip.gif` ~367KB, both ~8× under the 3MB GitHub-embed budget) and rebuilds the top of `README.md` into a front page: a punchy value one-liner ("Many Claude Code projects. One window. Zero hunting.") → two `problem → feature → demo-gif` blocks → then the original descriptive content, plus 2 new Philosophy bullets (single-screen/laptop-scenario design target; local-dev-env-over-cloud), and a dev-only codify guard (`tooling/demo/readme-assets.nodetest.mjs`) asserting every README-referenced GIF exists / is under budget / is an animated GIF; pushed live and operator-approved on the github.com-rendered README (both GIFs render inline + autoplay-loop).
- **Milestone:** M8 WP5 (Embed + place the demo GIFs) — the M8 milestone-exit WP; M8 (Demo assets) is now feature-complete, pending `/product-finalize` cycle-close.
- **Product cycle complete:** Milestone 8 (Demo assets — filmstrip & PiP value showcase) — Claudesk gained two looping demo GIFs (filmstrip = parallel-project attention; PiP = monitor-while-elsewhere), agent-produced via a synthesized-animation pipeline (HTML harness reusing the real status-dot CSS → Playwright → ffmpeg, no screen capture), committed to `docs/demo/` and embedded in a restructured README front page; roadmap M8 marked complete + arch.md confirmed drift-free (M8 added no production code), backlog swept, WBS + the WP1 capture-pipeline probe record archived to `docs/product/archive/milestone-8-demo-assets/`.
- **Backlog resolved:** SURFACE-2026-06-29-DEMO-ASSETS-FILMSTRIP-AND-PIP — closed by M8; both requested demos (filmstrip multitasking, PiP monitor-while-elsewhere) shipped as looping GIFs, embedded in the README, and operator-approved on the github.com render.

## 2026-06-28

- **Feature shipped:** FileTree reaches gitignored-but-editable files (M6 WP6) — the file-walker and filesystem-watcher exclusion criterion is re-based from "is gitignored" to "is a heavy/generated directory" (the thing gitignore was only ever a leaky proxy for), so gitignored-but-edited files like `.env`, `.session.md`, and `.claude/*` are now shown, openable, editable, and live-watched in-app while heavy dirs (`node_modules`/`target`/… by name or by a detected >500-immediate-child count) are listed-but-not-descended and rendered with a dim "(not indexed)" marker; the change is walker-wide (FileTree + Cmd+P finder + content search all re-based onto one shared `walk_project` DFS that replaced the `ignore::WalkBuilder`, whose `filter_entry` couldn't yield-a-row-but-skip-descent), with the watcher using a pure NAME-based predicate on its hot path so `.env` gets live external-change refresh while heavy-dir churn stays suppressed.
- **Backlog resolved:** SURFACE-2026-06-26-FILETREE-EXCLUDES-GITIGNORED-EDITABLE-FILES — closed by WP6; the policy debate rejected the allowlist candidates and landed on the heavy-dir re-base, walker-wide.
- **Milestone:** M6 WP6 (FileTree reaches gitignored-but-editable files).
- **Feature shipped:** Open CC without yolo by default — opt-out setting (M6 WP7) — the unconditional `--dangerously-skip-permissions` ("yolo") flag is now gated on a new app-global `AppSettings.cc_yolo` setting (default ON, vision-explicit; opt-out only), read at CC-spawn time so a change takes effect on the next spawn, and surfaced across three synchronized surfaces sharing one source of truth (the `cc-yolo` broadcast): a backend `build_cc_argv(yolo)` gate + `cc_get_yolo`/`cc_set_yolo` commands, a native View-menu "Skip Permission Prompts (yolo)" checkmark item (mirroring the pip-mode menu pattern), and an operator-requested project-picker checkbox (which also prompted a `.picker` flex-column layout fix so "Open Folder…" stays in-viewport); the command round-trip + cross-surface sync + picker layout were live-verified via the MCP bridge, with the installed-`.app` spawn-argv + native-menu-persist smoke test deferred to the v0.2.2 release gate.
- **Milestone:** M6 WP7 (Settings — open CC without yolo by default).
- **Feature shipped:** Suppress empty PiP when no workspace is open (M6 WP9) — the picture-in-picture status panel no longer appears when there is nothing to mirror: the Auto-mode blur auto-summon is gated on open-workspace-count > 0 (read from the backend `SharedRegistry`), and (after an operator verify-human reject broadened the scope to all modes) the `On`-mode pinned panel was made reactive — its visibility tracks the open-workspace count via a `reconcile_pip_for_workspace_count` call from `workspace_register`/`workspace_deregister`/`pip_set_mode`, shown when ≥1 workspace is open and hidden when the count returns to 0, with the launch-time unconditional On-show removed (the registry is empty at launch, so it had rendered an empty pinned panel); all reconcile callers are main-thread `#[command]` bodies so no new run_on_main_thread seam was needed, and the pure decision functions plus a registry count-transition test codify the logic while the live NSPanel show/hide was operator-verified.
- **Backlog resolved:** SURFACE-2026-06-27-PIP-SUMMONS-EMPTY-WITH-NO-WORKSPACE-OPEN — closed by WP9; the empty-PiP suppression was extended from the planned Auto-only guard to all modes at the operator's verify-human request.
- **Milestone:** M6 WP9 (Suppress empty PiP when no workspace is open).
- **Feature shipped:** Right-panel terminal font zoom (M6 WP10) — the right-half login-shell terminal now responds to the same focus-scoped ⌘+/⌘−/⌘0 font zoom WP4 shipped for the CC terminal: when the right-panel terminal holds focus the chord zooms *that* terminal (not the editor), when the editor holds focus the editor's own zoom keymap handles it, and the CC terminal is unchanged — a routing-only extension that reuses `XtermPane.setFontSize`, the WP4 zoom math, and the single shared `claudesk.terminal.fontSize` key (one persisted size for both terminals); the gap was closed with a new pure `deriveRightSurface` DOM-ancestry helper (mirroring `focusHalf.ts`), a `forwardRef` ref thread `RightPanelHost`→`TerminalPane`→`XtermPane`, and an `applyTerminalZoom(action, target)` branch in `Workspace.tsx`'s capture-phase keydown router, all verified live via the MCP bridge (all three focus cases) plus operator real-keyboard.
- **Backlog resolved:** SURFACE-2026-06-27-RIGHT-PANEL-TERMINAL-ZOOM-AND-MULTIPLE — sub-request (1) right-panel terminal zoom closed by WP10; sub-request (2) multiple right-panel terminals remains open as WP11.
- **Milestone:** M6 WP10 (Right-panel terminal font zoom).
- **Feature shipped:** Multiple terminals in the right panel (M6 WP11) — the right-panel Terminal slot now holds N login shells instead of one: a pure `terminalList` model (open/switch/close, monotonic `${workspaceId}-term-<n>` session ids, disallow-closing-the-last, soft cap of 8) drives an always-shown sub-tab row (●1 ●2 + ＋, per-tab hover ✕) with every terminal kept mounted (`display:none` for the non-active one) so each shell's scrollback survives switching; ⌘T opens a new terminal and a scoped ⌘W closes the focused one (routed before the editor's close-tab handler via `deriveRightSurface` and swallowed so the two never double-fire, inert on the last terminal, editor-focused ⌘W unchanged); the WP10 focus-scoped font zoom follows whichever terminal is focused for free because the forwarded zoom ref binds to the active pane (no per-terminal registry needed), and new terminals seed from the shared `claudesk.terminal.fontSize` key. Frontend-shape change only — the PTY backend was already N-session-ready and each `XtermPane`'s unmount `cc_kill` reaps its shell on close; zoom is shared across all terminals and terminals are ephemeral (one fresh terminal on restart), both operator-confirmed. Verified live via the MCP bridge (open/switch/close with scrollback intact, `pgrep` shell-reap, cap-8, chords, zoom-follows-focused) plus operator real-keyboard; installed-build smoke test deferred to the v0.2.2 release gate.
- **Backlog resolved:** SURFACE-2026-06-27-RIGHT-PANEL-TERMINAL-ZOOM-AND-MULTIPLE — now fully closed; sub-request (2) multiple right-panel terminals shipped as WP11 (supersedes the WP10-era note that it remained open).
- **Milestone:** M6 WP11 (Multiple terminals in the right panel).
- **Feature shipped:** Milestone-exit verification of Milestone 6 (M6 WP8) — a verification-only work package that confirmed every M6 exit criterion against the real running app: the agent drove the full agent-observable slice live through the `tauri` MCP bridge (split-ratio control + clean terminal re-fit, focus-scoped CC/right-panel terminal font zoom, editor auto-wrap toggle persisting + applying live, gitignored-but-editable `.env` reachable and editable in the FileTree, empty-PiP suppression when no workspace is open, and multiple right-panel terminals opening/switching/closing — including a live backend `/bin/zsh` shell-reap on close and the disallow-last guard) with zero blocking failures and zero back-loops, and composed the remaining installed-`.app` + backend-lifecycle outcomes (stuck-dot subdir-cwd flip, no-yolo next-spawn, `term_spawn` PATH parity, blur-driven empty-PiP, real-keyboard zoom routing, telemetry survival) into a DEFERRED-TO-RELEASE operator-carry checklist confirmed at the v0.2.2 release gate; the one code change folded in was a friend-requested polish — the editor default font dropped 13→11 so a fresh editor and ⌘0-reset match the CC/terminal size.
- **Milestone:** M6 WP8 (Milestone-exit verification) — **completes Milestone 6 (Friend-requested QoL polish); all 12 work packages shipped.**
- **Product cycle complete:** Milestone 6 — Friend-requested QoL polish (an open friend-sourced collection) shipped 12 work packages: the lead correctness fix (stuck-`Running` status dot, via a prod file-telemetry probe + a longest-prefix `resolve_cwd`), a drag/collapse split-ratio control, focus-scoped font zoom for both terminals + an editor/terminal default-size match, an editor auto-wrap toggle, gitignored-but-editable files in the FileTree (gitignore→heavy-dir walker re-base), a no-yolo opt-out setting, empty-PiP suppression, multiple right-panel terminals, and a milestone-exit verification pass; the `wbs.md` is archived to `docs/product/archive/milestone-6-friend-qol/` and the durable docs (`arch.md`, `roadmap.md`) are resynced.
- **Backlog resolved:** SURFACE-2026-06-26-M6-SETTING-NO-YOLO-DEFAULT — closed by WP7 (the no-yolo opt-out setting); swept to RESOLVED at M6 cycle-close.

## 2026-06-27

- **Feature shipped:** PiP toggle, lifecycle & auto-summon (M5 WP5) — the Picture-in-Picture panel gains a visibility control + lifecycle, and auto-summon is adopted as an explicit tri-state mode: **Off / On (pinned) / Auto (default)**, directly selectable via the right-panel PiP icon (cycles Off→On→Auto) and a View-menu radio; **Auto** hides the panel while Claudesk is focused, summons it on a 3s-debounced sustained main-window blur, and dismisses it on refocus (so the out-of-focus glance surface appears on its own — vision Success Metric 6), while **On** pins it shown and **Off** hides it, with the mode persisted in the app-settings store and the panel tearing down cleanly on quit; the implementation survived two verify-loop catches — an off-main-thread AppKit crash in the auto-summon debounce (fixed by marshaling the show onto the main thread) and an inferred-regime dead-end found at verify-human (reworked into the explicit tri-state).
- **Backlog resolved:** SURFACE-2026-06-26-PIP-AUTO-SUMMON-DISMISS — auto-summon/dismiss adopted + shipped as the tri-state PipMode (the scope decision it raised).
- **Backlog resolved:** SURFACE-2026-06-26-QUALITY-WP3-TEARDOWN-SKIPS-VISIBILITY-BROADCAST — `pip::teardown()` now emits `pip-visibility false` so the mirror-cost gate stays honest on the close path.
- **Backlog resolved:** SURFACE-2026-06-26-QUALITY-WP3-UNSYNCED-FILMSTRIP-INTERVAL — the filmstrip's second unsynced 1fps interval was folded into the single `useMirrorTicker` loop via a `subscribeMirrorFrame` push (no phase drift).
- **Feature shipped:** Milestone-exit verification of the M5 Picture-in-Picture surface (M5 WP6) — a verification-only work package (no source change): driving Claudesk's real dev WKWebView through the WP2-adopted `mcp__tauri__*` bridge (both the `main` and the `pip` NSPanel webviews), the agent confirmed every reachable M5 outcome — correct status dots driven by `workspace-status` events at N=2 workspaces (including the deliberately-mirrored center-staged tile), all four PiP layouts rendering + auto-resizing + persisting across a toggle, the minimal-layout attention weighting (awaiting-input sorts first and pops while all-busy reads quiet), display-only inertness (a PiP-tile click never promotes a workspace), and PiP↔filmstrip state agreement — with the static floor green (tsc/eslint/vite build/clippy/cargo test 266/0); the genuinely-native installed-`.app` checks (out-of-focus visibility, the native View-menu checkmark glyph, real ⌘Tab-away auto-summon, dev/prod isolation, the tauri#5566 release-vs-dev caveat) were operator-deferred to the `/release` packaging gate before Homebrew distribution.
- **Milestone:** M5 (Picture-in-picture) — COMPLETE: all six work packages shipped (WP1 NSPanel probe, WP2 UI-driver ADOPT, WP3 panel + status-subscribe core, WP4 four layouts + switcher + resize, WP5 toggle/lifecycle/auto-summon tri-state, WP6 milestone-exit verify); the always-on-top floating status surface ships and mirrors the filmstrip while Claudesk is out of focus.
- **Product cycle complete:** Milestone 5 (Picture-in-picture) closed — Claudesk gains an always-on-top, display-only floating NSPanel that mirrors the filmstrip's workspace status (four selectable layouts, content-driven auto-resize, tri-state Off/On/Auto visibility with auto-summon-on-blur) so "which project is awaiting input" is answerable while the main window is out of focus; durable docs resynced (arch §B.3 rewritten as-built, PiP-before-menu-bar correction), and the cycle WBS archived to `docs/product/archive/milestone-5-picture-in-picture/`.
- **Feature shipped:** Status-channel telemetry — backend logger (M6 WP1) — a best-effort, per-identity `status-channel.log` under `app_data_dir()` (readable from the launchd-launched prod `.app` where stderr is invisible) that records, per drained CC hook event, the event name + raw cwd + mapped state + resolved-workspace-id-or-`None` + emitted/dropped outcome (computed separately so a cwd-match-miss is distinguishable from a never-mapped event), plus `workspace_register`/`deregister` canonical-key lines and a startup breadcrumb — the diagnostic probe for the intermittent stuck-`Running` dot, verified live via the MCP bridge.
- **Feature shipped:** Status-channel telemetry — hook-edge write-failure trace (M6 WP1b) — the deployed Perl hook now appends a `- HOOK write-failed …` line to the same per-identity log when it cannot open the socket (while keeping `exit 0` unconditional so it never blocks CC), closing the one gap the backend logger can't see: a never-arrived `Stop` (no STATUS line at all) is now distinguishable from an arrived-but-unresolved one.
- **Milestone:** M6 WP1 + WP1b (stuck-`Running` dot probe) — both telemetry slices shipped; the probe now runs in prod (v0.2.1) to passively capture the intermittent bug for WP2's fix.
- **Feature shipped:** Workspace split-ratio control (M6 WP3) — the workspace header gains two collapse toggles (◀ CC / ED ▶) and a ratio cycle button (3:1 / 2:2 / 1:3) so the CC terminal or the editor can be given more room — or either half fully collapsed — in one click, persisted across launches; the originally-planned free-drag divider was reframed at spec time into this discrete, lower-bug-surface control (operator decision, captured as the broadened `explicit-selectable-mode-over-inferred-mode` design prior), and two CSS-grid layout traps caught at verify-human (a fixed-px file-tree rail crowding the editor at 3:1, and a `display:none` grid item leaving a `0` track that collapsed the visible half) were fixed via a panel-fraction rail cap and a single-`1fr`-track derivation.
- **Backlog resolved:** SURFACE-2026-06-26-FRIEND-QOL-BATCH-1 (sub-request #1, adjustable left/right split) — shipped as WP3; sub-requests #2 (terminal font zoom) and #3 (editor wrap) remain for WP4/WP5.
- **Milestone:** M6 WP3 (Workspace split-ratio control) — shipped; the first of the M6 parallel-polish-track WPs.
- **Feature shipped:** Adjustable CC terminal font size (M6 WP4) — the CC terminal gains ⌘+/⌘−/⌘0 font zoom like the editor, routed focus-scoped (it zooms whichever half holds keyboard focus — the terminal when the CC pane is focused, the editor when the editor is) via a capture-phase keydown listener that reads the live focused half and prevents the chord from reaching the PTY or triggering WKWebView page-zoom, with the size persisted across launches; a latent batch-fragility (rapid presses within one React render batch collapsing to a single step) was caught live at verify-self and fixed by computing the next size inside a functional setState updater.
- **Backlog resolved:** SURFACE-2026-06-26-FRIEND-QOL-BATCH-1 (sub-request #2, adjustable CC terminal font size) — shipped as WP4; sub-request #3 (editor wrap) remains for WP5.
- **Milestone:** M6 WP4 (Adjustable CC terminal font size) — shipped; second of the M6 parallel-polish-track WPs.
- **Feature shipped:** Editor auto-wrap toggle (M6 WP5) — the lite editor gains a persisted soft line-wrap toggle, default OFF (preserving the deliberate no-wrap behavior where long lines scroll horizontally), flipped live via a `⌘\` chord (confirmed disjoint from the editor's chord-ownership map) or a "wrap"/"no wrap" status-bar pill, using a CodeMirror compartment so wrap reconfigures without remounting the editor (cursor/scroll preserved); built as a near-textbook clone of the sibling font-zoom feature and driven through live verify-self via the tauri MCP bridge (chord + button + no-remount + persistence all confirmed).
- **Backlog resolved:** SURFACE-2026-06-26-FRIEND-QOL-BATCH-1 (sub-request #3, editor auto-wrap toggle) — shipped as WP5; all three original friend sub-requests (#1 split, #2 terminal zoom, #3 editor wrap) are now shipped.
- **Milestone:** M6 WP5 (Editor auto-wrap toggle) — shipped; third of the M6 parallel-polish-track WPs.
- **Feature shipped:** Fix the stuck-`Running` status dot (M6 WP2) — the workspace status dot was getting stuck on `Running` after a CC turn cleanly ended, because `WorkspaceRegistry::resolve_cwd` matched an event's cwd to a workspace by exact path equality, so a turn-end `Stop` fired while the CC shell had descended into a subdirectory of the workspace root (e.g. `cwd=…/claudesk/src-tauri` vs the registered `…/claudesk`) resolved to nothing and the idle transition was silently dropped; the WP1/WP1b prod telemetry captured the offending turn live (`resolved=none outcome=dropped`), pinning the cwd-match-miss root cause, and the fix rewrites `resolve_cwd` to ancestor / longest-prefix matching (boundary-safe via `Path::starts_with`, longest registered ancestor wins for nested workspaces) — proven by three unit tests (subdir-resolves, nested-longest-prefix-wins, sibling-boundary-guard) plus a consuming-surface integration test, with live dot-flip confirmation deferred to the planned v0.2.2 patch since the bug can't be triggered on demand.
- **Backlog resolved:** SURFACE-2026-06-25-STATUS-STUCK-RUNNING-AFTER-CLEAN-TURN-END — fixed by WP2's ancestor/longest-prefix `resolve_cwd`; the milestone's lead correctness item, diagnosed live via the WP1/WP1b prod telemetry.
- **Milestone:** M6 WP2 (Fix the stuck-`Running` status dot) — shipped; the milestone's lead correctness item closed.

## 2026-06-26

- **Feature shipped:** Agent UI-driver for verify-self (M5 WP2 probe) — VERDICT: **ADOPT**; a dev-only `tauri-plugin-mcp-bridge` (`#[cfg(debug_assertions)]`, 127.0.0.1:9223, MCP server in `.mcp.json`) lets an agent drive Claudesk's real WKWebView with live Tauri IPC, so `feature-verify-self` can now mount + inspect a live workspace (and the PiP NSPanel webview) instead of always carrying workspace-UI checks to native verify-human — proven by an agent-driven macOS smoke test that also surfaced + fixed a capability-suppression bug the bare-Vite path could never have caught, and confirmed release-safe (`nm` finds no bridge code symbols).
- **Backlog resolved:** SURFACE-2026-06-23-VERIFY-SELF-DRIVER-FOR-WORKSPACE-UI — closed by the M5 WP2 probe with an ADOPT verdict + the verify-self invocation recipe recorded in `docs/product/wbs.md`.
- **Feature shipped:** Picture-in-Picture NSPanel + status-subscribe core (M5 WP3) — the WP1 throwaway NSPanel probe is promoted into the real always-on-top, all-Spaces, non-activating PiP panel that renders the default horizontal-mirror layout: one display-only tile per open workspace (project name + idle/running/awaiting-input dot + a live ~1 fps `serializeAsHTML()` terminal mirror), the full roster **including the center-staged workspace** (the intentional divergence from the filmstrip, since the PiP is the surface you watch when Claudesk is out of focus); status reaches the PiP webview via the M3 all-webview broadcast, roster + mirror are forwarded from the main webview over `pip-frame`/`pip-mirror`, and a single shared serialize ticker feeds both the filmstrip and the PiP (no second serialize loop), gated on a backend `pip-visibility` broadcast so a hidden panel costs nothing.
- **Backlog resolved:** SURFACE-2026-06-26-QUALITY-WP2-RECIPE-WRONG-WAIT-TOKEN — closed by M5 WP3 (folded the wbs.md verify-self recipe wait-token fix into Phase 1).
- **Feature shipped:** PiP layout modes + persisted switcher + content-driven resize (M5 WP4) — the single-layout PiP becomes four (horizontal/vertical mirror, compact name+dot, minimal dots) with an own-row switcher carrying a per-layout SVG icon, layout persistence via a new Rust app-settings store (`config_store/settings.rs`, bundle-identity-keyed), content-driven auto-resize (`computePanelSize(layout, count, screen)` — fixed filmstrip-size tile unit × N, screen-capped then wrapped), and a minimal-layout "is anyone waiting on me?" attention weighting (awaiting-input sorts first + blink-and-glow POP, same M3 palette so the PiP never disagrees with the filmstrip); plus four operator-surfaced fixes landed during verification — the panel is now draggable (a Rust `pip_move` via AppKit `setFrameOrigin:`, since `data-tauri-drag-region`/`startDragging`/`setPosition` are all inert on the swizzled borderless NonactivatingPanel and dropping `.borderless()` re-crashes), the toggle button shows a PiP glyph instead of "PiP" text, and the live ~1fps mirror no longer freezes while the main Claudesk window is backgrounded (the `document.hidden` serialize gate now spares a mirror-needing PiP).
- **Milestone:** M5 WP4 — PiP layout modes + persisted switcher + auto-resize.

## 2026-06-25

- **Feature shipped:** Close a workspace (QoL temporary-WBS WP1) — each filmstrip tile (expanded thumbnail and collapsed pill) now has a hover-revealed × that fully tears the workspace down: it reaps both the Claude Code PTY and the second-terminal shell (via a per-pane `cc_kill`-on-unmount that also closes a latent session-leak gap), deregisters it from the status broadcaster, stops its filesystem watcher, and re-picks the center stage (promoting the left-neighbour, or returning to the full-screen picker when the last workspace closes) — with a discard/cancel confirm if the workspace has unsaved editor edits.
- **Backlog resolved:** SURFACE-2026-06-24-NO-WAY-TO-CLOSE-A-WORKSPACE — closed by QoL-WP1; the "Close Workspace ⌘⇧W" native-menu item remains a noted follow-up.
- **Feature shipped:** Status indicator — busy vs awaiting-input (QoL temporary-WBS WP2) — the workspace status dot no longer gets stuck on "awaiting input" (blue) after you answer an AskUserQuestion/permission prompt: a live hook-stream capture proved Claude Code fires `PostToolUse` on resume, so Claudesk now registers it and maps it to "running", flipping the dot back to orange the instant you answer; the `Notification → awaiting-input` mapping is also gated on `notification_type` so an idle nudge no longer flips a busy dot blue; and the dot now "breathes" while running, "blinks" while awaiting input, with the filmstrip tile caption made more transparent.
- **Backlog resolved:** SURFACE-2026-06-24-STATUS-INDICATOR-BUSY-VS-AWAITING-INPUT — closed by QoL-WP2 (PostToolUse→Running resume signal + notification_type gating, operator-verified live in the installed app).
- **Feature shipped:** Switch-workspace autofocus CC panel (QoL temporary-WBS WP3) — promoting a workspace to center stage (filmstrip tile click, ⌘⇧+digit, picker overlay, or close re-pick) now auto-focuses that workspace's left Claude Code terminal, so typing lands in the project's CC session with zero clicks; implemented as a focus-only imperative handle on the terminal pane (it never writes a byte to the PTY, so a switch can't inject a spurious prompt line) fired from a single `visible`-edge effect that all four promote triggers route through.
- **Backlog resolved:** SURFACE-2026-06-24-SWITCH-WORKSPACE-AUTOFOCUS-CC-PANEL — closed by QoL-WP3 (always-CC-left autofocus on promote, operator-verified live).
- **Feature shipped:** Terminal session no longer torn down + re-spawned on panel/workspace switch (QoL temporary-WBS WP4) — the right-panel shell (and its scrollback + command history) now survives switching between the Terminal/Editor/Diff tabs and between center-stage workspaces, because Claudesk spawns the shell exactly once instead of re-spawning a brand-new PTY on every switch-back (the real cause behind the reported "stack of empty prompts": `active` was an unconditional dependency of the spawn effect, so each re-activation re-ran it; the fix moves deferred first-spawn to a pure `shouldSpawnOnActive` predicate driven by a single trigger effect, removes `active` from the spawn deps, and latches the spawn so re-activation is inert).
- **Backlog resolved:** SURFACE-2026-06-24-TERMINAL-SPURIOUS-NEWLINE-ON-PANEL-SWITCH — closed by QoL-WP4 (spawn-once fix; reproduce reframed it from a stray newline to full session teardown/respawn; operator-verified shell history survives switches).
- **Feature shipped:** Editor file management — create + delete file (QoL temporary-WBS WP5) — the lite editor can now create a new file (a "＋ new file" button in the file-tree rail header or ⌘N opens an inline name input that writes an empty file under the workspace root and opens it in a tab, refusing to clobber an existing name) and delete an existing file (a per-row hover ✕ raises a confirm, then removes the file via a new root-confined `delete_file` backend command and closes any open tab for it across all split panes), with create-in-folder and recursive folder-delete deliberately left out of v1.
- **Backlog resolved:** SURFACE-2026-06-24-EDITOR-ADD-NEW-FILE — closed by QoL-WP5 (covers both create and delete; operator-verified all 8 live checks; v1 scope cuts logged as SURFACE-2026-06-25-EDITOR-FOLDER-FILE-OPS).
- **Feature shipped:** Editor file management, folder depth (QoL temporary-WBS WP5b) — the lite editor's file tree now goes a level deeper: you can create a file inside any folder (a per-directory-row "＋"), delete a whole folder (a directory-row "✕" raises a stronger confirm showing the item count and that it moves to the macOS Trash — recoverable, not a hard delete — then closes every open tab under it across all panes via a new prefix-match teardown), and create new folders / files in not-yet-existing folders (a header and per-directory "⊞" new-folder affordance, plus the new-file input now accepts a nested `sub/x.txt` path that `mkdir -p`s its parent), all routed through two new root-confined backend commands (`trash_path`, `create_dir`) with a parent-tolerant lexical containment guard for the create paths.
- **Backlog resolved:** SURFACE-2026-06-25-EDITOR-FOLDER-FILE-OPS — closed by QoL-WP5b (create-in-folder + Trash-backed folder delete + new-folder/nested-file create; the new-folder capability was folded in mid-feature as Phase 3 at the operator's request; operator-verified all 3 phases live + installed build).
- **Feature shipped:** New-workspace hotkey ⌘⇧N (QoL temporary-WBS WP6) — the native menu's "New Workspace" accelerator ⌘⇧N (previously display-only text that did nothing) is now a real keyboard chord that opens the project picker, implemented as a pure `newWorkspaceChord` predicate plus an app-level capture-phase keydown listener (gated to when a workspace is open) routing through the same `setShowPicker(true)` open-overlay path the filmstrip "+" and menu bridge already use; disjoint from ⌘N (editor new-file) and ⌘⇧+digit (workspace switch).
- **Backlog resolved:** SURFACE-2026-06-24-NEW-WORKSPACE-HOTKEY — closed by QoL-WP6 (⌘⇧N opens the picker; 8-case unit test; operator-verified live).
- **Feature shipped:** FileTree git-indicator bubble-up to parent folders (QoL temporary-WBS WP7) — the file-tree rail's git-status markers now roll up to parent folders, so a collapsed folder hiding a changed file shows a single dominant-status glyph (precedence deleted > modified ≈ renamed > added > untracked, the VS Code/Sublime single-dominant-color convention) and the marker stays visible whether the folder is collapsed or expanded; it's a pure frontend derivation (`dominantStatusByDir`) over the existing `git_file_statuses` map with no new backend, computed in the same key space and recomputed on the same save/load triggers as the per-file indicators so a folder's marker can never drift from the files inside it.
- **Backlog resolved:** SURFACE-2026-06-24-FILETREE-GIT-INDICATOR-BUBBLE-UP-TO-PARENTS — closed by QoL-WP7 (17 tests, operator-verified all 6 live checks; review-quality 0 CRITICAL / 0 MAJOR / 3 MINOR auto-backlogged).
- **Feature shipped:** Diff-viewer polish (QoL temporary-WBS WP8, the last QoL WP) — the diff viewer's Commits section now starts collapsed (commit history is opt-in), and its sticky headers no longer collide: the commits section, the commit-view banner, and each per-file filename header now pin in a stack (each offset below the cumulative height of the layers above it, driven by `--diff-commits-h`/`--diff-commit-banner-h` CSS vars a ResizeObserver keeps current) so the current file's header stays visible the whole time you scroll its diff instead of hiding behind the commits panel or being shoved off by the next file; the other two WP8 list-items (collapse/expand-all + open-in-editor badge) were found already shipped during M2 WP4 and verified at plan time.
- **Backlog resolved:** SURFACE-2026-06-20-WP4-DIFF-VIEWER-POLISH-FOLLOWUPS — closed by QoL-WP8 (item 2 sticky headers + operator requests A sticky-per-file-row & B commits-collapsed-default built; items 1 & 3 found already-shipped under M2 WP4; item 4 previously dropped).
- **Milestone:** QoL temporary-WBS COMPLETE — WP0–WP8 all shipped; the between-milestone QoL/lifecycle sweep (filed after M4 close, before M5 PiP planning) is done and its scratch `qol-wbs.md` retired.
- **Feature shipped:** M5 WP1 probe — `tauri-nspanel` NSPanel mechanics (verdict GO) — a throwaway always-on-top floating NSPanel (toggled by a temporary "PiP?" button) proved live that `tauri-nspanel` v2.1 delivers the Picture-in-picture window contract (non-activating on click, floating, visible on all Spaces, survives focus-loss, safe teardown) against Tauri 2.11.2 with no raw-objc2 fallback; the probe surfaced five WP3 must-follow AppKit constraints (born-borderless before the NonactivatingPanel style mask; never `.no_activate(true)`; `to_window()→close()` teardown only; no close-X button; `collection_behavior` spelling) now recorded in `docs/product/wbs.md` → "Probe outcomes", and the probe code is kept as the WP3 seed.
- **Milestone:** M5 WP1 (Probe — `tauri-nspanel` NSPanel mechanics) — GO; the first of Milestone 5 (Picture-in-picture)'s six work packages, de-risking the NSPanel crate before the build WPs assume its API.

## 2026-06-24

- **Feature shipped:** M4 WP5 — Verify multi-workspace at N: the milestone-exit verification gate proved Claudesk's full multi-workspace surface holds at N≥4 real CC sessions in the native app — independent sessions with no cross-workspace leak, zero-click idle/running/awaiting-input status across both the expanded filmstrip and the collapsed pill row (M3-hook-driven, agreeing with each workspace's header), both promote paths (tile/pill click and `⌘⇧+digit`, the chord firing from focus inside CM6 and the terminal) plus persistent drag-reorder, the left/right focus indicator, collapse/expand reclaiming space while halting the mirror loop, responsive window-close at N, and the headline success metric (find the awaiting-input workspace in <1s, zero clicks, keyboard-switch to it) — all 7 checks operator-PASSED in one clean autopilot pass.
- **Backlog resolved:** SURFACE-2026-06-22-N8-CC-BACKEND-RAM — RAM headroom re-confirmed with N real in-flight sessions; Claudesk's marginal webview cost matches the WP1 envelope and the ~8–10-workspace practical ceiling on 16 GB (inherent backend `claude` RSS) is recorded as operator guidance.
- **Milestone:** WP5 (Milestone 4, verify-multi-workspace-at-N) complete — the last M4 work package; **Milestone 4 is COMPLETE and the M3+M4 dogfood-replace point is reached** (Claudesk can replace the terminal + Sublime daily-driver setup). Next strategic step: `/product-finalize`.
- **Feature shipped:** Dev/prod isolation — the installed `.app` (`com.claudesk.app`) and `pnpm tauri:dev` (`com.claudesk.app.dev`, via a `tauri.dev.json` overlay) now run concurrently with zero interference (separate app-data dirs, project lists, hook sockets, and per-identity `~/.claude/settings.json` registrations matched on the exact hook-script basename), unblocking dogfooding Claudesk with Claudesk; ships a new app icon (base + a magenta "DEV"-badged dev variant so the two Dock tiles are tellable apart) and a quarter-turn rotation of the inline Sublime Merge launcher icon.
- **Task closed:** Added a "Reveal in Finder" icon button to the right-panel tab row (beside the Sublime Text and Sublime Merge launchers) that opens the focused workspace's project folder in the macOS Finder via a new `finder_open` backend command (`open <dir>`, no tool discovery needed).
- **Task closed:** Fixed the installed (Finder-launched) app failing to start Claude Code with "No viable candidates found in PATH /usr/bin:/bin:/usr/sbin:/sbin" — a GUI app inherits the minimal launchd PATH (not the shell's), so user-installed CLIs like `claude` in `~/.local/bin` were invisible; the app now captures the login-shell PATH at startup (`src-tauri/src/env_path`) and sets it process-wide before any spawn, fixing `claude` and every other external-CLI launch (bug surfaced only in installs, never in `pnpm tauri:dev`).
- **Product cycle complete:** Milestone 4 (Multi-workspace UX — filmstrip + center stage) closed — N projects open concurrently as workspaces in one window, switched via a live filmstrip with per-workspace idle/running/awaiting-input status (click or ⌘⇧+digit), collapse toggle, and a left/right focus indicator; **the M3+M4 dogfood-replace point is reached** — Claudesk now replaces the terminal + Sublime daily-driver setup. Durable docs resynced (arch.md + roadmap.md), backlog swept, M4 cycle docs archived to `docs/product/archive/milestone-4-multi-workspace-ux/`. Next milestone: M5 (picture-in-picture).
- **Task closed:** Claudesk is now installable via a personal Homebrew tap — `brew tap StaymanHou/claudesk && brew trust --cask StaymanHou/claudesk/claudesk && brew install --cask claudesk && xattr -dr com.apple.quarantine /Applications/Claudesk.app` — backed by a public `v0.1.0` GitHub release (unsigned, Apple-Silicon-only) and a sha256-matched cask in the `homebrew-claudesk` tap repo; README gained Install + Develop/contribute sections, and a manual-only `/release` skill (never auto-invoked by feature-ship/product-finalize) drives future build→tag→release→cask-bump cuts.
- **Backlog resolved:** SURFACE-2026-06-24-HOMEBREW-DISTRIBUTION-VIA-UNSIGNED-PERSONAL-TAP — shipped the unsigned-tap distribution path + the manual-only release skill; signing/notarization/auto-update deferred to M9.
- **Feature shipped:** Native macOS application menu bar (Claudesk · File · Edit · Find · View · Workspace · Window) replacing Tauri's default auto-menu and mirroring existing features only — an About panel showing the app version, plus clickable items for the existing keyboard chords and launcher buttons; the menu carries no real accelerators (which on macOS would steal keystrokes from the webview/CodeMirror), instead emitting a `menu` event the frontend bridges to the exact existing chord handler or React seam, with CodeMirror-internal shortcuts shown as a label-only cheat-sheet.
- **Feature shipped:** Filesystem watcher (QoL temporary-WBS WP0) — a per-workspace `notify` watcher now keeps the UI in sync with on-disk changes made outside Claudesk (a CLI, another editor, git, or Claude Code writing a file): the FileTree rail auto-refreshes (new/removed files appear within ~1–2s, expand/scroll preserved, no manual collapse/expand) and backgrounded editor tabs live-reload (clean buffer reloads silently, dirty buffer raises the existing conflict popup) — gitignore-filtered, `.git/`-excluded, self-writes suppressed; built on `notify-debouncer-full` reusing `fs_index`'s ignore matcher and the WP12 `diskConflict` seam.
- **Backlog resolved:** SURFACE-2026-06-21-EDITOR-FILE-WATCHER — closed by QoL-WP0; the pulled-forward + scope-broadened watcher feeds both the FileTree refresh (the live stale-tree gap the operator hit) and open-editor-doc reload from one `fs-change` event source.

## 2026-06-23

- **Feature shipped:** M4 WP2 — N>1 lift: opening a project now **appends** a workspace (N coexist, every one stays mounted with its CC session alive in the background) instead of replacing the single one; a "**+**" control in the filmstrip re-opens the picker as a dismissable overlay to add more (re-opening an already-open project focuses the existing one, no duplicate session); window-close at N is responsive again via a parallelized `kill_all` (the N per-session 3s SIGKILL grace windows now overlap on one thread each instead of serializing to N×3s under the registry lock); and the project picker now surfaces IPC failures (a malformed `projects.json`, a failed open/remove) via an error toast instead of swallowing them into a silent empty list or an unhandled promise rejection.
- **Milestone:** WP2 (Milestone 4, N>1 lift) complete — the synchronous core of multi-workspace; remaining M4 WPs are WP3 (filmstrip render), WP4 (collapse), WP4b (focus indicator), WP5 (verify-at-N).
- **Backlog resolved:** SURFACE-2026-06-18 wp6-project-config-store — the 2 picker IPC error-surfacing MAJORs (mount loader swallowing a rejected `list_projects`; mutation handlers dropping rejections) closed by WP2's `mapIpcError` + error-toast.
- **Backlog resolved:** wp9-phase1-polish finding #1 — the picker mount effect's empty `catch {}` partial-failure window, closed by the same error-surfacing work.
- **Backlog resolved:** wp7-pty-cc-session finding #2 — `kill_all` serializing 3s grace windows under the registry lock (blocking window-close at N>1), closed by WP2's per-session-thread parallelization.
- **Feature shipped:** M4 WP3 — Filmstrip: the top strip now shows one tile per open workspace (the center-staged one marked active), each with a live CC status dot driven by the M3 hook channel and, for backgrounded workspaces, a live ~1 fps dark-themed terminal mirror that tails the latest output; clicking a tile or pressing ⌘⇧+digit promotes that workspace to center stage (no remount, PTY survives), and tiles are drag-reorderable in real time (WYSIWYG) with the arrangement persisted per project across restarts.
- **Milestone:** WP3 (Milestone 4, filmstrip render) complete — the multi-workspace status surface + switch affordances; remaining M4 WPs are WP4 (collapse toggle), WP4b (focus indicator), WP5 (verify-at-N).
- **Backlog resolved:** SURFACE-2026-06-23-WP3-BACKGROUND-WORKSPACE-SWITCH-AFFORDANCE — clicking a filmstrip tile (or ⌘⇧+digit) is now the primary way to switch back to a backgrounded workspace, verified live at WP3 verify-human.
- **Feature shipped:** M4 WP4 — Filmstrip collapse toggle: a chevron in the strip toggles the filmstrip between expanded (the WP3 ~1 fps thumbnail tiles) and collapsed (a one-line row of mini status pills — project name + the same M3 status dot, no preview), reclaiming vertical space while halting the per-tile serialize mirror loop so the background-render CPU cost drops to zero (the xterm buffers keep filling, so expanding restores a live frame within ~1s); click-to-promote works from a collapsed pill, and the collapsed/expanded preference persists across restarts.
- **Milestone:** WP4 (Milestone 4, filmstrip collapse toggle) complete — the space-reclaiming display mode over the filmstrip; remaining M4 WPs are WP4b (left/right focus indicator) and WP5 (verify-at-N, the milestone-exit gate).
- **Feature shipped:** M4 WP4b — Left/right intra-workspace focus indicator: a subtle 2px `#6ea8ff` accent now marks which half of the center-stage workspace holds keyboard focus (left CC terminal vs right editor/diff/terminal panel), driven by capture-phase focus tracking on the workspace root and gated to the center-stage workspace only; it coexists with the finer M2 split-pane active-pane border (half accent on the outer edge, pane strip on the inner edge), and the same change repaired a pre-existing regression where that split-pane border was invisible whenever a file was open (an `inset` box-shadow the opaque editor painted over) by restoring it as a `::before` strip and deleting the dead orphaned rule the WP11-Phase-5 rename had left behind.
- **Milestone:** WP4b (Milestone 4, left/right focus indicator) complete — the last non-verification M4 WP; only WP5 (verify-multi-workspace-at-N, the milestone-exit gate + dogfood-replace point) remains, and its dependencies (WP2/WP3/WP4/WP4b) are now all satisfied.

## 2026-06-22

- **Feature shipped:** WP13 — ⌘W close-active-editor-tab chord: a bare-⌘W app-level chord closes the focused editor pane's active tab (Sublime/VS-Code parity), routed through WP12's existing `requestClose` dirty-guard so an unsaved last view raises the Save/Don't-Save/Cancel dialog instead of silently discarding, inert when no tab is open, and registered via the WP1 capture-phase document listener (so it fires with focus inside CodeMirror, `preventDefault` pre-empts the OS close-window ⌘W, and it's suppressed while the finder/search overlay is open) — wired through a new pure `isCloseTabChord` predicate plus a `closeActiveTab` method threaded down the PaneTabs→EditorSplit→RightPanelHost imperative-handle chain using a latest-ref so the guard always reads the current document state (the fix for a stale-closure bug caught at verify-human).
- **Milestone:** WP13 (Milestone 2, ⌘W close-active-tab) complete — clears the first of the three operator-gated blockers before the Milestone 2 cycle can close (the WP11 git-status path-keying MAJOR and the terminal blank-cursor incident remain).
- **Task closed:** WP11 git-status path-keying — `git_file_statuses` now re-bases each status path to the workspace root (stripping the workspace's within-repo prefix and dropping out-of-subtree entries) so a workspace nested below its repo root shows its file-tree git indicators instead of silently showing none, plus a fold-in `recurse_untracked_dirs(true)` fix so untracked subdirs report per-file; clears the second of the three operator-gated Milestone-2-close blockers (the terminal blank-cursor incident remains).
- **Backlog resolved:** SURFACE-2026-06-21-QUALITY-WP11-GIT-STATUS-PATH-KEYING — the WP11 review MAJOR, fixed by re-basing the status map to the workspace key space (3 cosmetic MINORs from the same review remain pending).
- **Incident resolved:** Terminal blank-cursor (P1) — the right-panel Terminal rendered blank because its `cc-output` listener was torn down by the spawn effect re-running on the `spawning→live` phase flip, dropping the shell's one-shot prompt flush; fixed by keying the spawn effect on a `spawnNonce` instead of `bridge.phase` (so the listener survives the transition) and locked with a pure `spawnTrigger` contract test, clearing the third and last operator-gated Milestone-2-close blocker.
- **Product cycle complete:** Milestone 2 (Lite Editor + Diff Viewer) — Claudesk's right half is now the primary in-app editing surface: a CodeMirror 6 editor with multi-cursor/find-replace/command-palette/split-panes/minimap/font-zoom, a multi-file tab strip with ⌘W close-tab, a left file-tree rail with Sublime-style git indicators, a Cmd+P fuzzy finder, project-wide find/replace, a `git2` diff viewer, a second-terminal panel, and both Sublime launchers kept as panel-tab icon buttons; durable docs (arch.md, roadmap.md) resynced and the M2 WBS + research archived to `docs/product/archive/milestone-2-lite-editor-diff-viewer/`.
- **Backlog resolved:** SURFACE-2026-06-21-QUALITY-WP11-GIT-STATUS-PATH-KEYING — already closed in-cycle (the WP11 git-status MAJOR), re-stated as the only backlog item resolved by this cycle; all other pending items deferred to the next cycle.
- **Feature shipped:** M3 WP2 — Claudesk's CC-hook script + `~/.claude/settings.json` registration: a stdlib-only Perl hook (`claudesk-hook.pl`) emitting one JSON line per `UserPromptSubmit`/`Stop`/`Notification` event to a Claudesk-owned socket, plus a `hook_install` Rust module that additively merges the hook into the user's settings (never clobbering claude-time/notify-telegram), is idempotent + self-healing by a stable script-basename marker, removable, atomic-write + never-wipe-on-parse-failure, and installed on launch via a Tauri `.setup()` hook with errors surfaced not swallowed; live-verified on the real config (additive coexistence with claude-time, idempotent re-launch) and shipped with a same-session follow-up fix shell-quoting the command paths after a spaced-app-data-path word-split bug surfaced live.
- **Milestone:** M3 WP2 (Milestone 3 — CC lifecycle & state plumbing) complete; the hook channel is now installed end-to-end, unblocking WP3 (the `AF_UNIX` socket listener).
- **Feature shipped:** M3 WP3 — the `AF_UNIX` hook-socket listener: Claudesk now binds the Unix socket its installed CC hook writes to (at `<app-data>/hook.sock`, stale-file cleared on bind), accepts the stream on a dedicated `std::thread` (blocking `UnixListener`, not tokio), parses each newline-delimited line into a typed snake_case `HookEvent` (skip-and-continue on garbage, never panicking the loop), and delivers parsed events into the core over an `mpsc` channel — the receive side of the status "central nervous system," with a shared `hook_socket_path` resolver that retires the writer/reader path-drift risk and a `.setup()`/`CloseRequested` socket lifecycle; the live end-to-end run against a real `claude` is deferred to WP6's frontend close-the-loop.
- **Milestone:** M3 WP3 (Milestone 3 — CC lifecycle & state plumbing) complete; a parsed `HookEvent` now flows synchronously into the core, unblocking WP4 (the status broadcaster + `WorkspaceStatusUpdate` DTO) and WP5 (the `.session.md` watcher, parallel off WP3).
- **Feature shipped:** M3 WP4 — the status broadcaster: Claudesk now drains WP3's parsed `HookEvent` stream on a dedicated thread, normalizes each event to a workspace state (`UserPromptSubmit`→Running, `Stop`→Idle, `Notification`→AwaitingInput; unknown → no-op), maps the event's `cwd` to a known open workspace via a canonicalized registry (unmatched cwd dropped, not errored — the M2 WP11 path-keying lesson), and emits a snake_case-end-to-end `WorkspaceStatusUpdate { workspace_id, state, last_event_at, last_output_snippet? }` on the `workspace-status` Tauri event that the M4/M5/M6 surfaces will subscribe to; the WP3 module-wide dead-code allow is deleted (its receiver + fields now live) with the genuinely WP6-owned residuals carrying item-scoped allows, and the design is fully unit-tested (180/180, incl. an end-to-end socket→transform test over real WP3 plumbing) with the live real-`claude`→emit close-the-loop deferred to WP6.
- **Backlog resolved:** SURFACE-2026-06-21-IPC-DTO-FIELD-CASE-TESTS-MISS-SERDE-SHAPE — closed by WP4's `WorkspaceStatusUpdate` `serde_json::to_value` key-shape contract test + the new arch.md "IPC DTOs are snake_case end-to-end" convention note, the cross-cutting frontend↔backend field-casing guard this SURFACE asked for.
- **Milestone:** M3 WP4 (Milestone 3 — CC lifecycle & state plumbing) complete; the status "central nervous system" now emits normalized `WorkspaceStatusUpdate` events, leaving WP6 (frontend indicator + workspace registration + live close-the-loop) and WP5 (`.session.md` watcher, parallel) before Milestone 3 closes.
- **Feature shipped:** M3 WP6 — the frontend workspace-status subscription + honest CC-state indicator that closes the Milestone-3 chain end-to-end: the React webview now subscribes to the `workspace-status` event (a snake_case-verbatim wire mirror), renders a dot+label indicator on a new workspace chrome header (Idle/Running/AwaitingInput/Unknown, dark-only palette — Running in Claude brand orange `#d97757`, AwaitingInput in a deliberately-distinct cool blue `#539bf5`), and wires workspace-open→`workspace_register` / close→`workspace_deregister` (driven by diffing the workspace list, so the cwd→workspace registry the WP4 broadcaster matches against is finally fed) — making the three WP4 item-scoped dead-code allows live (deleted; `WorkspaceState::Unknown` given a live presence via `#[default]`), and verified live at verify-human against a real `pnpm tauri dev` + real `claude` where idle→running→awaiting-input transitions were observed purely from the hook channel (confirmed with terminal output scrolled away — no PTY scraping).
- **Backlog resolved:** SURFACE-2026-06-22-WP1-NOTIFICATION-PAYLOAD-NOT-LIVE-CAPTURED — the WP6 Phase-3 live close-the-loop fired a real `Notification` from an actual `claude` and turned the indicator AwaitingInput, taking the `Notification`→AwaitingInput contract (and the WP3 socket-bind + WP4 emit residuals) from inference-grade to live-observed.
- **Milestone:** M3 WP6 (Milestone 3 — CC lifecycle & state plumbing) complete — the full chain (real `claude` → hook → `AF_UNIX` socket → listener → broadcaster → emit → frontend indicator) is now live; only WP5 (the `.session.md` file-watcher, parallel track) remains before Milestone 3 closes.
- **Product cycle complete:** Milestone 3 (CC lifecycle & state plumbing) — Claudesk now knows each workspace's CC state (idle/running/awaiting-input) purely from CC's official hook channel — a Perl hook installed additively into `~/.claude/settings.json`, an `AF_UNIX` socket listener, a status broadcaster emitting a snake_case `WorkspaceStatusUpdate`, and an honest dark-palette indicator in the workspace header, all verified live with no PTY-output scraping; the planned `workflow/.session.md` file-watcher (WP5) was dropped as the wrong signal (a manual pause bookmark, not a live stream), durable docs (arch.md, roadmap.md) were resynced, and the M3 WBS + WP1 probe outcome archived to `docs/product/archive/milestone-3-cc-lifecycle-state-plumbing/`.
- **Backlog resolved:** SURFACE-2026-06-22-APP-DATA-DIR-IS-BUNDLE-IDENTIFIER-NOT-PRODUCTNAME — the operational doc lines now state the live app-data path as `~/Library/Application Support/com.claudesk.app/` (the bundle identifier `app_data_dir()` returns), corrected in arch.md and CLAUDE.md at this cycle close.
- **Feature shipped:** M4 WP1 — N-workspace mount-cost probe: a throwaway `?nwsprobe` route mounting N=8 real workspaces (full M2 stack: CM6 editor + diff + second terminal + a real Claude Code session each) measured the keep-everything-mounted cost envelope and returned GO for eager-mount (idle CPU 0.0% / active 7.8% median, webview RAM 311/428 MB — editors add ~0% CPU + ~120-190 MB, no React.lazy needed), surfacing the decisive finding that the ~2.8 GB N=8 cost is the 8 `claude` backend processes (inherent to running 8 concurrent CC sessions, the same as 8 terminals — not Claudesk-introduced and not lazy-fixable).
- **Backlog resolved:** SURFACE-2026-06-21-WP9-N-EDITORS-COST-AT-MULTIWORKSPACE — closed by the M4 WP1 probe, which measured the N≈8 mounted-editor cost the WP9 check deferred to the multi-workspace milestone.

## 2026-06-21

- **Feature shipped:** WP12 — editor multi-file tab strip: the right-half editor now opens multiple files as a row of tabs per pane in VS Code split-editor-group style (panes are top-level via the reused `editorPanes` reducer, each pane owns its own tab strip + ordered open-file set via a new pure `openFiles` reducer), with a per-workspace SHARED document store (`editorDocs`, ref-counted by path) so the same file open in two panes is ONE live buffer (edits mirror, dirty + save are document-level) while cursor/scroll stay per-view, synchronous disk-change detection (a new `stat_file` mtime+size marker checked on tab-activate + pre-save → silent reload when clean, a keep-mine/load-disk conflict popup when dirty, and a save-guard that blocks clobbering), a ⌘1..⌘9 tab-switch chord (bare ⌘+digit; ⌘⇧+digit reserved for the future filmstrip), confirm-before-close on a dirty tab, and a generic synthetic read-only buffer hook (`SyntheticView` + a dev-only `window.__editorSynthetic`) that is the seam WP7's "Find Results" tab will render into; built across 5 phases through 3 operator verify-human back-loops (tabs→per-pane inversion, a split-flash-then-close fix, and the independent→shared buffer correction).
- **Backlog resolved:** SURFACE-2026-06-21-EDITOR-MULTI-FILE-TAB-STRIP — the driver SURFACE (from WP7 Phase-2 verify-human), shipped as WP12; WP7 is now unblocked to render Find Results into a WP12 synthetic tab.
- **Backlog resolved:** SURFACE-2026-06-20-WP3C-INDEPENDENT-FILE-SPLIT — realized by WP12's per-pane split-editor groups (a different file, or the same file with a shared buffer, can be open per pane).
- **Feature shipped:** WP7 — project-wide find/replace (app-layer): ⌘⇧F opens a query+replace overlay that searches the whole workspace by content (gitignore-honored, reusing WP6's `ignore` walker + the in-process `regex` engine, no `rg` binary) and renders results into a Sublime-style read-only "Find Results" tab in the editor (the WP12 synthetic-buffer seam) — file-path headers + `   <line>:  <text>` rows with the matched text highlighted and the tab font tracking the editor's zoom — where clicking a match row opens that file at the match (the open-at-match highlight seam); plus project-wide Replace All via an overlay Replace field gated behind a blast-radius confirm, backed by a new Rust `project_replace` that reuses search's exact composed regex + walk (regex `$1` capture groups / substring-literal replacement) and writes atomically through `editor_fs`; built by redefining the paused Phase-2 floating-overlay result list into the tab (after the F26 UX redirect) and narrowing replace to project-wide-only, through one verify-human font/highlight back-loop.
- **Milestone:** WP7 (Milestone 2, project-wide find/replace) complete — 12th of Milestone 2's 13 WPs shipped; WP9 and WP11 remain.
- **Feature shipped:** WP9 — second-terminal panel: a third right-half panel (⌘⇧T / Terminal tab, alongside Editor + Diff) runs the operator's login shell (`$SHELL` else `/bin/zsh`, `-l -i`) cd'd into the project, driven through the `CcSession` seam via a new generic `spawn_argv` core that both `cc_spawn` (claude) and a new `term_spawn` (shell) delegate to — reusing the existing input/resize/kill commands + output events, with a frontend-parameterized `XtermPane` + thin `TerminalPane`, an `active`-gated deferred spawn (no shell into a hidden zero-size pane), and a backend output buffer-and-flush (`cc_ready`) that holds the shell's one-shot prompt until the frontend's listener attaches; shaken out through three verify-human back-loops (terminal-seam render guard, a multi-spawn from callback-identity churn, and the listener-lifetime/prompt-race) plus a `/debug-empirical-telemetry` sidebar, and closed out Milestone 2's error-handling (already covered + tested) and exit criteria.
- **Backlog resolved:** SURFACE-2026-06-20-QUALITY-WP5-TERMINAL-SEAM-UNTESTED — closed by WP9: `"terminal"` added to `AVAILABLE_PANELS` together with its mounted slot, tab, and a `terminalSlotGuard` structural test in the same change.
- **Milestone:** WP9 (Milestone 2, second-terminal panel + M2 close-out) complete — the 13th and final core WP of Milestone 2 (Lite Editor + Diff Viewer); the milestone's planned scope is shipped. Only the operator-promoted follow-on WP11 (tree/editor density polish + git-change indicators + Files-nav-Editor-only) remains, as its own cycle.
- **Feature shipped:** WP11 — tree/editor density polish + Sublime-style git-change indicators: the file-tree rail is now structurally Editor-only (it lives inside the editor slot, so the Editor/Diff/Terminal tab row spans full width and Diff/Terminal get the whole panel), wider (299px) and denser with a narrower editor minimap; each file row carries a Sublime-sidebar-style colored git-status glyph (M/A/U/D/R right of the filename, dark palette) fed by a new backend `git_status` module (`status_map_core` reusing WP4's `git2`/`git_diff` plumbing via a `pub(crate)` lift, non-git dirs returning an empty map rather than an error) and refreshed on tree-load + on-save; the rail is drag-to-resize with the width persisted in localStorage; and a Phase-5 layout pass (driven by operator review) portaled the ⌘⇧P command palette to center over the full right panel like ⌘⇧F, shrank the overlay fonts, and turned the editor Split control into an overflow-safe SVG icon in the tab strip (beside a relocated close-pane ✕) reclaiming the old dedicated split-row; built across 5 phases through many operator verify-human iterations.

## 2026-06-20

- **Feature shipped:** WP3a — editor core-editing parity: layered the daily Sublime-parity editing features onto the WP2 editor shell via a new pure `editorExtensions.ts` builder — multi-cursor with Cmd-drag rectangular/column select and Cmd-D select-next, in-file find (Cmd+F) / replace (Cmd+R) via a dark-styled `@codemirror/search` panel, font-size zoom (Cmd+= / Cmd+- / Cmd+0) via a Compartment-swapped `fontSizeTheme` persisted globally in localStorage, a `@replit/codemirror-minimap`, and `scrollPastEnd()`; plus a flex/grid height-chain fix (`.workspace-right` grid-item `min-height:0` + the `@uiw` `.cm-theme` wrapper) so the editor scrolls both axes with word-wrap off.
- **Milestone:** WP3a (Milestone 2, editor core-editing parity) complete — the 3rd of Milestone 2's 12 WPs (after WP1 + WP2); WP3b/3c and WP4–WP10 remain.
- **Feature shipped:** WP3b — editor command palette: a net-new Cmd+Shift+P React overlay over CodeMirror 6's command set whose first command set is syntax/mode selection (Set Syntax: JS/JSX/TS/TSX/Rust/Markdown/Plain Text, overriding the extension-derived language for the open file and resetting on file change), built on an extensible `{id,title,run}` registry composed in EditorPanel and passed into the overlay, opened via the WP1-proven capture-phase document-keydown listener so the chord fires while focus is inside the editor (active-gated to the focused workspace) and a Shift-required predicate that stays distinct from WP6's bare Cmd+P — with a chord-exclusivity matrix and chord-ownership map locking in coexistence for WP5/WP6/WP8.
- **Milestone:** WP3b (Milestone 2, editor command palette) complete — the 4th of Milestone 2's 12 WPs (after WP1 + WP2 + WP3a); WP3c, WP4–WP7, WP10, WP8, WP9 remain.
- **Feature shipped:** WP3c — editor split panes: the right-half editor can now split into N vertically-stacked CodeMirror 6 panes that are viewports onto the _same_ file (a shared-document model — the high-value gesture in a half-width column; independent-file-per-pane was deferred to a follow-up WP), built on a new pure `editorPanes.ts` pane/focus reducer (split / close / focus / collapse, last-pane guard, focus-reassign-on-close) driving a refactored `EditorPanel` that renders N `<CodeMirror>` instances over one panel-level buffer, with a focused-pane tracked via capture-phase focus (chords like Cmd+S and Cmd+Shift+P act on the shared doc), a per-pane close affordance, collapse-back-to-one on opening a different file, and an active-pane accent rendered as a `::before` strip (an inset box-shadow was occluded by CodeMirror's opaque editor — caught and fixed at verify-human).
- **Milestone:** WP3c (Milestone 2, editor split panes) complete — the 5th of Milestone 2's 12 WPs (after WP1 + WP2 + WP3a + WP3b); WP4–WP7, WP10, WP8, WP9 remain.
- **Feature shipped:** WP4 — git diff viewer (Sublime-Merge-style): a view-only diff viewer in the workspace right half (behind the Editor↔Diff toggle) with a collapsible Commits section on top and a scrolling column of collapsible per-file sections showing inline +/− hunks, where a new Rust `git_diff` module (`git2`) computes the changed-file list, per-file diff hunks, a paginated commit history (revwalk, Load-more), and per-commit diffs (vs first parent), and the React frontend renders styled +/− lines directly (no `@codemirror/merge`); built via a full spec→plan→build×2 after the first attempt's file-list+single-diff UX was rejected at verify-human and redesigned to the Sublime-Merge model, operator-approved live in the real app.
- **Milestone:** WP4 (Milestone 2, git diff viewer) complete — the 6th of Milestone 2's 12 WPs (after WP1 + WP2 + WP3a + WP3b + WP3c); WP5–WP7, WP10, WP8, WP9 remain.
- **Feature shipped:** WP4 diff-viewer polish — four operator-requested follow-up enhancements to the shipped diff viewer: stronger changed-line legibility (a saturated green/red left accent bar + heavier row wash), word-wrapping diff lines so long lines wrap at the panel edge instead of each rendering its own horizontal scrollbar (the editor stays no-wrap — diff-only), a collapse-all/expand-all control over the file sections (backed by new pure `collapseAll`/`expandAll`/`allCollapsed` helpers + 6 vitest cases), pinning the whole Commits section sticky at the top of the scroll area (not just its header row), and an "Open in editor" button per file row that flips the right panel to the editor and opens the working-tree file; plus a doc-comment-drift fix in `git_diff/mod.rs`.
- **Feature shipped:** WP5 — RightPanelHost + panel-select hotkeys: extracted the right-half panel management out of `Workspace.tsx` into a per-workspace `RightPanelHost` that owns the active panel via per-panel direct-select hotkeys (⌘⇧E Editor / ⌘⇧D Diff / ⌘⇧T Terminal-reserved-for-WP9, registered via the WP1 capture-phase listener so they fire while focus is inside CodeMirror) plus clickable tabs, both panels staying mounted so per-workspace state survives center-stage switches; reassigned the transitional Sublime Text pop chord ⌘⇧E→⌘⇧O and added a permanent "Open in Sublime Merge" button backed by a new `smerge_open` command (the `sublime` resolver generalized to share one launch path across Text and Merge) — establishing that Sublime Merge stays a permanent companion surface while only Sublime Text is replaced by the in-app editor.
- **Backlog resolved:** SURFACE-2026-06-20-WP5-SUBLIME-MERGE-PERMANENT — closed by WP5: vision.md Core Principle 3, arch.md M2 section, CLAUDE.md, and the WBS WP8 task list were resynced to the "Sublime Text replaced / Sublime Merge kept permanently" split, with the `smerge_open` button shipped as the permanent surface.
- **Backlog resolved:** SURFACE-2026-06-20-WP5-PANEL-HOTKEY-DIRECT-SELECT — closed by WP5: the panel hotkey shipped as per-panel direct-select (not cycling) and the WBS WP5 wording + arch.md prose were corrected to match.
- **Backlog resolved:** SURFACE-2026-06-20-WP4-OPEN-IN-EDITOR-BLOB-AT-REV — dismissed as working-as-intended: "open" always opens the live working-tree file by design (inspecting a file at a past commit is Sublime Merge's job), and the stale "deferred to WP5" DiffPanel comment was corrected.
- **Milestone:** WP5 (Milestone 2, RightPanelHost + panel-select hotkey) complete — the 7th of Milestone 2's 12 WPs (after WP1 + WP2 + WP3a + WP3b + WP3c + WP4); WP6, WP7, WP10, WP8, WP9 remain.
- **Feature shipped:** WP6 — Cmd+P fuzzy file finder: an app-layer file-finder subsystem (a Rust `fs_index` module walking the workspace dir via the `ignore` crate — honoring `.gitignore`, excluding `.git/`, showing dotfiles, surfacing errors not swallowing them) plus a `⌘P` React overlay (pure subsequence `fuzzyMatch` ranker, capture-phase chord that fires while focus is inside CodeMirror, keyboard nav, inline IPC-error row) that opens the picked file into the editor via the existing `openPath` seam, replacing the WP2 path-input stopgap; folded in a DEV-only `?ws=`/`window.__seedWorkspace` workspace-seed seam.
- **Backlog resolved:** SURFACE-2026-06-20-WP4-VERIFY-SELF-DIALOG-STUB-WEDGE — closed by WP6's DEV-only `?ws=`/`window.__seedWorkspace` seed seam, which lets verify-self open a workspace without the Tauri folder dialog (confirmed: WP6's finder→editor flow was driven in a stub browser via `?ws=`), making all future editor/panel WPs verify-self-able.
- **Milestone:** WP6 (Milestone 2, Cmd+P fuzzy file finder) complete — the 8th of Milestone 2's 12 WPs (after WP1 + WP2 + WP3a + WP3b + WP3c + WP4 + WP5); WP7, WP10, WP8, WP9 remain.
- **Feature shipped:** WP10 — file-tree navigator: an app-layer collapsible left-rail file explorer of the workspace's project dir (the browse-an-unfamiliar-tree complement to WP6's Cmd+P fuzzy-open), built on a dirs-included `fs_index` walk (`walk_tree_core` returning tagged `TreeEntry{path,is_dir}` with empty dirs included, factored to share WP6's `check_root`/`project_walker`/`rel_posix` so finder + tree provably honor one `.gitignore`/`.git`-excluded/dotfiles-shown contract) behind a new `fs_tree` command, with a pure `buildTree` nester + pure `treeState` reducer driving a recursive `FileTree`/`TreeRow` component that opens a clicked file into the editor via the existing `openFile` seam, highlights the active file, surfaces IPC errors inline, and collapses to a strip to reclaim width in the 50/50 split — toggling via CSS `display:none` while the component stays mounted so the expanded-dir state and loaded tree survive a collapse→expand cycle (a post-ship `/feature-refactor` corrected an initial unmount-on-collapse and trimmed unused reducer actions).
- **Milestone:** WP10 (Milestone 2, file-tree navigator) complete — the 9th of Milestone 2's 12 WPs (after WP1 + WP2 + WP3a + WP3b + WP3c + WP4 + WP5 + WP6); WP7, WP8, WP9, WP11 remain.
- **Feature shipped:** WP8 (REDEFINED) — both Sublime launchers (Text + Merge) relocated from the standalone toolbar into the right-panel tab row as right-aligned inlined-SVG icon buttons, and the redundant Sublime-Text `⌘⇧O` keyboard hotkey deleted (`chord.ts` + `SublimeToolbar.tsx` removed, the two launch handlers extracted to a testable `sublimeLaunch.ts` with an injectable `Invoker`, the chord-exclusivity matrix updated to codify `⌘⇧O` as freed) — reversing the original "remove the Sublime Text pop at editor parity" plan: the operator chose to KEEP both launchers permanently (in-app editor is the primary surface, Sublime Text is a permanent one-click escape hatch alongside Sublime Merge), so the backend `sublime_open`/`smerge_open` commands are untouched, there is no parity gate, and WP8 is no longer gated on WP9.
- **Backlog resolved:** wp8-sublime-hotkey (2026-06-19 code-quality findings) — the `chord.ts` "Phase 2" header-tag MINOR nit is mooted by WP8 deleting `chord.ts` (the lone remaining nit is the unrelated backend `sublime/mod.rs` citation shorthand).
- **Milestone:** WP8 (Milestone 2, Sublime-launcher consolidation) complete — the 10th of Milestone 2's 12 WPs (after WP1 + WP2 + WP3a + WP3b + WP3c + WP4 + WP5 + WP6 + WP10); WP7, WP9, WP11 remain.

## 2026-06-19

- **Feature shipped:** WP7 — PtyCcSession (embedded CC terminal): clicking a project spawns a real `claude --dangerously-skip-permissions` session in a `portable-pty` (the WP2-proven mechanism) behind the `CcSession` trait seam and four Tauri commands (`cc_spawn`/`cc_input`/`cc_resize`/`cc_kill`), streaming PTY output as base64 over a `cc-output-<sid>` event into the workspace's xterm.js pane and piping keystrokes back, with a session-ended overlay + Re-launch, a spawn-error overlay + Retry, window-resize reflow, `TERM=xterm-256color` set explicitly for color, and clean process reaping (`/exit\r` then SIGKILL; `CloseRequested` → `kill_all`) so no orphaned `claude` survives a window close.
- **Backlog resolved:** SURFACE-2026-06-16-CC-SLASH-COMMANDS-NEED-CR-NOT-LF — closed by `cc_session::slash_command_bytes`, the single chokepoint that appends exactly one `\r` (CR, not LF) to every Claudesk-originated slash command, codified by three cargo tests and confirmed live against the real `claude` binary.
- **Milestone:** WP7 (Phase 1, PtyCcSession — embedded CC terminal) complete — the last critical-path build before WP9 polish; WP8 (Sublime hotkey) and WP9 (exit-criteria verification) remain in Phase 1.
- **Feature shipped:** WP8 — in-app Sublime Text pop: an in-app `⌘⇧E` keybinding (a webview keydown handler owned by the focused workspace, so it fires only while Claudesk is focused — not an OS-global shortcut) plus an "Open in Sublime" button in each workspace's right-panel toolbar, both opening Sublime Text at the active tab's project directory via a backend `sublime_open` command that resolves `subl` (PATH → `.app` bundle → `open -a`, per the WP3 probe) and spawns it with `std::process::Command`; needs no macOS Accessibility permission.
- **Backlog resolved:** SURFACE-2026-06-19-ARCH-SUBLIME-LAUNCH-MECHANISM — closed at WP8 finalize by resyncing arch.md + CLAUDE.md to document the `std::process::Command` launch via `sublime_open` and removing the superseded OS-global `tauri-plugin-global-shortcut` + Accessibility flow.
- **Backlog resolved:** SURFACE-2026-06-19-ARCH-SUBL-PROJECT-FLAG-SUPERSEDED — closed at WP8 finalize by resyncing arch.md to `subl <dir>` (never `--project`/`--new-window`), per the WP3 probe finding that `--project` does not activate Sublime on cold start.
- **Milestone:** WP8 (Phase 1, in-app hotkey + button for Sublime Text pop) complete — only WP9 (polish + exit-criteria verification) remains before Phase 1 is done.
- **Feature shipped:** WP9 — Phase 1 polish + exit-criteria verification: two unhappy-path fixes — a friendly "claude not on PATH" error (`CcError::CcNotFound` + a pure `classify_spawn_error` that promotes the not-found spawn failure to an actionable message naming Claude Code and the install docs, shown verbatim in the xterm error overlay instead of a raw `os error 2`) and deleted-project pruning (`config_store::prune_missing` + a `prune_missing_projects` command that the picker calls on mount to drop projects whose folder no longer exists and show a dismissible toast) — plus a README rewritten from the scaffold stub to a real Phase-1 placeholder and confirmation that the WP4 thumbnail report is linked from arch.md/roadmap.md and the tab-shell substrate is in place.
- **Milestone:** Phase 1 (Bare Shell + Tab Substrate PoC) complete — all nine work packages (WP1 scaffold, WP2/3/4 probes, WP5 UI, WP6 config store, WP7 embedded CC terminal, WP8 Sublime hotkey, WP9 polish/exit-criteria) shipped; clicking a project opens a working Claude Code session in a workspace inside the Claudesk window, Sublime pops via in-app `⌘⇧E`, and the WP4 probe selected live ~1 fps filmstrip mirrors for Phase 2.
- **Product cycle complete:** Phase 1 (Bare Shell + Tab Substrate PoC) cycle closed via `/product-finalize` — the launch-friction pain point is solved at the user-visible level (project pick → working CC session in a workspace, in-app Sublime pop, tab-shell substrate ready for Phase 2); durable docs (arch.md, roadmap.md) resynced to the as-built design, the Phase 1 WBS + research archived to `docs/product/archive/phase-1-bare-shell-poc/`, and the backlog swept (open items deferred to the Phase 2 cycle).
- **Feature shipped:** Milestone 2 WP1+WP2 — the in-app lite editor shell: a CodeMirror 6 editor mounted in each workspace's right half (replacing the Phase-1 placeholder card) that opens a file via a new `editor_fs` Rust backend (`read_file`/`write_file` with a workspace-root guard + atomic writes), edits with VS Code Dark+ syntax highlighting for js/ts/jsx/tsx/rust/markdown, and saves on Cmd+S with an inline status bar + never-swallowed error banner; preceded by the WP1 integration probe that settled the two CM6 unknowns (app chords fire via a capture-phase listener while focus is in the editor; N mounted editors stay within the WP4 perf envelope).
- **Milestone:** WP1 (CM6 integration probe) + WP2 (editor shell — open/save) complete — the first two of Milestone 2's ten work packages; the critical path continues WP3 (editor features) → WP8 (remove Sublime pop) → WP9 (polish).

## 2026-06-18

- **Feature shipped:** WP5 — frontend UI prototype: the VSCode-style Project Picker (scrollable mock recents with per-row delete, mocked Open-Folder) plus the tab-shell substrate (WorkspaceList with a Phase 1 N≤1 invariant, Center Stage, empty Filmstrip slot, 50/50 Workspace with an xterm.js DOM-renderer terminal on mock data and a "Coming in Phase 3" placeholder), driven by a `picker | workspace-open` view machine derived from state — all dark-mode only, with no backend wiring yet.
- **Milestone:** WP5 (Phase 1, Frontend UI prototype — tab-shell substrate) complete — the first critical-path build after the scaffold + probes; Phase 2/WP6/WP7 plug into this substrate rather than reshaping it.
- **Feature shipped:** WP6 — project config store: a Rust `config_store` module persists the project list to `~/Library/Application Support/Claudesk/projects.json` (atomic `.tmp`+rename writes, missing-file→empty, malformed→typed error) behind four Tauri commands (`list_projects` recency-ordered / `add_project` / `record_open` / `remove_project`), and the Project Picker is rewired off mock data to real IPC with a native folder dialog and an always-present name+path filter box.
- **Backlog resolved:** SURFACE-2026-06-18-PICKER-SCALES-TO-MANY-PROJECTS — closed by WP6 wiring persistence-backed delete, `last_opened_at`-desc recency ordering, and the filter/search input over the recents.
- **Milestone:** WP6 (Phase 1, Project config store — Rust backend) complete; the first backend build on the critical path, with the `Project` struct reserving `default_drive_mode` for the Phase 2 drive-mode selector.

## 2026-06-17

- **Feature shipped:** WP4 — thumbnail-rendering probe validated that ~1 fps live terminal mirrors of 8 backgrounded + 1 active xterm.js stay within budget on real macOS WKWebView (Apple M4: idle CPU 4.5%, active median 13.3%, RAM 240 MB, center frame p95 18 ms / 0 dropped), so **Phase 2 ships live mirrors** using `@xterm/addon-serialize` `serializeAsHTML()` from the buffer (which beat the `cloneNode` arm) — and corrected a non-viable mechanism the architecture had assumed (off-screen-DOM mirroring, defeated by one-parent-per-node and xterm's off-viewport renderer pause).
- **Backlog resolved:** SURFACE-2026-06-16-ARCH-THUMBNAIL-MECHANISM-NONVIABLE — closed by WP4's decision report and the atomic arch.md mechanism correction.
- **Milestone:** WP4 (Phase 1, Probe — Thumbnail-rendering cost at N=8 workspaces) complete; outcome gates Phase 2's filmstrip + PiP rendering strategy.

## 2026-06-16

- **Feature shipped:** WP1 — Tauri 2 + React 19 + TypeScript + Vite project scaffold landed in the repo (scaffold-and-merge preserved strategic docs and the `_ref/` symlink), with ESLint v9 / Prettier / Vitest on the JS side and `cargo fmt` / `cargo clippy -D warnings` / `cargo test` on the Rust side; `pnpm tauri dev` opens a Claudesk window and `pnpm tauri build` produces `Claudesk.app`.
- **Feature shipped:** WP2 — Claude Code PTY-byte-injection probe confirmed via `portable-pty` 0.9 harness at `src-tauri/examples/cc_pty_probe.rs` that ANSI rendering, `/help\n` slash-command byte-injection, `pty.resize()` SIGWINCH, and yolo-mode auth carry-over all work as required for WP7; surfaced one high-priority design constraint (CC's TUI requires `Ctrl+D`/`Ctrl+C` twice to exit) to backlog as SURFACE-2026-06-16-CC-EXIT-REQUIRES-TWO-KEYSTROKES.
- **Milestone:** WP2 (Phase 1, Probe — Claude Code under host-driven PTY byte-injection) complete.
- **Feature shipped:** WP2 follow-up probe — corrected the WP7 design constraint after observing CC's TUI live: raw-mode means `\n` (LF) is a literal character and `\r` (CR) is the Enter key. Every slash-command byte-injection MUST end in `\r`. `/exit\r` is a cleaner one-byte-sequence shutdown than the original Ctrl+D x2 path. Backlog SURFACE entry superseded by SURFACE-2026-06-16-CC-SLASH-COMMANDS-NEED-CR-NOT-LF.
- **Feature shipped:** WP3 — Sublime Text / Sublime Merge CLI shapes probe documented the (project-state × user-intent) → command matrix and the WP8 hand-off contract; key findings include that the `.app`-bundle path (`/Applications/<App>.app/Contents/SharedSupport/bin/<tool>`) is the _default_ discovery state on the canonical dev machine (neither tool symlinked to PATH) and that both `subl` and `smerge` ship a native `-b/--background` flag, so WP8 needs no `open -a -g` quirk; also captured a project-scope feedback memory making Sublime Text activation consent-gated after probe `osascript activate` calls yanked live ST windows across macOS Spaces (Sublime Merge exempt).
- **Milestone:** WP3 (Phase 1, Probe — Sublime Text / Sublime Merge CLI shapes across project styles) complete.
