# Changelog

## 2026-06-22

- **Feature shipped:** WP13 — ⌘W close-active-editor-tab chord: a bare-⌘W app-level chord closes the focused editor pane's active tab (Sublime/VS-Code parity), routed through WP12's existing `requestClose` dirty-guard so an unsaved last view raises the Save/Don't-Save/Cancel dialog instead of silently discarding, inert when no tab is open, and registered via the WP1 capture-phase document listener (so it fires with focus inside CodeMirror, `preventDefault` pre-empts the OS close-window ⌘W, and it's suppressed while the finder/search overlay is open) — wired through a new pure `isCloseTabChord` predicate plus a `closeActiveTab` method threaded down the PaneTabs→EditorSplit→RightPanelHost imperative-handle chain using a latest-ref so the guard always reads the current document state (the fix for a stale-closure bug caught at verify-human).
- **Milestone:** WP13 (Milestone 2, ⌘W close-active-tab) complete — clears the first of the three operator-gated blockers before the Milestone 2 cycle can close (the WP11 git-status path-keying MAJOR and the terminal blank-cursor incident remain).
- **Task closed:** WP11 git-status path-keying — `git_file_statuses` now re-bases each status path to the workspace root (stripping the workspace's within-repo prefix and dropping out-of-subtree entries) so a workspace nested below its repo root shows its file-tree git indicators instead of silently showing none, plus a fold-in `recurse_untracked_dirs(true)` fix so untracked subdirs report per-file; clears the second of the three operator-gated Milestone-2-close blockers (the terminal blank-cursor incident remains).
- **Backlog resolved:** SURFACE-2026-06-21-QUALITY-WP11-GIT-STATUS-PATH-KEYING — the WP11 review MAJOR, fixed by re-basing the status map to the workspace key space (3 cosmetic MINORs from the same review remain pending).
- **Incident resolved:** Terminal blank-cursor (P1) — the right-panel Terminal rendered blank because its `cc-output` listener was torn down by the spawn effect re-running on the `spawning→live` phase flip, dropping the shell's one-shot prompt flush; fixed by keying the spawn effect on a `spawnNonce` instead of `bridge.phase` (so the listener survives the transition) and locked with a pure `spawnTrigger` contract test, clearing the third and last operator-gated Milestone-2-close blocker.
- **Product cycle complete:** Milestone 2 (Lite Editor + Diff Viewer) — Claudesk's right half is now the primary in-app editing surface: a CodeMirror 6 editor with multi-cursor/find-replace/command-palette/split-panes/minimap/font-zoom, a multi-file tab strip with ⌘W close-tab, a left file-tree rail with Sublime-style git indicators, a Cmd+P fuzzy finder, project-wide find/replace, a `git2` diff viewer, a second-terminal panel, and both Sublime launchers kept as panel-tab icon buttons; durable docs (arch.md, roadmap.md) resynced and the M2 WBS + research archived to `docs/product/archive/milestone-2-lite-editor-diff-viewer/`.
- **Backlog resolved:** SURFACE-2026-06-21-QUALITY-WP11-GIT-STATUS-PATH-KEYING — already closed in-cycle (the WP11 git-status MAJOR), re-stated as the only backlog item resolved by this cycle; all other pending items deferred to the next cycle.
- **Feature shipped:** M3 WP2 — Claudesk's CC-hook script + `~/.claude/settings.json` registration: a stdlib-only Perl hook (`claudesk-hook.pl`) emitting one JSON line per `UserPromptSubmit`/`Stop`/`Notification` event to a Claudesk-owned socket, plus a `hook_install` Rust module that additively merges the hook into the user's settings (never clobbering claude-time/notify-telegram), is idempotent + self-healing by a stable script-basename marker, removable, atomic-write + never-wipe-on-parse-failure, and installed on launch via a Tauri `.setup()` hook with errors surfaced not swallowed; live-verified on the real config (additive coexistence with claude-time, idempotent re-launch) and shipped with a same-session follow-up fix shell-quoting the command paths after a spaced-app-data-path word-split bug surfaced live.
- **Milestone:** M3 WP2 (Milestone 3 — CC lifecycle & state plumbing) complete; the hook channel is now installed end-to-end, unblocking WP3 (the `AF_UNIX` socket listener).
- **Feature shipped:** M3 WP3 — the `AF_UNIX` hook-socket listener: Claudesk now binds the Unix socket its installed CC hook writes to (at `<app-data>/hook.sock`, stale-file cleared on bind), accepts the stream on a dedicated `std::thread` (blocking `UnixListener`, not tokio), parses each newline-delimited line into a typed snake_case `HookEvent` (skip-and-continue on garbage, never panicking the loop), and delivers parsed events into the core over an `mpsc` channel — the receive side of the status "central nervous system," with a shared `hook_socket_path` resolver that retires the writer/reader path-drift risk and a `.setup()`/`CloseRequested` socket lifecycle; the live end-to-end run against a real `claude` is deferred to WP6's frontend close-the-loop.
- **Milestone:** M3 WP3 (Milestone 3 — CC lifecycle & state plumbing) complete; a parsed `HookEvent` now flows synchronously into the core, unblocking WP4 (the status broadcaster + `WorkspaceStatusUpdate` DTO) and WP5 (the `.session.md` watcher, parallel off WP3).
- **Feature shipped:** M3 WP4 — the status broadcaster: Claudesk now drains WP3's parsed `HookEvent` stream on a dedicated thread, normalizes each event to a workspace state (`UserPromptSubmit`→Running, `Stop`→Idle, `Notification`→AwaitingInput; unknown → no-op), maps the event's `cwd` to a known open workspace via a canonicalized registry (unmatched cwd dropped, not errored — the M2 WP11 path-keying lesson), and emits a snake_case-end-to-end `WorkspaceStatusUpdate { workspace_id, state, last_event_at, last_output_snippet? }` on the `workspace-status` Tauri event that the M4/M5/M6 surfaces will subscribe to; the WP3 module-wide dead-code allow is deleted (its receiver + fields now live) with the genuinely WP6-owned residuals carrying item-scoped allows, and the design is fully unit-tested (180/180, incl. an end-to-end socket→transform test over real WP3 plumbing) with the live real-`claude`→emit close-the-loop deferred to WP6.
- **Backlog resolved:** SURFACE-2026-06-21-IPC-DTO-FIELD-CASE-TESTS-MISS-SERDE-SHAPE — closed by WP4's `WorkspaceStatusUpdate` `serde_json::to_value` key-shape contract test + the new arch.md "IPC DTOs are snake_case end-to-end" convention note, the cross-cutting frontend↔backend field-casing guard this SURFACE asked for.
- **Milestone:** M3 WP4 (Milestone 3 — CC lifecycle & state plumbing) complete; the status "central nervous system" now emits normalized `WorkspaceStatusUpdate` events, leaving WP6 (frontend indicator + workspace registration + live close-the-loop) and WP5 (`.session.md` watcher, parallel) before Milestone 3 closes.
- **Feature shipped:** M3 WP6 — the frontend workspace-status subscription + honest CC-state indicator that closes the Milestone-3 chain end-to-end: the React webview now subscribes to the `workspace-status` event (a snake_case-verbatim wire mirror), renders a dot+label indicator on a new workspace chrome header (Idle/Running/AwaitingInput/Unknown, dark-only palette — Running in Claude brand orange `#d97757`, AwaitingInput in a deliberately-distinct cool blue `#539bf5`), and wires workspace-open→`workspace_register` / close→`workspace_deregister` (driven by diffing the workspace list, so the cwd→workspace registry the WP4 broadcaster matches against is finally fed) — making the three WP4 item-scoped dead-code allows live (deleted; `WorkspaceState::Unknown` given a live presence via `#[default]`), and verified live at verify-human against a real `pnpm tauri dev` + real `claude` where idle→running→awaiting-input transitions were observed purely from the hook channel (confirmed with terminal output scrolled away — no PTY scraping).
- **Backlog resolved:** SURFACE-2026-06-22-WP1-NOTIFICATION-PAYLOAD-NOT-LIVE-CAPTURED — the WP6 Phase-3 live close-the-loop fired a real `Notification` from an actual `claude` and turned the indicator AwaitingInput, taking the `Notification`→AwaitingInput contract (and the WP3 socket-bind + WP4 emit residuals) from inference-grade to live-observed.
- **Milestone:** M3 WP6 (Milestone 3 — CC lifecycle & state plumbing) complete — the full chain (real `claude` → hook → `AF_UNIX` socket → listener → broadcaster → emit → frontend indicator) is now live; only WP5 (the `.session.md` file-watcher, parallel track) remains before Milestone 3 closes.
- **Product cycle complete:** Milestone 3 (CC lifecycle & state plumbing) — Claudesk now knows each workspace's CC state (idle/running/awaiting-input) purely from CC's official hook channel — a Perl hook installed additively into `~/.claude/settings.json`, an `AF_UNIX` socket listener, a status broadcaster emitting a snake_case `WorkspaceStatusUpdate`, and an honest dark-palette indicator in the workspace header, all verified live with no PTY-output scraping; the planned `workflow/.session.md` file-watcher (WP5) was dropped as the wrong signal (a manual pause bookmark, not a live stream), durable docs (arch.md, roadmap.md) were resynced, and the M3 WBS + WP1 probe outcome archived to `docs/product/archive/milestone-3-cc-lifecycle-state-plumbing/`.
- **Backlog resolved:** SURFACE-2026-06-22-APP-DATA-DIR-IS-BUNDLE-IDENTIFIER-NOT-PRODUCTNAME — the operational doc lines now state the live app-data path as `~/Library/Application Support/com.claudesk.app/` (the bundle identifier `app_data_dir()` returns), corrected in arch.md and CLAUDE.md at this cycle close.

## 2026-06-21

- **Feature shipped:** WP12 — editor multi-file tab strip: the right-half editor now opens multiple files as a row of tabs per pane in VS Code split-editor-group style (panes are top-level via the reused `editorPanes` reducer, each pane owns its own tab strip + ordered open-file set via a new pure `openFiles` reducer), with a per-workspace SHARED document store (`editorDocs`, ref-counted by path) so the same file open in two panes is ONE live buffer (edits mirror, dirty + save are document-level) while cursor/scroll stay per-view, synchronous disk-change detection (a new `stat_file` mtime+size marker checked on tab-activate + pre-save → silent reload when clean, a keep-mine/load-disk conflict popup when dirty, and a save-guard that blocks clobbering), a ⌘1..⌘9 tab-switch chord (bare ⌘+digit; ⌘⇧+digit reserved for the future filmstrip), confirm-before-close on a dirty tab, and a generic synthetic read-only buffer hook (`SyntheticView` + a dev-only `window.__editorSynthetic`) that is the seam WP7's "Find Results" tab will render into; built across 5 phases through 3 operator verify-human back-loops (tabs→per-pane inversion, a split-flash-then-close fix, and the independent→shared buffer correction).
- **Backlog resolved:** SURFACE-2026-06-21-EDITOR-MULTI-FILE-TAB-STRIP — the driver SURFACE (from WP7 Phase-2 verify-human), shipped as WP12; WP7 is now unblocked to render Find Results into a WP12 synthetic tab.
- **Backlog resolved:** SURFACE-2026-06-20-WP3C-INDEPENDENT-FILE-SPLIT — realized by WP12's per-pane split-editor groups (a different file, or the same file with a shared buffer, can be open per pane).
- **Feature shipped:** WP7 — project-wide find/replace (app-layer): ⌘⇧F opens a query+replace overlay that searches the whole workspace by content (gitignore-honored, reusing WP6's `ignore` walker + the in-process `regex` engine, no `rg` binary) and renders results into a Sublime-style read-only "Find Results" tab in the editor (the WP12 synthetic-buffer seam) — file-path headers + `   <line>:  <text>` rows with the matched text highlighted and the tab font tracking the editor's zoom — where clicking a match row opens that file at the match (the open-at-match highlight seam); plus project-wide Replace All via an overlay Replace field gated behind a blast-radius confirm, backed by a new Rust `project_replace` that reuses search's exact composed regex + walk (regex `$1` capture groups / substring-literal replacement) and writes atomically through `editor_fs`; built by redefining the paused Phase-2 floating-overlay result list into the tab (after the F26 UX redirect) and narrowing replace to project-wide-only, through one verify-human font/highlight back-loop.
- **Milestone:** WP7 (Milestone 2, project-wide find/replace) complete — 12th of Milestone 2's 13 WPs shipped; WP9 and WP11 remain.
- **Feature shipped:** WP9 — second-terminal panel: a third right-half panel (⌘⇧T / Terminal tab, alongside Editor + Diff) runs the operator's login shell (`$SHELL` else `/bin/zsh`, `-l -i`) cd'd into the project, driven through the `CcSession` seam via a new generic `spawn_argv` core that both `cc_spawn` (claude) and a new `term_spawn` (shell) delegate to — reusing the existing input/resize/kill commands + output events, with a frontend-parameterized `XtermPane` + thin `TerminalPane`, an `active`-gated deferred spawn (no shell into a hidden zero-size pane), and a backend output buffer-and-flush (`cc_ready`) that holds the shell's one-shot prompt until the frontend's listener attaches; shaken out through three verify-human back-loops (terminal-seam render guard, a multi-spawn from callback-identity churn, and the listener-lifetime/prompt-race) plus a `/debug-empirical-telemetry` sidebar, and closed out Milestone 2's error-handling (already covered + tested) and exit criteria.
- **Backlog resolved:** SURFACE-2026-06-20-QUALITY-WP5-TERMINAL-SEAM-UNTESTED — closed by WP9: `"terminal"` added to `AVAILABLE_PANELS` together with its mounted slot, tab, and a `terminalSlotGuard` structural test in the same change.
- **Milestone:** WP9 (Milestone 2, second-terminal panel + M2 close-out) complete — the 13th and final core WP of Milestone 2 (Lite Editor + Diff Viewer); the milestone's planned scope is shipped. Only the operator-promoted follow-on WP11 (tree/editor density polish + git-change indicators + Files-nav-Editor-only) remains, as its own cycle.
- **Feature shipped:** WP11 — tree/editor density polish + Sublime-style git-change indicators: the file-tree rail is now structurally Editor-only (it lives inside the editor slot, so the Editor/Diff/Terminal tab row spans full width and Diff/Terminal get the whole panel), wider (299px) and denser with a narrower editor minimap; each file row carries a Sublime-sidebar-style colored git-status glyph (M/A/U/D/R right of the filename, dark palette) fed by a new backend `git_status` module (`status_map_core` reusing WP4's `git2`/`git_diff` plumbing via a `pub(crate)` lift, non-git dirs returning an empty map rather than an error) and refreshed on tree-load + on-save; the rail is drag-to-resize with the width persisted in localStorage; and a Phase-5 layout pass (driven by operator review) portaled the ⌘⇧P command palette to center over the full right panel like ⌘⇧F, shrank the overlay fonts, and turned the editor Split control into an overflow-safe SVG icon in the tab strip (beside a relocated close-pane ✕) reclaiming the old dedicated split-row; built across 5 phases through many operator verify-human iterations.

## 2026-06-20

- **Feature shipped:** WP3a — editor core-editing parity: layered the daily Sublime-parity editing features onto the WP2 editor shell via a new pure `editorExtensions.ts` builder — multi-cursor with Cmd-drag rectangular/column select and Cmd-D select-next, in-file find (Cmd+F) / replace (Cmd+R) via a dark-styled `@codemirror/search` panel, font-size zoom (Cmd+= / Cmd+- / Cmd+0) via a Compartment-swapped `fontSizeTheme` persisted globally in localStorage, a `@replit/codemirror-minimap`, and `scrollPastEnd()`; plus a flex/grid height-chain fix (`.workspace-right` grid-item `min-height:0` + the `@uiw` `.cm-theme` wrapper) so the editor scrolls both axes with word-wrap off.
- **Milestone:** WP3a (Milestone 2, editor core-editing parity) complete — the 3rd of Milestone 2's 12 WPs (after WP1 + WP2); WP3b/3c and WP4–WP10 remain.
- **Feature shipped:** WP3b — editor command palette: a net-new Cmd+Shift+P React overlay over CodeMirror 6's command set whose first command set is syntax/mode selection (Set Syntax: JS/JSX/TS/TSX/Rust/Markdown/Plain Text, overriding the extension-derived language for the open file and resetting on file change), built on an extensible `{id,title,run}` registry composed in EditorPanel and passed into the overlay, opened via the WP1-proven capture-phase document-keydown listener so the chord fires while focus is inside the editor (active-gated to the focused workspace) and a Shift-required predicate that stays distinct from WP6's bare Cmd+P — with a chord-exclusivity matrix and chord-ownership map locking in coexistence for WP5/WP6/WP8.
- **Milestone:** WP3b (Milestone 2, editor command palette) complete — the 4th of Milestone 2's 12 WPs (after WP1 + WP2 + WP3a); WP3c, WP4–WP7, WP10, WP8, WP9 remain.
- **Feature shipped:** WP3c — editor split panes: the right-half editor can now split into N vertically-stacked CodeMirror 6 panes that are viewports onto the _same_ file (a shared-document model — the high-value gesture in a half-width column; independent-file-per-pane was deferred to a follow-up WP), built on a new pure `editorPanes.ts` pane/focus reducer (split / close / focus / collapse, last-pane guard, focus-reassign-on-close) driving a refactored `EditorPanel` that renders N `<CodeMirror>` instances over one panel-level buffer, with a focused-pane tracked via capture-phase focus (chords like Cmd+S and Cmd+Shift+P act on the shared doc), a per-pane close affordance, collapse-back-to-one on opening a different file, and an active-pane accent rendered as a `::before` strip (an inset box-shadow was occluded by CodeMirror's opaque editor — caught and fixed at verify-human).
- **Milestone:** WP3c (Milestone 2, editor split panes) complete — the 5th of Milestone 2's 12 WPs (after WP1 + WP2 + WP3a + WP3b); WP4–WP7, WP10, WP8, WP9 remain.
- **Feature shipped:** WP4 — git diff viewer (Sublime-Merge-style): a view-only diff viewer in the workspace right half (behind the Editor↔Diff toggle) with a collapsible Commits section on top and a scrolling column of collapsible per-file sections showing inline +/− hunks, where a new Rust `git_diff` module (`git2`) computes the changed-file list, per-file diff hunks, a paginated commit history (revwalk, Load-more), and per-commit diffs (vs first parent), and the React frontend renders styled +/− lines directly (no `@codemirror/merge`); built via a full spec→plan→build×2 after the first attempt's file-list+single-diff UX was rejected at verify-human and redesigned to the Sublime-Merge model, operator-approved live in the real app.
- **Milestone:** WP4 (Milestone 2, git diff viewer) complete — the 6th of Milestone 2's 12 WPs (after WP1 + WP2 + WP3a + WP3b + WP3c); WP5–WP7, WP10, WP8, WP9 remain.
- **Feature shipped:** WP4 diff-viewer polish — four operator-requested follow-up enhancements to the shipped diff viewer: stronger changed-line legibility (a saturated green/red left accent bar + heavier row wash), word-wrapping diff lines so long lines wrap at the panel edge instead of each rendering its own horizontal scrollbar (the editor stays no-wrap — diff-only), a collapse-all/expand-all control over the file sections (backed by new pure `collapseAll`/`expandAll`/`allCollapsed` helpers + 6 vitest cases), pinning the whole Commits section sticky at the top of the scroll area (not just its header row), and an "Open in editor" button per file row that flips the right panel to the editor and opens the working-tree file; plus a doc-comment-drift fix in `git_diff/mod.rs`.
- **Feature shipped:** WP5 — RightPanelHost + panel-select hotkeys: extracted the right-half panel management out of `Workspace.tsx` into a per-workspace `RightPanelHost` that owns the active panel via per-panel direct-select hotkeys (⌘⇧E Editor / ⌘⇧D Diff / ⌘⇧T Terminal-reserved-for-WP9, registered via the WP1 capture-phase listener so they fire while focus is inside CodeMirror) plus clickable tabs, both panels staying mounted so per-workspace state survives center-stage switches; reassigned the transitional Sublime Text pop chord ⌘⇧E→⌘⇧O and added a permanent "Open in Sublime Merge" button backed by a new `smerge_open` command (the `sublime` resolver generalized to share one launch path across Text and Merge) — establishing that Sublime Merge stays a permanent companion surface while only Sublime Text is replaced by the in-app editor.
- **Backlog resolved:** SURFACE-2026-06-20-WP5-SUBLIME-MERGE-PERMANENT — closed by WP5: vision.md Core Principle 3, arch.md M2 section, CLAUDE.md, and the WBS WP8 task list were resynced to the "Sublime Text replaced / Sublime Merge kept permanently" split, with the `smerge_open` button shipped as the permanent surface.
- **Backlog resolved:** SURFACE-2026-06-20-WP5-PANEL-HOTKEY-DIRECT-SELECT — closed by WP5: the panel hotkey shipped as per-panel direct-select (not cycling) and the WBS WP5 wording + arch.md prose were corrected to match.
- **Backlog resolved:** SURFACE-2026-06-20-WP4-OPEN-IN-EDITOR-BLOB-AT-REV — dismissed as working-as-intended: "open" always opens the live working-tree file by design (inspecting a file at a past commit is Sublime Merge's job), and the stale "deferred to WP5" DiffPanel comment was corrected.
- **Milestone:** WP5 (Milestone 2, RightPanelHost + panel-select hotkey) complete — the 7th of Milestone 2's 12 WPs (after WP1 + WP2 + WP3a + WP3b + WP3c + WP4); WP6, WP7, WP10, WP8, WP9 remain.
- **Feature shipped:** WP6 — Cmd+P fuzzy file finder: an app-layer file-finder subsystem (a Rust `fs_index` module walking the workspace dir via the `ignore` crate — honoring `.gitignore`, excluding `.git/`, showing dotfiles, surfacing errors not swallowing them) plus a `⌘P` React overlay (pure subsequence `fuzzyMatch` ranker, capture-phase chord that fires while focus is inside CodeMirror, keyboard nav, inline IPC-error row) that opens the picked file into the editor via the existing `openPath` seam, replacing the WP2 path-input stopgap; folded in a DEV-only `?ws=`/`window.__seedWorkspace` workspace-seed seam.
- **Backlog resolved:** SURFACE-2026-06-20-WP4-VERIFY-SELF-DIALOG-STUB-WEDGE — closed by WP6's DEV-only `?ws=`/`window.__seedWorkspace` seed seam, which lets verify-self open a workspace without the Tauri folder dialog (confirmed: WP6's finder→editor flow was driven in a stub browser via `?ws=`), making all future editor/panel WPs verify-self-able.
- **Milestone:** WP6 (Milestone 2, Cmd+P fuzzy file finder) complete — the 8th of Milestone 2's 12 WPs (after WP1 + WP2 + WP3a + WP3b + WP3c + WP4 + WP5); WP7, WP10, WP8, WP9 remain.
- **Feature shipped:** WP10 — file-tree navigator: an app-layer collapsible left-rail file explorer of the workspace's project dir (the browse-an-unfamiliar-tree complement to WP6's Cmd+P fuzzy-open), built on a dirs-included `fs_index` walk (`walk_tree_core` returning tagged `TreeEntry{path,is_dir}` with empty dirs included, factored to share WP6's `check_root`/`project_walker`/`rel_posix` so finder + tree provably honor one `.gitignore`/`.git`-excluded/dotfiles-shown contract) behind a new `fs_tree` command, with a pure `buildTree` nester + pure `treeState` reducer driving a recursive `FileTree`/`TreeRow` component that opens a clicked file into the editor via the existing `openFile` seam, highlights the active file, surfaces IPC errors inline, and collapses to a strip to reclaim width in the 50/50 split — toggling via CSS `display:none` while the component stays mounted so the expanded-dir state and loaded tree survive a collapse→expand cycle (a post-ship `/feature-refactor` corrected an initial unmount-on-collapse and trimmed unused reducer actions).
- **Milestone:** WP10 (Milestone 2, file-tree navigator) complete — the 9th of Milestone 2's 12 WPs (after WP1 + WP2 + WP3a + WP3b + WP3c + WP4 + WP5 + WP6); WP7, WP8, WP9, WP11 remain.
- **Feature shipped:** WP8 (REDEFINED) — both Sublime launchers (Text + Merge) relocated from the standalone toolbar into the right-panel tab row as right-aligned inlined-SVG icon buttons, and the redundant Sublime-Text `⌘⇧O` keyboard hotkey deleted (`chord.ts` + `SublimeToolbar.tsx` removed, the two launch handlers extracted to a testable `sublimeLaunch.ts` with an injectable `Invoker`, the chord-exclusivity matrix updated to codify `⌘⇧O` as freed) — reversing the original "remove the Sublime Text pop at editor parity" plan: the operator chose to KEEP both launchers permanently (in-app editor is the primary surface, Sublime Text is a permanent one-click escape hatch alongside Sublime Merge), so the backend `sublime_open`/`smerge_open` commands are untouched, there is no parity gate, and WP8 is no longer gated on WP9.
- **Backlog resolved:** wp8-sublime-hotkey (2026-06-19 code-quality findings) — the `chord.ts` "Phase 2" header-tag MINOR nit is mooted by WP8 deleting `chord.ts` (the lone remaining nit is the unrelated backend `sublime/mod.rs` citation shorthand).
- **Milestone:** WP8 (Milestone 2, Sublime-launcher consolidation) complete — the 10th of Milestone 2's 12 WPs (after WP1 + WP2 + WP3a + WP3b + WP3c + WP4 + WP5 + WP6 + WP10); WP7, WP9, WP11 remain.

## 2026-06-19

- **Feature shipped:** WP7 — PtyCcSession (embedded CC terminal): clicking a project spawns a real `claude --dangerously-skip-permissions` session in a `portable-pty` (the WP2-proven mechanism) behind the `CcSession` trait seam and four Tauri commands (`cc_spawn`/`cc_input`/`cc_resize`/`cc_kill`), streaming PTY output as base64 over a `cc-output-<sid>` event into the workspace's xterm.js pane and piping keystrokes back, with a session-ended overlay + Re-launch, a spawn-error overlay + Retry, window-resize reflow, `TERM=xterm-256color` set explicitly for color, and clean process reaping (`/exit\r` then SIGKILL; `CloseRequested` → `kill_all`) so no orphaned `claude` survives a window close.
- **Backlog resolved:** SURFACE-2026-06-16-CC-SLASH-COMMANDS-NEED-CR-NOT-LF — closed by `cc_session::slash_command_bytes`, the single chokepoint that appends exactly one `\r` (CR, not LF) to every Claudesk-originated slash command, codified by three cargo tests and confirmed live against the real `claude` binary.
- **Milestone:** WP7 (Phase 1, PtyCcSession — embedded CC terminal) complete — the last critical-path build before WP9 polish; WP8 (Sublime hotkey) and WP9 (exit-criteria verification) remain in Phase 1.
- **Feature shipped:** WP8 — in-app Sublime Text pop: an in-app `⌘⇧E` keybinding (a webview keydown handler owned by the focused workspace, so it fires only while Claudesk is focused — not an OS-global shortcut) plus an "Open in Sublime" button in each workspace's right-panel toolbar, both opening Sublime Text at the active tab's project directory via a backend `sublime_open` command that resolves `subl` (PATH → `.app` bundle → `open -a`, per the WP3 probe) and spawns it with `std::process::Command`; needs no macOS Accessibility permission.
- **Backlog resolved:** SURFACE-2026-06-19-ARCH-SUBLIME-LAUNCH-MECHANISM — closed at WP8 finalize by resyncing arch.md + CLAUDE.md to document the `std::process::Command` launch via `sublime_open` and removing the superseded OS-global `tauri-plugin-global-shortcut` + Accessibility flow.
- **Backlog resolved:** SURFACE-2026-06-19-ARCH-SUBL-PROJECT-FLAG-SUPERSEDED — closed at WP8 finalize by resyncing arch.md to `subl <dir>` (never `--project`/`--new-window`), per the WP3 probe finding that `--project` does not activate Sublime on cold start.
- **Milestone:** WP8 (Phase 1, in-app hotkey + button for Sublime Text pop) complete — only WP9 (polish + exit-criteria verification) remains before Phase 1 is done.
- **Feature shipped:** WP9 — Phase 1 polish + exit-criteria verification: two unhappy-path fixes — a friendly "claude not on PATH" error (`CcError::CcNotFound` + a pure `classify_spawn_error` that promotes the not-found spawn failure to an actionable message naming Claude Code and the install docs, shown verbatim in the xterm error overlay instead of a raw `os error 2`) and deleted-project pruning (`config_store::prune_missing` + a `prune_missing_projects` command that the picker calls on mount to drop projects whose folder no longer exists and show a dismissible toast) — plus a README rewritten from the scaffold stub to a real Phase-1 placeholder and confirmation that the WP4 thumbnail report is linked from arch.md/roadmap.md and the tab-shell substrate is in place.
- **Milestone:** Phase 1 (Bare Shell + Tab Substrate PoC) complete — all nine work packages (WP1 scaffold, WP2/3/4 probes, WP5 UI, WP6 config store, WP7 embedded CC terminal, WP8 Sublime hotkey, WP9 polish/exit-criteria) shipped; clicking a project opens a working Claude Code session in a workspace inside the Claudesk window, Sublime pops via in-app `⌘⇧E`, and the WP4 probe selected live ~1 fps filmstrip mirrors for Phase 2.
- **Product cycle complete:** Phase 1 (Bare Shell + Tab Substrate PoC) cycle closed via `/product-finalize` — the launch-friction pain point is solved at the user-visible level (project pick → working CC session in a workspace, in-app Sublime pop, tab-shell substrate ready for Phase 2); durable docs (arch.md, roadmap.md) resynced to the as-built design, the Phase 1 WBS + research archived to `docs/product/archive/phase-1-bare-shell-poc/`, and the backlog swept (open items deferred to the Phase 2 cycle).
- **Feature shipped:** Milestone 2 WP1+WP2 — the in-app lite editor shell: a CodeMirror 6 editor mounted in each workspace's right half (replacing the Phase-1 placeholder card) that opens a file via a new `editor_fs` Rust backend (`read_file`/`write_file` with a workspace-root guard + atomic writes), edits with VS Code Dark+ syntax highlighting for js/ts/jsx/tsx/rust/markdown, and saves on Cmd+S with an inline status bar + never-swallowed error banner; preceded by the WP1 integration probe that settled the two CM6 unknowns (app chords fire via a capture-phase listener while focus is in the editor; N mounted editors stay within the WP4 perf envelope).
- **Milestone:** WP1 (CM6 integration probe) + WP2 (editor shell — open/save) complete — the first two of Milestone 2's ten work packages; the critical path continues WP3 (editor features) → WP8 (remove Sublime pop) → WP9 (polish).

## 2026-06-18

- **Feature shipped:** WP5 — frontend UI prototype: the VSCode-style Project Picker (scrollable mock recents with per-row delete, mocked Open-Folder) plus the tab-shell substrate (WorkspaceList with a Phase 1 N≤1 invariant, Center Stage, empty Filmstrip slot, 50/50 Workspace with an xterm.js DOM-renderer terminal on mock data and a "Coming in Phase 3" placeholder), driven by a `picker | workspace-open` view machine derived from state — all dark-mode only, with no backend wiring yet.
- **Milestone:** WP5 (Phase 1, Frontend UI prototype — tab-shell substrate) complete — the first critical-path build after the scaffold + probes; Phase 2/WP6/WP7 plug into this substrate rather than reshaping it.
- **Feature shipped:** WP6 — project config store: a Rust `config_store` module persists the project list to `~/Library/Application Support/Claudesk/projects.json` (atomic `.tmp`+rename writes, missing-file→empty, malformed→typed error) behind four Tauri commands (`list_projects` recency-ordered / `add_project` / `record_open` / `remove_project`), and the Project Picker is rewired off mock data to real IPC with a native folder dialog and an always-present name+path filter box.
- **Backlog resolved:** SURFACE-2026-06-18-PICKER-SCALES-TO-MANY-PROJECTS — closed by WP6 wiring persistence-backed delete, `last_opened_at`-desc recency ordering, and the filter/search input over the recents.
- **Milestone:** WP6 (Phase 1, Project config store — Rust backend) complete; the first backend build on the critical path, with the `Project` struct reserving `default_drive_mode` for the Phase 2 drive-mode selector.

## 2026-06-17

- **Feature shipped:** WP4 — thumbnail-rendering probe validated that ~1 fps live terminal mirrors of 8 backgrounded + 1 active xterm.js stay within budget on real macOS WKWebView (Apple M4: idle CPU 4.5%, active median 13.3%, RAM 240 MB, center frame p95 18 ms / 0 dropped), so **Phase 2 ships live mirrors** using `@xterm/addon-serialize` `serializeAsHTML()` from the buffer (which beat the `cloneNode` arm) — and corrected a non-viable mechanism the architecture had assumed (off-screen-DOM mirroring, defeated by one-parent-per-node and xterm's off-viewport renderer pause).
- **Backlog resolved:** SURFACE-2026-06-16-ARCH-THUMBNAIL-MECHANISM-NONVIABLE — closed by WP4's decision report and the atomic arch.md mechanism correction.
- **Milestone:** WP4 (Phase 1, Probe — Thumbnail-rendering cost at N=8 workspaces) complete; outcome gates Phase 2's filmstrip + PiP rendering strategy.

## 2026-06-16

- **Feature shipped:** WP1 — Tauri 2 + React 19 + TypeScript + Vite project scaffold landed in the repo (scaffold-and-merge preserved strategic docs and the `_ref/` symlink), with ESLint v9 / Prettier / Vitest on the JS side and `cargo fmt` / `cargo clippy -D warnings` / `cargo test` on the Rust side; `pnpm tauri dev` opens a Claudesk window and `pnpm tauri build` produces `Claudesk.app`.
- **Feature shipped:** WP2 — Claude Code PTY-byte-injection probe confirmed via `portable-pty` 0.9 harness at `src-tauri/examples/cc_pty_probe.rs` that ANSI rendering, `/help\n` slash-command byte-injection, `pty.resize()` SIGWINCH, and yolo-mode auth carry-over all work as required for WP7; surfaced one high-priority design constraint (CC's TUI requires `Ctrl+D`/`Ctrl+C` twice to exit) to backlog as SURFACE-2026-06-16-CC-EXIT-REQUIRES-TWO-KEYSTROKES.
- **Milestone:** WP2 (Phase 1, Probe — Claude Code under host-driven PTY byte-injection) complete.
- **Feature shipped:** WP2 follow-up probe — corrected the WP7 design constraint after observing CC's TUI live: raw-mode means `\n` (LF) is a literal character and `\r` (CR) is the Enter key. Every slash-command byte-injection MUST end in `\r`. `/exit\r` is a cleaner one-byte-sequence shutdown than the original Ctrl+D x2 path. Backlog SURFACE entry superseded by SURFACE-2026-06-16-CC-SLASH-COMMANDS-NEED-CR-NOT-LF.
- **Feature shipped:** WP3 — Sublime Text / Sublime Merge CLI shapes probe documented the (project-state × user-intent) → command matrix and the WP8 hand-off contract; key findings include that the `.app`-bundle path (`/Applications/<App>.app/Contents/SharedSupport/bin/<tool>`) is the _default_ discovery state on the canonical dev machine (neither tool symlinked to PATH) and that both `subl` and `smerge` ship a native `-b/--background` flag, so WP8 needs no `open -a -g` quirk; also captured a project-scope feedback memory making Sublime Text activation consent-gated after probe `osascript activate` calls yanked live ST windows across macOS Spaces (Sublime Merge exempt).
- **Milestone:** WP3 (Phase 1, Probe — Sublime Text / Sublime Merge CLI shapes across project styles) complete.
